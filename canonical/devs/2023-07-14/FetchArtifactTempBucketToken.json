{
  "camel_example": "",
  "cmd_full_name": "devs fetch-artifact-temp-bucket-token",
  "cmd_name": "fetch-artifact-temp-bucket-token",
  "components": {
    "schemas": {
      "ArtifactTempBucketToken": {
        "properties": {
          "credentials": {
            "backendName": "credentials",
            "properties": {
              "accessKeyId": {
                "backendName": "accessKeyId",
                "title": "交付物上传临时accessKeyId",
                "type": "string"
              },
              "accessKeySecret": {
                "backendName": "accessKeySecret",
                "title": "交付物上传临时accessKeySecret",
                "type": "string"
              },
              "securityToken": {
                "backendName": "securityToken",
                "title": "交付物上传临时securityToken",
                "type": "string"
              }
            },
            "title": "交付物上传临时sts",
            "type": "object"
          },
          "ossBucketName": {
            "backendName": "ossBucketName",
            "title": "交付物上传临时object",
            "type": "string"
          },
          "ossObjectName": {
            "backendName": "ossObjectName",
            "title": "交付物上传临时bucket",
            "type": "string"
          },
          "ossRegion": {
            "backendName": "ossRegion",
            "title": "交付物上传区域",
            "type": "string"
          }
        },
        "title": "交付物上传临时sts 和 bucket/object",
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Retrieves the `bucket`, `object` path, and temporary Security Token Service (STS) credentials for uploading an `artifact`",
  "description_zh": "获取交付物临时上传的bucket、object和临时sts",
  "kebab_example": "aliyun devs fetch-artifact-temp-bucket-token --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "FetchArtifactTempBucketToken",
  "operation": {
    "action": "FetchArtifactTempBucketToken",
    "api_style": "ROA",
    "api_version": "2023-07-14",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/2023-07-14/artifacts/action/fetchTempBucketToken"
  },
  "parameters": [],
  "pathPattern": "/2023-07-14/artifacts/action/fetchTempBucketToken",
  "product_code": "Devs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "$ref": "#/components/schemas/ArtifactTempBucketToken",
        "description_en": "Contains the `object` name, `bucket` name, `region`, and temporary `credentials` for uploading an `artifact`",
        "description_zh": "交付上传临时地址",
        "parseType": "struct",
        "title_en": "交付上传临时地址",
        "title_zh": "交付上传临时地址"
      }
    },
    "201": {}
  }
}
