{
  "camel_example": "",
  "cmd_full_name": "devs delete-project",
  "cmd_name": "delete-project",
  "deprecated": false,
  "description_en": "Deletes a project and cascades the deletion to all its related entities, including environments, pipeline triggers, pipeline trigger records, pipeline templates, and pipelines",
  "description_zh": "删除一个项目，并级联删除项目下的其他实体（包括环境、流水线触发器、流水线触发记录、流水线模板、流水线）",
  "kebab_example": "aliyun devs delete-project --name demo-name --biz-force true --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "DeleteProject",
  "operation": {
    "action": "DeleteProject",
    "api_style": "ROA",
    "api_version": "2023-07-14",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/2023-07-14/projects/{name}"
  },
  "parameters": [
    {
      "example": "demo-name",
      "help_en": "The project name",
      "help_zh": "项目名称",
      "location": "path",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "name",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to forcibly delete a project that contains multiple environments. If this parameter is set to `true`, this operation deletes the project and all its related entities. If this parameter is set to `false` or omitted, the operation fails with an exception. You must first delete the additional environments",
      "help_zh": "是否强制删除。当项目中包含多个环境时，若force为true，则能够强制删除项目，并级联删除所有环境下的其他实体；若force为false，则抛出异常，需要您先删除环境",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/2023-07-14/projects/[name]",
  "product_code": "Devs",
  "protocol": "HTTP|HTTPS",
  "responses": {},
  "v1_parameters": [
    {
      "help_en": "Specifies whether to forcibly delete a project that contains multiple environments. If this parameter is set to `true`, this operation deletes the project and all its related entities. If this parameter is set to `false` or omitted, the operation fails with an exception. You must first delete the additional environments",
      "help_zh": "是否强制删除。当项目中包含多个环境时，若force为true，则能够强制删除项目，并级联删除所有环境下的其他实体；若force为false，则抛出异常，需要您先删除环境",
      "name": "force",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The project name",
      "help_zh": "项目名称",
      "name": "name",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
