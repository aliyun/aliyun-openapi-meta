{
  "camel_example": "aliyun sas ModifyPropertyScheduleConfig --region cn-hangzhou --Type scheduler_port_period --ScheduleTime 3",
  "cmd_full_name": "sas modify-property-schedule-config",
  "cmd_name": "modify-property-schedule-config",
  "deprecated": false,
  "description_en": "Modifies the collection frequency of asset fingerprints for an automatic periodic collection task",
  "description_zh": "修改资产指纹周期性自动采集的频率",
  "kebab_example": "aliyun sas modify-property-schedule-config --type scheduler_port_period --schedule-time 3 --api-version 2018-12-03 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ModifyPropertyScheduleConfig",
  "operation": {
    "action": "ModifyPropertyScheduleConfig",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "scheduler_port_period",
      "help_en": "The type of the asset fingerprints for which you want to modify the collection frequency. Valid values:\n\nscheduler_port_period: listening port\nscheduler_process_period: running process\nscheduler_account_period: account\nscheduler_software_period: software\nscheduler_cron_period: scheduled task\nscheduler_sca_period: middleware, database, or web service\nscheduler_autorun_period: startup item\nscheduler_lkm_period: kernel module\nscheduler_sca_proxy_period: website",
      "help_zh": "待修改自动采集频率的资产指纹的类型。取值：\n\nscheduler_port_period：监听端口\nscheduler_process_period：运行进程\nscheduler_account_period：账号资产\nscheduler_software_period：软件资产\nscheduler_cron_period：计划任务\nscheduler_sca_period：中间件以及数据库、Web服务\nscheduler_autorun_period：启动项\nscheduler_lkm_period：内核模块\nscheduler_sca_proxy_period：Web站点",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    },
    {
      "example": "3",
      "help_en": "The new collection frequency of asset fingerprints. Valid values:\n\n0: disabled, which indicates that the asset fingerprints are not automatically or periodically collected.\n1: collects asset fingerprints once an hour.\n3: collects asset fingerprints once every 3 hours.\n12: collects asset fingerprints once every 12 hours.\n24: collects asset fingerprints once a day.\n168: collects asset fingerprints once every 7 days",
      "help_zh": "设置资产指纹的采集频率。取值：\n\n0：关闭，即不进行周期性的资产指纹自动采集\n1：每1小时采集1次\n3：每3小时采集1次\n12：每12小时采集1次\n24：每天采集1次\n168：每7天采集1次",
      "location": "query",
      "name": "schedule_time",
      "options": [
        "--schedule-time"
      ],
      "raw_name": "ScheduleTime",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "headers": {},
      "schema": {
        "properties": {
          "ModifyResult": {
            "backendName": "data",
            "description_en": "Indicates whether the collection frequency of asset fingerprints is modified. Valid values:\n\ntrue: yes\nfalse: no",
            "description_zh": "资产指纹自动采集频率是否修改成功。取值：\n\ntrue：修改成功\nfalse：修改失败",
            "example": "true",
            "type": "boolean"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request, which is used to locate and troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "C0D1DF51-D879-55A8-B6C7-7D3B913EF34D",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
