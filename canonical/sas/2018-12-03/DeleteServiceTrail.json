{
  "camel_example": "aliyun sas DeleteServiceTrail --region cn-hangzhou --RegionId 'cn-hangzhou'",
  "cmd_full_name": "sas delete-service-trail",
  "cmd_name": "delete-service-trail",
  "deprecated": false,
  "description_en": "Deletes an ActionTrail data delivery configuration",
  "description_zh": "删除操作审计数据投递",
  "kebab_example": "aliyun sas delete-service-trail --biz-region-id cn-hangzhou --api-version 2018-12-03 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteServiceTrail",
  "operation": {
    "action": "DeleteServiceTrail",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region of the Security Center instance. Valid values:\n\ncn-hangzhou: China\nap-southeast-1: Singapore",
      "help_zh": "云安全中心实例地域。取值为：\n\ncn-hangzhou：  中心\nap-southeast-1：  新加坡",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID. Alibaba Cloud generates a unique identifier for each request. You can use the request ID to troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "20456DD5-5CBF-5015-9173-12CA4246B***",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
