{
  "camel_example": "aliyun sas DeleteTagWithUuid --region cn-hangzhou --TagName abc",
  "cmd_full_name": "sas delete-tag-with-uuid",
  "cmd_name": "delete-tag-with-uuid",
  "deprecated": false,
  "description_en": "Deletes custom labels bound to assets",
  "description_zh": "删除资产绑定的自定义标签",
  "kebab_example": "aliyun sas delete-tag-with-uuid --tag-name abc --api-version 2018-12-03 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteTagWithUuid",
  "operation": {
    "action": "DeleteTagWithUuid",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "abc",
      "help_en": "The label name",
      "help_zh": "标签名称",
      "location": "query",
      "name": "tag_name",
      "options": [
        "--tag-name"
      ],
      "raw_name": "TagName",
      "required": true,
      "type": "string"
    },
    {
      "example": "111-xx,aa-bb",
      "help_en": "The list of server UUIDs.\nIf UuidList is specified, Security Center deletes the label only from the servers included in UuidList. If UuidList is empty, Security Center deletes the label from all servers",
      "help_zh": "服务器的UUID列表。\n如果设置了UuidList，调用本接口时云安全中心只删除UuidList涉及到的服务器上的该标签。如果UuidList为空，云安全中心会删除所有服务器上的该标签",
      "location": "query",
      "name": "uuid_list",
      "options": [
        "--uuid-list"
      ],
      "raw_name": "UuidList",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID, which is a unique identifier generated by Alibaba Cloud for this request. You can use it to troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "7E0618A9-D5EF-4220-9471-C42B5E92719F",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
