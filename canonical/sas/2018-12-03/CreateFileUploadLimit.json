{
  "camel_example": "aliyun sas CreateFileUploadLimit --region cn-hangzhou --Limit 100",
  "cmd_full_name": "sas create-file-upload-limit",
  "cmd_name": "create-file-upload-limit",
  "deprecated": false,
  "description_en": "Specifies the queries per second (QPS) limit on the files uploaded from the client",
  "description_zh": "创建客户端上传文件的QPS上限",
  "kebab_example": "aliyun sas create-file-upload-limit --limit 100 --api-version 2018-12-03 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "CreateFileUploadLimit",
  "operation": {
    "action": "CreateFileUploadLimit",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "100",
      "help_en": "The QPS limit on the files uploaded from the client. Valid values: 100 to 10000",
      "help_zh": "设置客户端上传文件的QPS限制阈值。取值范围：100~10000",
      "location": "query",
      "name": "limit",
      "options": [
        "--limit"
      ],
      "raw_name": "Limit",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "backendName": "result",
        "description_en": "result",
        "description_zh": "result",
        "name": "Result",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "CE500770-42D3-442E-9DDD-156E0F9F3B45",
            "name": "RequestId",
            "title_en": "requestId",
            "title_zh": "requestId",
            "type": "string"
          }
        },
        "title_en": "result",
        "title_zh": "result",
        "type": "object"
      }
    }
  }
}
