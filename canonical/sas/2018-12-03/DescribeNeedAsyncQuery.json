{
  "camel_example": "aliyun sas DescribeNeedAsyncQuery --region cn-hangzhou --Type suspicious",
  "cmd_full_name": "sas describe-need-async-query",
  "cmd_name": "describe-need-async-query",
  "deprecated": false,
  "description_en": "Queries whether slow queries need to be optimized",
  "description_zh": "查询是否需要做慢查询优化",
  "kebab_example": "aliyun sas describe-need-async-query --type suspicious --api-version 2018-12-03 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DescribeNeedAsyncQuery",
  "operation": {
    "action": "DescribeNeedAsyncQuery",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "suspicious",
      "help_en": "The type of the query. Valid values:\n\nsuspicious: alerts",
      "help_zh": "查询的类型。取值：\nsuspicious：安全告警",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Data": {
            "backendName": "data",
            "description_en": "Indicates whether the result is returned. Valid values:\n\ntrue\nfalse",
            "description_zh": "结果。取值：\ntrue：是\nfalse：否",
            "example": "true",
            "type": "boolean"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "5DFD6277-CC36-57F7-ACE6-F5952XXXXXXXX",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
