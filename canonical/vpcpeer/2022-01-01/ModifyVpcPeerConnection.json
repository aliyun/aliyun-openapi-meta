{
  "camel_example": "aliyun vpcpeer ModifyVpcPeerConnection --region cn-hangzhou --InstanceId 'pcc-lnk0m24khwvtkm0000'",
  "cmd_full_name": "vpcpeer modify-vpc-peer-connection",
  "cmd_name": "modify-vpc-peer-connection",
  "deprecated": false,
  "description_en": "Modifies the name or description of a VPC peering connection",
  "description_zh": "调用ModifyVpcPeerConnection接口修改VPC对等连接实例的名称或者描述信息",
  "kebab_example": "aliyun vpcpeer modify-vpc-peer-connection --instance-id pcc-lnk0m24khwvtkm0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyVpcPeerConnection",
  "operation": {
    "action": "ModifyVpcPeerConnection",
    "api_style": "RPC",
    "api_version": "2022-01-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pcc-lnk0m24khwvtkm****",
      "help_en": "The ID of the VPC peering connection",
      "help_zh": "需要修改名称或者描述信息的VPC对等连接实例ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "vpcpeername",
      "help_en": "The new name of the VPC peering connection.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`",
      "help_zh": "需要修改的VPC对等连接的名称，即修改后的名称。\n\n名称长度为1～128个字符，不能以`http://`或`https://`开头",
      "location": "formData",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "newdescription",
      "help_en": "The new description of the VPC peering connection.\n\nThe description must be 1 to 256 characters in length and cannot start with `http://` or `https://`",
      "help_zh": "需要修改的VPC对等连接的描述信息，即修改后的描述信息。\n\n描述长度为1～256个字符，不能以`http://`或`https://`开头",
      "location": "formData",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The new bandwidth of the VPC peering connection. Unit: Mbps. The value must be an integer greater than 0",
      "help_zh": "需要修改的VPC对等连接的带宽，单位：Mbps，取值范围为大于0的整数",
      "location": "formData",
      "name": "bandwidth",
      "options": [
        "--bandwidth"
      ],
      "raw_name": "Bandwidth",
      "required": false,
      "type": "int"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: Performs a dry run. The system checks the required parameters, request format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (Default): Sends a normal request. After the check passes, an HTTP 2xx status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会修改信息。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "formData",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "0c593ea1-3bea-11e9-b96b-88e9fe637760",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a unique token on your client for each request. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the request as the ClientToken. The RequestId may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "Gold",
      "help_en": "The link type.\n\nValid values: Platinum and Gold. The default value is Gold.\n\nIf you specify this parameter, make sure that you create a cross-region peering connection",
      "help_zh": "链路类型。\n\n可选值为 Platinum（铂金），Gold（金），默认为 Gold（金）。\n\n如需指定该参数，请确保您创建的是跨地域的对等连接",
      "location": "query",
      "name": "link_type",
      "options": [
        "--link-type"
      ],
      "raw_name": "LinkType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "VpcPeer",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response object",
        "description_zh": "RpcResponse<ModifyVpcPeerResponse>",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "880C99E1-449B-524A-B81F-1EC53D2A7EAC",
            "type": "string"
          }
        },
        "title_en": "RpcResponse<ModifyVpcPeerResponse>",
        "title_zh": "RpcResponse<ModifyVpcPeerResponse>",
        "type": "object"
      }
    }
  }
}
