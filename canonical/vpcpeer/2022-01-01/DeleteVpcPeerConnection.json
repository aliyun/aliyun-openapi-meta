{
  "camel_example": "aliyun vpcpeer DeleteVpcPeerConnection --region cn-hangzhou --InstanceId 'pcc-lnk0m24khwvtkm0000'",
  "cmd_full_name": "vpcpeer delete-vpc-peer-connection",
  "cmd_name": "delete-vpc-peer-connection",
  "deprecated": false,
  "description_en": "Deletes a VPC peering connection",
  "description_zh": "调用DeleteVpcPeerConnection接口删除VPC对等连接实例",
  "kebab_example": "aliyun vpcpeer delete-vpc-peer-connection --instance-id pcc-lnk0m24khwvtkm0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteVpcPeerConnection",
  "operation": {
    "action": "DeleteVpcPeerConnection",
    "api_style": "RPC",
    "api_version": "2022-01-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "pcc-lnk0m24khwvtkm****",
      "help_en": "The ID of the VPC peering connection",
      "help_zh": "需要删除的VPC对等连接的实例ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to force delete the VPC peering connection. Valid values:\n\nfalse (default): does not force delete the VPC peering connection. Delete the routes that point to the VPC peering connection.\n\ntrue: force deletes the VPC peering connection. The system deletes the routes that point to the VPC peering connection in the route table",
      "help_zh": "是否强制删除VPC对等连接。取值：\n\nfalse（默认值）：不强制删除。删除前，需要将路由表中指向VPC对等连接的路由条目删除。\n\ntrue：强制删除。系统会将VPC路由表中指向该VPC对等连接的路由条目删除",
      "location": "formData",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run but does not delete the VPC peering connection. The system checks the request for required parameters, format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): sends the request. If the request passes the check, an HTTP 2xx status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会删除VPC对等连接。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "formData",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44-001****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a value for this parameter from your client to make sure that the value is unique among different requests. The client token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the request ID as the client token. The request ID may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "VpcPeer",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "请求ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "54B48E3D-DF70-471B-AA93-08E683A1B45",
            "type": "string"
          }
        },
        "title_en": "RpcResponse<DeleteVpcPeerResponse>",
        "title_zh": "RpcResponse<DeleteVpcPeerResponse>",
        "type": "object"
      }
    }
  }
}
