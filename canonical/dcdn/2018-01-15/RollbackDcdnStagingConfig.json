{
  "camel_example": "aliyun dcdn RollbackDcdnStagingConfig --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "dcdn rollback-dcdn-staging-config",
  "cmd_name": "rollback-dcdn-staging-config",
  "deprecated": false,
  "description_en": "Rolls back the staging Dynamic Content Delivery Network (DCDN) configuration to the production version",
  "description_zh": "调用RollbackDcdnStagingConfig将模拟环境的全站加速配置回滚成生产环境配置",
  "kebab_example": "aliyun dcdn rollback-dcdn-staging-config --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RollbackDcdnStagingConfig",
  "operation": {
    "action": "RollbackDcdnStagingConfig",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name. Only one domain name is supported",
      "help_zh": "加速域名，仅支持传单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "04F0F334-1335-436C-A1D7-6C044FE73368",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
