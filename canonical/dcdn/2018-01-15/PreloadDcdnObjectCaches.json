{
  "camel_example": "aliyun dcdn PreloadDcdnObjectCaches --region cn-hangzhou --ObjectPath 'http://example.com/examplefile.txt_http://example.com/examplefile2.txt'",
  "cmd_full_name": "dcdn preload-dcdn-object-caches",
  "cmd_name": "preload-dcdn-object-caches",
  "deprecated": false,
  "description_en": "Prefetches content from the origin server to L2 Cache nodes so that users can directly hit the cache on their first visit, which reduces the load on the origin server",
  "description_zh": "调用PreloadDcdnObjectCaches将源站的内容主动预热到L2 Cache节点上，用户首次访问可直接命中缓存，缓解源站压力",
  "kebab_example": "aliyun dcdn preload-dcdn-object-caches --object-path http://example.com/examplefile.txt_http://example.com/examplefile2.txt --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "PreloadDcdnObjectCaches",
  "operation": {
    "action": "PreloadDcdnObjectCaches",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "http://example.com/examplefile.txt\nhttp://example.com/examplefile2.txt",
      "help_en": "The URLs to prefetch. The format is accelerated domain name/file to prefetch.\nSeparate multiple URLs with line breaks. Each URL can be up to 1,024 characters in length",
      "help_zh": "预热URL，格式为加速域名/预热的文件。\n多个URL之间用换行符分隔，ObjectPath的单条长度最长为1024个字符",
      "location": "query",
      "name": "object_path",
      "options": [
        "--object-path"
      ],
      "raw_name": "ObjectPath",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "domestic",
        "overseas"
      ],
      "example": "domestic",
      "help_en": "The prefetch region. Valid values:\ndomestic: the Chinese mainland only.\noverseas: global (excluding the Chinese mainland).\n\nIf you do not specify this parameter, the prefetch region defaults to the DCDN acceleration region configured for your domain name:\n\nIf the acceleration region of the domain name is Chinese Mainland Only, the prefetch region is the Chinese mainland only.\nIf the acceleration region of the domain name is Global, the prefetch region is global.\nIf the acceleration region of the domain name is Global (Excluding the Chinese Mainland), the prefetch region is global (excluding the Chinese mainland)",
      "help_zh": "预热区域，取值：\ndomestic：仅中国内地。\noverseas：全球（不包含中国内地）。\n\n如果不传该参数，默认的预热区域为您的域名所配置的DCDN加速区域。具体如下：\n\n域名的加速区域为“仅中国内地”，预热区域是仅中国内地。\n域名的加速区域为“全球”，预热区域是全球。\n域名的加速区域为“全球（不包含中国内地）”，预热区域是全球（不包含中国内地）",
      "location": "query",
      "name": "area",
      "options": [
        "--area"
      ],
      "raw_name": "Area",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to prefetch content directly to L2 nodes. Valid values:\n\ntrue: The prefetch node hierarchy must include L2 nodes.\n\nfalse (default): Only origin-layer nodes are prefetched. Origin-layer nodes can be L2 nodes or L3 nodes",
      "help_zh": "是否直接预热到L2节点。取值：\n\ntrue：代表预热的节点层级必须包含L2节点。\n\nfalse：代表仅预热回源层节点（false为默认值，回源层节点可能是L2节点，也可能是L3节点）",
      "location": "query",
      "name": "l2_preload",
      "options": [
        "--l2-preload"
      ],
      "raw_name": "L2Preload",
      "required": false,
      "type": "bool"
    },
    {
      "example": "{\n      \"Accept-Encoding\": [\n            \"gzip\"\n      ]\n}",
      "help_en": "The custom prefetch headers. Specify the value in JSON format",
      "help_zh": "支持用户自定义预热Header，用JSON串格式提交",
      "location": "query",
      "name": "with_header",
      "options": [
        "--with-header"
      ],
      "raw_name": "WithHeader",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "This parameter is used when certain rules (such as URL rewrite or parameter filtering) are configured. It specifies whether to enable hashkey query mode when executing prefetch tasks. Valid values:\nfalse: default mode. This mode is used when this parameter is not specified. The submitted URL is directly used as the hashkey of the prefetched file.\ntrue: queries the actual hashkey used by the prefetch URL based on the domain name configuration",
      "help_zh": "配置了某些规则（如 URL 重写、参数过滤等）开启使用。该参数用于控制执行预热任务时是否开启hashkey查询模式。取值范围：\nfalse：默认模式，不传参数的时候使用该模式，指的是直接使用提交的URL作为预热文件的hashkey。\ntrue：根据域名的配置来查询预热URL实际使用的hashkey",
      "location": "query",
      "name": "query_hashkey",
      "options": [
        "--query-hashkey"
      ],
      "raw_name": "QueryHashkey",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "PreloadTaskId": {
            "backendName": "data",
            "description_en": "The IDs of the prefetch tasks. Multiple task IDs are separated by commas (,). The returned task IDs are merged based on the following rules:\n\nPrefetch tasks (at URL granularity) submitted for the same domain name within the same second are merged into a single RushTaskId.\nIf more than 500 prefetch tasks (at URL granularity) are submitted for the same domain name within the same second, the tasks are merged into RushTaskIds in batches of 500",
            "description_zh": "预热返回的任务ID，多个任务ID用半角逗号（,）分隔。预热返回的任务ID会按照以下两条规则对预热任务做合并：\n\n同一个域名、同一秒钟提交的预热任务（URL颗粒度）会被合并为同一个RushTaskId。\n同一个域名、同一秒钟提交的预热任务（URL颗粒度）如果超过500条，那么会按照每500条合并为一个RushTaskId的方式处理",
            "example": "95248880",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "E5BD4B50-7A02-493A-AE0B-97B9024B4135",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
