{
  "camel_example": "aliyun dcdn StartDcdnIpaDomain --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "dcdn start-dcdn-ipa-domain",
  "cmd_name": "start-dcdn-ipa-domain",
  "deprecated": false,
  "description_en": "You can call the StartDcdnIpaDomain operation to enable a disabled accelerated domain name. This changes the status of the domain name to `online`",
  "description_zh": "调用StartDcdnIpaDomain启用状态为停用的加速域名，将DomainStatus变更为online",
  "kebab_example": "aliyun dcdn start-dcdn-ipa-domain --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "StartDcdnIpaDomain",
  "operation": {
    "action": "StartDcdnIpaDomain",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The domain name to accelerate with DCDN IPA. You can enable only one domain name at a time",
      "help_zh": "需要接入全站加速IPA的域名，仅支持单个启用",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "0AEDAF20-4DDF-4165-8750-47FF9C1929C9",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
