{
  "camel_example": "aliyun dcdn DeleteDcdnDomain --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "dcdn delete-dcdn-domain",
  "cmd_name": "delete-dcdn-domain",
  "deprecated": false,
  "description_en": "Deletes an accelerated domain name",
  "description_zh": "调用DeleteDcdnDomain删除已添加的加速域名",
  "kebab_example": "aliyun dcdn delete-dcdn-domain --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteDcdnDomain",
  "operation": {
    "action": "DeleteDcdnDomain",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name that you want to delete. You can delete only one domain name at a time",
      "help_zh": "待删除的加速域名，仅支持删除单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "0AEDAF20-4DDF-4165-8750-47FF9C1929C9",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
