{
  "camel_example": "aliyun dcdn ModifyDcdnWafPolicyDomains --region cn-hangzhou --PolicyId 1000001",
  "cmd_full_name": "dcdn modify-dcdn-waf-policy-domains",
  "cmd_name": "modify-dcdn-waf-policy-domains",
  "deprecated": false,
  "description_en": "You can call the ModifyDcdnWafPolicyDomains operation to modify the domain names that are attached to a specific mitigation policy",
  "description_zh": "调用ModifyDcdnWafPolicyDomains修改指定防护策略绑定的域名",
  "kebab_example": "aliyun dcdn modify-dcdn-waf-policy-domains --policy-id 1000001 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDcdnWafPolicyDomains",
  "operation": {
    "action": "ModifyDcdnWafPolicyDomains",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "1000001",
      "help_en": "The ID of the mitigation policy. You can specify only one ID",
      "help_zh": "防护策略ID，仅支持单个输入",
      "location": "formData",
      "name": "policy_id",
      "options": [
        "--policy-id"
      ],
      "raw_name": "PolicyId",
      "required": true,
      "type": "int"
    },
    {
      "example": "0",
      "help_en": "The binding method. Valid values:\n\n0: Replace\n1: Add\nIf you do not specify this parameter, the existing domain names are replaced.\n\nThis parameter takes effect only when you specify BindDomains. If a domain name in BindDomains is already bound to the default policy, the `Policy.DefaultAndCustom.BindToSameDomain` error is returned.\nThis parameter does not take effect if the specified policy is the default policy. In this case, only the replace method is used",
      "help_zh": "绑定方式，取值：\n\n0：替换\n1：添加\n不填默认替换。\n\n仅BindDomains有值时生效。当BindDomains中存在域名已绑定到默认策略上时，返回错误`Policy.DefaultAndCustom.BindToSameDomain`。\n当指定策略为默认策略时不生效（仅替换）",
      "location": "formData",
      "name": "method",
      "options": [
        "--biz-method"
      ],
      "raw_name": "Method",
      "required": false,
      "type": "int"
    },
    {
      "example": "example.com,example2.com",
      "help_en": "The domain names to bind to the mitigation policy. You can specify up to 50 domain names. Separate multiple domain names with a comma (,).\n\nYou must specify either BindDomains or UnbindDomains",
      "help_zh": "需绑定指定防护策略的域名，最多输入50个，多个域名用英文逗号（,）分隔。\n\nBindDomains和UnbindDomains字段二选一",
      "location": "formData",
      "name": "bind_domains",
      "options": [
        "--bind-domains"
      ],
      "raw_name": "BindDomains",
      "required": false,
      "type": "string"
    },
    {
      "example": "example3.com",
      "help_en": "The domain names to unbind from the mitigation policy. You can specify up to 50 domain names. Separate multiple domain names with a comma (,).\n\nYou must specify either BindDomains or UnbindDomains",
      "help_zh": "需解绑指定防护策略的域名，最多输入50个，多个域名用英文逗号（,）分隔。\nBindDomains和UnbindDomains字段二选一",
      "location": "formData",
      "name": "unbind_domains",
      "options": [
        "--unbind-domains"
      ],
      "raw_name": "UnbindDomains",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "CB1A380B-09F0-41BB-2B35-72F8FD6DA2FE",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
