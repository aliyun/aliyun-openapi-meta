{
  "camel_example": "aliyun dcdn BatchStartDcdnDomain --region cn-hangzhou --DomainNames 'example.com'",
  "cmd_full_name": "dcdn batch-start-dcdn-domain",
  "cmd_name": "batch-start-dcdn-domain",
  "deprecated": false,
  "description_en": "Enables multiple disabled accelerated domain names in a batch. This operation changes the DomainStatus to online",
  "description_zh": "批量启用状态为停用的加速域名，将DomainStatus变更为online",
  "kebab_example": "aliyun dcdn batch-start-dcdn-domain --domain-names example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "BatchStartDcdnDomain",
  "operation": {
    "action": "BatchStartDcdnDomain",
    "api_style": "RPC",
    "api_version": "2018-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain names. Separate multiple domain names with commas (,)",
      "help_zh": "加速域名，多个域名用半角逗号（,）分隔",
      "location": "query",
      "name": "domain_names",
      "options": [
        "--domain-names"
      ],
      "raw_name": "DomainNames",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "dcdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "0AEDAF20-4DDF-4165-8750-47FF9C1929C9",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
