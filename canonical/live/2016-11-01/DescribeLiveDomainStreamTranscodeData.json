{
  "camel_example": "aliyun live DescribeLiveDomainStreamTranscodeData --region cn-hangzhou --EndTime '2017-12-10T22:00:00Z' --StartTime '2017-12-10T20:00:00Z' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "live describe-live-domain-stream-transcode-data",
  "cmd_name": "describe-live-domain-stream-transcode-data",
  "deprecated": false,
  "description_en": "Queries the transcoding usage data of a domain name by calling DescribeLiveDomainStreamTranscodeData",
  "description_zh": "调用DescribeLiveDomainStreamTranscodeData查询域名转码用量数据",
  "kebab_example": "aliyun live describe-live-domain-stream-transcode-data --biz-region-id cn-hangzhou --start-time 2017-12-10T20:00:00Z --end-time 2017-12-10T22:00:00Z --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeLiveDomainStreamTranscodeData",
  "operation": {
    "action": "DescribeLiveDomainStreamTranscodeData",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The streaming domain of the streamer to query.\n\nYou can query a single domain name or multiple domain names at a time. Separate multiple domain names with commas (,).\nIf this parameter is left empty, the merged data of all live streaming domain names is returned by default.\nWhen you specify DomainName, make sure that the specified domain name is a live streaming domain name and that the user calling this operation has the permissions to operate on the specified domain name",
      "help_zh": "需要查询的主播流域名。\n\n支持单个或批量域名查询。批量查询时多个域名用英文逗号（,）分隔。\n若参数为空，默认返回所有直播域名合并后的数据。\n传入DomainName时，请保证传入域名为直播域名，同时调用本接口的用户拥有传入域名的操作权限",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": false,
      "type": "string"
    },
    {
      "example": "2017-12-10T20:00:00Z",
      "help_en": "The beginning of the time range to query. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC.\n\nThe minimum data time granularity is 1 hour.\nIf this parameter is left empty, data from the last 24 hours is read by default",
      "help_zh": "查询起始时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\n\n最小数据时间粒度为1小时。\n不填默认读取过去24小时数据",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "2017-12-10T22:00:00Z",
      "help_en": "The end of the time range to query. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC",
      "help_zh": "查询结束时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "min",
        "sec"
      ],
      "example": "min",
      "help_en": "The time precision of the query. Valid values:\nmin (default): in minutes.\nsec: in seconds",
      "help_zh": "查询的时间精度。取值：\nmin（默认）：以分钟为单位。\nsec：以秒为单位",
      "location": "query",
      "name": "precision",
      "options": [
        "--precision"
      ],
      "raw_name": "Precision",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "3600",
        "86400"
      ],
      "example": "3600",
      "help_en": "The time granularity of the query. Unit: seconds. Valid values:\n\n3600: by hour.\n86400: by day.\n\nIf this parameter is left empty, the default granularity is by hour",
      "help_zh": "查询的时间粒度。单位：秒。取值：\n\n3600：按小时粒度。\n86400：按天粒度。\n\n不填默认按小时",
      "location": "query",
      "name": "interval",
      "options": [
        "--interval"
      ],
      "raw_name": "Interval",
      "required": false,
      "type": "string"
    },
    {
      "example": "domain",
      "help_en": "The grouping key. Valid values:\n\ndomain: domain name. If the Split (grouping key) parameter is set to domain, the Domain response parameter takes effect.\nregion: live center region. If the Split (grouping key) parameter is set to region, the Region response parameter takes effect.\ntranscode_type: transcoding type. If the Split (grouping key) parameter is set to transcode_type, the TanscodeType response parameter takes effect.\nresolution: resolution. If the Split (grouping key) parameter is set to resolution, the Resolution response parameter takes effect.\nfps: frame rate. If the Split (grouping key) parameter is set to fps, the Fps response parameter takes effect.\n\nYou can specify one or more values. Separate multiple values with commas (,).\n\nDefault value: `domain,region,transcode_type,resolution,fps`, which means all grouping keys are applied",
      "help_zh": "分组键。取值：\n\ndomain：域名。如果此参数Split（分组键）取值为domain时，返回参数Domain才生效。\nregion：直播中心地域。如果此参数Split（分组键）取值为region时，返回参数Region才生效。\ntranscode_type：转码类型。如果此参数Split（分组键）取值为transcode_type时，返回参数TanscodeType才生效。\nresolution：分辨率。如果此参数Split（分组键）取值为resolution时，返回参数Resolution才生效。\nfps：帧率。如果此参数Split（分组键）取值为fps时，返回参数Fps才生效。\n\n可选一个，也可多选，用英文逗号（,）分隔。\n\n默认为`domain,region,transcode_type,resolution,fps`，即全部进行分组",
      "location": "query",
      "name": "split",
      "options": [
        "--split"
      ],
      "raw_name": "Split",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "3C6CCEC4-6B88-4D4A-93E4-D47B******",
            "type": "string"
          },
          "TranscodeDataList": {
            "backendName": "data.content",
            "itemName": "TranscodeData",
            "items": {
              "properties": {
                "Domain": {
                  "backendName": "domain",
                  "type": "string"
                },
                "Duration": {
                  "backendName": "duration",
                  "format": "int32",
                  "type": "integer"
                },
                "Fps": {
                  "backendName": "fps",
                  "type": "string"
                },
                "Region": {
                  "backendName": "region",
                  "type": "string"
                },
                "Resolution": {
                  "backendName": "resolution",
                  "type": "string"
                },
                "TanscodeType": {
                  "backendName": "transcode_type",
                  "type": "string"
                },
                "TimeStamp": {
                  "backendName": "time_stp",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "nullToEmpty": true,
            "type": "array"
          }
        },
        "type": "object"
      }
    }
  }
}
