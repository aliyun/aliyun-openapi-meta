{
  "camel_example": "aliyun live RealTimeRecordCommand --region cn-hangzhou --StreamName liveStream0000 --Command start --DomainName 'example.com' --RegionId 'cn-hangzhou' --AppName liveApp0000",
  "cmd_full_name": "live real-time-record-command",
  "cmd_name": "real-time-record-command",
  "deprecated": false,
  "description_en": "Performs on-demand manual recording. For example, dynamically starts or stops recording",
  "description_zh": "按需完成手动录制。例如动态启动录制、动态停止录制",
  "kebab_example": "aliyun live real-time-record-command --biz-region-id cn-hangzhou --command start --domain-name example.com --app-name liveApp0000 --stream-name liveStream0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RealTimeRecordCommand",
  "operation": {
    "action": "RealTimeRecordCommand",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "start",
      "help_en": "The operation action. Valid values:\n\nstart: forcibly starts recording. This must be called as the first operation and cannot be called again before stopping.\nstop: forcibly pauses recording. After the stream interruption delay (180 seconds by default) elapses, a recording is generated. This can only be called after start or restart. To generate the file immediately after calling stop, call cancel_delay.\ncancel_delay: immediately terminates the wait and generates a recording, completely stopping recording. This must be called after stop to generate the file in advance.\nrestart: forcibly restarts recording. If recording is in progress before restart, a file is immediately generated. This can only be called when the task is in the started or stopped state",
      "help_zh": "操作行为。取值：\n\nstart：强制启动录制。必须作为首次操作调用，不可在未停止前重复调用。\nstop：强制暂停录制，断流延迟时间（默认180秒）到达后，生成录制文件。 仅可在 start 或 restart 后调用；调用后如需立即出文件，可以调用 cancel_delay。\ncancel_delay：立即终止等待并生成录制文件，完全停止录制。需要在 stop 后调用以提前生成文件。\nrestart：强制重新开始录制。如果restart之前在录制，立即生成文件。仅可在任务启动或停止状态下调用",
      "location": "query",
      "name": "command",
      "options": [
        "--command"
      ],
      "raw_name": "Command",
      "required": true,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The streamer's streaming domain",
      "help_zh": "主播流域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    },
    {
      "example": "liveApp****",
      "help_en": "The name of the application to which the stream belongs. You can view the AppName on the https://help.aliyun.com/document_detail/197397.html page",
      "help_zh": "播流所属应用名称。您可在https://help.aliyun.com/document_detail/197397.html页面查看AppName",
      "location": "query",
      "name": "app_name",
      "options": [
        "--app-name"
      ],
      "raw_name": "AppName",
      "required": true,
      "type": "string"
    },
    {
      "example": "liveStream****",
      "help_en": "The stream name. Make sure that the StreamName is correct. You can view the StreamName on the https://help.aliyun.com/document_detail/197397.html page.\n\nThis operation supports only single-stream operations and does not support wildcards",
      "help_zh": "播流名称。请确保StreamName正确，您可在https://help.aliyun.com/document_detail/197397.html页面查看StreamName。\n\n该接口仅支持单条流操作，不支持通配符",
      "location": "query",
      "name": "stream_name",
      "options": [
        "--stream-name"
      ],
      "raw_name": "StreamName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "16A96B9A-F203-4EC5-8E43-CB92E68F4CD8",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
