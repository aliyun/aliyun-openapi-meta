{
  "camel_example": "aliyun live DeleteLiveDelayConfig --region cn-hangzhou --App liveApp0000 --Stream liveStream0000 --RegionId 'cn-hangzhou' --Domain 'example.com'",
  "cmd_full_name": "live delete-live-delay-config",
  "cmd_name": "delete-live-delay-config",
  "deprecated": false,
  "description_en": "Deletes a stream delay configuration",
  "description_zh": "调用DeleteLiveDelayConfig删除直播延播配置",
  "kebab_example": "aliyun live delete-live-delay-config --biz-region-id cn-hangzhou --domain example.com --app liveApp0000 --stream liveStream0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteLiveDelayConfig",
  "operation": {
    "action": "DeleteLiveDelayConfig",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The streaming domain",
      "help_zh": "主播流域名",
      "location": "query",
      "name": "domain",
      "options": [
        "--domain"
      ],
      "raw_name": "Domain",
      "required": true,
      "type": "string"
    },
    {
      "example": "liveApp****",
      "help_en": "The AppName of the live stream. You can specify an asterisk (*) to match all AppNames under the domain name",
      "help_zh": "直播流所属应用名称。支持通配符*，代表该域名下所有的AppName",
      "location": "query",
      "name": "app",
      "options": [
        "--app"
      ],
      "raw_name": "App",
      "required": true,
      "type": "string"
    },
    {
      "example": "liveStream****",
      "help_en": "The name of the live stream. You can use the wildcard (*) to specify all StreamNames under the AppName",
      "help_zh": "直播流流名称。支持通配符*，代表该AppName下所有的StreamName",
      "location": "query",
      "name": "stream",
      "options": [
        "--stream"
      ],
      "raw_name": "Stream",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "16A96B9A-F203-4EC5-8E43-CB92E68F4CD8",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
