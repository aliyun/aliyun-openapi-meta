{
  "camel_example": "aliyun live UpdateRtcMPUEventSub --region cn-hangzhou --AppId yourAppId",
  "cmd_full_name": "live update-rtc-mpu-event-sub",
  "cmd_name": "update-rtc-mpu-event-sub",
  "deprecated": false,
  "description_en": "Updates a subscription to mixed-stream relay events",
  "description_zh": "更新混流转推事件订阅",
  "kebab_example": "aliyun live update-rtc-mpu-event-sub --app-id yourAppId --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateRtcMPUEventSub",
  "operation": {
    "action": "UpdateRtcMPUEventSub",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "yourAppId",
      "help_en": "The ID of the application.\n\nThe ID can be up to 64 characters in length and can contain letters, digits, underscores (_), and hyphens (-)",
      "help_zh": "订阅的应用ID。\n应用ID由大小写字母、数字、下划线、短划线（-）组成，最大64字符",
      "location": "query",
      "name": "app_id",
      "options": [
        "--app-id"
      ],
      "raw_name": "AppId",
      "required": true,
      "type": "string"
    },
    {
      "example": "yourChannelIds",
      "help_en": "The ID of the channel to which you want to send mixed-stream relay event callbacks. Separate multiple channel IDs with commas (,).\n\n\n\nIf you leave this parameter empty, you are subscribed to all mixed-stream relay events submitted in the application.\n\nYou cannot specify duplicate channel IDs. You can specify up to 20 channel IDs in each call.\n\nThe ID can be up to 64 characters in length and contain letters, digits, underscores (_), and hyphens (-)",
      "help_zh": "指定接收回调的混流任务的频道ID，可以同时填多个频道ID，多个频道ID之间采用英文逗号“,”隔开。\n如果不填，则默认接收该AppId下的所有混流转推的回调事件。\n同时填多个频道ID时，不能填写重复的频道ID，最多同时填20个频道ID。\n每个频道ID由大小写字母、数字、下划线、短划线（-）组成，最大64字符",
      "location": "query",
      "name": "channel_ids",
      "options": [
        "--channel-ids"
      ],
      "raw_name": "ChannelIds",
      "required": false,
      "type": "string"
    },
    {
      "example": "http://****.com/callback",
      "help_en": "The callback URL.\n\nYou can use headers such as HTTP and HTTPS in callback URLs. The URL can be up to 2,083 characters and contain letters, digits, and the following special characters: - _ ? % = # . / +",
      "help_zh": "回调地址。\n回调地址协议头为HTTP、HTTPS等，仅可包含以下字符：a-z 、A-Z、0-9、-、_、?、%、=、#、.、/ 和 +，不超过2083个字符",
      "location": "query",
      "name": "callback_url",
      "options": [
        "--callback-url"
      ],
      "raw_name": "CallbackUrl",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "content.requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "******3B-0E1A-586A-AC29-742247******",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
