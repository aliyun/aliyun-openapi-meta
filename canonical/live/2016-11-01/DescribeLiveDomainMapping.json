{
  "camel_example": "aliyun live DescribeLiveDomainMapping --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "live describe-live-domain-mapping",
  "cmd_name": "describe-live-domain-mapping",
  "deprecated": false,
  "description_en": "Retrieves the mapping between ingest domains, main streaming domains, and sub-streaming domains",
  "description_zh": "查询推流域名、主播流域名和子播流域名映射",
  "kebab_example": "aliyun live describe-live-domain-mapping --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeLiveDomainMapping",
  "operation": {
    "action": "DescribeLiveDomainMapping",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "GET",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The domain that you want to query. Valid types:\n\nIngest domain\n\nMain streaming domain\n\nSub-streaming domain",
      "help_zh": "您要查询的域名。域名可以是以下类型：\n\n推流域名。\n\n主播流域名。\n\n子播流域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "LiveDomainModels": {
            "backendName": "data.content",
            "itemName": "LiveDomainModel",
            "items": {
              "properties": {
                "DomainName": {
                  "backendName": "domain",
                  "type": "string"
                },
                "Type": {
                  "backendName": "type",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "type": "array"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "16A96B9A-F203-4EC5-8E43-CB92E68F6CD8",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
