{
  "camel_example": "aliyun live DisableLiveRealtimeLogDelivery --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "live disable-live-realtime-log-delivery",
  "cmd_name": "disable-live-realtime-log-delivery",
  "deprecated": false,
  "description_en": "Call DisableLiveRealtimeLogDelivery to pause real-time log delivery for a domain name",
  "description_zh": "调用DisableLiveRealtimeLogDelivery暂停域名实时日志投递",
  "kebab_example": "aliyun live disable-live-realtime-log-delivery --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DisableLiveRealtimeLogDelivery",
  "operation": {
    "action": "DisableLiveRealtimeLogDelivery",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "GET",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The streaming domain for which to pause real-time log delivery. To specify multiple domain names, separate them with commas (,)",
      "help_zh": "暂停实时日志投递的播流域名。\n多个域名之间用英文逗号（,）分隔",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "9732E117-8A37-49FD-A36F-ABBB87556CA7",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
