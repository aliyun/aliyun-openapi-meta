{
  "camel_example": "aliyun ons OnsGroupConsumerUpdate --region cn-hangzhou --ReadEnable true --GroupId GID_test_groupId",
  "cmd_full_name": "ons ons-group-consumer-update",
  "cmd_name": "ons-group-consumer-update",
  "deprecated": false,
  "description_en": "Sets the message read permission for the consumer cluster that corresponds to a specified group ID. This operation is typically used to prevent a specific consumer group from reading messages",
  "description_zh": "配置指定Group ID对应的消费集群的消息读取权限。本接口一般用于禁止特定Group ID读取消息的场景",
  "kebab_example": "aliyun ons ons-group-consumer-update --group-id GID_test_groupId --read-enable true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "OnsGroupConsumerUpdate",
  "operation": {
    "action": "OnsGroupConsumerUpdate",
    "api_style": "RPC",
    "api_version": "2019-02-14",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "GID_test_groupId",
      "help_en": "The group ID to configure",
      "help_zh": "需配置的Group ID。 ",
      "location": "query",
      "name": "group_id",
      "options": [
        "--group-id"
      ],
      "raw_name": "GroupId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "true",
      "help_en": "Specifies whether the consumer cluster for the group ID has permission to read messages. Valid values:\n\ntrue: The consumer cluster has permission to read messages.\n\nfalse: The consumer cluster does not have permission to read messages.\n\nDefault value: true",
      "help_zh": "设置Group ID对应的消费者集群是否有读取消息权限。取值说明如下：\n\ntrue：指定Group ID对应的消费者集群有读消息权限。\n\nfalse：指定Group ID对应的消费者集群没有读消息权限。\n\n默认值为true",
      "location": "query",
      "name": "read_enable",
      "options": [
        "--read-enable"
      ],
      "raw_name": "ReadEnable",
      "required": true,
      "type": "bool"
    },
    {
      "doc_required": true,
      "example": "MQ_INST_111111111111_DOxxxxxx",
      "help_en": "The ID of the instance to which the group ID belongs",
      "help_zh": "需配置的Group ID所对应的实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ons",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request. This is a common parameter. Each request has a unique ID. You can use this ID to troubleshoot and locate issues",
            "description_zh": "公共参数，每个请求的ID都是唯一的，可用于排查和定位问题",
            "example": "A07E3902-B92E-44A6-B6C5-6AA111111****",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
