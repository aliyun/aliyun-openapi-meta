{
  "camel_example": "aliyun sls DELETE /resources/sls.alert.user/records --region cn-hangzhou --ids 'record-001,record-002' --header \"Content-Type=application/json\"",
  "cmd_full_name": "sls delete-resource-record",
  "cmd_name": "delete-resource-record",
  "deprecated": false,
  "description_en": "Deletes resource records in batches",
  "description_zh": "批量删除资源记录",
  "kebab_example": "aliyun sls delete-resource-record --resource-name sls.alert.user --ids record-001,record-002 --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "DeleteResourceRecord",
  "operation": {
    "action": "DeleteResourceRecord",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/resources/{resourceName}/records"
  },
  "parameters": [
    {
      "example": "sls.alert.user",
      "help_en": "The resource name",
      "help_zh": "资源名称",
      "location": "path",
      "max_length": "128",
      "min_length": "3",
      "name": "resource_name",
      "options": [
        "--resource-name"
      ],
      "raw_name": "resourceName",
      "required": true,
      "type": "string"
    },
    {
      "example": "record-001,record-002",
      "help_en": "The IDs of the records to delete. Separate multiple IDs with commas (,). You can specify up to 200 IDs at a time",
      "help_zh": "要删除的记录 ID，多个 ID 使用半角逗号分隔，一次最多 200 个",
      "location": "query",
      "name": "ids",
      "options": [
        "--ids"
      ],
      "raw_name": "ids",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/resources/[resourceName]/records",
  "product_code": "Sls",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "headers": {
        "x-log-requestid": {
          "schema": {
            "title_en": "服务端产生的请求唯一 ID。",
            "title_zh": "服务端产生的请求唯一 ID。",
            "type": "string"
          }
        }
      }
    }
  }
}
