{
  "camel_example": "aliyun sls GET /logstores/ali-test-logstore/consumergroups --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "sls list-consumer-group",
  "cmd_name": "list-consumer-group",
  "components": {
    "schemas": {
      "ConsumerGroup": {
        "description_en": "The data structure of the consumer group",
        "description_zh": "消费组数据结构",
        "properties": {
          "name": {
            "description_en": "The name of the consumer group",
            "description_zh": "消费组名称",
            "example": "test-consumer-group",
            "title": "消费者名称。",
            "type": "string"
          },
          "order": {
            "description_en": "Specifies whether to consume data in sequence. Valid values:\n\ntrue: If a shard is split, the data in the original shard is consumed first. Then, the data in the new shards is consumed at the same time. If shards are merged, the data in the original shards is consumed first. Then, the data in the new shard is consumed.\nfalse: The data in all shards is consumed at the same time. If a new shard is generated after a shard is split or shards are merged, the data in the new shard is immediately consumed",
            "description_zh": "是否按顺序消费。\n\ntrue：在Shard中按顺序消费。Shard分裂后，先消费原Shard中数据，然后同时消费由该Shard分裂的Shard中数据。Shard合并后，先消费原Shard中数据，然后消费由原Shard合并后的新Shard中数据。\nfalse：不按顺序消费。即所有同时开始消费，Shard分裂或者合并后产生的新Shard，也会立即消费",
            "example": "false",
            "title": "是否有序消费",
            "type": "boolean"
          },
          "timeout": {
            "description_en": "The timeout period. If Simple Log Service does not receive heartbeats from a consumer within the timeout period, Simple Log Service deletes the consumer. Unit: seconds",
            "description_zh": "超时时间。在超时时间段内没有收到心跳，消费者将被删除。单位：秒",
            "example": "300",
            "format": "int32",
            "title": "消费超时时长，单位为秒",
            "type": "integer"
          }
        },
        "title": "Consumer Group",
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Queries all consumer groups of a Logstore",
  "description_zh": "查询指定Logstore的所有消费组",
  "kebab_example": "aliyun sls list-consumer-group --project ali-test-project --logstore ali-test-logstore --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "ListConsumerGroup",
  "operation": {
    "action": "ListConsumerGroup",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/logstores/{logstore}/consumergroups"
  },
  "parameters": [
    {
      "example": "ali-test-project",
      "help_en": "The name of the project",
      "help_zh": "Project名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "ali-test-logstore",
      "help_en": "The name of the Logstore",
      "help_zh": "Logstore名称",
      "location": "path",
      "name": "logstore",
      "options": [
        "--logstore"
      ],
      "raw_name": "logstore",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/logstores/[logstore]/consumergroups",
  "product_code": "Sls",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The consumer groups",
        "description_zh": "消费组列表",
        "example": "[{''name'': ''test-consumer-group'', ''timeout'': 30, ''order'': False}]",
        "items": {
          "$ref": "#/components/schemas/ConsumerGroup",
          "description_en": "The information about the consumer group",
          "description_zh": "消费组结构体",
          "example": "test-consumer-group"
        },
        "type": "array"
      }
    }
  }
}
