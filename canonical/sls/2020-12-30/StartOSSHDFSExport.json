{
  "camel_example": "aliyun sls PUT /osshdfsexports/export-osshdfs-1234567890-123456?action=START --project my-project --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "sls start-oss-hdfs-export",
  "cmd_name": "start-oss-hdfs-export",
  "deprecated": false,
  "description_en": "Starts an OSS-HDFS data shipping job",
  "description_zh": "启动指定的OSS-HDFS投递任务",
  "kebab_example": "aliyun sls start-oss-hdfs-export --project my-project --oss-export-name export-osshdfs-1234567890-123456 --region cn-hangzhou",
  "method": "PUT",
  "multi_version": false,
  "name": "StartOSSHDFSExport",
  "operation": {
    "action": "StartOSSHDFSExport",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "PUT",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/osshdfsexports/{ossExportName}?action=START"
  },
  "parameters": [
    {
      "example": "my-project",
      "help_en": "The name of the project",
      "help_zh": "Project名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "export-osshdfs-1234567890-123456\n",
      "help_en": "The unique name of the job. Make sure that the job exists",
      "help_zh": "已存在的唯一任务名称",
      "location": "path",
      "name": "oss_export_name",
      "options": [
        "--oss-export-name"
      ],
      "raw_name": "ossExportName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/osshdfsexports/[ossExportName]?action=START",
  "product_code": "Sls",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "headers": {
        "Connection": {
          "schema": {
            "title_en": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "title_zh": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "type": "string"
          }
        },
        "Content-Length": {
          "schema": {
            "title_en": "响应内容长度。",
            "title_zh": "响应内容长度。",
            "type": "string"
          }
        },
        "Content-Type": {
          "schema": {
            "title_en": "返回的响应体的内容格式。",
            "title_zh": "返回的响应体的内容格式。",
            "type": "string"
          }
        },
        "Date": {
          "schema": {
            "title_en": "返回响应的时间。",
            "title_zh": "返回响应的时间。",
            "type": "string"
          }
        },
        "Server": {
          "schema": {
            "title_en": "服务器名称。",
            "title_zh": "服务器名称。",
            "type": "string"
          }
        },
        "x-log-requestid": {
          "schema": {
            "title_en": "服务端产生的标识，该请求的唯一ID。",
            "title_zh": "服务端产生的标识，该请求的唯一ID。",
            "type": "string"
          }
        }
      }
    }
  }
}
