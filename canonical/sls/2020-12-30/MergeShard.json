{
  "camel_example": "aliyun sls POST /logstores/ali-test-logstore/shards/3?action=merge --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "sls merge-shard",
  "cmd_name": "merge-shard",
  "components": {
    "schemas": {
      "shard": {
        "description_en": "The data structure of the shard",
        "description_zh": "Shard数据结构",
        "properties": {
          "createTime": {
            "description_en": "The time at which the shard was created. The value is a UNIX timestamp representing the number of seconds that have elapsed since January 1, 1970, 00:00:00 UTC",
            "description_zh": "分区的创建时间。UNIX时间戳格式，表示从1970-1-1 00:00:00 UTC计算起的秒数",
            "example": "1524222931",
            "format": "int32",
            "title": "Shard的创建时间。Unix时间戳格式，表示从1970-1-1 00:00:00 UTC计算起的秒数。",
            "type": "integer"
          },
          "exclusiveEndKey": {
            "description_en": "The end of the MD5 hash range",
            "description_zh": "分区结束的Key值",
            "example": "8000000000000000000000000000000",
            "title": "指定Shard范围的结束值，Shard范围中不包含该值。即 shard 包含MD5值在 [inclusiveBeginKey, exclusiveEndKey) 之间的日志。",
            "type": "string"
          },
          "inclusiveBeginKey": {
            "description_en": "The start of the MD5 hash range. The value is included in the MD5 hash range of the shard",
            "description_zh": "Shard起始的Key值，在Shard MD5范围中包含该值",
            "example": "00000000000000000000000000000000",
            "title": "指定Shard范围的起始值，Shard范围中包含该值。即 shard 包含MD5值在 [inclusiveBeginKey, exclusiveEndKey) 之间的日志。",
            "type": "string"
          },
          "shardID": {
            "description_en": "shard id",
            "description_zh": "shard id",
            "example": "0",
            "format": "int32",
            "title": "shard id",
            "type": "integer"
          },
          "status": {
            "description_en": "The status of the shard. After you create a shard, the shard enters the readwrite state. If you split a shard or merge shards, the shard status changes to readonly. The newly generated shards are in the readwrite state. The status of a shard does not affect the performance of read operations that are performed on the shard. Data can be written to the shards that are in the readwrite state, but data cannot be written to the shards that are in the readonly state. Valid values:\n\nreadwrite\nreadonly",
            "description_zh": "Shard状态。创建Shard时，所有Shard状态均为readwrite状态。执行分裂或合并操作后，Shard状态变更为readonly，并生成新的readwrite状态的Shard。Shard状态不影响其数据读取的性能。readwrite状态的Shard可保证数据写入性能，readonly状态的Shard不提供数据写入服务。取值包括：\n\nreadwrite（读写）\nreadonly（只读）",
            "example": "readwrite",
            "title": "shard 的读写状态，readwrite 或者 readonly。",
            "type": "string"
          }
        },
        "title": "A short description of struct",
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Merges adjacent read/write shards",
  "description_zh": "合并相邻位置的读写Shard",
  "kebab_example": "aliyun sls merge-shard --project ali-test-project --logstore ali-test-logstore --shard 3 --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "MergeShard",
  "operation": {
    "action": "MergeShard",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": "/logstores/{logstore}/shards/{shard}?action=merge"
  },
  "parameters": [
    {
      "example": "ali-test-project",
      "help_en": "The project name",
      "help_zh": "Project名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "ali-test-logstore",
      "help_en": "The logstore name",
      "help_zh": "Logstore名称",
      "location": "path",
      "name": "logstore",
      "options": [
        "--logstore"
      ],
      "raw_name": "logstore",
      "required": true,
      "type": "string"
    },
    {
      "example": "3",
      "help_en": "The shard ID",
      "help_zh": "Shard ID",
      "location": "path",
      "name": "shard",
      "options": [
        "--shard"
      ],
      "raw_name": "shard",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "/logstores/[logstore]/shards/[shard]?action=merge",
  "product_code": "Sls",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "headers": {
        "Connection": {
          "schema": {
            "title_en": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "title_zh": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "type": "string"
          }
        },
        "Content-Length": {
          "schema": {
            "title_en": "响应内容长度。",
            "title_zh": "响应内容长度。",
            "type": "string"
          }
        },
        "Content-Type": {
          "schema": {
            "title_en": "返回的响应体的内容格式。",
            "title_zh": "返回的响应体的内容格式。",
            "type": "string"
          }
        },
        "Date": {
          "schema": {
            "title_en": "返回响应的时间。",
            "title_zh": "返回响应的时间。",
            "type": "string"
          }
        },
        "Server": {
          "schema": {
            "title_en": "服务器名称。",
            "title_zh": "服务器名称。",
            "type": "string"
          }
        },
        "x-log-requestid": {
          "schema": {
            "title_en": "服务端产生的标识，该请求的唯一ID。",
            "title_zh": "服务端产生的标识，该请求的唯一ID。",
            "type": "string"
          }
        }
      },
      "schema": {
        "description_en": "The data structure of the shard",
        "description_zh": "Shard数据结构",
        "items": {
          "$ref": "#/components/schemas/shard",
          "description_en": "An array that consists of three shard elements. The first element is the shard after the merge. The other two elements are the shards before the merge",
          "description_zh": "包含3个Shard元素组成的数组，第一个Shard为合并之后的Shard，后两个为合并之前的Shard"
        },
        "type": "array"
      }
    }
  }
}
