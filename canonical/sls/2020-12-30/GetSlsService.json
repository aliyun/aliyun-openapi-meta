{
  "camel_example": "aliyun sls GET /slsservice --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "sls get-sls-service",
  "cmd_name": "get-sls-service",
  "components": {
    "schemas": {
      "serviceStatus": {
        "description_en": "Activation status of Simple Log Service",
        "description_zh": "日志服务开通状态",
        "properties": {
          "enabled": {
            "description_en": "Indicates whether the service is activated",
            "description_zh": "是否开通",
            "example": "false",
            "title": "是否开通",
            "type": "boolean"
          },
          "status": {
            "description_en": "Current status.\n\nOpened: The service is activated.\nNotExist: The service does not exist.\nOpening: The service is activating.\nClosed: The service has overdue payments and is disabled",
            "description_zh": "当前状态。\nOpened：已开通。\nNotExist：不存在。\nOpening：正在开通。\nClosed：已欠费禁用",
            "enumValueTitles": {
              "Closed": "Closed",
              "NotExist": "NotExist",
              "Opened": "Opened",
              "Opening": "Opening"
            },
            "example": "NotExist",
            "title": "当前状态",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Call the GetSlsService operation to retrieve the activation status of Simple Log Service. The endpoint must be China (Shanghai) or Singapore",
  "description_zh": "调用GetSlsService接口获取日志服务的开通状态。服务地址只能是华东2（上海）或新加坡",
  "kebab_example": "aliyun sls get-sls-service --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "GetSlsService",
  "operation": {
    "action": "GetSlsService",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/slsservice"
  },
  "parameters": [],
  "pathPattern": "/slsservice",
  "product_code": "Sls",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "headers": {
        "Connection": {
          "schema": {
            "title_en": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "title_zh": "是否长链接。取值包括：  close：不是长链接，则每个HTTP请求都会重新建立TCP连接。 keep-alive：长链接，TCP连接建立后保持连接状态，节省连接所需时间和带宽。",
            "type": "string"
          }
        },
        "Content-Length": {
          "schema": {
            "title_en": "响应内容长度。",
            "title_zh": "响应内容长度。",
            "type": "string"
          }
        },
        "Content-Type": {
          "schema": {
            "title_en": "返回的响应体的内容格式。",
            "title_zh": "返回的响应体的内容格式。",
            "type": "string"
          }
        },
        "Date": {
          "schema": {
            "title_en": "响应内容长度。",
            "title_zh": "响应内容长度。",
            "type": "string"
          }
        },
        "Server": {
          "schema": {
            "title_en": "返回响应的时间。",
            "title_zh": "返回响应的时间。",
            "type": "string"
          }
        },
        "x-log-requestid": {
          "schema": {
            "title_en": "服务端产生的标识，该请求的唯一ID。",
            "title_zh": "服务端产生的标识，该请求的唯一ID。",
            "type": "string"
          }
        }
      },
      "schema": {
        "$ref": "#/components/schemas/serviceStatus",
        "description_en": "The response body",
        "description_zh": "响应消息体"
      }
    }
  }
}
