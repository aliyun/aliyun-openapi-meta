{
  "camel_example": "aliyun ecs DeleteSnapshot --region cn-hangzhou --SnapshotId 's-bp1c0doj0taqyzzl0000'",
  "cmd_full_name": "ecs delete-snapshot",
  "cmd_name": "delete-snapshot",
  "deprecated": false,
  "description_en": "Deletes a specified snapshot. You can also call this operation to cancel a snapshot that is being created",
  "description_zh": "删除一份指定的快照。如果需要取消正在创建的快照，也可以调用该接口删除快照，即取消创建快照任务",
  "kebab_example": "aliyun ecs delete-snapshot --snapshot-id s-bp1c0doj0taqyzzl0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteSnapshot",
  "operation": {
    "action": "DeleteSnapshot",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "s-bp1c0doj0taqyzzl****",
      "help_en": "The snapshot ID",
      "help_zh": "快照ID",
      "location": "query",
      "name": "snapshot_id",
      "options": [
        "--snapshot-id"
      ],
      "raw_name": "SnapshotId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to force delete the snapshot that has been used to create cloud disks. Valid values:\n\ntrue: Force deletes the snapshot. After the snapshot is forcibly deleted, the cloud disk created from the snapshot cannot be reinitialized.\n\nfalse: Does not force delete the snapshot.\n\nDefault value: false",
      "help_zh": "是否强制删除已经被用于创建云盘的快照。取值范围：\n\ntrue：强制删除。强制删除后该磁盘无法重新初始化。\n\nfalse：不强制删除。\n\n默认值：false",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
