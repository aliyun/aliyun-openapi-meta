{
  "camel_example": "aliyun ecs ResetDisk --region cn-hangzhou --SnapshotId 's-bp199lyny9b30000' --DiskId 'd-bp199lyny9b30000'",
  "cmd_full_name": "ecs reset-disk",
  "cmd_name": "reset-disk",
  "deprecated": false,
  "description_en": "Rolls back a cloud disk to a previous state by using a cloud disk snapshot",
  "description_zh": "使用云盘快照，将云盘回滚到之前某一特定的历史状态",
  "kebab_example": "aliyun ecs reset-disk --disk-id d-bp199lyny9b30000 --snapshot-id s-bp199lyny9b30000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ResetDisk",
  "operation": {
    "action": "ResetDisk",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "d-bp199lyny9b3****",
      "help_en": "The ID of the cloud disk to be rolled back",
      "help_zh": "待回滚的云盘ID",
      "location": "query",
      "name": "disk_id",
      "options": [
        "--disk-id"
      ],
      "raw_name": "DiskId",
      "required": true,
      "type": "string"
    },
    {
      "example": "s-bp199lyny9b3****",
      "help_en": "The ID of the snapshot to use for rolling back the cloud disk",
      "help_zh": "需要回滚的云盘某一阶段的历史快照ID",
      "location": "query",
      "name": "snapshot_id",
      "options": [
        "--snapshot-id"
      ],
      "raw_name": "SnapshotId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run without actually rolling back the cloud disk. The system checks whether required parameters are specified, whether the request format is valid, and whether resource status constraints are met. If the check fails, the corresponding error message is returned. If the check succeeds, the error code `DryRunOperation` is returned.\nfalse: performs a dry run and sends the request. If the check succeeds, the cloud disk rollback operation is initiated.\n\nDefault value: false",
      "help_zh": "是否预检本次请求。取值范围：\n\ntrue：发送检查请求，不会直接回滚云盘。检查项包括是否填写了必选参数、请求格式和资源状态限制。如果检查不通过，则返回对应错误信息。如果检查通过，则返回错误码`DryRunOperation`。\nfalse：发送正常请求，通过检查后直接发起云盘回滚操作。\n\n默认值：false",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "F3CD6886-D8D0-4FEE-B93E-1B732396****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
