{
  "camel_example": "aliyun ecs DeleteInstances --region cn-hangzhou --RegionId 'cn-hangzhou' --InstanceId.1 'i-bp1g6zv0ce8oghu70000'",
  "cmd_full_name": "ecs delete-instances",
  "cmd_name": "delete-instances",
  "deprecated": false,
  "description_en": "Releases multiple pay-as-you-go instances or expired subscription instances in a batch. You can specify whether to release or convert associated disks to pay-as-you-go disks",
  "description_zh": "本接口用于批量删除或者释放按量付费实例或者到期的包年包月实例，支持通过参数设置决定云盘是否释放或转换为按量付费保留",
  "kebab_example": "aliyun ecs delete-instances --biz-region-id cn-hangzhou --instance-id i-bp1g6zv0ce8oghu70000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteInstances",
  "operation": {
    "action": "DeleteInstances",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "false",
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request.\n\ntrue: sends a check request without querying resource status. The check items include whether your AccessKey pair is valid, whether the Resource Access Management (RAM) user is granted the required authorization, and whether the required parameters are specified. If the check fails, the corresponding error is returned. If the check passes, the error code DRYRUN.SUCCESS is returned.\nfalse: sends a Normal request. After the check passes, a 2xx HTTP status code is returned and the resource status is queried.\n\nDefault value: false",
      "help_zh": "是否只预检此次请求。\n\ntrue：发送检查请求，不会查询资源状况。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码DRYRUN.SUCCESS。\nfalse：发送正常请求，通过检查后返回2XX HTTP状态码并直接查询资源状况。\n\n默认值：false",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcefully release ECS instance that is in the Running state.\n\ntrue: forcefully releases ECS instance in the Running state.\nfalse: releases ECS instance only when it is in the Stopped state.\n\nDefault value: false.\n<warning>Forceful release is equivalent to powering off the instance. All in-memory data and temporary storage data of the instance is erased and cannot be recovered.></warning>",
      "help_zh": "是否强制释放运行中（`Running`）的ECS实例。\n\ntrue：强制释放运行中（`Running`）的实例。\nfalse：正常释放实例，此时实例必须处于已停止（`Stopped`）状态。\n\n默认值：false。\n<warning>强制释放相当于断电，实例内存以及存储中的临时数据都会被擦除，无法恢复。></warning>",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to forcefully stop an instance in the Running state before release. This parameter takes effect only when `Force=true`. Valid values:\n\ntrue: forcefully stops and releases the instance. This is equivalent to a power-off operation. The instance directly enters the resource release process.\n<warning>Forceful release is equivalent to powering off the instance. All in-memory data and temporary storage data of the instance is erased and cannot be recovered.></warning>\nfalse: performs a graceful shutdown before releasing the instance. This mode causes the release operation to take several minutes. You can configure service draining actions during the operating system shutdown to reduce noise in your business systems.\n\nDefault value: true",
      "help_zh": "释放运行中（`Running`）的实例时的是否采取强制关机策略。仅当`Force=true`时生效。取值范围：\n\ntrue：强制关机并释放实例。相当于典型的断电操作，实例会直接进入资源释放流程。\n<warning>强制释放相当于断电，实例内存以及存储中的临时数据都会被擦除，无法恢复。></warning>\nfalse：在实例释放前，系统将优先执行标准关机流程，该模式会导致实例释放动作持续几分钟。用户在操作系统关机时，配置一些业务排水动作，从而减少业务系统的噪声。\n\n默认值：true",
      "location": "query",
      "name": "force_stop",
      "options": [
        "--force-stop"
      ],
      "raw_name": "ForceStop",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to release an expired subscription instance.\n\ntrue: releases the instance.\nfalse: does not release the instance.\n\nDefault value: false",
      "help_zh": "是否释放已到期的包年包月实例。\n\ntrue：释放。\nfalse：不释放。\n\n默认值：false",
      "location": "query",
      "name": "terminate_subscription",
      "options": [
        "--terminate-subscription"
      ],
      "raw_name": "TerminateSubscription",
      "required": false,
      "type": "bool"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the instances. You can call https://help.aliyun.com/document_detail/25609.html to query the most recent region list",
      "help_zh": "实例所属的地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "i-bp1g6zv0ce8oghu7****",
      "help_en": "The instance ID array. Array length: 1 to 100",
      "help_zh": "实例ID数组。数组长度：1~100",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "7B7813C6-57BF-41XX-B12B-F172F65A6046",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
