{
  "camel_example": "aliyun ecs RedeployDedicatedHost --region cn-hangzhou --DedicatedHostId 'dh-bp165p6xk2tlw61e0000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "ecs redeploy-dedicated-host",
  "cmd_name": "redeploy-dedicated-host",
  "deprecated": false,
  "description_en": "Performs fault migration for a dedicated host",
  "description_zh": "执行专有宿主机的故障迁移",
  "kebab_example": "aliyun ecs redeploy-dedicated-host --biz-region-id cn-hangzhou --dedicated-host-id dh-bp165p6xk2tlw61e0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RedeployDedicatedHost",
  "operation": {
    "action": "RedeployDedicatedHost",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the dedicated host. You can call https://help.aliyun.com/document_detail/25609.html to query the most recent region list",
      "help_zh": "专有宿主机所属的地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "dh-bp165p6xk2tlw61e****",
      "help_en": "The ID of the dedicated host",
      "help_zh": "专有宿主机ID",
      "location": "query",
      "name": "dedicated_host_id",
      "options": [
        "--dedicated-host-id"
      ],
      "raw_name": "DedicatedHostId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "Reboot",
        "LiveMigrationFirst"
      ],
      "example": "Reboot",
      "help_en": "Specifies whether to stop ECS instance before migrating it to the destination dedicated host. Valid values:\n\nReboot: stops ECS instance before migration.\n\nLiveMigrationFirst: migrates ECS instance without stopping it. You must specify the DedicatedHostId parameter. This value does not support changing ECS instance type during migration. If live migration fails, cold migration is performed by default.\n\nDefault value: Reboot",
      "help_zh": "是否先停止实例，再迁移到目标专有宿主机。取值范围：\n\nReboot：先停止实例再迁移。\n\nLiveMigrationFirst：不停止实例，直接迁移。此时，您必须指定参数DedicatedHostId。该取值不支持在迁移ECS实例的同时变更实例规格。如果在线迁移失败会默认走关机迁移。\n\n默认值：Reboot",
      "location": "query",
      "name": "migration_type",
      "options": [
        "--migration-type"
      ],
      "raw_name": "MigrationType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "FCED4B7A-53D5-4C04-ABE3-26D4F3890D57",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
