{
  "camel_example": "aliyun ecs StartTerminalSession --region cn-hangzhou --RegionId 'cn-hangzhou' --InstanceId.1 'i-bp1eifrtpxa9tb0000'",
  "cmd_full_name": "ecs start-terminal-session",
  "cmd_name": "start-terminal-session",
  "deprecated": false,
  "description_en": "Invokes StartTerminalSession to create a session based on the session management feature. You can establish a WebSocket session with an ECS instance by specifying the instance ID. The WebSocketUrl returned by the operation can be used to establish a remote connection to the ECS instance",
  "description_zh": "调用StartTerminalSession基于会话管理功能创建一个会话。您可以通过指定ECS实例ID与该实例建立一个WebSocket会话，通过接口返回的WebSocketUrl可以远程连接到ECS实例",
  "kebab_example": "aliyun ecs start-terminal-session --biz-region-id cn-hangzhou --instance-id i-bp1eifrtpxa9tb0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "StartTerminalSession",
  "operation": {
    "action": "StartTerminalSession",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the instance. You can call https://help.aliyun.com/document_detail/25609.html to query the most recent region list",
      "help_zh": "实例所在地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of instance IDs",
      "help_zh": "实例ID列表",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": true,
      "type": "array"
    },
    {
      "example": "22",
      "help_en": "The port number of the ECS instance for data forwarding. After this parameter is set, the Cloud Assistant Agent forwards data to the specified port number for port forwarding. For example, SSH uses port 22.\n\nDefault value: empty, which indicates that no port number is set for data forwarding",
      "help_zh": "指定ECS实例的端口号，用于数据转发。一旦设置该参数，云助手Agent的数据转发会传到指定的端口号，用于端口转发。例如，SSH使用22端口。\n\n默认值为空，表示不设置数据转发的端口号",
      "location": "query",
      "name": "port_number",
      "options": [
        "--port-number"
      ],
      "raw_name": "PortNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "ssh root@192.168.0.246",
      "help_en": "The command to run after the session is initiated. The command can be up to 512 characters in length.\n\nAfter you specify CommandLine, you cannot specify PortNumber or TargetServer",
      "help_zh": "发起会话后，指定执行的命令内容。长度不能超过512个字符。\n\n指定了`CommandLine`后，不能再指定`PortNumber`和`TargetServer`",
      "location": "query",
      "name": "command_line",
      "options": [
        "--command-line"
      ],
      "raw_name": "CommandLine",
      "required": false,
      "type": "string"
    },
    {
      "example": "192.168.0.246",
      "help_en": "The address of the destination server in the VPC that you want to access through the instance.\n\nWhen this parameter is not empty, PortNumber specifies the port number of the destination server in the VPC that you want to access through the managed instance",
      "help_zh": "指定通过实例访问VPC内目标服务地址。\n\n当该参数不为空时，`PortNumber`表示通过托管实例访问VPC内目标服务的端口号",
      "location": "query",
      "name": "target_server",
      "options": [
        "--target-server"
      ],
      "raw_name": "TargetServer",
      "required": false,
      "type": "string"
    },
    {
      "example": "testUser",
      "help_en": "The username used for the connection",
      "help_zh": "指定连接时的用户名称",
      "location": "query",
      "name": "username",
      "options": [
        "--username"
      ],
      "raw_name": "Username",
      "required": false,
      "type": "string"
    },
    {
      "example": "Intranet",
      "help_en": "The network type of the WebSocket URL required for the remote connection to the instance. Valid values:\nInternet: public network. This is the default value.\nIntranet: internal network",
      "help_zh": "远程连接实例所需WebSocket URL的网络类型。\nInternet：公网（默认）\nIntranet：内网",
      "location": "query",
      "name": "connection_type",
      "options": [
        "--connection-type"
      ],
      "raw_name": "ConnectionType",
      "required": false,
      "type": "string"
    },
    {
      "example": "axtSecretPassword",
      "help_en": "The name of the password for the user when using Session Manager on a Windows instance. The name can be up to 255 characters in length.\nWhen you want to use Session Manager on a Windows instance as a non-default user (System), specify both Username and this parameter. To reduce the risk of password leaks, store the plaintext password in the parameter repository of operations management and specify only the password name here. For more information, see https://help.aliyun.com/document_detail/186828.html",
      "help_zh": "在 Windows 实例中使用Session Manager时用户的密码名称。长度不得超过 255 个字符。\n当您希望以非默认用户（System）在 Windows 实例中使用Session Manager时，需要同时传入Username和该参数。为降低密码泄露的风险，需要将密码明文托管在系统运维管理的参数仓库中，此处仅传入密码的名称。更多信息，请参见https://help.aliyun.com/document_detail/186828.html",
      "location": "query",
      "name": "password_name",
      "options": [
        "--password-name"
      ],
      "raw_name": "PasswordName",
      "required": false,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "fields": [
        {
          "example": "true",
          "help_en": "Specifies whether to enable end-to-end encryption for the session connection",
          "help_zh": "开启会话连接端到端加密",
          "name": "enabled",
          "options": [
            "--enabled"
          ],
          "raw_name": "Enabled",
          "required": false,
          "type": "bool"
        },
        {
          "example": "xxx",
          "help_en": "The KMS key ID.\nNote:\nOnly KMS symmetric keys are supported.\nThis parameter can be specified only when the encryption mode is set to Kms",
          "help_zh": "KMS秘钥ID。\n注意事项：\n仅支持KMS对称密钥。\n仅当加密模式为Kms时，支持指定该参数",
          "name": "kms_key_id",
          "options": [
            "--kms-key-id"
          ],
          "raw_name": "KMSKeyId",
          "required": false,
          "type": "string"
        },
        {
          "example": "Auto",
          "help_en": "The encryption pattern. Valid values:\nAuto: Uses automatically negotiated secret key encryption for the session.\nKms: Uses a KMS key to encrypt the session.\nDefault value: Auto.\n\nNote:\nThis parameter can be specified only when session encryption is enabled",
          "help_zh": "加密模式。可选值：\nAuto：使用自动协商密钥加密会话。\nKms：使用KMS密钥加密会话。\n默认值：Auto。\n\n注意事项：\n仅当开启会话加密时，支持指定该参数",
          "name": "mode",
          "options": [
            "--mode"
          ],
          "raw_name": "Mode",
          "required": false,
          "type": "string"
        }
      ],
      "help_en": "The session encryption configuration",
      "help_zh": "会话加密配置项",
      "location": "query",
      "name": "encryption_options",
      "options": [
        "--encryption-options"
      ],
      "param_style": "json",
      "raw_name": "EncryptionOptions",
      "required": false,
      "type": "object"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "EB5173B0-8E80-564E-AAD1-3135412*****",
            "type": "string",
            "visibility": "Public"
          },
          "SecurityToken": {
            "backendName": "data.securityToken",
            "description_en": "The security token that is appended to the WebSocket request header for system verification of the request",
            "description_zh": "附加在WebSocket的请求头部，用于系统校验本次请求",
            "example": "d86c2df2-d19c-4bd8-b817-a19ef123****",
            "type": "string",
            "visibility": "Public"
          },
          "SessionId": {
            "backendName": "data.sessionId",
            "description_en": "The session ID",
            "description_zh": "会话ID",
            "example": "s-hz023od0x9****",
            "type": "string",
            "visibility": "Public"
          },
          "WebSocketUrl": {
            "backendName": "data.webSocketUrl",
            "description_en": "The URL of the WebSocket session for the remote connection to the ECS instance. The URL contains the session ID (SessionId) and the SecurityToken used for system verification",
            "description_zh": "远程连接ECS实例的WebSocket会话对应的URL。包含了会话ID（`SessionId`）以及用于系统校验的`SecurityToken`",
            "example": "wss://cn-hangzhou.axt.aliyuncs.com/session?sessionId=s-hz023od0x9****&token=d86c2df2-d19c-4bd8-b817-a19ef123****",
            "nullToEmpty": true,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
