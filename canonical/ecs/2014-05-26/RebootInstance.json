{
  "camel_example": "aliyun ecs RebootInstance --region cn-hangzhou --InstanceId 'i-bp67acfmxazb4ph0000'",
  "cmd_full_name": "ecs reboot-instance",
  "cmd_name": "reboot-instance",
  "deprecated": false,
  "description_en": "Restarts an ECS instance",
  "description_zh": "本接口用于重启一台ECS实例",
  "kebab_example": "aliyun ecs reboot-instance --instance-id i-bp67acfmxazb4ph0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RebootInstance",
  "operation": {
    "action": "RebootInstance",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "i-bp67acfmxazb4ph****",
      "help_en": "The ID of the instance",
      "help_zh": "指定实例的ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcefully stop ECS instance before restarting it. Valid values:\n\ntrue: Forcefully stops ECS instance. This is equivalent to a power-off operation. Cached data that has not been written to storage devices is lost.\n\nfalse: Normally stops ECS instance.\n\nDefault value: false",
      "help_zh": "重启ECS实例前，是否强制关机。取值范围：\n\ntrue：强制关机。相当于典型的断电操作，所有未写入存储设备的缓存数据会丢失。\n\nfalse：正常关机。\n\n默认值：false",
      "location": "query",
      "name": "force_stop",
      "options": [
        "--force-stop"
      ],
      "raw_name": "ForceStop",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform only a dry run. Valid values:\n\ntrue: Performs only a dry run without restarting the instance. The system checks the required parameters, request syntax, business restrictions, and ECS inventory. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\nfalse: Performs a dry run and sends the request. If the check succeeds, the instance is restarted.\n\nDefault value: false",
      "help_zh": "是否只预检此次请求。取值范围：\n\ntrue：发送检查请求，不会重启实例。检查项包括是否填写了必选参数、请求格式、业务限制和ECS库存。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse：发送正常请求，通过检查后直接重启实例。\n\n默认值：false",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
