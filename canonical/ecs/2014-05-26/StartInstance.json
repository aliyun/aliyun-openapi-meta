{
  "camel_example": "aliyun ecs StartInstance --region cn-hangzhou --InstanceId 'i-bp67acfmxazb4p0000'",
  "cmd_full_name": "ecs start-instance",
  "cmd_name": "start-instance",
  "deprecated": false,
  "description_en": "Starts an ECS instance. You can start an instance by specifying the instance ID and optionally setting parameters such as InitLocalDisk",
  "description_zh": "本接口用于启动一台ECS实例，您可以通过指定实例ID以及按需设置 InitLocalDisk 等参数启动实例",
  "kebab_example": "aliyun ecs start-instance --instance-id i-bp67acfmxazb4p0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "StartInstance",
  "operation": {
    "action": "StartInstance",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "i-bp67acfmxazb4p****",
      "help_en": "The instance ID of the instance that you want to start",
      "help_zh": "指定启动的实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to recover the instance to its initial health state when a local disk fails. This parameter is applicable to instance families that use local disks, such as the d1, i1, and i2 instance families. Valid values:\n\ntrue: Recovers the instance to its initial health state.\n<warning>All data stored on the local disks of the instance will be lost.></warning>\n\nfalse: Does not perform any action and maintains the current state.\n\nDefault value: false",
      "help_zh": "当本地盘出现故障时，可通过此参数指定启动实例时，是否将实例恢复到最初的健康状态。适用于实例规格族d1、i1或者i2等包含本地盘的实例。取值范围：\n\ntrue：将实例恢复到最初的健康状态。\n<warning>实例原有本地盘中的数据将会丢失。></warning>\n\nfalse：不做任何处理，维持现状。\n\n默认值为false",
      "location": "query",
      "name": "init_local_disk",
      "options": [
        "--init-local-disk"
      ],
      "raw_name": "InitLocalDisk",
      "required": false,
      "type": "bool"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\ntrue: performs only a dry run. The instance is not started. The system checks whether the request parameters are valid, whether the Resource Access Management (RAM) user has the required authorization, and whether the required parameters are specified. If the check fails, the corresponding error is returned. If the check succeeds, the DryRunOperation error code is returned.\nfalse: sends a normal request. If the check succeeds, a 2XX HTTP status code is returned and the instance is started.\n\nDefault value: false",
      "help_zh": "是否只预检查此次请求。取值范围：\n\ntrue：仅检查此次请求，不会启动实例。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码DryRunOperation。\nfalse：发送正常请求，请求通过检查后，返回2XX的HTTP状态码并直接启动实例。\n\n默认值为false",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
