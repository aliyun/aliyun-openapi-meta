{
  "camel_example": "aliyun ecs AttachDisk --region cn-hangzhou --InstanceId 'i-bp1dq5lozx5f4pmd0000' --DiskId 'd-bp1j4l5axzdy6ftk0000'",
  "cmd_full_name": "ecs attach-disk",
  "cmd_name": "attach-disk",
  "deprecated": false,
  "description_en": "Attaches a data disk or system disk to an ECS instance",
  "description_zh": "为一台ECS实例挂载一块按数据盘或系统盘",
  "kebab_example": "aliyun ecs attach-disk --instance-id i-bp1dq5lozx5f4pmd0000 --disk-id d-bp1j4l5axzdy6ftk0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "AttachDisk",
  "operation": {
    "action": "AttachDisk",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "i-bp1dq5lozx5f4pmd****",
      "help_en": "The ID of the ECS instance to which you want to attach the disk",
      "help_zh": "待挂载ECS实例的ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "d-bp1j4l5axzdy6ftk****",
      "help_en": "The ID of the disk to be attached. The disk (`DiskId`) and the instance (`InstanceId`) must be in the same zone.\n\nYou can attach data disks and system disks. For related constraints, see the operation description section above",
      "help_zh": "待挂载的磁盘ID。磁盘（`DiskId`）和实例（`InstanceId`）必须在同一个可用区。\n\n支持挂载数据盘和系统盘，相关约束条件请参见上文接口说明部分",
      "location": "query",
      "name": "disk_id",
      "options": [
        "--disk-id"
      ],
      "raw_name": "DiskId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testDeviceName",
      "help_en": "The device name of the disk.\n\nThis parameter will be deprecated soon. To improve compatibility, use other parameters to identify the disk",
      "help_zh": "磁盘设备名称。\n\n该参数即将被弃用，为提高兼容性，建议您尽量使用其他参数标识磁盘",
      "location": "query",
      "name": "device",
      "options": [
        "--device"
      ],
      "raw_name": "Device",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to release the disk when the instance is released. Valid values:\n\ntrue: The disk is released together with the instance.\nfalse: The disk is not released together with the instance. The disk is retained as a pay-as-you-go data disk.\n\nDefault value: false.\n\nTake note of the following items when you set this parameter:\n\nIf you set `DeleteWithInstance` to `false` and the ECS instance is locked for security reasons, meaning that `OperationLocks` contains `\"LockReason\" : \"security\"`, this attribute is ignored when the ECS instance is released, and the disk is released together with the instance.\n\nIf the destination disk is an `elastic ephemeral disk`, you must set `DeleteWithInstance` to `true`.\n\nDisks with the multi-attach feature enabled do not support this parameter",
      "help_zh": "释放实例时，该磁盘是否随实例一起释放。取值范围：\n\ntrue：释放。\nfalse：不释放。磁盘会转换成按量付费数据盘而被保留下来。\n\n默认值：false。\n\n设置该参数时，您需要注意：\n\n将`DeleteWithInstance`置为`false`后，一旦ECS实例被安全控制，即`OperationLocks`中标记了`\"LockReason\" : \"security\"`，释放ECS实例时会忽略磁盘的该属性，被同时释放。\n\n若您需要挂载的目标磁盘为`弹性临时盘`，则必须将`DeleteWithInstance`参数设置为`true`。\n\n开启多重挂载特性的云盘，不支持设置该参数",
      "location": "query",
      "name": "delete_with_instance",
      "options": [
        "--delete-with-instance"
      ],
      "raw_name": "DeleteWithInstance",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to attach the disk as a system disk. Valid values:\n\ntrue: The disk is attached as a system disk.\n\nfalse: The disk is not attached as a system disk.\n\nDefault value: false.\n\nIf you set `Bootable=true`, the destination ECS instance must have no system disk attached",
      "help_zh": "是否作为系统盘挂载。取值范围：\n\ntrue：是。\n\nfalse：否。\n\n默认值：false。\n\n设置为`Bootable=true`时，目标ECS实例必须处于无系统盘状态",
      "location": "query",
      "name": "bootable",
      "options": [
        "--bootable"
      ],
      "raw_name": "Bootable",
      "required": false,
      "type": "bool"
    },
    {
      "example": "EcsV587!",
      "help_en": "The password that is set for the instance when you attach a system disk. The password is effective only for the administrator and root usernames and is not effective for other usernames. The password must be 8 to 30 characters in length and must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters. The following special characters are supported:\n\n```\n()`~!@#$%^&*-_+=|{}[]:;'<>,.?/\n```\nFor Windows instances, the password cannot start with a forward slash (/).\n\nIf you specify the `Password` parameter, send the request over HTTPS to prevent password leaks",
      "help_zh": "挂载系统盘时，设置实例的用户名密码，仅对administrator和root用户名生效，其他用户名不生效。长度为8至30个字符，必须同时包含大小写英文字母、数字和特殊符号中的三类字符。特殊符号可以是：\n\n```\n()`~!@#$%^&*-_+=|{}[]:;'<>,.?/\n```\n其中，Windows实例不能以斜线号（/）为密码首字符。\n\n如果传入`Password`参数，建议您使用HTTPS协议发送请求，避免密码泄露",
      "location": "query",
      "name": "password",
      "options": [
        "--password"
      ],
      "raw_name": "Password",
      "required": false,
      "type": "string"
    },
    {
      "example": "KeyPairTestName",
      "help_en": "The name of the SSH key pair that is bound to the Linux ECS instance when you attach a system disk.\n\nWindows Server instances: SSH key pairs are not supported. Even if this parameter is specified, only the `Password` configuration takes effect.\n\nLinux instances: The password logon method is disabled by default",
      "help_zh": "挂载系统盘时，为Linux系统ECS实例绑定的SSH密钥对的名称。\n\nWindows Server系统：不支持SSH密钥对。即使填写了该参数，只执行`Password`的配置。\n\nLinux系统：密码登录方式会被初始化成禁止",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcefully attach the disk. Valid values:\n\ntrue: Forcefully attaches the disk.\nfalse: Does not forcefully attach the disk.\n\nDefault value: false.\n\n\nCurrently, only regional ESSDs (cloud_regional_disk_auto) support setting this parameter to true",
      "help_zh": "是否是强制挂载请求。取值范围：\n\ntrue：是。\nfalse：否。\n\n默认值：false。\n\n\n当前仅 ESSD 同城冗余类型（cloud_regional_disk_auto）支持设置该字段为 true",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
