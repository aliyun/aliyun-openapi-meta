{
  "camel_example": "aliyun dds DestroyInstance --region cn-hangzhou --ResourceGroupId 'rg-acfmyiu4ekp0000' --InstanceId 'dds-bp147acd47830000' --DBInstanceId 'dds-bp147acd47830000' --ClientToken ETnLKlblzczshOTUbOCz0000",
  "cmd_full_name": "dds destroy-instance",
  "cmd_name": "destroy-instance",
  "deprecated": false,
  "description_en": "Destroys an ApsaraDB for MongoDB instance",
  "description_zh": "销毁MongoDB实例",
  "kebab_example": "aliyun dds destroy-instance --instance-id dds-bp147acd47830000 --db-instance-id dds-bp147acd47830000 --client-token ETnLKlblzczshOTUbOCz0000 --resource-group-id rg-acfmyiu4ekp0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DestroyInstance",
  "operation": {
    "action": "DestroyInstance",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bp147acd4783****",
      "help_en": "The instance ID.\n\nInstanceId and DBInstanceId serve the same function. You need only to specify one of them",
      "help_zh": "实例ID。\n\nInstanceId参数与DBInstanceId参数作用相同，只需选择一个传入即可",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "dds-bp147acd4783****",
      "help_en": "The instance ID.\n\nInstanceId and DBInstanceId serve the same function. You need only to specify one of them",
      "help_zh": "实例ID。\n\nInstanceId参数与DBInstanceId参数作用相同，只需选择一个传入即可",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must ensure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "rg-acfmyiu4ekp****",
      "help_en": "The ID of the resource group",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_zh": "请求ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "65BDA532-28AF-4122-AA39-B382721E****",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
