{
  "camel_example": "aliyun dds UpgradeDBInstanceEngineVersion --region cn-hangzhou --EngineVersion '4.0' --DBInstanceId 'dds-bpxxxxxxxx'",
  "cmd_full_name": "dds upgrade-db-instance-engine-version",
  "cmd_name": "upgrade-db-instance-engine-version",
  "deprecated": false,
  "description_en": "Upgrades the database version of an ApsaraDB for MongoDB instance",
  "description_zh": "升级MongoDB实例的数据库版本",
  "kebab_example": "aliyun dds upgrade-db-instance-engine-version --db-instance-id dds-bpxxxxxxxx --engine-version 4.0 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpgradeDBInstanceEngineVersion",
  "operation": {
    "action": "UpgradeDBInstanceEngineVersion",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bpxxxxxxxx",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "3.4",
        "4.0",
        "4.2",
        "4.4",
        "5.0",
        "6.0",
        "7.0",
        "8.0"
      ],
      "example": "4.0",
      "help_en": "The database version to which you want to upgrade. Valid values: 3.4, 4.0, and 4.2.\n\nThis database version must be later than the current database version of the instance",
      "help_zh": "升级的目标数据库版本。\n\n实例的存储引擎对数据库版本的选择有约束性，详情请参见https://help.aliyun.com/document_detail/398673.html，您也可以调用https://help.aliyun.com/document_detail/468471.html接口查询实例可升级的版本",
      "location": "query",
      "name": "engine_version",
      "options": [
        "--engine-version"
      ],
      "raw_name": "EngineVersion",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "0",
        "1"
      ],
      "example": "1",
      "help_en": "The time when to perform the upgrade. Valid values:\n\n0: immediately performs the upgrade.\n\n1: performs the upgrade during the maintenance window",
      "help_zh": "实例版本升级模式。取值：\n\n0：立即升级。\n1：在可运维时间段内升级",
      "location": "query",
      "maximum": "1",
      "minimum": "0",
      "name": "switch_mode",
      "options": [
        "--switch-mode"
      ],
      "raw_name": "SwitchMode",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "C4907B00-A208-4E0C-A636-AA85140E406C",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
