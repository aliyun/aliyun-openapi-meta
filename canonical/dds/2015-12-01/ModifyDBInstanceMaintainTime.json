{
  "camel_example": "aliyun dds ModifyDBInstanceMaintainTime --region cn-hangzhou --MaintainEndTime '02:00Z' --DBInstanceId 'dds-bpac23450000' --MaintainStartTime '01:00Z'",
  "cmd_full_name": "dds modify-db-instance-maintain-time",
  "cmd_name": "modify-db-instance-maintain-time",
  "deprecated": false,
  "description_en": "Modifies the maintenance window of an ApsaraDB for MongoDB instance",
  "description_zh": "修改MongoDB实例的可维护时间",
  "kebab_example": "aliyun dds modify-db-instance-maintain-time --db-instance-id dds-bpac23450000 --maintain-start-time 01:00Z --maintain-end-time 02:00Z --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBInstanceMaintainTime",
  "operation": {
    "action": "ModifyDBInstanceMaintainTime",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bpac2345****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "01:00Z",
      "help_en": "The start time of the maintenance window. Specify the time in the ISO 8601 standard in the HH:mmZ format. The time must be in UTC",
      "help_zh": "实例可运维时间段的开始时间，格式为<i>HH:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "maintain_start_time",
      "options": [
        "--maintain-start-time"
      ],
      "raw_name": "MaintainStartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "02:00Z",
      "help_en": "The end time of the maintenance window. Specify the time in the ISO 8601 standard in the HH:mmZ format. The time must be in UTC.\n\nThe end time must be later than the start time of the maintenance window",
      "help_zh": "实例可运维时间段的结束时间，格式为<i>HH:mm</i>Z（UTC时间）。\n\n可运维时间段的结束时间需晚于可运维时间段的开始时间",
      "location": "query",
      "name": "maintain_end_time",
      "options": [
        "--maintain-end-time"
      ],
      "raw_name": "MaintainEndTime",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "A9310426-C763-42A2-A3AD-70A8DA204531",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
