{
  "camel_example": "aliyun dds DeleteDBInstance --region cn-hangzhou --DBInstanceId 'dds-bpxxxxxxxx'",
  "cmd_full_name": "dds delete-db-instance",
  "cmd_name": "delete-db-instance",
  "deprecated": false,
  "description_en": "Releases a pay-as-you-go MongoDB instance",
  "description_zh": "释放MongoDB实例",
  "kebab_example": "aliyun dds delete-db-instance --db-instance-id dds-bpxxxxxxxx --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteDBInstance",
  "operation": {
    "action": "DeleteDBInstance",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bpxxxxxxxx",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ETnLKlblzczshOTUbOCzxxxxxxxxxx",
      "help_en": "A token that ensures the idempotence of the request. The client generates this value, which must be unique among different requests. The value can be up to 64 ASCII characters in length and cannot contain non-ASCII characters",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "72651AF9-7897-41A7-8B67-6C15C7F26A0A",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
