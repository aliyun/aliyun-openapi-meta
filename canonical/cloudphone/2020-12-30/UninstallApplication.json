{
  "camel_example": "",
  "cmd_full_name": "cloudphone uninstall-application",
  "cmd_name": "uninstall-application",
  "deprecated": false,
  "description_en": "Uninstalls an application",
  "description_zh": "卸载应用",
  "kebab_example": "aliyun cloudphone uninstall-application --biz-region-id cn-hangzhou --package-name com.theapp.test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UninstallApplication",
  "operation": {
    "action": "UninstallApplication",
    "api_style": "RPC",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The instance ID of the instance on which the command is to be executed. Valid values of N: 1 to 10",
      "help_zh": "待执行命令的实例ID。N的范围：1~10",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": false,
      "type": "array"
    },
    {
      "example": "com.theapp.test",
      "help_en": "The name of the application to uninstall. The name can be up to 1024 bytes in length and can contain uppercase letters, lowercase letters, digits, underscores (_), periods (.), forward slashes (/), colons (:), and hyphens (-)",
      "help_zh": "待卸载的APP名称\n最大长度为1024字节，只支持大小写字母、数字、下划线（_）、点（.）、斜线（/）、冒号（:）、中划线（-）",
      "location": "query",
      "name": "package_name",
      "options": [
        "--package-name"
      ],
      "raw_name": "PackageName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudphone",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "5D9157CC-96A5-519E-80C0-D7047FF939D3",
            "type": "string"
          },
          "TaskId": {
            "backendName": "data.taskIds",
            "itemName": "TaskId",
            "items": {
              "backendName": "data.taskIds",
              "nullToEmpty": true,
              "type": "string"
            },
            "nullToEmpty": true,
            "parseType": "null",
            "type": "array"
          }
        },
        "type": "object"
      }
    }
  }
}
