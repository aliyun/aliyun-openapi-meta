{
  "camel_example": "",
  "cmd_full_name": "cloudphone delete-instances",
  "cmd_name": "delete-instances",
  "deprecated": false,
  "description_en": "Deletes cloud phone instances in batches",
  "description_zh": "批量删除云手机实例",
  "kebab_example": "aliyun cloudphone delete-instances --biz-region-id cn-hangzhou --instance-id cp-xxxxxxxx --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteInstances",
  "operation": {
    "action": "DeleteInstances",
    "api_style": "RPC",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region to which the cloud phone instances belong",
      "help_zh": "云手机实例所属地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The instance ID of the cloud phone. Valid values of N: 1 to 100",
      "help_zh": "云手机实例ID，N的取值范围：1~100",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": true,
      "type": "array"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcibly release cloud phone instances that are in the Running state. Default value: false.\n\ntrue: Forcibly releases instances that are in the Running state. A forcible release is equivalent to a power-off. All in-memory and temporary storage data of the instance is erased and cannot be recovered.\nfalse (default): Releases instances in the normal way. The instances must be in the Stopped state",
      "help_zh": "是否强制释放运行中（Running）的云手机实例，默认为false。\n\ntrue：强制释放运行中（Running）的实例。强制释放相当于断电，实例内存以及存储中的临时数据都会被擦除，无法恢复。\nfalse（默认值）：正常释放实例，此时实例必须处于已停止（Stopped）状态",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudphone",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
