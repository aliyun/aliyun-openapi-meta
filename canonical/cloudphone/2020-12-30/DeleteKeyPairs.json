{
  "camel_example": "",
  "cmd_full_name": "cloudphone delete-key-pairs",
  "cmd_name": "delete-key-pairs",
  "deprecated": false,
  "description_en": "Deletes one or more cloud phone key pairs",
  "description_zh": "删除一对或者多对云手机密钥对",
  "kebab_example": "aliyun cloudphone delete-key-pairs --biz-region-id cn-hangzhou --biz-key-pair-name testKey --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteKeyPairs",
  "operation": {
    "action": "DeleteKeyPairs",
    "api_style": "RPC",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "[keyPairName1, keyPairNam2, keyPairName3]",
      "help_en": "The name of the cloud phone key pair. The value can be a JSON array that consists of multiple key pair names. A maximum of 100 SSH key pairs are supported. Separate multiple names with commas (,)",
      "help_zh": "云手机密钥对名称。取值可以由多个密钥对名称组成一个JSON数组，最多支持100对SSH密钥对，名称之间用半角逗号（,）隔开",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "param_style": "repeatList",
      "raw_name": "KeyPairName",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudphone",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
