{
  "camel_example": "aliyun imm EncodeBlindWatermark --region cn-hangzhou --ProjectName 'test-project' --SourceURI 'oss://test-bucket/test-object.jpg' --TargetURI 'oss://test-bucket/target-object.jpg'",
  "cmd_full_name": "imm encode-blind-watermark",
  "cmd_name": "encode-blind-watermark",
  "deprecated": false,
  "description_en": "Embeds specific textual information into an image as watermarks. These watermarks are visually imperceptible and do not affect the aesthetics of the image or the integrity of the original data. The watermarks can be extracted by using the CreateDecodeBlindWatermarkTask operation",
  "description_zh": "将特定的文本信息嵌入到图片中，而这些信息在视觉观察下是不可见的。由于这些水印在视觉上是隐蔽的，它们不会影响图片的美观性或原始信息的传递，但可以通过 CreateDecodeBlindWatermarkTask 接口提取出来",
  "kebab_example": "aliyun imm encode-blind-watermark --project-name test-project --source-uri oss://test-bucket/test-object.jpg --target-uri oss://test-bucket/target-object.jpg --api-version 2020-09-30 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "EncodeBlindWatermark",
  "operation": {
    "action": "EncodeBlindWatermark",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "test-project",
      "help_en": "The name of the project. For more information, see https://help.aliyun.com/document_detail/478153.html",
      "help_zh": "项目名称，获取方式请参见https://help.aliyun.com/document_detail/478153.html",
      "location": "query",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": true,
      "type": "string"
    },
    {
      "example": "oss://test-bucket/test-object.jpg",
      "help_en": "The Object Storage Service (OSS) URI of the image.\n\nSpecify the value in the oss://${Bucket}/${Object} format. `${Bucket}` specifies the name of the OSS bucket that resides in the same region with the current project. `${Object}` specifies the path of the object with the extension included.\n\nSupported image formats: JPG, PNG, BMP, TIFF, and WebP.\n\nImage size limit: 10,000 px maximum and 80 px x 80 px minimum",
      "help_zh": "图片的OSS URI。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径。\n\n支持图片格式：jpg, png, bmp, tiff, webp\n\n图片大小限制：长宽最小80px ，最大10000px",
      "location": "query",
      "name": "source_uri",
      "options": [
        "--source-uri"
      ],
      "raw_name": "SourceURI",
      "required": true,
      "type": "string"
    },
    {
      "example": "oss://test-bucket/target-object.jpg",
      "help_en": "The URI of the output image in OSS.\n\nSpecify the URI in the oss://${Bucket}/${Object} format, where `${Bucket}` is the name of the bucket in the same region as the current project and `${Object}` is the path of the object with the extension included.\n\n\n\nThe format of the output image is the same as that of the input image",
      "help_zh": "输出图片存储的OSS地址。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径。\n\n\n输出图片格式与输入相同",
      "location": "query",
      "name": "target_uri",
      "options": [
        "--target-uri"
      ],
      "raw_name": "TargetURI",
      "required": true,
      "type": "string"
    },
    {
      "example": "Copyright Alibaba Cloud",
      "help_en": "The text content of watermarks. It can be up to 256 characters in length",
      "help_zh": "作为水印的文字内容，最多256个字符",
      "location": "query",
      "name": "content",
      "options": [
        "--content"
      ],
      "raw_name": "Content",
      "required": false,
      "type": "string"
    },
    {
      "example": "low",
      "help_en": "The watermark strength level. The higher the strength, the more resistant the watermarked image is to attacks, but the more the image is distorted. Default value: low. Valid values: [low, medium, high]",
      "help_zh": "水印添加强度，强度越高，加水印后的图像抵抗攻击性能越强，但引起的失真越明显，默认值为low，可取值为[low, medium, high]",
      "location": "query",
      "name": "strength_level",
      "options": [
        "--strength-level"
      ],
      "raw_name": "StrengthLevel",
      "required": false,
      "type": "string"
    },
    {
      "example": "90",
      "help_en": "This parameter takes effect only if the input image format is JPG.\n\nThe storage quality of the output image that carries the watermarks. Default value: 90. Valid values: 70 to 100. The higher the quality, the larger the image size and the higher the watermark resolution quality",
      "help_zh": "仅输入图片格式为jpg时生效。\n\n携带水印信息的输出图片的存储质量，默认值为90，取值范围为70~100。\n质量越高，图片尺寸越大，水印解析质量越高",
      "location": "query",
      "name": "image_quality",
      "options": [
        "--image-quality"
      ],
      "raw_name": "ImageQuality",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "当次请求的Request ID",
            "example": "8E0DD64B-28C6-4653-8FF7-93E4C234BCF0",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
