{
  "camel_example": "",
  "cmd_full_name": "tablestore describe-regions",
  "cmd_name": "describe-regions",
  "deprecated": false,
  "description_en": "Retrieves a list of available regions",
  "description_zh": "获取可用地域列表",
  "kebab_example": "aliyun tablestore describe-regions --client-token ETnLKlblzczshOTUbOCzxxxxxxxxxx --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeRegions",
  "operation": {
    "action": "DescribeRegions",
    "api_style": "ROA",
    "api_version": "2020-12-09",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/region/DescribeRegions"
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCzxxxxxxxxxx",
      "help_en": "A parameter to ensure idempotence.\n\nThis parameter ensures that each request is processed only once. Generate a unique value on the client for each request. The value can be up to 64 ASCII characters long and must not contain non-ASCII characters",
      "help_zh": "幂等参数。\n用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过 64 个 ASCII 字符，且该参数值中不能包含非 ASCII 字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/region/DescribeRegions",
  "product_code": "Tablestore",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response object",
        "description_zh": "返回结构体",
        "properties": {
          "Regions": {
            "description_en": "A list of regions",
            "description_zh": "地域信息列表",
            "itemName": "Regions",
            "items": {
              "description_en": "A region object",
              "description_zh": "地域对象",
              "properties": {
                "I18nKey": {
                  "description_en": "The region key",
                  "description_zh": "地域KEY",
                  "example": "yunjiankong.cloud.region.cn-hangzhou",
                  "title_en": "region key",
                  "title_zh": "region key",
                  "type": "string"
                },
                "RegionId": {
                  "description_en": "The region ID",
                  "description_zh": "地域ID",
                  "example": "cn-hangzhou",
                  "title_en": "region id",
                  "title_zh": "region id",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "title_en": "region list",
            "title_zh": "region list",
            "type": "array"
          },
          "RequestId": {
            "description_en": "The ID of the request",
            "description_zh": "Id of the request",
            "example": "39871ED2-62C0-578F-A32E-B88072D5582F",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
