{
  "camel_example": "aliyun ga CreateAcl --region cn-hangzhou --AddressIPVersion IPv4 --RegionId 'cn-hangzhou'",
  "cmd_full_name": "ga create-acl",
  "cmd_name": "create-acl",
  "deprecated": false,
  "description_en": "Invokes the CreateAcl operation to create an access control policy group",
  "description_zh": "调用CreateAcl接口创建访问控制策略组",
  "kebab_example": "aliyun ga create-acl --biz-region-id cn-hangzhou --address-ip-version IPv4 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateAcl",
  "operation": {
    "action": "CreateAcl",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to cn-hangzhou",
      "help_zh": "全球加速实例所在的地域ID，仅取值：cn-hangzhou",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test-acl",
      "help_en": "The name of the access control policy group.\n\nThe name must be 1 to 128 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (_), and hyphens (-)",
      "help_zh": "访问控制策略组的名称。\n\n名称长度为 1~128 个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）",
      "location": "query",
      "name": "acl_name",
      "options": [
        "--acl-name"
      ],
      "raw_name": "AclName",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "enum": [
        "IPv4",
        "IPv6"
      ],
      "example": "IPv4",
      "help_en": "The IP version of the access control policy group. Valid values:\nIPv4\nIPv6",
      "help_zh": "访问控制策略组的IP版本。取值：\nIPv4。\nIPv6",
      "location": "query",
      "name": "address_ip_version",
      "options": [
        "--address-ip-version"
      ],
      "raw_name": "AddressIPVersion",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "10.0.XX.XX/24",
            "help_en": "The access control policy group entry, which is an IP address entry (192.168.XX.XX) or a CIDR block entry (10.0.XX.XX/24).\n\nYou can add up to 50 entries at a time",
            "help_zh": "访问控制策略组条目，即IP地址条目(192.168.XX.XX)或IP地址段条目(10.0.XX.XX/24)。\n\n一次最多支持添加50个条目",
            "name": "entry",
            "options": [
              "--entry"
            ],
            "raw_name": "Entry",
            "required": false,
            "type": "string"
          },
          {
            "example": "test-entry",
            "help_en": "The description of the access control policy group entry.\n\nYou can add descriptions for up to 50 entries at a time.\n\nThe description must be 1 to 256 characters in length and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), underscores (_), and Chinese characters",
            "help_zh": "访问控制策略组条目备注内容。\n\n一次最多支持添加50个条目备注。\n\n长度限制为1~256个字符，允许包含字母、数字、短划线（-）、正斜线（/）、半角句号（.）和下划线（_），支持中文字符",
            "name": "entry_description",
            "options": [
              "--entry-description"
            ],
            "raw_name": "EntryDescription",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The access control policy group entries, which are IP address entries or CIDR block entries.\n\nYou can add up to 50 entries at a time",
      "help_zh": "访问控制策略组条目，即IP地址条目或IP地址段条目。\n\n一次最多支持添加50个条目",
      "location": "query",
      "name": "acl_entries",
      "options": [
        "--acl-entries"
      ],
      "param_style": "repeatList",
      "raw_name": "AclEntries",
      "required": false,
      "type": "array"
    },
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF3898",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId value as the ClientToken value. The RequestId value is different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\ntrue: performs a dry run without creating the access control policy group. The system checks the required parameters, request format, and business limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\nfalse (default): sends a Normal request, passes the dry run, and returns an HTTP 2xx status code and directly performs the operation",
      "help_zh": "是否只预检此次请求，取值：\ntrue：发送检查请求，不会创建访问控制策略组。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "rg-acfmwj7wvng3jbi",
      "help_en": "The resource group ID",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "tag-key",
            "help_en": "The label key of the access control policy group. Once specified, the label key cannot be an empty string.\n\nThe label key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 label keys",
            "help_zh": "访问控制策略组的标签键。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签键",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "tag-value",
            "help_en": "The label value of the access control policy group. Once specified, the label value can be an empty string.\n\nThe label value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 label values",
            "help_zh": "访问控制策略组的标签值。一旦输入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签值",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The label information of the access control policy group",
      "help_zh": "访问控制策略组的标签信息",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回信息列表",
        "properties": {
          "AclId": {
            "backendName": "data.aclId",
            "description_en": "The access control policy group ID",
            "description_zh": "访问控制策略组ID",
            "example": "nacl-hp34s2h0xx1ht4nwo****",
            "mappingResourcePropertyName": "$.AclId",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "CEF72CEB-54B6-4AE8-B225-F876FF7BA984",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
