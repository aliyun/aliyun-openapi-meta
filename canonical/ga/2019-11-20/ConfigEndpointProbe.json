{
  "camel_example": "aliyun ga ConfigEndpointProbe --region cn-hangzhou --EndpointGroupId 'epg-bp1dmlohjjz4kqaun0000' --Endpoint '127.0.XX.XX' --EndpointType Ip --Enable true --RegionId 'cn-hangzhou'",
  "cmd_full_name": "ga config-endpoint-probe",
  "cmd_name": "config-endpoint-probe",
  "deprecated": false,
  "description_en": "Configures latency monitoring for an endpoint",
  "description_zh": "调用ConfigEndpointProbe接口配置终端节点时延探测",
  "kebab_example": "aliyun ga config-endpoint-probe --biz-region-id cn-hangzhou --endpoint-group-id epg-bp1dmlohjjz4kqaun0000 --biz-endpoint-type Ip --biz-endpoint 127.0.XX.XX --enable true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ConfigEndpointProbe",
  "operation": {
    "action": "ConfigEndpointProbe",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the GA instance. Set the value to cn-hangzhou",
      "help_zh": "全球加速实例所在的地域ID，仅取值cn-hangzhou",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the request ID as the client token. The request ID may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "epg-bp1dmlohjjz4kqaun****",
      "help_en": "The endpoint group ID",
      "help_zh": "终端节点组ID",
      "location": "query",
      "name": "endpoint_group_id",
      "options": [
        "--endpoint-group-id"
      ],
      "raw_name": "EndpointGroupId",
      "required": true,
      "type": "string"
    },
    {
      "example": "Ip",
      "help_en": "The type of the endpoint. Valid values:\n\nIp: a custom IP address.\nDomain: a custom domain name.\nEIP: an Alibaba Cloud elastic IP address (EIP).\nPublicIp: an Alibaba Cloud public IP address",
      "help_zh": "终端节点类型。取值：\nIp：云下自定义IP。\nDomain：云下自定义域名。\nEIP：云上EIP。\nPublicIp：云上公网IP",
      "location": "query",
      "name": "endpoint_type",
      "options": [
        "--biz-endpoint-type"
      ],
      "raw_name": "EndpointType",
      "required": true,
      "type": "string"
    },
    {
      "example": "127.0.XX.XX",
      "help_en": "The endpoint",
      "help_zh": "终端节点",
      "location": "query",
      "name": "endpoint",
      "options": [
        "--biz-endpoint"
      ],
      "raw_name": "Endpoint",
      "required": true,
      "type": "string"
    },
    {
      "example": "tcp",
      "help_en": "The protocol that is used to monitor latency. Valid values:\n\ntcp: TCP.\nicmp: ICMP",
      "help_zh": "时延探测协议。取值：\ntcp：TCP协议。\nicmp：ICMP协议",
      "location": "query",
      "name": "probe_protocol",
      "options": [
        "--probe-protocol"
      ],
      "raw_name": "ProbeProtocol",
      "required": false,
      "type": "string"
    },
    {
      "example": "80",
      "help_en": "The port that is used to monitor latency. Valid values: 0 to 65535",
      "help_zh": "时延探测端口。取值范围：0～65535",
      "location": "query",
      "name": "probe_port",
      "options": [
        "--probe-port"
      ],
      "raw_name": "ProbePort",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to enable latency monitoring. Valid values:\n\ntrue\nfalse (default)",
      "help_zh": "是否开启时延探测。取值：\n\ntrue：开启。\n\nfalse（默认值）：不开启",
      "location": "query",
      "name": "enable",
      "options": [
        "--enable"
      ],
      "raw_name": "Enable",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回列表信息",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "6FEA0CF3-D3B9-43E5-A304-D217037876A8",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
