{
  "camel_example": "aliyun ga UntagResources --region cn-hangzhou --ResourceId.1 'ga-bp149u6o36qt1as9b0000' --ResourceType accelerator --RegionId 'cn-hangzhou'",
  "cmd_full_name": "ga untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from Global Accelerator (GA) resources",
  "description_zh": "调用UntagResources接口为全球加速相关资源删除标签",
  "kebab_example": "aliyun ga untag-resources --biz-region-id cn-hangzhou --resource-type accelerator --resource-id ga-bp149u6o36qt1as9b0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the GA instance is deployed. Set the value to cn-hangzhou",
      "help_zh": "全球加速实例所在的地域ID，仅取值cn-hangzhou",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the request ID as the client token. The request ID may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "accelerator",
      "help_en": "The type of the resource whose tags you want to remove. Valid values:\n\naccelerator: a standard GA instance\nbasicaccelerator: a basic GA instance\nbandwidthpackage: a bandwidth plan\nacl: an access control list (ACL).\nendpointgroup: an endpoint group",
      "help_zh": "待删除标签的全球加速资源类型。取值：\naccelerator：标准型全球加速实例。\nbasicaccelerator：基础型全球加速实例。\nbandwidthpackage：带宽包。\nacl：访问控制策略组。\nendpointgroup：终端节点组",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags of the specified resource. Valid values:\n\ntrue: yes\nfalse (default): no",
      "help_zh": "是否将指定全球加速资源下所有标签全部删除。 取值： \n\ntrue：是。\n  \nfalse（默认值）：否",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The IDs of the resources whose tags you want to remove.\n\nIf you set ResourceType to accelerator, set the value of ResourceId to the ID of a standard GA instance.\nIf you set ResourceType to basicaccelerator, set the value of ResourceId to the ID of a basic GA instance.\nIf you set ResourceType to bandwidthpackage, set the value of ResourceId to the ID of a bandwidth plan.\nIf you set ResourceType to acl, set the value of ResourceId to the ID of an ACL.\nIf you set ResourceType to endpointgroup, set the value of ResourceId to the ID of an endpoint group.\n\nYou can specify up to 50 GA resource IDs",
      "help_zh": "待删除标签的全球加速资源ID。\n当ResourceType取值为accelerator时，取值为标准型全球加速实例ID。\n当ResourceType取值为basicaccelerator时，取值为基础型全球加速实例ID。\n当ResourceType取值为bandwidthpackage时，取值为带宽包ID。\n当ResourceType取值为acl时，取值为访问控制策略组ID。\n当ResourceType取值为endpointgroup时，取值为终端节点组ID。\n\n最多支持输入50个全球加速资源ID",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The key of the tag to be removed.\n\nThe system removes all tags with this tag key.\n\nYou can specify up to 20 tag keys.\n\nIf the All parameter is set to true, this parameter does not take effect",
      "help_zh": "指定待删除标签的标签键。\n\n系统会删除全球加速资源上所有该标签键的标签。\n\n最多支持输入20个标签键。\n\nAll取值为true时，本参数不生效",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The list of response parameters",
        "description_zh": "返回参数列表",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "54B48E3D-DF70-471B-AA93-08E683A1B45",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
