{
  "camel_example": "aliyun ga EnableApplicationMonitor --region cn-hangzhou --TaskId 'sm-bp1fpdjfju9k8yr1y0000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "ga enable-application-monitor",
  "cmd_name": "enable-application-monitor",
  "deprecated": false,
  "description_en": "Enables an origin probing task",
  "description_zh": "调用EnableApplicationMonitor接口开启源站探测",
  "kebab_example": "aliyun ga enable-application-monitor --biz-region-id cn-hangzhou --task-id sm-bp1fpdjfju9k8yr1y0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "EnableApplicationMonitor",
  "operation": {
    "action": "EnableApplicationMonitor",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the Global Accelerator (GA) instance. Set the value to cn-hangzhou",
      "help_zh": "全球加速实例所属的地域，仅取值：cn-hangzhou",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\nIf you do not specify this parameter, the system automatically uses the request ID as the client token. The request ID may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "sm-bp1fpdjfju9k8yr1y****",
      "help_en": "The ID of the origin probing task that you want to enable",
      "help_zh": "需要开启的源站探测任务ID",
      "location": "query",
      "name": "task_id",
      "options": [
        "--task-id"
      ],
      "raw_name": "TaskId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回请求列表",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "04F0F334-1335-436C-A1D7-6C044FE73368",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
