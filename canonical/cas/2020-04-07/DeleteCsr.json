{
  "camel_example": "aliyun cas DeleteCsr --region cn-hangzhou --CsrId 3013",
  "cmd_full_name": "cas delete-csr",
  "cmd_name": "delete-csr",
  "deprecated": false,
  "description_en": "Deletes a certificate signing request (CSR)",
  "description_zh": "如果您不再需要某个CSR，可以将其删除",
  "kebab_example": "aliyun cas delete-csr --csr-id 3013 --api-version 2020-04-07 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteCsr",
  "operation": {
    "action": "DeleteCsr",
    "api_style": "RPC",
    "api_version": "2020-04-07",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "3013",
      "help_en": "The ID of the CSR",
      "help_zh": "CSR ID",
      "location": "query",
      "name": "csr_id",
      "options": [
        "--csr-id"
      ],
      "raw_name": "CsrId",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "cas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "对象",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "D3F1FA43-1C26-50A2-8F0F-7A03851DBB46",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
