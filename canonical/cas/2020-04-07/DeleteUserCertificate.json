{
  "camel_example": "aliyun cas DeleteUserCertificate --region cn-hangzhou --CertId 7562353",
  "cmd_full_name": "cas delete-user-certificate",
  "cmd_name": "delete-user-certificate",
  "deprecated": false,
  "description_en": "Deletes an expired, revoked, or uploaded certificate",
  "description_zh": "删除已过期、已吊销或上传的证书",
  "kebab_example": "aliyun cas delete-user-certificate --cert-id 7562353 --api-version 2020-04-07 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteUserCertificate",
  "operation": {
    "action": "DeleteUserCertificate",
    "api_style": "RPC",
    "api_version": "2020-04-07",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "7562353",
      "help_en": "The certificate ID.\nYou can call https://help.aliyun.com/document_detail/455804.html to obtain the ID",
      "help_zh": "证书ID。\n此ID可通过调用https://help.aliyun.com/document_detail/455804.html获取",
      "location": "query",
      "name": "cert_id",
      "options": [
        "--cert-id"
      ],
      "raw_name": "CertId",
      "required": true,
      "type": "int"
    },
    {
      "example": "XXX",
      "help_en": "The client token that is used to ensure the idempotence of the request. The value of this parameter is generated by the client and must be unique among different requests. The value can be up to 64 characters in length and cannot contain non-ASCII characters",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response object",
        "description_zh": "对象",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求消息的返回ID",
            "example": "3E50D480-9765-5CFD-9650-9BACCECA5135",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
