{
  "camel_example": "aliyun vpc DeleteIpv6Gateway --region cn-hangzhou --Ipv6GatewayId 'ipv6gw-hp3y0l3ln89j80000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "vpc delete-ipv6-gateway",
  "cmd_name": "delete-ipv6-gateway",
  "deprecated": false,
  "description_en": "Deletes an IPv6 gateway",
  "description_zh": "调用DeleteIpv6Gateway接口删除IPv6网关",
  "kebab_example": "aliyun vpc delete-ipv6-gateway --biz-region-id cn-hangzhou --ipv6-gateway-id ipv6gw-hp3y0l3ln89j80000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteIpv6Gateway",
  "operation": {
    "action": "DeleteIpv6Gateway",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-huhehaote",
      "help_en": "The region ID of the IPv6 gateway. You can call the https://help.aliyun.com/document_detail/36063.html operation to query the region ID",
      "help_zh": "IPv6网关所属的地域ID。您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID。  ",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ipv6gw-hp3y0l3ln89j8****",
      "help_en": "The instance ID of the IPv6 gateway that you want to delete",
      "help_zh": "要删除的IPv6网关实例ID",
      "location": "query",
      "name": "ipv6_gateway_id",
      "options": [
        "--ipv6-gateway-id"
      ],
      "raw_name": "Ipv6GatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run. The system checks the required parameters, request syntax, and business restrictions. If the request fails the dry run, an error message is returned. If the request passes the dry run, `DryRunOperation` is returned.\n\nfalse (default): performs a dry run and sends the request. If the request passes the dry run, the IPv6 gateway is deleted",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：只预检此次请求，不会删除IPsec服务端。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误信息。如果检查通过，则返回`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后直接删除IPsec服务端",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "E9A8AABE-A84B-4AF2-A68A-8E2EA190E7AE",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
