{
  "camel_example": "aliyun vpc DeleteIpv4Gateway --region cn-hangzhou --Ipv4GatewayId 'ipv4gw-5tsnc6s4ogsedtp3k0000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "vpc delete-ipv4-gateway",
  "cmd_name": "delete-ipv4-gateway",
  "deprecated": false,
  "description_en": "Deletes an IPv4 gateway",
  "description_zh": "调用DeleteIpv4Gateway接口删除IPv4网关",
  "kebab_example": "aliyun vpc delete-ipv4-gateway --ipv4-gateway-id ipv4gw-5tsnc6s4ogsedtp3k0000 --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteIpv4Gateway",
  "operation": {
    "action": "DeleteIpv4Gateway",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "ipv4gw-5tsnc6s4ogsedtp3k****",
      "help_en": "The instance ID of the IPv4 gateway that you want to delete",
      "help_zh": "要删除的IPv4网关的实例ID",
      "location": "query",
      "name": "ipv4_gateway_id",
      "options": [
        "--ipv4-gateway-id"
      ],
      "raw_name": "Ipv4GatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "public",
      "help_en": "The public pattern of the VPC after the IPv4 gateway is deleted. Valid values:\n\nprivate: default value. After the IPv4 gateway is deleted, the VPC becomes a purely private network VPC without public network access.\npublic: after the IPv4 gateway is deleted, public network access of the VPC is no longer centrally controlled by the IPv4 gateway. After a public IP address is attached to an instance, the instance can directly access the Internet",
      "help_zh": "选择删除IPv4网关后VPC的公网模式，取值：\n\nprivate：默认值，删除IPv4网关后，VPC将变成纯私网VPC，不具备公网访问能力。\npublic：删除IPv4网关后，VPC公网访问不再受IPv4网关集中控制，公网IP绑定实例后缺省可以直接访问公网",
      "location": "query",
      "name": "internet_mode",
      "options": [
        "--internet-mode"
      ],
      "raw_name": "InternetMode",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "ap-southeast-6",
      "help_en": "The region ID of the IPv4 gateway that you want to delete.\n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the most recent region list",
      "help_zh": "要删除的IPv4网关所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\ntrue: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\nfalse (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the IPv4 gateway is deleted",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会删除IPv4网关。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接删除IPv4网关",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "21133AC0-0636-521B-A400-253818691A56",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
