{
  "camel_example": "aliyun vpc CreateForwardEntry --region cn-hangzhou --ExternalPort 8080 --ExternalIp '116.28.XX.XX' --IpProtocol TCP --RegionId 'cn-hangzhou' --InternalPort 80 --InternalIp '192.168.XX.XX' --ForwardTableId 'ftb-bp1mbjubq34hlcqpa0000'",
  "cmd_full_name": "vpc create-forward-entry",
  "cmd_name": "create-forward-entry",
  "deprecated": false,
  "description_en": "Adds a DNAT entry to a DNAT table by calling the CreateForwardEntry operation",
  "description_zh": "调用CreateForwardEntry接口在DNAT列表中添加DNAT条目",
  "kebab_example": "aliyun vpc create-forward-entry --biz-region-id cn-hangzhou --forward-table-id ftb-bp1mbjubq34hlcqpa0000 --external-ip 116.28.XX.XX --external-port 8080 --internal-ip 192.168.XX.XX --internal-port 80 --ip-protocol TCP --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateForwardEntry",
  "operation": {
    "action": "CreateForwardEntry",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the NAT gateway.\n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the region ID",
      "help_zh": "NAT网关所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID。  ",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "ftb-bp1mbjubq34hlcqpa****",
      "help_en": "The ID of the DNAT table",
      "help_zh": "DNAT列表的ID。 ",
      "location": "query",
      "name": "forward_table_id",
      "options": [
        "--forward-table-id"
      ],
      "raw_name": "ForwardTableId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "116.28.XX.XX",
      "help_en": "If you add a DNAT entry for an Internet NAT gateway, this parameter specifies the elastic IP address (EIP) that provides public network access.\nIf you add a DNAT entry for a VPC NAT gateway, this parameter specifies the NAT IP address that provides external network access",
      "help_zh": "当为公网NAT网关配置DNAT条目时，提供公网访问的弹性公网IP地址。\n当为VPC NAT网关配置DNAT条目时，提供外部网络访问的NAT IP地址",
      "location": "query",
      "name": "external_ip",
      "options": [
        "--external-ip"
      ],
      "raw_name": "ExternalIp",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "8080",
      "help_en": "If you add a DNAT entry for an Internet NAT gateway, this parameter specifies the external port or port range for port forwarding.\n \n    - Valid port values: 1 to 65535.\n    - To specify a port range, separate the start and end ports with a forward slash (/), such as `10/20`.\n    - If ExternalPort is set to a port range, InternalPort must also be set to a port range with the same number of ports. For example, if ExternalPort is set to `10/20`, InternalPort can be set to `80/90`.\n\nIf you add a DNAT entry for a VPC NAT gateway, this parameter specifies the port on the NAT IP address that is accessed by the external network. Valid values: 1 to 65535",
      "help_zh": "当为公网NAT网关配置DNAT条目时， 需要进行端口转发的外部端口或端口段。\n \n    - 输入的端口范围需要在1~65535之间。\n    - 如果需要在端口段内转发，请在输入时以正斜线（/）隔开起始端口，例如`10/20`。\n    - 如果ExternalPort设置为端口段，则InternalPort也需要设置为端口段，且端口段的端口个数相同，例如ExternalPort设置为`10/20`，InternalPort设置为`80/90`。\n\n当为VPC NAT网关配置DNAT条目时，NAT IP地址被外部网络访问的端口，取值范围：1~65535",
      "location": "query",
      "name": "external_port",
      "options": [
        "--external-port"
      ],
      "raw_name": "ExternalPort",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "192.168.XX.XX",
      "help_en": "If you add a DNAT entry for an Internet NAT gateway, this parameter specifies the private IP address of the ECS instance that needs to communicate over the Internet. The private IP address must meet the following conditions: \n        \n    - It must belong to the CIDR block of the VPC in which the NAT gateway resides. \n\n    - The DNAT entry takes effect only when the IP address is used by an ECS instance that is not associated with an EIP. \n\nIf you add a DNAT entry for a VPC NAT gateway, this parameter specifies the private IP address that communicates through the DNAT rule",
      "help_zh": "当为公网NAT网关配置DNAT条目时，需要进行公网通信的ECS实例的私网IP地址，该私网IP地址需满足以下条件： \n        \n    - 必须属于NAT网关所在的VPC的网段。 \n\n    - 必须被一个ECS实例使用且该实例没有绑定EIP时，DNAT条目才生效。 \n\n当为VPC NAT网关配置DNAT条目时，需要通过DNAT规则进行通信的私网IP地址",
      "location": "query",
      "name": "internal_ip",
      "options": [
        "--internal-ip"
      ],
      "raw_name": "InternalIp",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "80",
      "help_en": "If you add a DNAT entry for an Internet NAT gateway, this parameter specifies the internal port or port range for port forwarding. Valid values: 1 to 65535.\n\nIf you add a DNAT entry for a VPC NAT gateway, this parameter specifies the destination port of the ECS instance to which traffic is mapped. Valid values: 1 to 65535",
      "help_zh": "当为公网NAT网关配置DNAT条目时，需要进行端口转发的内部端口或端口段，取值范围：1~65535。\n\n当为VPC NAT网关配置DNAT条目时，需要映射的目标ECS实例端口，取值范围：1~65535",
      "location": "query",
      "name": "internal_port",
      "options": [
        "--internal-port"
      ],
      "raw_name": "InternalPort",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "TCP",
      "help_en": "The protocol type. Valid values: \n\nTCP: forwards TCP packets.   \n\nUDP: forwards UDP packets.   \n\nAny: forwards packets of all protocols. If IpProtocol is set to Any, ExternalPort and InternalPort must also be set to Any to implement DNAT IP mapping",
      "help_zh": "协议类型，取值： \n\nTCP：转发TCP协议的报文。   \n\nUDP：转发UDP协议的报文。   \n\nAny：转发所有协议的报文。如果IpProtocol配置为Any，则ExternalPort和InternalPort也必须配置为Any，实现DNAT IP映射",
      "location": "query",
      "name": "ip_protocol",
      "options": [
        "--ip-protocol"
      ],
      "raw_name": "IpProtocol",
      "required": true,
      "type": "string"
    },
    {
      "example": "ForwardEntry-1",
      "help_en": "The name of the DNAT rule.\n\nThe name must be 2 to 128 characters in length and must start with a letter or Chinese character. It cannot start with `http://` or `https://`",
      "help_zh": "DNAT规则的名称。\n\n长度为2~128个字符，必须以大小写字母或中文开头，但不能以`http://`或`https://`开头",
      "location": "query",
      "name": "forward_entry_name",
      "options": [
        "--forward-entry-name"
      ],
      "raw_name": "ForwardEntryName",
      "required": false,
      "type": "string"
    },
    {
      "example": "0c593ea1-3bea-11e9-b96b-88e9fe6****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable port breaking. Valid values:\n\ntrue: enables port breaking.\n\nfalse (default): disables port breaking.\n\nIf a DNAT entry and an SNAT entry use the same public IP address, and you want to configure a port number greater than 1024, set PortBreak to true",
      "help_zh": "是否开启端口突破，取值：\n\ntrue：开启端口突破。\n\nfalse（默认值）：不开启端口突破。\n\n当DNAT条目和SNAT条目使用同一个公网IP地址时，如果您需要配置大于1024的端口号，您需要指定PortBreak为true",
      "location": "query",
      "name": "port_break",
      "options": [
        "--port-break"
      ],
      "raw_name": "PortBreak",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run without creating a DNAT entry. The system checks the AccessKey pair, the authorization of the Resource Access Management (RAM) user, and the required parameters. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\n\nfalse (default): sends a Normal request. If the check succeeds, a 2xx HTTP status code is returned and the DNAT entry is created",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：发送检查请求，不会创建DNAT条目。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并创建DNAT条目",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "ForwardEntryId": {
            "backendName": "data.forwardEntryId",
            "description_en": "The ID of the DNAT entry",
            "description_zh": "DNAT条目的ID",
            "example": "fwd-119smw5tkasdf****",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "A4AEE536-A97A-40EB-9EBE-53A6948A6928",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
