{
  "camel_example": "aliyun vpc DeleteIpsecServer --region cn-hangzhou --IpsecServerId 'iss-bp1jougp8cfsbo8y90000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "vpc delete-ipsec-server",
  "cmd_name": "delete-ipsec-server",
  "deprecated": false,
  "description_en": "Deletes an IPsec server",
  "description_zh": "调用DeleteIpsecServer接口删除IPsec服务端",
  "kebab_example": "aliyun vpc delete-ipsec-server --biz-region-id cn-hangzhou --ipsec-server-id iss-bp1jougp8cfsbo8y90000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteIpsecServer",
  "operation": {
    "action": "DeleteIpsecServer",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the IPsec server",
      "help_zh": "IPsec服务端所属地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "iss-bp1jougp8cfsbo8y9****",
      "help_en": "The ID of the IPsec server",
      "help_zh": "IPsec服务端ID",
      "location": "query",
      "name": "ipsec_server_id",
      "options": [
        "--ipsec-server-id"
      ],
      "raw_name": "IpsecServerId",
      "required": true,
      "type": "string"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44-00****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId of each API request is different",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run without deleting the IPsec server. The system checks the required parameters, request syntax, and business restrictions. If the check fails, the corresponding error message is returned. If the check succeeds, `DryRunOperation` is returned.\n\nfalse (default): performs a dry run and sends the request. After the check succeeds, the IPsec server is directly deleted",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：只预检此次请求，不会删除IPsec服务端。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误信息。如果检查通过，则返回`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后直接删除IPsec服务端",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "0ED8D006-F706-4D23-88ED-E11ED28DCAC0",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
