{
  "camel_example": "aliyun vpc CreateSnatEntry --region cn-hangzhou --RegionId 'cn-hangzhou' --SnatTableId 'stb-bp190wu8io1vgev0000'",
  "cmd_full_name": "vpc create-snat-entry",
  "cmd_name": "create-snat-entry",
  "deprecated": false,
  "description_en": "Calls the CreateSnatEntry operation to add an SNAT entry to an SNAT table",
  "description_zh": "调用CreateSnatEntry接口在SNAT列表中添加SNAT条目",
  "kebab_example": "aliyun vpc create-snat-entry --biz-region-id cn-hangzhou --snat-table-id stb-bp190wu8io1vgev0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateSnatEntry",
  "operation": {
    "action": "CreateSnatEntry",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the NAT gateway. \n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the region ID",
      "help_zh": "NAT网关所在的地域ID。 \n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "stb-bp190wu8io1vgev****",
      "help_en": "The ID of the SNAT table",
      "help_zh": "SNAT表的ID。 ",
      "location": "query",
      "name": "snat_table_id",
      "options": [
        "--snat-table-id"
      ],
      "raw_name": "SnatTableId",
      "required": true,
      "type": "string"
    },
    {
      "example": "vsw-bp1nhx2s9ui5o****",
      "help_en": "The ID of the vSwitch.\n\nWhen you add an SNAT entry for an Internet NAT gateway, this parameter specifies that ECS instances in the vSwitch can access the Internet by using the SNAT rule. If you specify multiple EIPs to configure an SNAT IP IPAM pool, connections are allocated to multiple EIPs by using a hash algorithm. Because the traffic of each connection varies, service traffic may be unevenly distributed among the EIPs. Add each EIP to the same Internet Shared Bandwidth instance to prevent service interruptions caused by bandwidth exhaustion on a single EIP.\n\nWhen you add an SNAT entry for a VPC NAT gateway, this parameter specifies that ECS instances in the vSwitch can access external networks by using the SNAT rule.\n\nYou must specify one of the SourceCIDR and SourceVSwitchId parameters, but you cannot specify both",
      "help_zh": "交换机的ID。\n\n当为公网NAT网关添加SNAT条目时，该参数表示该交换机内的ECS实例均可以通过SNAT规则访问公网。指定多个EIP配置SNAT IP地址池时，业务连接会通过哈希算法分配到多个EIP，由于每个连接的流量不同，可能会出现多EIP业务流量不均匀的情况，建议您将每个EIP都加入到同一个共享带宽中以避免单EIP带宽达到上限导致业务受损。\n\n当为VPC NAT网关添加SNAT条目时，该参数表示该交换机内的ECS实例均可以通过SNAT规则访问外部网络。\n\n参数SourceCIDR和参数SourceVSwtichId必须输入一个且不能都输入",
      "location": "query",
      "name": "source_vswitch_id",
      "options": [
        "--source-vswitch-id"
      ],
      "raw_name": "SourceVSwitchId",
      "required": false,
      "type": "string"
    },
    {
      "example": "10.1.1.0/24",
      "help_en": "The CIDR block of a VPC, vSwitch, or ECS instance. You can also specify a custom CIDR block.\n\nSNAT entries support the following granularities: \n\nVPC granularity: the CIDR block of the VPC to which the NAT gateway belongs. All ECS instances in the VPC can access the Internet or external networks by using the SNAT rule.\n\nvSwitch granularity: the CIDR block of a specified vSwitch (such as 192.168.1.0/24). ECS instances in the vSwitch can access the Internet or external networks by using the SNAT rule.\n\nECS granularity: the IP address of a specified ECS instance (such as 192.168.1.1/32). The ECS instance can access the Internet or external networks by using the SNAT rule.\n\nCustom CIDR block: all ECS instances in the specified CIDR block can access the Internet or external networks by using the SNAT service.\n\nYou must specify one of the SourceCIDR and SourceVSwitchId parameters, but you cannot specify both",
      "help_zh": "输入VPC、交换机或ECS实例的网段，还可以输入任意网段。\n\nSNAT条目有以下几种粒度： \n\nVPC粒度：NAT网关所在VPC的网段，该VPC下的所有ECS实例均可以通过SNAT规则访问互联网或者外部网络。\n\n交换机粒度：指定交换机的网段（如192.168.1.0/24），该交换机下的ECS实例均可以通过SNAT规则访问互联网或者外部网络。\n\nECS粒度：指定ECS实例的地址（如192.168.1.1/32），该ECS实例可以通过SNAT规则访问互联网或者外部网络。\n\n输入任意网段，该网段下的所有ECS实例均可以通过SNAT服务访问互联网或者外部网络。\n\n参数SourceCIDR和参数SourceVSwtichId必须输入一个且不能都输入",
      "location": "query",
      "name": "source_cidr",
      "options": [
        "--source-cidr"
      ],
      "raw_name": "SourceCIDR",
      "required": false,
      "type": "string"
    },
    {
      "example": "47.98.XX.XX",
      "help_en": "When you add an SNAT entry for an Internet NAT gateway:\nThe SnatIp parameter is required.\n\nThis parameter specifies the EIPs in the SNAT entry. Separate multiple EIPs with commas (,).\n\nIf SnatIp specifies only one public IP address, the ECS instance uses the specified public IP address to access the Internet.\n\nIf SnatIp specifies multiple public IP addresses, the ECS instance randomly uses one of the public IP addresses in SnatIp to access the Internet.\n\nIf you specify multiple EIPs to configure an SNAT IP IPAM pool, connections are allocated to multiple EIPs by using a hash algorithm. Because the traffic of each connection varies, service traffic may be unevenly distributed among the EIPs. Add each EIP to the same Internet Shared Bandwidth instance to prevent service interruptions caused by bandwidth exhaustion on a single EIP.\n\nWhen you add an SNAT entry for a VPC NAT gateway:\n\nThis parameter specifies the NAT IP addresses in the SNAT entry. Separate multiple NAT IP addresses with commas (,).\n\nYou must specify one of the SnatIp and NetworkInterfaceId parameters, but you cannot specify both",
      "help_zh": "为公网NAT网关添加SNAT条目时 ：\n参数SnatIp必填。\n\n该参数为SNAT条目中的EIP，多个EIP之间用半角逗号（,）隔开。\n\n如果 SnatIp 仅指定了一个公网 IP，ECS 实例使用指定的公网 IP 访问互联网。\n\n如果 SnatIp 指定了多个公网 IP，ECS 实例随机使用 SnatIp 中的公网 IP 访问互联网。\n\n指定多个EIP配置SNAT IP地址池时，业务连接会通过哈希算法分配到多个EIP，由于每个连接的流量不同，可能会出现多EIP业务流量不均匀的情况，建议您将每个EIP都加入到同一个共享带宽中以避免单EIP带宽达到上限导致业务受损。\n\n为VPC NAT网关添加SNAT条目时：\n\n该参数为SNAT条目中的NAT IP地址，多个NAT IP地址之间用半角逗号（,）隔开。\n\n参数 SnatIp 和参数 NetworkInterfaceId 必须输入一个且不能都输入",
      "location": "query",
      "name": "snat_ip",
      "options": [
        "--snat-ip"
      ],
      "raw_name": "SnatIp",
      "required": false,
      "type": "string"
    },
    {
      "example": "SnatEntry-1",
      "help_en": "The name of the SNAT entry.\n\nThe name must be 2 to 128 characters in length and must start with a letter or Chinese character. It cannot start with `http://` or `https://`",
      "help_zh": "SNAT条目的名称。\n\n长度为2~128个字符，必须以大小写字母或中文开头，但不能以`http://`或`https://`开头",
      "location": "query",
      "name": "snat_entry_name",
      "options": [
        "--snat-entry-name"
      ],
      "raw_name": "SnatEntryName",
      "required": false,
      "type": "string"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The `ClientToken` value can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。`ClientToken`只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的Requestld作为ClientToken标识。每次API请求的Requestld可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "Specifies whether to enable EIP affinity. Valid values:\n\n0 (default): disables EIP affinity.\n\n1: enables EIP affinity.\n\nAfter EIP affinity is enabled, if the SNAT entry is bindded with multiple EIPs or NAT IP addresses, the same client uses the same EIP or NAT IP address to access the same destination IP address. Otherwise, the client randomly selects an EIP or NAT IP address from the bindded ones",
      "help_zh": "是否打开IP亲和性。取值：\n\n0（默认值）：关闭IP亲和性。\n\n1：打开IP亲和性。\n\n打开IP亲和性开关后，如果SNAT条目绑定多个EIP或NAT IP，同一个客户端访问同一目的IP时将使用相同的EIP或NAT IP进行访问，否则客户端将从绑定的EIP或NAT IP中随机选取进行访问",
      "location": "query",
      "name": "eip_affinity",
      "options": [
        "--eip-affinity"
      ],
      "raw_name": "EipAffinity",
      "required": false,
      "type": "int"
    },
    {
      "example": "eni-gw8g131ef2dnbu3k****",
      "help_en": "The ID of the elastic network interface (ENI).\n\nThe IPv4 address set of the ENI is used as the SNAT address",
      "help_zh": "弹性网卡ID。\n\n会将弹性网卡的IPv4地址集作为SNAT地址",
      "location": "query",
      "name": "network_interface_id",
      "options": [
        "--network-interface-id"
      ],
      "raw_name": "NetworkInterfaceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run without creating the SNAT entry. The system checks the AccessKey pair, the authorization of the Resource Access Management (RAM) user, and the required parameters. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n\nfalse (default): sends a Normal request, and the SNAT entry is created after the check succeeds. A 2xx HTTP status code is returned",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：发送检查请求，不会创建SNAT条目。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并创建SNAT条目",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The ID of the SNAT entry",
        "description_zh": "SNAT条目ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "2315DEB7-5E92-423A-91F7-4C1EC9AD****",
            "type": "string",
            "visibility": "Public"
          },
          "SnatEntryId": {
            "backendName": "data.snatEntryId",
            "description_en": "The ID of the SNAT entry",
            "description_zh": "SNAT条目ID",
            "example": "snat-kmd6nv8fy****",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
