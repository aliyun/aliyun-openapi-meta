{
  "camel_example": "aliyun vpc DeleteNatGateway --region cn-hangzhou --RegionId 'cn-hangzhou' --NatGatewayId 'ngw-bp1uewa15k4iy57700000'",
  "cmd_full_name": "vpc delete-nat-gateway",
  "cmd_name": "delete-nat-gateway",
  "deprecated": false,
  "description_en": "Deletes a specified Internet NAT gateway",
  "description_zh": "调用DeleteNatGateway接口删除指定的公网NAT网关",
  "kebab_example": "aliyun vpc delete-nat-gateway --biz-region-id cn-hangzhou --nat-gateway-id ngw-bp1uewa15k4iy57700000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteNatGateway",
  "operation": {
    "action": "DeleteNatGateway",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the NAT gateway.\n\nYou can call https://help.aliyun.com/document_detail/36063.html to query the region ID",
      "help_zh": "NAT网关所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID。  ",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "ngw-bp1uewa15k4iy5770****",
      "help_en": "The instance ID of the NAT gateway that you want to delete",
      "help_zh": "要删除的NAT网关的实例ID。  ",
      "location": "query",
      "name": "nat_gateway_id",
      "options": [
        "--nat-gateway-id"
      ],
      "raw_name": "NatGatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcefully delete the NAT gateway. Valid values:\n\ntrue: forcefully deletes the NAT gateway. If you set this parameter to true:\n\n    - If the NAT gateway has SNAT rules, the system force deletes the SNAT rules.\n\n    - If the NAT gateway has DNAT rules, the system force deletes the DNAT rules.\n\n    - If the NAT gateway has associated elastic IP addresses (EIPs), the system automatically disassociates the EIPs.\n\n    - If the NAT gateway has NAT service plans that are not deleted, the system force deletes the NAT service plans.\n\nfalse (default): does not forcefully delete the NAT gateway. If you set this parameter to false:\n\n    - If the NAT gateway has NAT service plans that are not deleted, delete the NAT service plans first.\n\n    - If the NAT gateway has SNAT rules, delete the SNAT rules first.\n\n    - If the NAT gateway has DNAT rules, delete the DNAT rules first.\n\n    - If the NAT gateway has associated EIPs, disassociate the EIPs first",
      "help_zh": "是否强制删除NAT网关，取值：\n\ntrue：强制删除。当取值为true时，说明如下：\n\n    - 如果NAT网关有SNAT规则，系统会自动帮您删除SNAT规则。\n\n    - 如果NAT网关有DNAT规则，系统会自动帮您删除DNAT规则。\n\n    - 如果NAT网关有绑定EIP，系统会自动帮您解绑。\n\n    - 如果NAT网关有未删除的NAT带宽包，系统会自动帮您删除NAT带宽包。\n\nfalse（默认值）：不强制删除。当取值为false时，说明如下：\n\n    - 如果NAT网关有未删除的NAT带宽包，请先删除NAT带宽包。\n\n    - 如果NAT网关有SNAT规则，请先删除SNAT规则。\n\n    - 如果NAT网关有DNAT规则，请先删除DNAT规则。\n\n    - 如果NAT网关有绑定EIP，请先解绑EIP",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The request ID",
        "description_zh": "请求ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "0ED8D006-F706-4D23-88ED-E11ED28DCAC0",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
