{
  "camel_example": "aliyun vpc AssociateVpnGatewayWithCertificate --region cn-hangzhou --CertificateType Signature --VpnGatewayId 'vpn-bp1q8bgx4xnkm2ogj0000' --CertificateId '6bfe4218-ea1d0000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "vpc associate-vpn-gateway-with-certificate",
  "cmd_name": "associate-vpn-gateway-with-certificate",
  "deprecated": false,
  "description_en": "Associates a certificate with a VPN gateway by calling the AssociateVpnGatewayWithCertificate operation",
  "description_zh": "调用AssociateVpnGatewayWithCertificate接口为VPN网关绑定证书",
  "kebab_example": "aliyun vpc associate-vpn-gateway-with-certificate --biz-region-id cn-hangzhou --vpn-gateway-id vpn-bp1q8bgx4xnkm2ogj0000 --certificate-type Signature --certificate-id 6bfe4218-ea1d0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "AssociateVpnGatewayWithCertificate",
  "operation": {
    "action": "AssociateVpnGatewayWithCertificate",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the VPN gateway.\n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the region ID",
      "help_zh": "VPN网关所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "vpn-bp1q8bgx4xnkm2ogj****",
      "help_en": "The instance ID of the VPN gateway.\n\nOnly ShangMi (SM) VPN gateways support attaching certificates",
      "help_zh": "VPN网关实例ID。\n\n仅国密型VPN网关支持绑定证书",
      "location": "query",
      "name": "vpn_gateway_id",
      "options": [
        "--vpn-gateway-id"
      ],
      "raw_name": "VpnGatewayId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "Signature",
      "help_en": "The certificate type. Valid values:\n\nEncryption: specifies the SSL certificate as the encryption certificate.\nSignature: specifies the SSL certificate as the signing certificate",
      "help_zh": "证书类型。取值：\n\nEncryption：表示指定SSL证书为加密证书。\nSignature：表示指定SSL证书为签名证书",
      "location": "query",
      "name": "certificate_type",
      "options": [
        "--certificate-type"
      ],
      "raw_name": "CertificateType",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "6bfe4218-ea1d****",
      "help_en": "The certificate ID",
      "help_zh": "证书ID",
      "location": "query",
      "name": "certificate_id",
      "options": [
        "--certificate-id"
      ],
      "raw_name": "CertificateId",
      "required": true,
      "type": "string"
    },
    {
      "example": "0c593ea1-3bea****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run without actually associating the certificate. The system checks the required parameters, request syntax, and instance status. If the check fails, the corresponding error is returned. If the check succeeds, the corresponding request ID is returned.\n\nfalse (default): sends the request. After the request passes the check, the VPN gateway is associated with the certificate",
      "help_zh": "是否只预检此次请求。取值：\n\ntrue：表示只预检此次请求合法性，不会进行绑定操作。检查项包括是否填写了必需参数、请求格式、实例状态等。如果检查不通过，则返回对应错误；如果检查通过，则返回对应请求ID。\n\nfalse（默认值）：表示发送正常请求，通过检查后直接将VPN网关和证书进行绑定",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response struct",
        "description_zh": "返回数据结构体",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "4EC47282-1B74-4534-BD0E-403F3EE64CAF",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
