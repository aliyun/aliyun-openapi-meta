{
  "camel_example": "aliyun vpc CreateVSwitchCidrReservation --region cn-hangzhou --VSwitchId 'vsw-25navfgbue4g0000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "vpc create-vswitch-cidr-reservation",
  "cmd_name": "create-vswitch-cidr-reservation",
  "deprecated": false,
  "description_en": "Creates a reserved CIDR block for a vSwitch",
  "description_zh": "调用CreateVSwitchCidrReservation接口创建交换机预留网段",
  "kebab_example": "aliyun vpc create-vswitch-cidr-reservation --vswitch-id vsw-25navfgbue4g0000 --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateVSwitchCidrReservation",
  "operation": {
    "action": "CreateVSwitchCidrReservation",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "fields": [
          {
            "example": "FinanceDept",
            "help_en": "The tag key of the resource. You can specify up to 20 tag keys. If you specify this parameter, the value cannot be an empty string.\n\nA tag key can be up to 128 characters in length. It cannot start with aliyun or acs: and cannot contain `http://` or `https://`",
            "help_zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "FinanceJoshua",
            "help_en": "The tag value of the resource. You can specify up to 20 tag values. If you specify this parameter, the value can be an empty string.\n\nThe tag value can be up to 128 characters in length. It cannot start with aliyun or acs: and cannot contain `http://` or `https://`",
            "help_zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The resource tags",
      "help_zh": "资源标签",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "example": "vsw-25navfgbue4g****",
      "help_en": "The ID of the vSwitch for which you want to create a reserved CIDR block for a vSwitch",
      "help_zh": "要创建的交换机预留网段所属的交换机ID",
      "location": "query",
      "name": "vswitch_id",
      "options": [
        "--vswitch-id"
      ],
      "raw_name": "VSwitchId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ReservationName",
      "help_en": "The name of the reserved CIDR block for a vSwitch.\n\nThe name must be 1 to 128 characters in length and must start with a letter or Chinese character. It can contain digits, underscores (_), and hyphens (-). It cannot start with `http://` or `https://`",
      "help_zh": "交换机预留网段的名称。\n\n名称长度为1~128个字符，以英文字母或中文开头，可包含数字、下划线（_）和短划线（-）。不能以`http://`和`https://`开头",
      "location": "query",
      "name": "vswitch_cidr_reservation_name",
      "options": [
        "--vswitch-cidr-reservation-name"
      ],
      "raw_name": "VSwitchCidrReservationName",
      "required": false,
      "type": "string"
    },
    {
      "example": "ReservationDescription",
      "help_en": "The description of the reserved CIDR block for a vSwitch. If you leave this parameter empty, the default value is empty.\n\nThe description must be 1 to 256 characters in length and must start with a letter or Chinese character. It cannot start with `http://` or `https://`",
      "help_zh": "交换机预留网段的描述信息。不填则为空，默认值为空。\n\n描述长度为1~256个字符，以英文字母或中文开头，不能以`http://`和`https://`开头",
      "location": "query",
      "name": "vswitch_cidr_reservation_description",
      "options": [
        "--vswitch-cidr-reservation-description"
      ],
      "raw_name": "VSwitchCidrReservationDescription",
      "required": false,
      "type": "string"
    },
    {
      "example": "IPv4",
      "help_en": "The IP version of the reserved CIDR block for a vSwitch. Valid values:\n\nIPv4 (default)\nIPv6\n\nYou do not need to specify this parameter when creating an IPv4 reserved CIDR block for a vSwitch. This parameter is required when creating an IPv6 reserved CIDR block for a vSwitch",
      "help_zh": "交换机预留网段的IP版本。取值：\n\nIPv4 （默认值）\nIPv6\n\n创建 IPv4 预留网段时可以不填写，创建 IPv6 预留网段时必填",
      "location": "query",
      "name": "ip_version",
      "options": [
        "--ip-version"
      ],
      "raw_name": "IpVersion",
      "required": false,
      "type": "string"
    },
    {
      "example": "28",
      "help_en": "The mask of the reserved CIDR block for a vSwitch.\n\nIf IpVersion is set to IPv4, the mask length of the reserved CIDR block must be at least 2 bits longer than the IPv4 CIDR block mask of the vSwitch and cannot exceed 28.\nIf IpVersion is set to IPv6, the mask length of the reserved CIDR block must be longer than the IPv6 CIDR block mask of the vSwitch and cannot exceed 80.\n\nYou must specify either the VSwitchCidrReservationMask parameter or the VSwitchCidrReservationCidr parameter.\nThe reserved CIDR block cannot contain the system reserved IP addresses of the vSwitch",
      "help_zh": "交换机预留网段的掩码。\n\n当IpVersion取值为IPv4时，交换机预留网段的掩码长度必须比所属交换机的IPv4网段掩码大2且最大不超过28。\n当IpVersion取值为IPv6时，交换机预留网段的掩码长度必须要大于所属交换机的IPv6网段掩码长度且最大不超过80。\n\nVSwitchCidrReservationMask参数和VSwitchCidrReservationCidr参数必须输入一个。\n预留网段不能包含所属交换机的系统保留地址",
      "location": "query",
      "name": "vswitch_cidr_reservation_mask",
      "options": [
        "--vswitch-cidr-reservation-mask"
      ],
      "raw_name": "VSwitchCidrReservationMask",
      "required": false,
      "type": "string"
    },
    {
      "example": "192.168.1.64/28",
      "help_en": "The reserved CIDR block for a vSwitch.\n\nIf IpVersion is set to IPv4, the reserved CIDR block for a vSwitch must be a proper subset of the IPv4 CIDR block of the vSwitch, and the mask length cannot exceed 28.\nIf IpVersion is set to IPv6, the reserved CIDR block for a vSwitch must be a proper subset of the IPv6 CIDR block of the vSwitch, and the mask length cannot exceed 80.\n\nYou must specify either the VSwitchCidrReservationMask parameter or the VSwitchCidrReservationCidr parameter.\nThe reserved CIDR block cannot contain the system reserved IP addresses of the vSwitch",
      "help_zh": "交换机预留网段。\n\n当IpVersion取值为IPv4时，交换机预留网段必须是其所属交换机IPv4网段的真子集且网段的掩码长度不超过28。\n当IpVersion取值为IPv6时，交换机预留网段必须是其所属交换机IPv6网段的真子集且网段的掩码长度不超过80。\n\nVSwitchCidrReservationMask参数和VSwitchCidrReservationCidr参数必须输入一个。\n预留网段不能包含所属交换机的系统保留地址",
      "location": "query",
      "name": "vswitch_cidr_reservation_cidr",
      "options": [
        "--vswitch-cidr-reservation-cidr"
      ],
      "raw_name": "VSwitchCidrReservationCidr",
      "required": false,
      "type": "string"
    },
    {
      "example": "prefix",
      "help_en": "The type of the reserved CIDR block for a vSwitch. Valid values: prefix, which indicates that IP addresses are allocated by CIDR block.\n\nWhen users or cloud services automatically assign CIDR blocks to elastic network interfaces (ENIs), the CIDR blocks must be allocated from the reserved CIDR block for a vSwitch. If the IP addresses in the reserved CIDR block for a vSwitch are exhausted, the system returns an error",
      "help_zh": "交换机预留网段的类型。取值：prefix，表示按段分配地址。\n\n用户及云服务自动为弹性网卡ENI（Elastic Network Interface）分配网段时，必须从预留地址段分配。如果预留地址段的地址已经分配完，则系统会报错",
      "location": "query",
      "name": "vswitch_cidr_reservation_type",
      "options": [
        "--vswitch-cidr-reservation-type"
      ],
      "raw_name": "VSwitchCidrReservationType",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The expected number of IP prefixes to reserve. Valid values: 1 to 32",
      "help_zh": "期望预留的 IpPrefix 数量，范围1～32",
      "location": "query",
      "name": "ip_prefix_number",
      "options": [
        "--ip-prefix-number"
      ],
      "raw_name": "IpPrefixNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\ntrue: sends a check request without creating the reserved CIDR block for a vSwitch. The system checks whether the required parameters are specified, the request format is valid, and the service limits are not exceeded. If the check fails, the corresponding error message is returned. If the check passes, the `DryRunOperation` error code is returned.\nfalse (default): sends a Normal request. After the check passes, an HTTP 2xx status code is returned and the vSwitch reserved CIDR block for a vSwitch is created",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会创建交换机预留网段。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行创建",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the vSwitch.\n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the most recent region list",
      "help_zh": "交换机所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The instance ID of the reserved CIDR block for a vSwitch",
        "description_zh": "交换机预留网段的实例ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "54B48E3D-DF70-471B-AA93-08E683A1B45",
            "type": "string",
            "visibility": "Public"
          },
          "VSwitchCidrReservationId": {
            "backendName": "data.instanceId",
            "description_en": "The instance ID of the reserved CIDR block for a vSwitch",
            "description_zh": "交换机预留网段的实例ID",
            "example": "vcr-bp1m12saqteraw3rp****",
            "mappingResourcePropertyName": "$.VSwitchCidrReservationId",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
