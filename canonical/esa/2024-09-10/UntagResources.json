{
  "camel_example": "aliyun esa UntagResources --region cn-hangzhou --ResourceId.1 'example.com' --ResourceType site --RegionId 'cn-hangzhou'",
  "cmd_full_name": "esa untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Deletes a resource tag based on a specified resource ID",
  "description_zh": "根据指定的资源ID删除资源标签",
  "kebab_example": "aliyun esa untag-resources --biz-region-id cn-hangzhou --resource-id example.com --resource-type site --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2024-09-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the resources reside. Set the value to cn-hangzhou",
      "help_zh": "地域ID，取值范围如下：\n\n中国站：cn-hangzhou\n国际站：ap-southeast-1",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The ID of resource N. Valid values of N: 1 to 50",
      "help_zh": "资源ID。请输入站点ID或者DNS记录ID",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The key of tag N to remove from the resource. Valid values of N: 1 to 20",
      "help_zh": "标签键。N的取值范围：1~20",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    },
    {
      "example": "site",
      "help_en": "The resource type",
      "help_zh": "资源类型。取值范围如下：\n\n站点：site\nDNS记录：record",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags. Valid values:\n\ntrue\nfalse\n\nDefault value: false",
      "help_zh": "删除所有标签。取值：\ntrue：是。\nfalse：否。\n\n默认取值为false",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "ESA",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "85H66C7B-671A-4297-9187-2C4477247A74",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
