{
  "camel_example": "aliyun esa UpdateList --region cn-hangzhou --Description a_custom_list --Items '[\"10.1.1.1\"]' --Id 40000001 --Name example",
  "cmd_full_name": "esa update-list",
  "cmd_name": "update-list",
  "deprecated": false,
  "description_en": "Updates a custom list",
  "description_zh": "该接口用于更新自定义列表",
  "kebab_example": "aliyun esa update-list --id 40000001 --name example --description a_custom_list --items 10.1.1.1 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateList",
  "operation": {
    "action": "UpdateList",
    "api_style": "RPC",
    "api_version": "2024-09-10",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "40000001",
      "help_en": "The ID of the custom list. You can obtain the ID by calling the https://help.aliyun.com/document_detail/2850217.html operation",
      "help_zh": "自定义列表的ID，可通过调用https://help.aliyun.com/document_detail/2850217.html接口获取",
      "location": "formData",
      "name": "id",
      "options": [
        "--id"
      ],
      "raw_name": "Id",
      "required": true,
      "type": "int"
    },
    {
      "example": "example",
      "help_en": "The new name of the custom list. If this parameter is not specified, the original name is retained.\n\nNaming rules: Only letters, digits, and underscores are supported (`^\\w{1,64}$`). The name must be 1 to 64 characters in length",
      "help_zh": "更新后的自定义列表名称（需同时传入；不传保持原名称）。\n\n命名规则：仅支持字母、数字、下划线（`^\\w{1,64}$`），长度 1–64；",
      "location": "formData",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    },
    {
      "example": "a custom list",
      "help_en": "The new description of the list",
      "help_zh": "更新后的列表描述",
      "location": "formData",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "a custom list",
      "help_en": "The new list content. The value is a JSON array string, for example, `[\"1.1.1.1\",\"2.2.2.2\"]`.\n\nFull overwrite semantics: The specified `Items` value completely overwrites the existing list content instead of appending to it.\n\n⚠️ If this parameter is not specified or is set to an empty value, the existing list content is cleared. To retain existing items and append new ones, call `GetList` to retrieve the current `Items`, merge them, and then submit the combined list.\n\nElement format: The format depends on the `Kind` value specified when the list was created. UpdateList does not support modifying Kind.\nKind = `ip`: Each element must be a valid IP address or CIDR block. If an element is invalid, `WrongValueMatched` is returned.\nOther Kind values: The element format is subject to the relevant specifications. The number of elements is limited by the tenant quota `NumberItemsPerList`. This limit does not apply to the `ip` Kind",
      "help_zh": "更新后的列表内容（JSON 数组字符串，例 `[\"1.1.1.1\",\"2.2.2.2\"]`）。\n\n全量覆盖语义：传入的 `Items` 会完全覆盖原有列表内容，不是追加。\n\n⚠️ 不传或传空时，原列表内容会被清空。若需保留原项并追加，请先调用 `GetList` 取现有 `Items`，拼接后一次性提交。\n\n元素格式：与列表创建时的 `Kind` 相关（UpdateList 不支持修改 Kind）：\nKind = `ip`：每个元素必须为合法 IP 地址或 CIDR，不合法时返回 `WrongValueMatched`\n其他 Kind：元素格式以约定为准，数量受租户 quota `NumberItemsPerList` 限制（仅 `ip` Kind 不受此限制）",
      "location": "formData",
      "name": "items",
      "options": [
        "--items"
      ],
      "param_style": "json",
      "raw_name": "Items",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "ESA",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "36af3fcc-43d0-441c-86b1-428951dc8225",
            "title_en": "请求ID",
            "title_zh": "请求ID",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
