{
  "camel_example": "aliyun ehpc GetIfEcsTypeSupportHtConfig --region cn-hangzhou --InstanceType 'ecs.g6.large'",
  "cmd_full_name": "ehpc get-if-ecs-type-support-ht-config",
  "cmd_name": "get-if-ecs-type-support-ht-config",
  "deprecated": false,
  "description_en": "Queries whether hyper-threading can be enabled or disabled for an instance type",
  "description_zh": "调用GetIfEcsTypeSupportHtConfig查询指定的实例规格是否支持开启和关闭超线程",
  "kebab_example": "aliyun ehpc get-if-ecs-type-support-ht-config --instance-type ecs.g6.large --api-version 2018-04-12 --region cn-hangzhou",
  "method": "GET",
  "multi_version": true,
  "name": "GetIfEcsTypeSupportHtConfig",
  "operation": {
    "action": "GetIfEcsTypeSupportHtConfig",
    "api_style": "RPC",
    "api_version": "2018-04-12",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "ecs.g6.large",
      "help_en": "The Elastic Compute Service (ECS) instance type",
      "help_zh": "ECS实例规格",
      "location": "query",
      "name": "instance_type",
      "options": [
        "--instance-type"
      ],
      "raw_name": "InstanceType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "EHPC",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "DefaultHtEnabled": {
            "backendName": "DefaultHtEnabled",
            "description_en": "Indicates whether Hyper-Threading is enabled by default. Valid values:\n\ntrue: Hyper-Threading is enabled by default.\nfalse: Hyper-Threading is disabled by default.\n\nBy default, Hyper-Threading is not enabled for Super Computing Cluster (SCC) instance families but is enabled for other instance families",
            "description_zh": "是否默认开启超线程。可能值：\ntrue：默认开启超线程\nfalse：默认不开启超线程\nSCC规格族默认不开启超线程，其他规格族默认开启超线程",
            "example": "true",
            "type": "boolean",
            "visibility": "Public"
          },
          "InstanceType": {
            "backendName": "InstanceType",
            "description_en": "The ECS instance type",
            "description_zh": "ECS实例规格",
            "example": "ecs.g6.large",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "80CA7A93-7291-4402-B63D-86DF334E0A1F",
            "type": "string",
            "visibility": "Public"
          },
          "SupportHtConfig": {
            "backendName": "SupportHtConfig",
            "description_en": "Indicates whether hyper-threading is supported. Valid values:\n\ntrue: Hyper-threading is supported.\nfalse: Hyper-threading is not supported",
            "description_zh": "是否支持超线程配置。可能值：\ntrue：支持超线程\nfalse：不支持超线程",
            "example": "true",
            "type": "boolean",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
