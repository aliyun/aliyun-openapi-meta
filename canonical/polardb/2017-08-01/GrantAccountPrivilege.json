{
  "camel_example": "aliyun polardb GrantAccountPrivilege --region cn-hangzhou --AccountPrivilege 'ReadWrite,ReadOnly' --DBClusterId 'pc-0000000000000000' --DBName 'testdb_1,testdb_2' --AccountName testacc",
  "cmd_full_name": "polardb grant-account-privilege",
  "cmd_name": "grant-account-privilege",
  "deprecated": false,
  "description_en": "Grants a standard account the permissions to access one or more databases in a specified PolarDB cluster",
  "description_zh": "授权普通账号访问PolarDB集群的某个数据库",
  "kebab_example": "aliyun polardb grant-account-privilege --db-cluster-id pc-0000000000000000 --account-name testacc --db-name testdb_1,testdb_2 --account-privilege ReadWrite,ReadOnly --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GrantAccountPrivilege",
  "operation": {
    "action": "GrantAccountPrivilege",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-****************",
      "help_en": "The ID of the cluster",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testacc",
      "help_en": "The username of the account",
      "help_zh": "账号名",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    },
    {
      "example": "testdb_1,testdb_2",
      "help_en": "The names of the databases that the account can access. You can grant the access permissions on one or more databases to the specified standard account. If you need to specify multiple database names, separate the database names with commas (,)",
      "help_zh": "需要授权的数据库名称。支持同时对一个或多个数据库授权，多个数据库名称之间用英文逗号（,）隔开。 ",
      "location": "query",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "raw_name": "DBName",
      "required": true,
      "type": "string"
    },
    {
      "example": "ReadWrite,ReadOnly",
      "help_en": "The permissions that are granted to the account. Valid values:\n\nReadWrite: read and write permissions\n\nReadOnly: read-only permissions\n\nDMLOnly: The account is granted the permissions to execute only DML statements on the database.\n\nDDLOnly: The account is granted the permissions to execute only DDL statements on the database.\n\nReadIndex: The account has the read and index permissions on the database.\n\nThe number of AccountPrivilege values must be the consistent with the number of DBName values. Each account permission must correspond to a database name in sequence. For example, you can set DBName to `testdb_1,testdb_2` and set AccountPrivilege to `ReadWrite,ReadOnly`. In this case, the specified standard account is granted the read and write permissions on the testdb_1 database and the read permission on the testdb_2 database",
      "help_zh": "账号权限，取值范围如下： \n\nReadWrite：读写\nReadOnly：只读\nDMLOnly：只允许DML \nDDLOnly：只允许DDL\nReadIndex：只读+索引\n\nAccountPrivilege需与DBName输入的参数数量保持一致，且顺序对应。例如，输入DBName为`testdb_1,testdb_2`，AccountPrivilege为`ReadWrite,ReadOnly`，则表示授予目标账号对数据库testdb_1的读写权限，对数据库testdb_2的只读权限",
      "location": "query",
      "name": "account_privilege",
      "options": [
        "--account-privilege"
      ],
      "raw_name": "AccountPrivilege",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "2FED790E-FB61-4721-8C1C-07C627*****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
