{
  "camel_example": "aliyun polardb DescribeDBClusterAuditLogCollector --region cn-hangzhou --DBClusterId 'pc-000000000000000'",
  "cmd_full_name": "polardb describe-db-cluster-audit-log-collector",
  "cmd_name": "describe-db-cluster-audit-log-collector",
  "deprecated": false,
  "description_en": "Describe SQL collector for a PolarDB cluster. Features related to SQL collector include audit log and SQL Explorer",
  "description_zh": "查询PolarDB集群的SQL采集功能（例如审计日志、SQL洞察等）",
  "kebab_example": "aliyun polardb describe-db-cluster-audit-log-collector --db-cluster-id pc-000000000000000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDBClusterAuditLogCollector",
  "operation": {
    "action": "DescribeDBClusterAuditLogCollector",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "pc-***************",
      "help_en": "The ID of the cluster",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "CollectorStatus": {
            "backendName": "data.configValue",
            "description_en": "The status of SQL collector. Valid values:\n\nEnable\n\nDisabled",
            "description_zh": "SQL采集功能的状态，取值范围如下：\nEnable：开启。\nDisabled：关闭",
            "example": "Disabled",
            "mappingResourcePropertyName": "$.CollectorStatus",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "59011D2B-2A38-4207-A86C-72BC1F882D19",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
