{
  "camel_example": "aliyun polardb ResetAccount --region cn-hangzhou --DBClusterId 'pc-00000000000000' --AccountName testacc",
  "cmd_full_name": "polardb reset-account",
  "cmd_name": "reset-account",
  "deprecated": false,
  "description_en": "Resets the permissions of a privileged account for a PolarDB cluster",
  "description_zh": "重置PolarDB高权限账号",
  "kebab_example": "aliyun polardb reset-account --db-cluster-id pc-00000000000000 --account-name testacc --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ResetAccount",
  "operation": {
    "action": "ResetAccount",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-**************",
      "help_en": "The ID of the cluster",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testacc",
      "help_en": "The username of the account.\n\nYou can reset only the permissions of a privileged account",
      "help_zh": "账号名称。 \n\n仅支持重置高权限账号",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    },
    {
      "example": "Pw123456",
      "help_en": "The password of the account. The password must meet the following requirements:\n\nIt must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n\nThe password must be 8 to 32 characters in length.\n\nSpecial characters include `! @ # $ % ^ & * ( ) _ + - =`",
      "help_zh": "账号密码，密码需满足如下要求： \n\n大写字母、小写字母、数字、特殊字符至少占三种。 \n长度为8~32位。\n特殊字符为`!@#$%^&*()_+-=` ",
      "location": "query",
      "name": "account_password",
      "options": [
        "--account-password"
      ],
      "raw_name": "AccountPassword",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "2FED790E-FB61-4721-8C1C-07C627******",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
