{
  "camel_example": "aliyun polardb ModifyAccountPassword --region cn-hangzhou --NewAccountPassword Pw123456 --DBClusterId 'pc-000000000000' --AccountName testacc",
  "cmd_full_name": "polardb modify-account-password",
  "cmd_name": "modify-account-password",
  "deprecated": false,
  "description_en": "Changes the password of a database account for a specified PolarDB cluster",
  "description_zh": "修改PolarDB数据库的账号密码",
  "kebab_example": "aliyun polardb modify-account-password --db-cluster-id pc-000000000000 --account-name testacc --new-account-password Pw123456 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyAccountPassword",
  "operation": {
    "action": "ModifyAccountPassword",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testacc",
      "help_en": "The username of the account",
      "help_zh": "账号名称",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    },
    {
      "example": "Pw123456",
      "help_en": "The new password of the account. The new password must meet the following requirements:\n\nIt must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\nIt must be 8 to 32 characters in length.\nSpecial characters include `! @ # $ % ^ & * ( ) _ + - =`",
      "help_zh": "账号的新密码，需符合如下要求：\n至少包含大写字母、小写字母、数字或特殊字符中的任意三种。\n长度为8~32个字符。\n特殊字符为`!@#$%^&*()_+-=`",
      "location": "query",
      "name": "new_account_password",
      "options": [
        "--new-account-password"
      ],
      "raw_name": "NewAccountPassword",
      "required": true,
      "type": "string"
    },
    {
      "example": "Tair",
      "help_en": "The password type",
      "help_zh": "密码类型",
      "location": "query",
      "name": "password_type",
      "options": [
        "--password-type"
      ],
      "raw_name": "PasswordType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "2FED790E-FB61-4721-8C1C-07C627******",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
