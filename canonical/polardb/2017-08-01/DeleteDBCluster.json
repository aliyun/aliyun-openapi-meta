{
  "camel_example": "aliyun polardb DeleteDBCluster --region cn-hangzhou --DBClusterId 'pc-00000000000000'",
  "cmd_full_name": "polardb delete-db-cluster",
  "cmd_name": "delete-db-cluster",
  "deprecated": false,
  "description_en": "Deletes a pay-as-you-go PolarDB cluster",
  "description_zh": "释放按量付费的PolarDB集群",
  "kebab_example": "aliyun polardb delete-db-cluster --db-cluster-id pc-00000000000000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteDBCluster",
  "operation": {
    "action": "DeleteDBCluster",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "ENS",
      "help_en": "The cloud service provider of the instance",
      "help_zh": "实例所属的云服务商",
      "location": "query",
      "name": "cloud_provider",
      "options": [
        "--cloud-provider"
      ],
      "raw_name": "CloudProvider",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "pc-**************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "ALL",
        "LATEST",
        "NONE"
      ],
      "example": "NONE",
      "help_en": "The retention policy for backup sets when the cluster is deleted. Valid values:\n\nALL: permanently retains all backup sets.\n\nLATEST: permanently retains the last backup set. The system automatically creates a new backup before the cluster is deleted.\n\nNONE: does not retain any backup sets after the cluster is deleted",
      "help_zh": "删除集群时备份集保留策略，取值范围如下：\nALL：长期保留全部备份。\nLATEST：长期保留最后一个备份（删除前自动备份）。\nNONE：集群删除时不保留备份集",
      "location": "query",
      "name": "backup_retention_policy_on_cluster_deletion",
      "options": [
        "--backup-retention-policy-on-cluster-deletion"
      ],
      "raw_name": "BackupRetentionPolicyOnClusterDeletion",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "headers": {},
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "D0CEC6AC-7760-409A-A0D5-E6CD86******",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
