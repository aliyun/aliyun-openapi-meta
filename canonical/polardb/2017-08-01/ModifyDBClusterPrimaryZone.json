{
  "camel_example": "aliyun polardb ModifyDBClusterPrimaryZone --region cn-hangzhou --ZoneId 'cn-hangzhou-g' --DBClusterId 'pc-00000000000000'",
  "cmd_full_name": "polardb modify-db-cluster-primary-zone",
  "cmd_name": "modify-db-cluster-primary-zone",
  "deprecated": false,
  "description_en": "Changes the primary zone of a PolarDB cluster",
  "description_zh": "更换PolarDB集群的可用区",
  "kebab_example": "aliyun polardb modify-db-cluster-primary-zone --db-cluster-id pc-00000000000000 --zone-id cn-hangzhou-g --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBClusterPrimaryZone",
  "operation": {
    "action": "ModifyDBClusterPrimaryZone",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "pc-**************",
      "help_en": "The cluster ID.\n\nCall the https://help.aliyun.com/document_detail/173433.html operation to query the details of all clusters in a destination region, including their cluster IDs",
      "help_zh": "集群ID。\n您可以调用https://help.aliyun.com/document_detail/173433.html接口查看目标地域下所有的集群信息，包括集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "cn-hangzhou-g",
      "help_en": "The ID of the new zone.\n\nCall the https://help.aliyun.com/document_detail/98041.html operation to query available zones",
      "help_zh": "新的可用区ID。\n\n可通过接口https://help.aliyun.com/document_detail/98041.html查看可用的可用区",
      "location": "query",
      "name": "zone_id",
      "options": [
        "--zone-id"
      ],
      "raw_name": "ZoneId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "enum": [
        "Primary",
        "Standby"
      ],
      "example": "Primary",
      "help_en": "The type of the zone. Valid values:\n\nPrimary: The primary zone.\n\nStandby: The secondary zone",
      "help_zh": "更换的可用区类型。取值范围如下：\n\nPrimary：主可用区。\nStandby：备可用区",
      "location": "query",
      "name": "zone_type",
      "options": [
        "--zone-type"
      ],
      "raw_name": "ZoneType",
      "required": false,
      "type": "string"
    },
    {
      "example": "vsw-**************",
      "help_en": "The ID of the vSwitch in the destination zone.\n\nThis parameter is required for PolarDB for Oracle and PolarDB for PostgreSQL clusters.\n\nFor PolarDB for MySQL clusters, this parameter is required if a vSwitch is created in the destination zone. If no vSwitch is created, the default vSwitch is used and this parameter is optional",
      "help_zh": "目标可用区的虚拟交换机（VSwitch）ID。\n\n当集群为PolarDB O引擎或PolarDB PostgreSQL引擎时，该参数必填。\n当集群为PolarDB MySQL引擎时，如果目标可用区没有创建过Vswitch，则使用默认的Vswitch，该参数非必填。如果目标可用区已创建过Vswitch，该参数必填",
      "location": "query",
      "name": "vswitch_id",
      "options": [
        "--vswitch-id"
      ],
      "raw_name": "VSwitchId",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to fail back to the original zone. Valid values:\n\ntrue: Fails back to the original zone.\n\nfalse: Does not fail back to the original zone",
      "help_zh": "是否回切原可用区，取值范围如下：\n\ntrue：回切原可用区。\nfalse：不回切原可用区",
      "location": "query",
      "name": "is_switch_over_for_disaster",
      "options": [
        "--is-switch-over-for-disaster"
      ],
      "raw_name": "IsSwitchOverForDisaster",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-01-14T09:00:00Z",
      "help_en": "The earliest time to start the scheduled task to change the zone. Specify the time in the `YYYY-MM-DDThh:mm:ssZ` format. The time must be in UTC.\n\nThe start time must be a point in time within the next 24 hours. For example, if the current time is `2021-01-14T09:00:00Z`, you can set the start time to a value from `2021-01-14T09:00:00Z` to `2021-01-15T09:00:00Z`.\n\nIf you do not specify this parameter, the zone change task is performed immediately",
      "help_zh": "开始执行定时（即在目标时间段内执行）变更可用区任务的最早时间。格式为`YYYY-MM-DDThh:mm:ssZ`（UTC）。\n开始时间的取值范围为未来24小时内的任意时间点。例如当前时间为`2021-01-14T09:00:00Z`，此处允许填入的开始时间范围为`2021-01-14T09:00:00Z`~`2021-01-15T09:00:00Z`。\n若该参数留空，默认立即执行变更可用区任务",
      "location": "query",
      "name": "planned_start_time",
      "options": [
        "--planned-start-time"
      ],
      "raw_name": "PlannedStartTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-01-14T09:30:00Z",
      "help_en": "The latest time to start the scheduled task. Specify the time in the `YYYY-MM-DDThh:mm:ssZ` format. The time must be in UTC.\n\nThe latest start time must be at least 30 minutes later than the earliest start time.\n\nIf you specify `PlannedStartTime` but not this parameter, the latest start time of the task is the value of `PlannedStartTime` plus 30 minutes by default. For example, if you set `PlannedStartTime` to `2021-01-14T09:00:00Z` and leave this parameter empty, the task starts no later than `2021-01-14T09:30:00Z`",
      "help_zh": "开始执行目标定时任务的最晚时间。格式为`YYYY-MM-DDThh:mm:ssZ`（UTC）。\n最晚时间需晚于开始时间的30分钟或以上。\n若设置了`PlannedStartTime`但未指定该参数，执行目标任务的最晚时间默认为`开始时间+30分钟`。例如当设置`PlannedStartTime`为`2021-01-14T09:00:00Z`且该参数留空时，目标任务最晚将于`2021-01-14T09:30:00Z`开始执行",
      "location": "query",
      "name": "planned_end_time",
      "options": [
        "--planned-end-time"
      ],
      "raw_name": "PlannedEndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform the zone change immediately or at a scheduled time. Valid values:\n\nfalse (default): The zone change is performed at the scheduled time.\n\ntrue: The zone change is performed immediately",
      "help_zh": "立即执行或定时执行更换可用区。取值范围如下：\n\nfalse（默认值）：定时执行。\ntrue：立即执行",
      "location": "query",
      "name": "from_time_service",
      "options": [
        "--from-time-service"
      ],
      "raw_name": "FromTimeService",
      "required": false,
      "type": "bool"
    },
    {
      "example": "vpc-**********",
      "help_en": "The ID of the virtual private cloud (VPC)",
      "help_zh": "切换可用区支持指定的专有网络ID",
      "location": "query",
      "name": "vpc_id",
      "options": [
        "--vpc-id"
      ],
      "raw_name": "VPCId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "ED12C6FF-3261-4571-AB57-3570F6******",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
