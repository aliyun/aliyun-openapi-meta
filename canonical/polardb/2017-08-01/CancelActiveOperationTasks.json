{
  "camel_example": "aliyun polardb CancelActiveOperationTasks --region cn-hangzhou --TaskIds '2355,2352' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "polardb cancel-active-operation-tasks",
  "cmd_name": "cancel-active-operation-tasks",
  "deprecated": false,
  "description_en": "Cancels O\\\\&M events at a time",
  "description_zh": "批量取消运维事件",
  "kebab_example": "aliyun polardb cancel-active-operation-tasks --biz-region-id cn-hangzhou --task-ids 2355,2352 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CancelActiveOperationTasks",
  "operation": {
    "action": "CancelActiveOperationTasks",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-beijing",
      "help_en": "The region ID.\n\n\n\nYou can call the https://help.aliyun.com/document_detail/98041.html operation to query the region information about all clusters within a specified account.\n\nIf you do not specify this parameter, scheduled tasks on your clusters that are deployed in all regions are queried",
      "help_zh": "地域ID。\n您可以调用https://help.aliyun.com/document_detail/98041.html接口查看目标账号下所有的集群的地域信息。\n若该参数留空，默认查询当前账号下所有地域内的计划任务",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2355,2352",
      "help_en": "The IDs of O\\&M events that are canceled at a time. Separate multiple IDs with commas (,)",
      "help_zh": "批量取消的运维事件 ID，多个 ID 间使用英文逗号（,）分隔",
      "location": "query",
      "name": "task_ids",
      "options": [
        "--task-ids"
      ],
      "raw_name": "TaskIds",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求id",
            "example": "25C70FF3-D49B-594D-BECE-0DE2BA1D8BBB",
            "type": "string",
            "visibility": "Public"
          },
          "TaskIds": {
            "backendName": "data.ids",
            "description_en": "The IDs of O\\&M events that are canceled at a time. Separate multiple IDs with commas (,)",
            "description_zh": "批量取消的运维事件 ID，多个 ID 间使用英文逗号（,）分隔",
            "example": "2355,2352",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
