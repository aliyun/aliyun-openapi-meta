{
  "camel_example": "aliyun polardb ModifyDBClusterAuditLogCollector --region cn-hangzhou --CollectorStatus Enable --DBClusterId 'pc-000000000000000'",
  "cmd_full_name": "polardb modify-db-cluster-audit-log-collector",
  "cmd_name": "modify-db-cluster-audit-log-collector",
  "deprecated": false,
  "description_en": "Enables or disables SQL collector for a PolarDB cluster. The features related to SQL collector include Audit Logs and SQL Explorer",
  "description_zh": "开启或关闭PolarDB集群的SQL采集功能（例如审计日志、SQL洞察等）",
  "kebab_example": "aliyun polardb modify-db-cluster-audit-log-collector --db-cluster-id pc-000000000000000 --collector-status Enable --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBClusterAuditLogCollector",
  "operation": {
    "action": "ModifyDBClusterAuditLogCollector",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "pc-***************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "Enable",
      "help_en": "Specifies whether to enable or disable SQL collector. Valid values:\n\nEnable\n\nDisable",
      "help_zh": "开启或关闭SQL采集功能，取值范围如下：\n\nEnable ：开启。\nDisabled ：关闭",
      "location": "query",
      "name": "collector_status",
      "options": [
        "--collector-status"
      ],
      "raw_name": "CollectorStatus",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "22C0ACF0-DD29-4B67-9190-B7A48C******",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
