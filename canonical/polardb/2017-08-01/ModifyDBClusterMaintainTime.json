{
  "camel_example": "aliyun polardb ModifyDBClusterMaintainTime --region cn-hangzhou --DBClusterId 'pc-000000000000000' --MaintainTime '02:00Z-03:00Z'",
  "cmd_full_name": "polardb modify-db-cluster-maintain-time",
  "cmd_name": "modify-db-cluster-maintain-time",
  "deprecated": false,
  "description_en": "Modifies the routine maintenance window for a PolarDB cluster",
  "description_zh": "修改PolarDB集群可例行维护的时间",
  "kebab_example": "aliyun polardb modify-db-cluster-maintain-time --db-cluster-id pc-000000000000000 --maintain-time 02:00Z-03:00Z --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBClusterMaintainTime",
  "operation": {
    "action": "ModifyDBClusterMaintainTime",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-***************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "02:00Z-03:00Z",
      "help_en": "The maintenance window of the cluster. The format is `HH:mmZ- HH:mmZ`. For example, `16:00Z-17:00Z` indicates that routine maintenance can be performed from 00:00 to 01:00 (UTC+08:00).\n\nThe maintenance window must start on the hour and last for one hour",
      "help_zh": "集群的可维护时间，格式为`HH:mmZ- HH:mmZ`。例如`16:00Z-17:00Z`，表示0点到1点（UTC+08:00）可进行例行维护。 \n\n可维护时间为整点，时长为1小时",
      "location": "query",
      "name": "maintain_time",
      "options": [
        "--maintain-time"
      ],
      "raw_name": "MaintainTime",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "70656639-1416-479F-AF13-D08197******",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
