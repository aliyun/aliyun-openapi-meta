{
  "camel_example": "aliyun polardb FailoverDBCluster --region cn-hangzhou --DBClusterId 'pc-00000000000000'",
  "cmd_full_name": "polardb failover-db-cluster",
  "cmd_name": "failover-db-cluster",
  "deprecated": false,
  "description_en": "Performs a manual failover to promote a read-only node to the primary node in a PolarDB cluster",
  "description_zh": "对PolarDB集群进行手动主备切换，可以指定一个只读节点为新的主节点",
  "kebab_example": "aliyun polardb failover-db-cluster --db-cluster-id pc-00000000000000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "FailoverDBCluster",
  "operation": {
    "action": "FailoverDBCluster",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "pc-**************",
      "help_en": "The ID of the cluster",
      "help_zh": "数据库集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pi-***********",
      "help_en": "The ID of the read-only node that you want to promote to the primary node. You can call the https://help.aliyun.com/document_detail/98094.html operation to query node information, such as node IDs.\n\nIf you leave this parameter empty, the system selects one or more available read-only nodes that have the highest failover priority as candidate primary nodes. If the failover to the first read-only node fails due to network issues, abnormal replication status, or other reasons, the system attempts to fail over your applications to the next read-only node until the failover is successful.\nThis parameter is required for PolarDB for Oracle and PolarDB for PostgreSQL clusters. This parameter is optional for PolarDB for MySQL clusters",
      "help_zh": "将要切换为主节点的节点ID，如果不填，则系统自动选举。您可以通过接口https://help.aliyun.com/document_detail/98094.html查询节点ID等信息。\n\n自动选举主节点： 系统找出当前可以被选举的所有只读节点。选择优先级最高的一个或多个只读节点。如果切换第一个节点失败（例如，网络原因、复制状态异常等），则会尝试切换下一个，直至成功。\n当集群为PolarDB PostgreSQL版（兼容Oracle）或PolarDB PostgreSQL版时，该参数必填；当集群为PolarDB MySQL版时，该参数非必填",
      "location": "query",
      "name": "target_db_node_id",
      "options": [
        "--target-db-node-id"
      ],
      "raw_name": "TargetDBNodeId",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to switch back services to the original primary zone when the original primary zone recovers.\n\ntrue\nfalse",
      "help_zh": "是否是故障切换后的回切至原主可用区，取值范围如下：\n\ntrue：是。\nfalse：不是",
      "location": "query",
      "name": "roll_back_for_disaster",
      "options": [
        "--roll-back-for-disaster"
      ],
      "raw_name": "RollBackForDisaster",
      "required": false,
      "type": "bool"
    },
    {
      "example": "6000170000591aed949d0f54a343f1a4233c1e7d1c5******",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "Primary",
        "Standby"
      ],
      "example": "Primary",
      "help_en": "Whether it is a primary-standby switch within the primary availability zone, with the following values:\n\nPrimary: Primary-standby switch within the primary availability zone.\nStandby: Switch to the storage hot backup cluster",
      "help_zh": "是否是主可用区内主备切换，取值如下：\nPrimary：主可用区内主备切换。\nStandby：切换至存储热备集群",
      "location": "query",
      "name": "target_zone_type",
      "options": [
        "--target-zone-type"
      ],
      "raw_name": "TargetZoneType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "D0CEC6AC-7760-409A-A0D5-E6CD86******",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
