{
  "camel_example": "aliyun polardb ModifyDBClusterStorageSpace --region cn-hangzhou --StorageSpace 50 --DBClusterId 'pc-0000000000000'",
  "cmd_full_name": "polardb modify-db-cluster-storage-space",
  "cmd_name": "modify-db-cluster-storage-space",
  "deprecated": false,
  "description_en": "This operation modifies the storage space for pay-as-you-go enterprise edition and standard edition clusters",
  "description_zh": "变更企业版按空间计费集群和标准版集群的存储空间",
  "kebab_example": "aliyun polardb modify-db-cluster-storage-space --db-cluster-id pc-0000000000000 --storage-space 50 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBClusterStorageSpace",
  "operation": {
    "action": "ModifyDBClusterStorageSpace",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "pc-*************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "50",
      "help_en": "Specifies the new storage space. Unit: GB.\n\nFor PolarDB for MySQL Standard Edition clusters, the storage space must be between 20 GB and 32,000 GB",
      "help_zh": "变更后的存储空间。单位：GB。\nPolarDB MySQL标准版的存储空间取值范围：20~32000",
      "location": "query",
      "name": "storage_space",
      "options": [
        "--storage-space"
      ],
      "raw_name": "StorageSpace",
      "required": true,
      "type": "int"
    },
    {
      "example": "6000170000591aed949d0f54a343f1a4233c1e7d1c5c******",
      "help_en": "A client-generated token that ensures the idempotence of the request. The token must be unique for each request, case-sensitive, and a maximum of 64 ASCII characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、长度不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-01-14T09:00:00Z",
      "help_en": "Specifies the earliest time to start the scheduled task. Specify the time in UTC in the `YYYY-MM-DDThh:mm:ssZ` format.\n\nThis parameter takes effect only when `ModifyType` is set to `Upgrade`.\n\nThe start time can be a point in time within the next 24 hours. For example, if the current time is `2021-01-14T09:00:00Z`, you can set the start time to a value that is between `2021-01-14T09:00:00Z` and `2021-01-15T09:00:00Z`.\n\nIf you leave this parameter empty, the task runs immediately",
      "help_zh": "开始执行定时（即在目标时间段内执行）升级配置任务的最早时间。格式为`YYYY-MM-DDThh:mm:ssZ（UTC）`。\n仅当`ModifyType`取值为`Upgrade`时，该参数生效。\n开始时间的取值范围为未来24小时内的任意时间点。例如当前时间为`2021-01-14T09:00:00Z`，此处允许填入的开始时间范围为`2021-01-14T09:00:00Z`~`2021-01-15T09:00:00Z`。\n若该参数留空，默认立即执行升级配置任务",
      "location": "query",
      "name": "planned_start_time",
      "options": [
        "--planned-start-time"
      ],
      "raw_name": "PlannedStartTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-01-14T09:30:00Z",
      "help_en": "Specifies the latest time to start the scheduled task. Specify the time in UTC in the `YYYY-MM-DDThh:mm:ssZ` format.\n\nThe latest start time must be at least 30 minutes later than the earliest start time.\n\nIf you specify `PlannedStartTime` but not this parameter, the latest start time is `PlannedStartTime + 30 minutes` by default. For example, if you set `PlannedStartTime` to `2021-01-14T09:00:00Z` and leave this parameter empty, the task starts no later than `2021-01-14T09:30:00Z`",
      "help_zh": "开始执行目标定时任务的最晚时间。格式为`YYYY-MM-DDThh:mm:ssZ（UTC）`。\n最晚时间需晚于开始时间的30分钟或以上。\n若设置了`PlannedStartTime`但未指定该参数，执行目标任务的最晚时间默认为`开始时间+30分钟`。例如当设置`PlannedStartTime`为`2021-01-14T09:00:00Z`且该参数留空时，目标任务最晚将于`2021-01-14T09:30:00Z`开始执行",
      "location": "query",
      "name": "planned_end_time",
      "options": [
        "--planned-end-time"
      ],
      "raw_name": "PlannedEndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "normal_general",
      "help_en": "Specifies the subcategory of the cluster. Valid values:\n\nnormal_exclusive: dedicated\n\nnormal_general: general-purpose",
      "help_zh": "集群子系列。取值范围如下：\n\nnormal_exclusive：独享规格\nnormal_general：通用规格",
      "location": "query",
      "name": "sub_category",
      "options": [
        "--sub-category"
      ],
      "raw_name": "SubCategory",
      "required": false,
      "type": "string"
    },
    {
      "example": "ENS",
      "help_en": "Specifies the cloud provider of the instance",
      "help_zh": "实例所属的云服务商",
      "location": "query",
      "name": "cloud_provider",
      "options": [
        "--cloud-provider"
      ],
      "raw_name": "CloudProvider",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies whether to automatically use a coupon. Valid values:\n\n`true` (default): A coupon is automatically used.\n\n`false`: A coupon is not used",
      "help_zh": "是否自动使用优惠券，取值：\ntrue（默认）：使用优惠券。\nfalse：不使用优惠券",
      "location": "query",
      "name": "auto_use_coupon",
      "options": [
        "--auto-use-coupon"
      ],
      "raw_name": "AutoUseCoupon",
      "required": false,
      "type": "bool"
    },
    {
      "example": "727xxxxxx934",
      "help_en": "Specifies the promotion code. If you do not specify this parameter, the system uses the default coupon",
      "help_zh": "优惠券编号，不填时使用默认优惠券",
      "location": "query",
      "name": "promotion_code",
      "options": [
        "--promotion-code"
      ],
      "raw_name": "PromotionCode",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "DBClusterId": {
            "backendName": "data.dbClusterId",
            "description_en": "The ID of the database cluster",
            "description_zh": "数据库集群ID",
            "example": "pc-*************",
            "type": "string",
            "visibility": "Public"
          },
          "OrderId": {
            "backendName": "data.orderId",
            "description_en": "The ID of the order",
            "description_zh": "订单ID",
            "example": "2035629******",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "685F028C-4FCD-407D-A559-072D63******",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
