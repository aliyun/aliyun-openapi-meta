{
  "camel_example": "aliyun polardb ResetAccountZonal --region cn-hangzhou --DBClusterId 'pc-0000000000000000' --AccountName testacc",
  "cmd_full_name": "polardb reset-account-zonal",
  "cmd_name": "reset-account-zonal",
  "deprecated": false,
  "description_en": "Resets an account in a PolarDB for Xscale cluster",
  "description_zh": "重置PolarDB边缘集群账号",
  "kebab_example": "aliyun polardb reset-account-zonal --db-cluster-id pc-0000000000000000 --account-name testacc --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "ResetAccountZonal",
  "operation": {
    "action": "ResetAccountZonal",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-****************",
      "help_en": "The cluster ID",
      "help_zh": "集群 ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testacc",
      "help_en": "The account name",
      "help_zh": "账号名称",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    },
    {
      "example": "Test1111",
      "help_en": "The password of the account. The password must meet the following requirements:\n\nIt must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n\nIt must be 8 to 32 characters in length.\n\nThe special characters are `!@#$%^&*()_+-=`",
      "help_zh": "账号密码，密码需满足如下要求： \n\n大写字母、小写字母、数字、特殊字符至少占三种。 \n长度为8~32位。\n特殊字符为`!@#$%^&*()_+-=` ",
      "location": "query",
      "name": "account_password",
      "options": [
        "--account-password"
      ],
      "raw_name": "AccountPassword",
      "required": false,
      "type": "string"
    },
    {
      "example": "6000170000591aed949d0f54a343f1a42***********",
      "help_en": "The client token that is used to ensure the idempotence of the request. This token is generated by the client and must be unique for each request. The token is case-sensitive and can be up to 64 ASCII characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 ID",
            "example": "24A1990B-4F6E-482B-B8CB-75C612******",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
