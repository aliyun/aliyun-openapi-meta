{
  "camel_example": "aliyun kms CreateAlias --region cn-hangzhou --AliasName 'alias/example' --KeyId '7906979c-8e06-46a2-be2d-68e3ccbc0000'",
  "cmd_full_name": "kms create-alias",
  "cmd_name": "create-alias",
  "deprecated": false,
  "description_en": "Creates an alias for a key",
  "description_zh": "为主密钥（CMK）创建一个别名",
  "kebab_example": "aliyun kms create-alias --key-id 7906979c-8e06-46a2-be2d-68e3ccbc0000 --alias-name alias/example --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateAlias",
  "operation": {
    "action": "CreateAlias",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "7906979c-8e06-46a2-be2d-68e3ccbc****",
      "help_en": "The ID of the CMK. The ID must be globally unique",
      "help_zh": "CMK的全局唯一标识符",
      "location": "query",
      "name": "key_id",
      "options": [
        "--key-id"
      ],
      "raw_name": "KeyId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "alias/example",
      "help_en": "The alias of the CMK.\n\nThe alias must be 1 to 255 characters in length and must contain the prefix `alias/`. The alias cannot be prefixed with the reserved word `alias/acs`",
      "help_zh": "CMK的别名名称。  \n长度为1~255个字符，必须包含前缀`alias/`，但不能以`alias/acs`保留字为前缀",
      "location": "query",
      "name": "alias_name",
      "options": [
        "--alias-name"
      ],
      "raw_name": "AliasName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "description_en": "The ID of the request, which is used to locate and troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "1d2baaf3-d357-46c2-832e-13560c2bd9cd",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
