{
  "camel_example": "aliyun kms DeleteApplicationAccessPoint --region cn-hangzhou --Name aap_test",
  "cmd_full_name": "kms delete-application-access-point",
  "cmd_name": "delete-application-access-point",
  "deprecated": false,
  "description_en": "Deletes an application access point (AAP)",
  "description_zh": "删除一个应用接入点",
  "kebab_example": "aliyun kms delete-application-access-point --name aap_test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteApplicationAccessPoint",
  "operation": {
    "action": "DeleteApplicationAccessPoint",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "aap_test",
      "help_en": "The name of the AAP that you want to delete",
      "help_zh": "要删除的应用接入点的名称",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "description_en": "The ID of the request. This ID is a unique identifier generated by Alibaba Cloud for the request. You can use this ID to troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "bcfefe15-46f0-44a3-bd96-3d422474b71a",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
