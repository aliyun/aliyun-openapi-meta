{
  "camel_example": "aliyun kms DeleteNetworkRule --region cn-hangzhou --Name networkrule_test",
  "cmd_full_name": "kms delete-network-rule",
  "cmd_name": "delete-network-rule",
  "deprecated": false,
  "description_en": "Deletes a network access rule",
  "description_zh": "删除一条网络控制规则",
  "kebab_example": "aliyun kms delete-network-rule --name networkrule_test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteNetworkRule",
  "operation": {
    "action": "DeleteNetworkRule",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "networkrule_test",
      "help_en": "The name of the network control rule to delete",
      "help_zh": "要删除的网络控制规则的名称",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "description_en": "The ID of the request. This ID is generated by Alibaba Cloud and is unique for each request. You can use it to troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "3bf02f7a-015b-4f93-be0f-cc043fda2d4",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
