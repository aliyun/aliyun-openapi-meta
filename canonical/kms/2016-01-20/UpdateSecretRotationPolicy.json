{
  "camel_example": "aliyun kms UpdateSecretRotationPolicy --region cn-hangzhou --SecretName 'RdsSecret/Mysql5.4/MyCred' --EnableAutomaticRotation true",
  "cmd_full_name": "kms update-secret-rotation-policy",
  "cmd_name": "update-secret-rotation-policy",
  "deprecated": false,
  "description_en": "Updates the rotation policy of a secret",
  "description_zh": "更新凭据轮转策略",
  "kebab_example": "aliyun kms update-secret-rotation-policy --secret-name RdsSecret/Mysql5.4/MyCred --enable-automatic-rotation true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateSecretRotationPolicy",
  "operation": {
    "action": "UpdateSecretRotationPolicy",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "RdsSecret/Mysql5.4/MyCred",
      "help_en": "The name of the secret",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "true",
      "help_en": "Specifies whether to enable automatic rotation. Valid values:\n\ntrue: enables automatic rotation.\nfalse: does not enable automatic rotation. This is the default value",
      "help_zh": "是否开启自动轮转，取值：\ntrue：开启自动轮转。\nfalse（默认值）：不开启自动轮转",
      "location": "query",
      "name": "enable_automatic_rotation",
      "options": [
        "--enable-automatic-rotation"
      ],
      "raw_name": "EnableAutomaticRotation",
      "required": true,
      "type": "bool"
    },
    {
      "example": "30d",
      "help_en": "The interval for automatic rotation. Valid values: 6 hours to 8,760 hours (365 days).\n\nThe value is in the `integer[unit]` format.````\n\nThe unit can be d (day), h (hour), m (minute), or s (second). For example, both 7d and 604800s indicate a seven-day interval.\n\nThis parameter is required if you set the EnableAutomaticRotation parameter to true. This parameter is ignored if you set the EnableAutomaticRotation parameter to false or does not specify the EnableAutomaticRotation parameter",
      "help_zh": "自动轮转的周期。取值范围：168小时（7天）~8,760小时（365天）。\n  \n格式为`integer\\[unit\\]`，其中`integer`表示时间长度，`unit`表示时间单位。  \nunit取值：d（天）、h（小时）、m（分钟）、s（秒）。例如：7d或者604,800s均表示7天的周期。\n\n当EnableAutomaticRotation取值为true时，必须设置该参数。反之，将忽略该参数",
      "location": "query",
      "name": "rotation_interval",
      "options": [
        "--rotation-interval"
      ],
      "raw_name": "RotationInterval",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "description_en": "The ID of the request, which is used to locate and troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "2c124f6f-4210-499f-b88a-69f54004d2d8",
            "type": "string"
          },
          "SecretName": {
            "description_en": "The name of the secret",
            "description_zh": "凭据名称",
            "example": "RdsSecret/Mysql5.4/MyCred",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
