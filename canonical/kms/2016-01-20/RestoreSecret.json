{
  "camel_example": "aliyun kms RestoreSecret --region cn-hangzhou --SecretName secret001",
  "cmd_full_name": "kms restore-secret",
  "cmd_name": "restore-secret",
  "deprecated": false,
  "description_en": "Restores a deleted secret",
  "description_zh": "恢复被删除的凭据",
  "kebab_example": "aliyun kms restore-secret --secret-name secret001 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RestoreSecret",
  "operation": {
    "action": "RestoreSecret",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "secret001",
      "help_en": "The name of the secret you want to restore",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "description_en": "The ID of the request",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "e4885adf-548f-4ca5-8075-f540bbd3a55f",
            "type": "string"
          },
          "SecretName": {
            "description_en": "The name of the secret",
            "description_zh": "凭据名称",
            "example": "secret001",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
