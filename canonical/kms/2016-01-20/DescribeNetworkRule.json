{
  "camel_example": "aliyun kms DescribeNetworkRule --region cn-hangzhou --Name networkrule_test",
  "cmd_full_name": "kms describe-network-rule",
  "cmd_name": "describe-network-rule",
  "deprecated": false,
  "description_en": "Retrieves the details of a network access rule",
  "description_zh": "查询一个网络控制规则的详情",
  "kebab_example": "aliyun kms describe-network-rule --name networkrule_test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeNetworkRule",
  "operation": {
    "action": "DescribeNetworkRule",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "networkrule_test",
      "help_en": "The name of the network rule to query",
      "help_zh": "要查询的网络控制规则名称",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Arn": {
            "description_en": "The ARN of the network rule",
            "description_zh": "网络控制规则的ARN",
            "example": "acs:kms:cn-hangzhou:119285303511****:network/networkrule_test",
            "type": "string"
          },
          "Description": {
            "description_en": "The description",
            "description_zh": "描述信息",
            "example": "Create by kst-hzz62ee817bvyyr5****",
            "type": "string"
          },
          "RequestId": {
            "description_en": "The ID of the request. Alibaba Cloud generates a unique ID for each request. You can use this ID to troubleshoot issues",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "3bf02f7a-015b-4f93-be0f-cc043fda2d33",
            "type": "string"
          },
          "SourcePrivateIp": {
            "description_en": "The private IP addresses or private CIDR blocks",
            "description_zh": "私网IP地址或者私网网段",
            "example": "[\"192.10.XX.XX\",\"192.168.XX.XX/24\"]",
            "type": "string"
          },
          "Type": {
            "description_en": "The network type. The only valid value is Private, which means only private IP addresses are supported",
            "description_zh": "网络类型。取值仅支持Private，即仅支持私网IP",
            "example": "Private",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
