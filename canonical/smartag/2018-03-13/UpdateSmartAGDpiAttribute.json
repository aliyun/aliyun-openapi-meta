{
  "camel_example": "",
  "cmd_full_name": "smartag update-smart-ag-dpi-attribute",
  "cmd_name": "update-smart-ag-dpi-attribute",
  "deprecated": false,
  "description_en": "Enables or disables the deep packet inspection (DPI) feature for a Smart Access Gateway (SAG) instance",
  "description_zh": "开启或关闭智能接入网关实例应用识别DPI（Deep Packet Inspection）功能",
  "kebab_example": "aliyun smartag update-smart-ag-dpi-attribute --biz-region-id cn-hangzhou --smart-ag-id sag-tq3sazs17smldn0000 --dpi-enabled true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateSmartAGDpiAttribute",
  "operation": {
    "action": "UpdateSmartAGDpiAttribute",
    "api_style": "RPC",
    "api_version": "2018-03-13",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-shanghai",
      "help_en": "The ID of the region where the SAG instance is deployed",
      "help_zh": "智能接入网关实例所属地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "sag-tq3sazs17smldn****",
      "help_en": "The ID of the SAG instance",
      "help_zh": "智能接入网关实例ID",
      "location": "query",
      "name": "smart_ag_id",
      "options": [
        "--smart-ag-id"
      ],
      "raw_name": "SmartAGId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "true",
      "help_en": "Specifies whether to enable the DPI feature for the SAG instance. Valid values:\n\ntrue: enables DPI\nfalse: disables DPI",
      "help_zh": "是否开启智能接入网关实例DPI功能。取值：\n\ntrue：开启。\nfalse：关闭",
      "location": "query",
      "name": "dpi_enabled",
      "options": [
        "--dpi-enabled"
      ],
      "raw_name": "DpiEnabled",
      "required": true,
      "type": "bool"
    },
    {
      "example": "02fb3da4-130e****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must ensure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to check the validity of the request without actually making the request. Valid values:\n\ntrue: checks the validity of the request but does not enable or disable the DPI feature. Check items include the request format, instance status, and whether the required parameters are set. If the request fails the check, an error message is returned. If the request passes the check, the ID of the request is returned.\nfalse: makes the request and enables or disables the DPI feature after the request passes the check. This is the default value",
      "help_zh": "是否只预检此次请求。取值：\n\ntrue：表示只预检此次请求合法性，不会开启或关闭智能接入网关实例的DPI功能。检查项包括是否填写了必需参数、请求格式、实例状态等。如果检查不通过，则返回对应错误；如果检查通过，则返回对应请求ID。\nfalse（默认）：表示发送正常请求，通过检查后直接开启或关闭智能接入网关实例的DPI功能",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Smartag",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "0C2EE7A8-74D4-4081-8236-CEBDE3BBCF50",
            "nullToEmpty": true,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
