{
  "camel_example": "",
  "cmd_full_name": "aisc generate-relay-poller-script",
  "cmd_name": "generate-relay-poller-script",
  "deprecated": false,
  "description_en": "Generates a one-time installation script for a scan target of the enterprise_relay type in AI Red Teaming. Only Linux platforms are supported. The script contains a one-time access code",
  "description_zh": "AI Red Teaming 为 enterprise_relay 类型的扫描目标生成一次性安装脚本，仅支持 Linux 平台，脚本内含一次性接入码",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "GenerateRelayPollerScript",
  "operation": {
    "action": "GenerateRelayPollerScript",
    "api_style": "RPC",
    "api_version": "2026-01-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "target-abc123def4567",
      "help_en": "The unique identifier of the scan target. The target must use the enterprise_relay connection method (see CreateAttackTarget). Otherwise, HTTP status code 400 is returned. If the target does not exist or belongs to another tenant, HTTP status code 400 is returned without exposing whether the resource exists. This parameter is registered as optional but is required in practice",
      "help_zh": "扫描目标的唯一标识；必须为 enterprise_relay 连接方式的目标（见 CreateAttackTarget），否则返回 400；目标不存在或归属其他租户时统一返回 400，不暴露资源是否存在；登记为选填但实际必填",
      "location": "query",
      "name": "target_id",
      "options": [
        "--target-id"
      ],
      "raw_name": "TargetId",
      "required": false,
      "type": "string"
    },
    {
      "example": "linux-amd64",
      "help_en": "The target platform in the \"operating system-architecture\" format. Only linux-amd64 and linux-arm64 are supported. Compatible architecture values include amd64, x86_64, x86, arm64, and aarch64. If only the architecture is specified, the operating system defaults to linux. Other operating systems such as macOS and Windows return HTTP status code 400. If this parameter is not specified, the default value is linux-amd64",
      "help_zh": "目标平台，取\"操作系统-架构\"格式；仅支持 linux-amd64 与 linux-arm64（架构兼容 amd64、x86_64、x86、arm64、aarch64 写法；仅传架构时操作系统默认 linux）；macOS、Windows 等其他操作系统一律返回 400；不传默认为 linux-amd64",
      "location": "query",
      "name": "platform",
      "options": [
        "--platform"
      ],
      "raw_name": "Platform",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AISC",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "Data": {
            "backendName": "data",
            "description_en": "The generation result, which contains the target identifier, the normalized platform, and the installation script",
            "description_zh": "生成结果，包含目标标识、归一化平台与安装脚本",
            "properties": {
              "Platform": {
                "backendName": "platform",
                "description_en": "The normalized target platform in the operating system-architecture format",
                "description_zh": "归一化后的目标平台（操作系统-架构格式）",
                "example": "linux-amd64",
                "type": "string"
              },
              "Script": {
                "backendName": "script",
                "description_en": "The installation script content (bash). The script contains a one-time access code, a temporary download link for the poller binary (signed and valid for 1 hour), and a checksum. The script does not contain the actual endpoint or credentials of the target, which are interactively entered during installation. The script carries access credential semantics. Transmit it through a trusted channel and re-download the script to obtain a new one after use",
                "description_zh": "安装脚本内容（bash），内含一次性接入码、poller 二进制临时下载链接（1 小时签名）与校验值；不包含目标真实端点与凭据（安装时交互输入）。脚本具有接入凭据语义，请经可信渠道传输，使用后重新下载获取新脚本",
                "example": "#!/bin/bash",
                "type": "string"
              },
              "TargetId": {
                "backendName": "targetId",
                "description_en": "The scan target identifier echoed from the request",
                "description_zh": "请求中扫描目标标识的回显",
                "example": "target-abc123def4567",
                "type": "string"
              }
            },
            "type": "object"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID. You can use this ID for troubleshooting and log tracing",
            "description_zh": "本次请求的唯一标识，用于问题排查与日志追踪",
            "example": "1EBD0C05-6C1F-4C95-9C63-B7AB7B5A9C8E",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
