{
  "camel_example": "",
  "cmd_full_name": "ice delete-category",
  "cmd_name": "delete-category",
  "deprecated": false,
  "description_en": "Deletes a media asset category",
  "description_zh": "调用DeleteCategory删除媒资分类",
  "kebab_example": "aliyun ice delete-category --cate-id 46 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteCategory",
  "operation": {
    "action": "DeleteCategory",
    "api_style": "RPC",
    "api_version": "2020-11-09",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "46",
      "help_en": "The category ID. You can obtain the category ID by using the following methods:\nLog on to the IMS console (https://ims.console.aliyun.com), and choose Media Asset Management > Category Management to view the category ID.\nWhen you create a category by calling the create category operation, the category ID is the value of CateId in the response.\nWhen you query a category by calling the get category operation, the category ID is the value of CateId in the response",
      "help_zh": "分类ID。可通过以下方式获取：\n登录IMS控制台 (https://ims.console.aliyun.com)，选择 媒资管理 > 分类管理查看分类ID。\n通过创建分类接口创建分类时，分类ID为返回参数CateId的值。\n通过获取分类接口查询分类时，为请求后返回参数CateId的值",
      "location": "query",
      "name": "cate_id",
      "options": [
        "--cate-id"
      ],
      "raw_name": "CateId",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "ICE",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "****63E8B7C7-4812-46AD-0FA56029AC86****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
