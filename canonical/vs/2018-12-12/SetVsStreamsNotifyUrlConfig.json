{
  "camel_example": "",
  "cmd_full_name": "vs set-vs-streams-notify-url-config",
  "cmd_name": "set-vs-streams-notify-url-config",
  "deprecated": false,
  "description_en": "Configure stream ingest callbacks",
  "description_zh": "设置推流回调配置",
  "kebab_example": "aliyun vs set-vs-streams-notify-url-config --domain-name example.com --notify-url http://xxx.com/xx --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SetVsStreamsNotifyUrlConfig",
  "operation": {
    "action": "SetVsStreamsNotifyUrlConfig",
    "api_style": "RPC",
    "api_version": "2018-12-12",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "Your accelerated domain name",
      "help_zh": "您的加速域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    },
    {
      "example": "http://xxx.com/xx",
      "help_en": "The URL to which live stream information is pushed.\n\nThis URL must start with http\\:// or https\\://",
      "help_zh": "设置直播流信息推送到的URL地址。\n必须以 http:// 或者 https:// 开头",
      "location": "query",
      "name": "notify_url",
      "options": [
        "--notify-url"
      ],
      "raw_name": "NotifyUrl",
      "required": true,
      "type": "string"
    },
    {
      "example": "type_a",
      "help_en": "The authentication type. Valid values:\n\nno_auth (disabled)\n\ntype_a (method A)\n\ntype_b (method B)\n\ntype_c (method C)",
      "help_zh": "鉴权类型，取值范围：\nno_auth（关闭）\ntype_a（A方式）\ntype_b（B方式）\ntype_c（C方式）",
      "location": "query",
      "name": "auth_type",
      "options": [
        "--auth-type"
      ],
      "raw_name": "AuthType",
      "required": false,
      "type": "string"
    },
    {
      "example": "ocs*****ace",
      "help_en": "The primary key associated with the playback domain name. This key generates authenticated URLs.\n\nCall the DescribeVsDomainConfigs (https://help.aliyun.com/document_detail/464513.html) operation to query the AuthKey value",
      "help_zh": "播放域名关联的主Key，用于生成鉴权URL。\n通过 DescribeVsDomainConfigs (https://help.aliyun.com/document_detail/464513.html) 接口查询对应的AuthKey信息",
      "location": "query",
      "name": "auth_key",
      "options": [
        "--auth-key"
      ],
      "raw_name": "AuthKey",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "vs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "BEA5625F-8FCF-48F4-851B-CA63946DA664",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
