{
  "cmd_full_name": "config trigger-report-send",
  "cmd_name": "trigger-report-send",
  "deprecated": false,
  "description_en": "Tests a notification in real time",
  "description_zh": "实时测试通知",
  "kebab_example": "aliyun config trigger-report-send --report-template-id crt-xxx --api-version 2020-09-07 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "TriggerReportSend",
  "operation": {
    "action": "TriggerReportSend",
    "api_style": "RPC",
    "api_version": "2020-09-07",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "crt-xxx",
      "help_en": "The ID of the report template. Before calling this operation, ensure that the report template exists and that a report with the status of COMPLETE has been generated under the template. Otherwise, the error ReportNotExist (400) may be returned",
      "help_zh": "报告模板 ID。调用前需要该报告模板已存在，且模板下已生成状态为 COMPLETE（已完成）的报告，否则可能返回 ReportNotExist（400）",
      "location": "formData",
      "name": "report_template_id",
      "options": [
        "--report-template-id"
      ],
      "raw_name": "ReportTemplateId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Config",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "traceId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "DE9FFFE5-FCAD-4B24-9546-BF49273C562B",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
