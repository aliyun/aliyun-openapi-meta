{
  "camel_example": "aliyun ndr ModifyIdcAssetAutoTractionConfig --region cn-hangzhou --Config '{\"key\":\"example-string\"}' --version 2024-01-24 --method POST --force",
  "cmd_full_name": "ndr modify-idc-asset-auto-traction-config",
  "cmd_name": "modify-idc-asset-auto-traction-config",
  "deprecated": false,
  "description_en": "Modifies the automatic access configuration for resource types in a specified region. If no region is specified, the same configuration is applied to all regions",
  "description_zh": "修改指定地域的自动接入资源类型配置；不指定地域时，将同一配置应用到所有地域",
  "kebab_example": "aliyun ndr modify-idc-asset-auto-traction-config --config key=example-string --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyIdcAssetAutoTractionConfig",
  "operation": {
    "action": "ModifyIdcAssetAutoTractionConfig",
    "api_style": "RPC",
    "api_version": "2024-01-24",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. If this parameter is not specified or an empty string is passed, the same configuration is applied to all regions",
      "help_zh": "地域 ID。不传或传入空字符串时，将同一配置应用到所有地域",
      "location": "query",
      "name": "region",
      "options": [
        "--biz-region"
      ],
      "raw_name": "Region",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The automatic access configuration for resource types. The key is a supported resource type, and the value specifies whether automatic access is enabled. Supported resource types that are not included are set to false",
      "help_zh": "自动接入资源类型配置。键为受支持的资源类型，值表示是否启用自动接入；未提供的受支持资源类型将设为 false",
      "location": "query",
      "name": "config",
      "options": [
        "--config"
      ],
      "param_style": "json",
      "raw_name": "Config",
      "required": true,
      "type": "map",
      "value": {
        "type": "bool"
      }
    }
  ],
  "pathPattern": "",
  "product_code": "ndr",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The request result",
        "description_zh": "请求结果",
        "properties": {
          "ErrorCode": {
            "backendName": "errorCode",
            "description_en": "The error code. This parameter is empty if the request is successful",
            "description_zh": "错误码。请求成功时为空",
            "example": "InvalidParam",
            "type": "string"
          },
          "Message": {
            "backendName": "message",
            "description_en": "The response message",
            "description_zh": "返回信息",
            "example": "success",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 ID",
            "example": "F655A8D5-B967-440B-8683-DAD6FF8DE990",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          },
          "Success": {
            "backendName": "success",
            "description_en": "Indicates whether the request was successful",
            "description_zh": "请求是否成功",
            "type": "boolean"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
