{
  "camel_example": "aliyun iacservice DELETE /tasks/task-123456 --region cn-hangzhou --resourceRetentionPolicy 'example-string' --header \"Content-Type=application/json\"",
  "cmd_full_name": "iacservice delete-task",
  "cmd_name": "delete-task",
  "deprecated": false,
  "description_en": "Deletes a node",
  "description_zh": "删除任务",
  "kebab_example": "aliyun iacservice delete-task --task-id task-123456 --api-version 2021-08-06 --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": true,
  "name": "DeleteTask",
  "operation": {
    "action": "DeleteTask",
    "api_style": "ROA",
    "api_version": "2021-08-06",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/tasks/{taskId}"
  },
  "parameters": [
    {
      "example": "task-123456",
      "help_en": "The node ID",
      "help_zh": "任务ID",
      "location": "path",
      "name": "task_id",
      "options": [
        "--task-id"
      ],
      "raw_name": "taskId",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The data retention policy. If this parameter is not specified, the policy is unconfirmed. If the node has resources or the resource status is unknown, the operation returns a confirmation fault. Set this parameter to RETAIN to delete only the node management record and retain the cloud resources",
      "help_zh": "资源保留策略。不传表示未确认；当任务存在资源或资源状态未知时接口返回确认错误。传RETAIN表示仅删除任务管理记录并保留云资源",
      "location": "query",
      "name": "resource_retention_policy",
      "options": [
        "--resource-retention-policy"
      ],
      "raw_name": "resourceRetentionPolicy",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/tasks/[taskId]",
  "product_code": "IaCService",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response struct",
        "description_zh": "响应体结构",
        "properties": {
          "requestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "73B38F77-62BA-5878-8952-530DFE21C93B",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  },
  "v1_parameters": [
    {
      "help_en": "The data retention policy. If this parameter is not specified, the policy is unconfirmed. If the node has resources or the resource status is unknown, the operation returns a confirmation fault. Set this parameter to RETAIN to delete only the node management record and retain the cloud resources",
      "help_zh": "资源保留策略。不传表示未确认；当任务存在资源或资源状态未知时接口返回确认错误。传RETAIN表示仅删除任务管理记录并保留云资源",
      "name": "resourceRetentionPolicy",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The node ID",
      "help_zh": "任务ID",
      "name": "taskId",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
