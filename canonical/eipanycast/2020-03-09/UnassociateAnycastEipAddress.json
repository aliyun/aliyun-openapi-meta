{
  "camel_example": "aliyun eipanycast UnassociateAnycastEipAddress --region cn-hangzhou --BindInstanceId 'lb-2zebb08phyczzawe0000' --BindInstanceRegionId 'us-west-1' --BindInstanceType SlbInstance --AnycastId 'aeip-2zeerraiwb7ujsxdc0000'",
  "cmd_full_name": "eipanycast unassociate-anycast-eip-address",
  "cmd_name": "unassociate-anycast-eip-address",
  "deprecated": false,
  "description_en": "Disassociates an Anycast EIP from an endpoint",
  "description_zh": "调用UnassociateAnycastEipAddress接口来解除Anycast EIP与其绑定的后端云资源实例的关联",
  "kebab_example": "aliyun eipanycast unassociate-anycast-eip-address --bind-instance-id lb-2zebb08phyczzawe0000 --bind-instance-region-id us-west-1 --bind-instance-type SlbInstance --anycast-id aeip-2zeerraiwb7ujsxdc0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UnassociateAnycastEipAddress",
  "operation": {
    "action": "UnassociateAnycastEipAddress",
    "api_style": "RPC",
    "api_version": "2020-03-09",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "lb-2zebb08phyczzawe****",
      "help_en": "The ID of the endpoint from which you want to disassociate the Anycast EIP",
      "help_zh": "要解绑的云资源实例ID",
      "location": "query",
      "name": "bind_instance_id",
      "options": [
        "--bind-instance-id"
      ],
      "raw_name": "BindInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "us-west-1",
      "help_en": "The region where the endpoint is deployed",
      "help_zh": "要解绑的云资源实例地域ID",
      "location": "query",
      "name": "bind_instance_region_id",
      "options": [
        "--bind-instance-region-id"
      ],
      "raw_name": "BindInstanceRegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "SlbInstance",
      "help_en": "The type of endpoint from which you want to disassociate the Anycast EIP. Valid values:\n\nSlbInstance: an internal-facing Server Load Balancer (SLB) instance that is deployed in a virtual private cloud (VPC)\n\nNetworkInterface: elastic network interface (ENI)",
      "help_zh": "要解绑的云资源实例类型。取值：\n\nSlbInstance：专有网络类型的CLB实例。\nNetworkInterface：弹性网卡",
      "location": "query",
      "name": "bind_instance_type",
      "options": [
        "--bind-instance-type"
      ],
      "raw_name": "BindInstanceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "aeip-2zeerraiwb7ujsxdc****",
      "help_en": "The ID of the Anycast EIP",
      "help_zh": "Anycast EIP实例ID",
      "location": "query",
      "name": "anycast_id",
      "options": [
        "--anycast-id"
      ],
      "raw_name": "AnycastId",
      "required": true,
      "type": "string"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44-001****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\nIf you do not specify this parameter, the system automatically uses the request ID as the client token. The request ID may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run, without performing the actual request. Valid values:\n\ntrue: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会解绑后端实例。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "string"
    },
    {
      "example": "192.168.XX.XX",
      "help_en": "The secondary private IP address of the ENI from which you want to disassociate the Anycast EIP.\n\nThis parameter is valid only when you set BindInstanceType to NetworkInterface. If you do not specify this parameter, the primary private IP address of the ENI is used",
      "help_zh": "要解绑的弹性网卡的辅助私网IP。\n\n\n只有当BindInstanceType取值为NetworkInterface时，该参数生效。不输入时，该参数默认为弹性网卡的主私网IP",
      "location": "query",
      "name": "private_ip_address",
      "options": [
        "--private-ip-address"
      ],
      "raw_name": "PrivateIpAddress",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Eipanycast",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回信息列表",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "FBDB18D8-E91E-4978-8D6C-6E2E3EE10133",
            "title_en": "私网IP地址",
            "title_zh": "私网IP地址",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
