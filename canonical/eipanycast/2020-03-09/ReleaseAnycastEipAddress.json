{
  "camel_example": "aliyun eipanycast ReleaseAnycastEipAddress --region cn-hangzhou --AnycastId 'aeip-bp1ix34fralt4ykf30000'",
  "cmd_full_name": "eipanycast release-anycast-eip-address",
  "cmd_name": "release-anycast-eip-address",
  "deprecated": false,
  "description_en": "Releases an Anycast EIP instance",
  "description_zh": "调用ReleaseAnycastEipAddress接口释放指定Anycast EIP实例",
  "kebab_example": "aliyun eipanycast release-anycast-eip-address --anycast-id aeip-bp1ix34fralt4ykf30000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ReleaseAnycastEipAddress",
  "operation": {
    "action": "ReleaseAnycastEipAddress",
    "api_style": "RPC",
    "api_version": "2020-03-09",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "aeip-bp1ix34fralt4ykf3****",
      "help_en": "The ID of the Anycast EIP instance that you want to release",
      "help_zh": "要释放的Anycast EIP实例ID",
      "location": "query",
      "name": "anycast_id",
      "options": [
        "--anycast-id"
      ],
      "raw_name": "AnycastId",
      "required": true,
      "type": "string"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44-001****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the request as the ClientToken. The RequestId of each API request may be different",
      "help_zh": "客户端Token，用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Eipanycast",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "FBDB18D8-E91E-4978-8D6C-6E2E3EE10133",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
