{
  "cmd_full_name": "adb modify-auto-renewal-attribute",
  "cmd_name": "modify-auto-renewal-attribute",
  "deprecated": false,
  "description_en": "Configures auto-renewal for a subscription cluster",
  "description_zh": "设置包年包月集群自动续费状态",
  "kebab_example": "aliyun adb modify-auto-renewal-attribute --db-cluster-id amv-uf6pl56w1j8h0000 --biz-region-id cn-hangzhou --api-version 2021-12-01 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "ModifyAutoRenewalAttribute",
  "operation": {
    "action": "ModifyAutoRenewalAttribute",
    "api_style": "RPC",
    "api_version": "2021-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "amv-uf6pl56w1j8h****",
      "help_en": "The ID of cluster",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "AutoRenewal",
      "help_en": "The renewal method. Valid values:\n\nAutoRenewal: The cluster is automatically renewed.\nNormal: The cluster is manually renewed. Before the cluster expires, the system sends you a reminder by SMS message.\nNotRenewal: The cluster is not renewed. Reminders are only sent three days before cluster expiration",
      "help_zh": "自动续费状态，取值说明：\n\nAutoRenewal：自动续费。\nNormal：手动续费，集群到期前通过短信提醒。\nNotRenewal：到期不续费，集群到期前无提醒，只在到期前第三天发送不续费提醒",
      "location": "query",
      "name": "auto_renewal_status",
      "options": [
        "--auto-renewal-status"
      ],
      "raw_name": "AutoRenewalStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The duration of the auto-renewal. Default value: 1. Valid values:\n\nWhen AutoRenewalPeriod is set to Month, the value ranges from 1 to 11 (integer).\nWhen AutoRenewalPeriod is set to Month, the valid values are 1, 2, 3, and 5 (integer).\n\nLonger renewal periods offer better pricing. Renewing for 1 year is more cost-effective than renewing for 10 or 11 months",
      "help_zh": "自动续费时长，默认值为1，取值说明：\n\n当AutoRenewalPeriod为Month时，取值为1~11（整数）。\n当AutoRenewalPeriod为Year时，取值为1、2、3、5（整数）。\n续费的时间越长价格越优惠，续费1年的实际价格会比10个月或11个月更划算",
      "location": "query",
      "name": "auto_renewal_period",
      "options": [
        "--auto-renewal-period"
      ],
      "raw_name": "AutoRenewalPeriod",
      "required": false,
      "type": "string"
    },
    {
      "example": "Year",
      "help_en": "Auto-renewal duration. Valid values:\n\nYear.\nMonth",
      "help_zh": "自动续费时长，取值说明：\n\nYear：年。\nMonth：月",
      "location": "query",
      "name": "auto_renewal_period_unit",
      "options": [
        "--auto-renewal-period-unit"
      ],
      "raw_name": "AutoRenewalPeriodUnit",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "adb",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "421794A3-72A5-5D27-9E8B-A75A4C503E17",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
