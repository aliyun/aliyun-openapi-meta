{
  "camel_example": "aliyun resourcesharing UntagResources --region cn-hangzhou --ResourceType ResourceShare --RegionId 'cn-hangzhou'",
  "cmd_full_name": "resourcesharing untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from one or more resource shares",
  "description_zh": "为共享单元解绑标签",
  "kebab_example": "aliyun resourcesharing untag-resources --resource-type ResourceShare --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2020-01-10",
    "method": "POST",
    "operation_type": "update",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ResourceShare",
      "help_en": "The resource type.\n\nSet this parameter to `ResourceShare`, which indicates a resource share",
      "help_zh": "资源类型。\n\n取值为`ResourceShare`，表示共享单元",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The IDs of the resource shares.\n\nYou can specify up to 20 resource share IDs",
      "help_zh": "共享单元ID。\n\n最多可以添加20个共享单元",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": false,
      "type": "array"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the resource share resides",
      "help_zh": "共享单元所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The tag keys.\n\nYou can remove up to 20 tag keys.\n\nYou do not need to specify tag keys if you set the `All` parameter to `true`",
      "help_zh": "标签键。\n\n最多可以解绑20个标签键。\n当解绑全部标签的参数`All`取值为`true`时，不需要设置标签键",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags from the specified resources. Valid values:\n\nfalse\n\ntrue\n\nDefault value: false",
      "help_zh": "是否解绑指定资源的全部标签。取值：\nfalse（默认值）：否。\ntrue：是",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "ResourceSharing",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回结果",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "E7747EDF-EDDC-5B38-9B6A-6392B9C92B1C",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
