{
  "camel_example": "aliyun resourcesharing AssociateResourceSharePermission --region cn-hangzhou --ResourceShareId 'rs-6GRmdD3X0000' --PermissionName AliyunRSDefaultPermissionVSwitch",
  "cmd_full_name": "resourcesharing associate-resource-share-permission",
  "cmd_name": "associate-resource-share-permission",
  "deprecated": false,
  "description_en": "Associates permissions with a resource share",
  "description_zh": "调用AssociateResourceSharePermission为共享单元关联共享权限",
  "kebab_example": "aliyun resourcesharing associate-resource-share-permission --resource-share-id rs-6GRmdD3X0000 --permission-name AliyunRSDefaultPermissionVSwitch --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "AssociateResourceSharePermission",
  "operation": {
    "action": "AssociateResourceSharePermission",
    "api_style": "RPC",
    "api_version": "2020-01-10",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "rs-6GRmdD3X****",
      "help_en": "The ID of the resource share",
      "help_zh": "共享单元ID",
      "location": "query",
      "name": "resource_share_id",
      "options": [
        "--resource-share-id"
      ],
      "raw_name": "ResourceShareId",
      "required": true,
      "type": "string"
    },
    {
      "example": "AliyunRSDefaultPermissionVSwitch",
      "help_en": "The name of the permission",
      "help_zh": "共享权限名称",
      "location": "query",
      "name": "permission_name",
      "options": [
        "--permission-name"
      ],
      "raw_name": "PermissionName",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to use the specified permission to replace an existing permission. Valid values:\n\nfalse: does not use the specified permission to replace an existing permission. This is the default value. If you set the value to false for a resource share that does not have associated permissions, the system associates the specified permission with the resource share. In a resource share, one resource type can have only one permission. If you set the value to false for a resource share that already has a permission for the resource type indicated by the specified permission, the system reports an error. This prevents you from replacing the existing permission by mistake.\ntrue: uses the specified permission to replace an existing permission of the same resource type",
      "help_zh": "指定的权限是否替换现有权限。取值：\nfalse（默认值）：否，不替换现有权限，适合于将当前权限添加到没有权限的共享单元。由于同一个共享单元中相同资源类型只能具有一个权限，如果共享单元中某资源类型已经有设置权限，同时您将Replace设置为fasle，此时该操作会报错。防止您误操作替换现有权限。\n\ntrue：是，替换共享单元中相同资源类型的权限",
      "location": "query",
      "name": "replace",
      "options": [
        "--replace"
      ],
      "raw_name": "Replace",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "ResourceSharing",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_zh": "返回结果",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "111FB84A-60A9-403E-9067-E55D7EE95BD1",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
