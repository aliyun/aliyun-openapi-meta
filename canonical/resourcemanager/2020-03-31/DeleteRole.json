{
  "camel_example": "aliyun resourcemanager DeleteRole --region cn-hangzhou --RoleName ECSAdmin",
  "cmd_full_name": "resourcemanager delete-role",
  "cmd_name": "delete-role",
  "deprecated": false,
  "description_en": "Deletes a RAM role",
  "description_zh": "调用DeleteRole删除角色",
  "kebab_example": "aliyun resourcemanager delete-role --role-name ECSAdmin --api-version 2020-03-31 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteRole",
  "operation": {
    "action": "DeleteRole",
    "api_style": "RPC",
    "api_version": "2020-03-31",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "ECSAdmin",
      "help_en": "The name of the role.\n\nThe name must be 1 to 64 characters in length and can contain letters, digits, periods (.), and hyphens (-)",
      "help_zh": "角色名称。\n\n长度为1~64个字符，可包含英文字母、数字、点号（.）和短横线（-）",
      "location": "query",
      "name": "role_name",
      "options": [
        "--role-name"
      ],
      "raw_name": "RoleName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ResourceManager",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "898FAB24-7509-43EE-A287-086FE4C44394",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
