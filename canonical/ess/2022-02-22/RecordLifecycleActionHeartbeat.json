{
  "cmd_full_name": "ess record-lifecycle-action-heartbeat",
  "cmd_name": "record-lifecycle-action-heartbeat",
  "deprecated": false,
  "description_en": "Extends the time window during which Elastic Compute Service (ECS) instances stay in a Pending state. If the current time window during which an ECS instance stays in a Pending state is not sufficient for you to complete custom operations on the ECS instance, you can call the RecordLifecycleActionHeartbeat operation to extend the time window. When you call this operation, you can specify lifecycleHookId, lifecycleActionToken, and heartbeatTimeout to extend the time window for the desired ECS instance",
  "description_zh": "如果伸缩组内生命周期挂钩触发后被挂起的ECS实例的等待时间不能满足您的自定义操作，在ECS实例被挂起状态下，调用RecordLifecycleActionHeartbeat，通过指定生命周期挂钩的ID、伸缩活动的等待状态标识符、需设置的等待时间等配置参数，延长挂起时间",
  "kebab_example": "aliyun ess record-lifecycle-action-heartbeat --lifecycle-hook-id ash-bp1fxuqyi98w0aib0000 --lifecycle-action-token F324B880-900E-4968-85DD-816911130000 --biz-region-id cn-hangzhou --api-version 2022-02-22 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "RecordLifecycleActionHeartbeat",
  "operation": {
    "action": "RecordLifecycleActionHeartbeat",
    "api_style": "RPC",
    "api_version": "2022-02-22",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ash-bp1fxuqyi98w0aib****",
      "help_en": "The ID of the lifecycle hook",
      "help_zh": "生命周期挂钩的ID",
      "location": "query",
      "name": "lifecycle_hook_id",
      "options": [
        "--lifecycle-hook-id"
      ],
      "raw_name": "lifecycleHookId",
      "required": true,
      "type": "string"
    },
    {
      "example": "F324B880-900E-4968-85DD-81691113****",
      "help_en": "The action token of the lifecycle hook. You can obtain the token from the details page of the Simple Message Queue (SMQ, formerly MNS) queue specified for the lifecycle hook.\n\nYou can also call the https://help.aliyun.com/document_detail/459333.html operation to obtain the action token of the lifecycle hook.\n\nIf you specified an SMQ topic for the lifecycle hook, you can obtain the token from the MNS topic",
      "help_zh": "伸缩活动的等待状态标识符，您可以在实例被挂起的状态下，从生命周期挂钩指定的MNS队列中，接收消息对应的详情页中获取。\n\n也可以调用https://help.aliyun.com/document_detail/459333.html获取。\n\n还可以从生命周期挂钩指定的MNS主题中获取该值",
      "location": "query",
      "name": "lifecycle_action_token",
      "options": [
        "--lifecycle-action-token"
      ],
      "raw_name": "lifecycleActionToken",
      "required": true,
      "type": "string"
    },
    {
      "example": "600",
      "help_en": "The time window during which the ECS instance stays in a Pending state. When the time window ends, Auto Scaling executes the default action. Valid values: 30 to 21600. Unit: seconds.\n\nAfter you create a lifecycle hook, you can call this operation to extend the time window during which the ECS instance stays in a Pending state. You can also call the https://help.aliyun.com/document_detail/459335.html operation to remove the ECS instance from the Pending state ahead of schedule.\n\nDefault value: 600",
      "help_zh": "生命周期挂钩为伸缩组活动设置的等待时间，等待状态超时后会执行默认执行策略。取值范围：30~21600，单位：秒。\n\n创建了生命周期挂钩后，您可以调用本接口延长ECS实例的等待时间，也可以调用https://help.aliyun.com/document_detail/459335.html提前结束伸缩活动的等待状态。\n\n默认值：600",
      "location": "query",
      "name": "heartbeat_timeout",
      "options": [
        "--heartbeat-timeout"
      ],
      "raw_name": "heartbeatTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the scaling group",
      "help_zh": "伸缩组所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ess",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
