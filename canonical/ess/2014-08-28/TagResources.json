{
  "camel_example": "aliyun ess TagResources --region cn-hangzhou --ResourceId.1 'asg-2zej7nf7hea77k5a0000' --Tag.1.Key TestKey --Tag.1.Value TestValue --ResourceType scalinggroup --RegionId 'cn-hangzhou' --Tag.1.Propagate false",
  "cmd_full_name": "ess tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Creates and binds tags to a specified list of Auto Scaling resources",
  "description_zh": "为指定的弹性伸缩资源列表统一创建并绑定标签",
  "kebab_example": "aliyun ess tag-resources --biz-region-id cn-hangzhou --resource-type scalinggroup --resource-id asg-2zej7nf7hea77k5a0000 --tag Value=TestValue Propagate=false Key=TestKey --api-version 2014-08-28 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2014-08-28",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the resource. You can call https://help.aliyun.com/document_detail/25609.html to view the latest list of Alibaba Cloud regions",
      "help_zh": "资源所属的地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "scalinggroup",
      "help_en": "The resource type. Currently, only scaling groups are supported. Set the value to scalinggroup",
      "help_zh": "资源类型定义，目前仅支持伸缩组。取值：scalinggroup",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "asg-bp17xb4x1vr29lgt****",
      "help_en": "The resource ID. Valid values of N: 1 to 50",
      "help_zh": "资源ID。N的取值范围为1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "TestKey",
            "help_en": "The tag key of the resource. Valid values of N: 1 to 20.\n\nOnce specified, the tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`, or contain `http://` or `https://`",
            "help_zh": "资源的标签键。N的取值范围：1~20。\n\n一旦输入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "false",
            "help_en": "Specifies whether the tag is a propagated tag. Valid values:\n\ntrue: The tags on the scaling group are propagated only to instances that are about to be created, and are not propagated to instances that are already running in the scaling group.\nfalse: The tags on the scaling group are not propagated to instances.\n\nDefault value: false",
            "help_zh": "标识该标签是否为可传播标签，取值范围：\n\ntrue：伸缩组上的标签只会传播到即将新创建的实例上，不会传播到已经在伸缩组中运行的实例上。\nfalse：伸缩组上的标签不会传播到实例上。\n\n默认值：false",
            "name": "propagate",
            "options": [
              "--propagate"
            ],
            "raw_name": "Propagate",
            "required": false,
            "type": "bool"
          },
          {
            "example": "TestValue",
            "help_en": "The tag value of the resource. Valid values of N: 1 to 20.\n\nThe tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`",
            "help_zh": "资源的标签值。N的取值范围：1~20。\n\n输入该值时，可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tag information of the resource",
      "help_zh": "资源的标签信息",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ess",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "74C4E313-8570-479F-8791-DC25360D****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
