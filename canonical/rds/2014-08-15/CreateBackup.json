{
  "camel_example": "aliyun rds CreateBackup --region cn-hangzhou --DBInstanceId 'rm-uf6wjk50000'",
  "cmd_full_name": "rds create-backup",
  "cmd_name": "create-backup",
  "deprecated": false,
  "description_en": "Creates a backup set for an ApsaraDB RDS instance",
  "description_zh": "该接口用于为RDS实例创建一个备份集",
  "kebab_example": "aliyun rds create-backup --db-instance-id rm-uf6wjk50000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateBackup",
  "operation": {
    "action": "CreateBackup",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call DescribeDBInstances to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "rds_mysql",
      "help_en": "The list of databases. Separate multiple databases with commas (,).\nThis parameter takes effect only when the BackupStrategy parameter is set to db",
      "help_zh": "数据库列表，多个数据库之间用英文逗号（,）隔开。\n满足以下条件时，该参数生效：已传入BackupStrategy参数（取值为db）",
      "location": "query",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "raw_name": "DBName",
      "required": false,
      "type": "string"
    },
    {
      "example": "db",
      "help_en": "The backup strategy. Valid values:\ndb: single-database backup\ninstance: instance backup\n\nThis parameter takes effect only when the following conditions are met:\nMySQL: The BackupMethod parameter is set to Logical.\nSQL Server: The BackupType parameter is set to FullBackup",
      "help_zh": "备份策略，取值：\ndb：单库备份\ninstance：实例备份\n\n满足以下条件时，该参数生效：\nMySQL：已传入BackupMethod参数（取值Logical）。\nSQL Server：已传入BackupType参数（取值为FullBackup）",
      "location": "query",
      "name": "backup_strategy",
      "options": [
        "--backup-strategy"
      ],
      "raw_name": "BackupStrategy",
      "required": false,
      "type": "string"
    },
    {
      "example": "Physical",
      "help_en": "The backup type. Valid values:\nLogical: logical backup. Only MySQL instances with local disks support this type.\nPhysical: physical backup. MySQL instances with local disks, SQL Server instances, and PostgreSQL instances support this type.\nSnapshot: snapshot backup. MySQL instances with cloud disks, SQL Server instances, PostgreSQL instances, and MariaDB instances support this type.\n\nDefault value: Physical.\n\nWhen you use logical backup, the database must contain data (the data cannot be empty).\nMariaDB instances support only snapshot backup. However, set this parameter to Physical",
      "help_zh": "备份类型，取值：\nLogical：逻辑备份（仅MySQL本地盘支持）\nPhysical：物理备份（MySQL本地盘、SQL Server、PostgreSQL支持）\nSnapshot：快照备份（MySQL云盘、SQL Server、PostgreSQL、MariaDB均支持）\n\n默认值：Physical。\n\n使用逻辑备份时，数据库内需要有存储数据（数据不为空）。\nMariaDB实例仅支持快照备份，但参数取值请填写Physical",
      "location": "query",
      "name": "backup_method",
      "options": [
        "--backup-method"
      ],
      "raw_name": "BackupMethod",
      "required": false,
      "type": "string"
    },
    {
      "example": "Auto",
      "help_en": "The backup method for SQL Server instances. Valid values:\nAuto (default): automatically selects full backup or incremental backup.\nFullBackup: full backup.\n\nThis parameter takes effect only when the BackupMethod parameter is set to Physical",
      "help_zh": "SQL Server实例备份方式，取值：\nAuto（默认值）：自动选择全量备份或增量备份\nFullBackup：全量备份\n\n该值仅在BackupMethod参数为Physical时生效",
      "location": "query",
      "name": "backup_type",
      "options": [
        "--backup-type"
      ],
      "raw_name": "BackupType",
      "required": false,
      "type": "string"
    },
    {
      "example": "7",
      "help_en": "SQL Server: When the BackupStrategy parameter is set to db, the BackupMethod parameter is set to Physical, and the BackupType parameter is set to FullBackup, you can specify the retention period of the backup set. Valid values: 7 to 730 days, or -1 (long-term retention (LTR)).\nMySQL: You can specify the retention period of the backup set. Valid values: 7 to 730 days, or -1 (long-term retention (LTR))",
      "help_zh": "SQL Serevr：当参数BackupStrategy为db、BackupMethod为Physical且BackupType为FullBackup时，支持指定备份集的保留时间，取值7~730天、-1（长期保留）。\nMySQL：支持指定备份集的保留时间，取值 7~730 天、-1（长期保留）",
      "location": "query",
      "name": "backup_retention_period",
      "options": [
        "--backup-retention-period"
      ],
      "raw_name": "BackupRetentionPeriod",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "BackupJobId": {
            "backendName": "data.backupjobId",
            "description_en": "The backup task ID",
            "description_zh": "备份任务ID",
            "example": "507****",
            "mappingResourcePropertyName": "$.BackupId",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "2C125605-266F-41CA-8AC5-3A643D4F42C5",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
