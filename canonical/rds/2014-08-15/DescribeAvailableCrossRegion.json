{
  "camel_example": "aliyun rds DescribeAvailableCrossRegion --region cn-hangzhou --RegionId 'cn-hangzhou'",
  "cmd_full_name": "rds describe-available-cross-region",
  "cmd_name": "describe-available-cross-region",
  "deprecated": false,
  "description_en": "Queries the available destination regions to which the cross-region backup files from a specific source region can be replicated",
  "description_zh": "该接口用于查询所选地域当前可以进行跨地域备份的目的地域",
  "kebab_example": "aliyun rds describe-available-cross-region --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeAvailableCrossRegion",
  "operation": {
    "action": "DescribeAvailableCrossRegion",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call the DescribeRegions operation to query the most recent zone list",
      "help_zh": "地域ID。可以通过接口DescribeRegions查看地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "Regions": {
            "backendName": "data.regionList",
            "itemName": "Region",
            "items": {
              "backendName": "data.regionList",
              "type": "string",
              "visibility": "Public"
            },
            "type": "array",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "39265F46-EC77-4036-8AC4-F035F32F6BE2",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
