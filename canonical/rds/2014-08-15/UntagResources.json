{
  "camel_example": "aliyun rds UntagResources --region cn-hangzhou --ResourceId.1 'rm-uf6wjk50000' --ResourceType INSTANCE --RegionId 'cn-hangzhou'",
  "cmd_full_name": "rds untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from one or more instances",
  "description_zh": "该接口用于为指定的RDS实例解绑标签",
  "kebab_example": "aliyun rds untag-resources --biz-region-id cn-hangzhou --resource-type INSTANCE --resource-id rm-uf6wjk50000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call the DescribeRegions operation to query the most recent region list",
      "help_zh": "地域ID，可以通过接口DescribeRegions查看可用的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "INSTANCE",
      "help_en": "The type of the resource. Set the value to INSTANCE",
      "help_zh": "资源类型定义。取值：\n\nINSTANCE：普通RDS实例。\nCUSTOM：RDS Custom实例。\nCUSTOMDEPLOYMENTSET：RDS Custom部署集。\nCUSTOMDISK：RDS Custom云盘。\nCUSTOMSNAPSHOT：RDS Custom快照",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to delete all tags of the instance. Valid values:\n\ntrue\nfalse\n\nDefault value: false.\n\nThis parameter is valid if parameters that contain TagKey.N are not specified",
      "help_zh": "是否删除实例的全部标签。取值：\ntrue\nfalse\n\n默认值：false。\n未传入TagKey.N时该参数有效",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "rm-uf6wjk5xxxxxxx",
      "help_en": "The instance ID. You can remove tags from N instances at a time. Valid values of N: 1 to 50",
      "help_zh": "实例ID列表。可以同时对N个实例解绑标签，N的取值范围：1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "testkey1",
      "help_en": "The list of tag keys. You can delete N tag keys at a time. Valid values of N: 1 to 20. The value of this parameter cannot be an empty string",
      "help_zh": "标签键列表。可以同时删除N个标签键，N的取值范围：1~20。不允许传入空字符串",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "601B6F25-21E7-4484-99D5-3EF2625C0088",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
