{
  "camel_example": "aliyun rds ModifyDBInstanceMaintainTime --region cn-hangzhou --DBInstanceId 'rm-uf6wjk0000' --MaintainTime '22:00Z-02:00Z'",
  "cmd_full_name": "rds modify-db-instance-maintain-time",
  "cmd_name": "modify-db-instance-maintain-time",
  "deprecated": false,
  "description_en": "Modifies the maintenance window of an instance",
  "description_zh": "该接口用于修改RDS实例的可维护时间段",
  "kebab_example": "aliyun rds modify-db-instance-maintain-time --db-instance-id rm-uf6wjk0000 --maintain-time 22:00Z-02:00Z --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBInstanceMaintainTime",
  "operation": {
    "action": "ModifyDBInstanceMaintainTime",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "rm-uf6wjk****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "22:00Z-02:00Z",
      "help_en": "The maintenance window of the instance. Specify the time in the HH:mmZ-HH:mmZ format. The time must be in UTC",
      "help_zh": "实例的可维护时间段。格式：<i>HH:mm</i>Z-<i>HH:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "maintain_time",
      "options": [
        "--maintain-time"
      ],
      "raw_name": "MaintainTime",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "65BDA532-28AF-4122-AA39-B382721EEE64",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
