{
  "camel_example": "aliyun rds DescribeRegionInfos --region cn-hangzhou --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000",
  "cmd_full_name": "rds describe-region-infos",
  "cmd_name": "describe-region-infos",
  "deprecated": false,
  "description_en": "Queries the regions",
  "description_zh": "该接口用于获取地域列表",
  "kebab_example": "aliyun rds describe-region-infos --client-token ETnLKlblzczshOTUbOCz0000 --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeRegionInfos",
  "operation": {
    "action": "DescribeRegionInfos",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the generated token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "Regions": {
            "backendName": "data.regions",
            "itemName": "RDSRegion",
            "items": {
              "properties": {
                "RegionId": {
                  "backendName": "regionId",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "nullToEmpty": true,
            "type": "array"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "5414A4E5-4C36-4461-95FC-****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
