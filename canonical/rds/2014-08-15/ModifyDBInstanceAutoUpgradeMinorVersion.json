{
  "camel_example": "aliyun rds ModifyDBInstanceAutoUpgradeMinorVersion --region cn-hangzhou --DBInstanceId 'rm-uf6wjk50000' --AutoUpgradeMinorVersion Auto",
  "cmd_full_name": "rds modify-db-instance-auto-upgrade-minor-version",
  "cmd_name": "modify-db-instance-auto-upgrade-minor-version",
  "deprecated": false,
  "description_en": "Changes the method that is used to update the minor engine version of an ApsaraDB RDS for MySQL instance or an ApsaraDB RDS for PostgreSQL instance",
  "description_zh": "该接口用于修改RDS MySQL或RDS PostgreSQL实例升级小版本的方式",
  "kebab_example": "aliyun rds modify-db-instance-auto-upgrade-minor-version --db-instance-id rm-uf6wjk50000 --auto-upgrade-minor-version Auto --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBInstanceAutoUpgradeMinorVersion",
  "operation": {
    "action": "ModifyDBInstanceAutoUpgradeMinorVersion",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "Auto",
      "help_en": "The method that is used to update the minor engine version of the instance. Valid values:\n\nAuto: automatic update.\nManual: manual update. ApsaraDB RDS automatically updates the current minor engine version of the instance only when the current minor engine version is phased out",
      "help_zh": "实例升级小版本的方式，取值：\nAuto：自动升级小版本。\nManual：不自动升级，仅在当前版本下线时才强制升级",
      "location": "query",
      "name": "auto_upgrade_minor_version",
      "options": [
        "--auto-upgrade-minor-version"
      ],
      "raw_name": "AutoUpgradeMinorVersion",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回参数详情",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "A31818D5-0550-4A81-8D13-B45948D7193F",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
