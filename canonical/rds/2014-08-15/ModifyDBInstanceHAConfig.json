{
  "camel_example": "aliyun rds ModifyDBInstanceHAConfig --region cn-hangzhou --HAMode RPO --DbInstanceId 'rm-uf6wjk5430000' --SyncMode Sync",
  "cmd_full_name": "rds modify-db-instance-ha-config",
  "cmd_name": "modify-db-instance-ha-config",
  "deprecated": false,
  "description_en": "Changes the high availability (HA) and data replication mode of an instance",
  "description_zh": "该接口用于修改RDS实例的高可用模式和数据复制方式",
  "kebab_example": "aliyun rds modify-db-instance-ha-config --sync-mode Sync --ha-mode RPO --db-instance-id rm-uf6wjk5430000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDBInstanceHAConfig",
  "operation": {
    "action": "ModifyDBInstanceHAConfig",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "Sync",
      "help_en": "The data replication mode of the instance. For more information, see https://help.aliyun.com/document_detail/96055.html.\n\nSemi-sync: the semi-synchronous mode.\nSync: the synchronous mode.\ngAsyncg: the asynchronous mode.\nMgr: the MySQL group replication (MGR) mode. This mode is available only for the China site (aliyun.com).\n\nThis parameter is not supported for instances that run SQL Server 2017 on RDS Cluster Edition",
      "help_zh": "数据复制方式。\n\nSemi-sync：半同步。\nSync：强同步。\nAsync：异步。\n\n<props=\"china\">- Mgr：组复制。</props>",
      "location": "query",
      "name": "sync_mode",
      "options": [
        "--sync-mode"
      ],
      "raw_name": "SyncMode",
      "required": true,
      "type": "string"
    },
    {
      "example": "RPO",
      "help_en": "The HA mode of the instance.\n\nRPO: Data consistency is preferred. The instance ensures data reliability to minimize data losses. If you have high requirements on data consistency, select this mode.\nRTO: Service availability is preferred. The instance restores the database service at the earliest opportunity to ensure service availability. If you have high requirements for service availability, select this mode",
      "help_zh": "高可用模式。\n\nRPO：数据一致性优先，实例会尽可能保障数据的可靠性，即数据丢失量最少。对于数据一致性要求比较高的用户应该使用RPO模式。\nRTO：实例可用性优先，实例会尽快恢复服务，即可用时间最长。对于数据库在线时间要求比较高的用户应该使用RTO模式",
      "location": "query",
      "name": "ha_mode",
      "options": [
        "--ha-mode"
      ],
      "raw_name": "HAMode",
      "required": true,
      "type": "string"
    },
    {
      "example": "rm-uf6wjk543****",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DbInstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "headers": {},
      "schema": {
        "description_zh": "返回参数详情",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "D4D4BE8A-DD46-440A-BFCD-EE31DA81C9DD",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
