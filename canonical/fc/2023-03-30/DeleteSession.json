{
  "camel_example": "aliyun fc DELETE /2023-03-30/functions/my-func/sessions/test-session-id-1 --region cn-hangzhou --qualifier aliasName1 --header \"Content-Type=application/json\"",
  "cmd_full_name": "fc delete-session",
  "cmd_name": "delete-session",
  "deprecated": false,
  "description_en": "Deletes a specified session, blocks new request routing, and purges session metadata from the database. Subsequent requests from the client carrying the same SessionID are treated as a new session. This operation releases resources and cleans up the session. In session isolation scenarios, the system stops running requests and releases instances bound to the session. In non-session isolation scenarios, running requests continue to run and terminate gracefully",
  "description_zh": "删除指定会话，禁止新请求路由，会话元数据从数据库清除，客户携带相同 SessionID 的后续请求视为新会话。实现资源释放与会话清理。在会话隔离场景下，系统将中止运行的请求，释放会话绑定的实例。非会话隔离场景下，正在运行中的请求将继续运行，优雅终止",
  "kebab_example": "aliyun fc delete-session --function-name my-func --session-id test-session-id-1 --qualifier aliasName1 --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "DeleteSession",
  "operation": {
    "action": "DeleteSession",
    "api_style": "ROA",
    "api_version": "2023-03-30",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/2023-03-30/functions/{functionName}/sessions/{sessionId}"
  },
  "parameters": [
    {
      "example": "my-func",
      "help_en": "The function name",
      "help_zh": "函数名称",
      "location": "path",
      "name": "function_name",
      "options": [
        "--function-name"
      ],
      "raw_name": "functionName",
      "required": true,
      "type": "string"
    },
    {
      "example": "test-session-id-1",
      "help_en": "The session ID to delete",
      "help_zh": "需要删除的会话Id值",
      "location": "path",
      "name": "session_id",
      "options": [
        "--session-id"
      ],
      "raw_name": "sessionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "aliasName1",
      "help_en": "The function alias or version associated with the session ID to delete",
      "help_zh": "需要删除的SessionId关联的函数别名或版本信息",
      "location": "query",
      "name": "qualifier",
      "options": [
        "--qualifier"
      ],
      "raw_name": "qualifier",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/2023-03-30/functions/[functionName]/sessions/[sessionId]",
  "product_code": "FC",
  "protocol": "HTTPS",
  "responses": {
    "204": {}
  },
  "v1_parameters": [
    {
      "help_en": "The function name",
      "help_zh": "函数名称",
      "name": "functionName",
      "position": "path",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The function alias or version associated with the session ID to delete",
      "help_zh": "需要删除的SessionId关联的函数别名或版本信息",
      "name": "qualifier",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The session ID to delete",
      "help_zh": "需要删除的会话Id值",
      "name": "sessionId",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
