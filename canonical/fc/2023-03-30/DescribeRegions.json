{
  "camel_example": "aliyun fc GET /2023-03-30/regions --region cn-hangzhou --AcceptLanguage 'zh-CN' --header \"Content-Type=application/json\"",
  "cmd_full_name": "fc describe-regions",
  "cmd_name": "describe-regions",
  "components": {
    "schemas": {
      "DescribeRegionsOutput": {
        "description_en": "The response",
        "description_zh": "响应",
        "properties": {
          "Regions": {
            "backendName": "Regions",
            "description_en": "The region information",
            "description_zh": "地域信息",
            "properties": {
              "Region": {
                "backendName": "Region",
                "description_en": "The collection of region information",
                "description_zh": "地域信息集合",
                "items": {
                  "description_en": "The region object",
                  "description_zh": "地域对象",
                  "properties": {
                    "LocalName": {
                      "backendName": "LocalName",
                      "description_en": "The region name",
                      "description_zh": "地域名称",
                      "example": "华北1（青岛）\n",
                      "type": "string"
                    },
                    "RegionId": {
                      "backendName": "RegionId",
                      "description_en": "The region ID",
                      "description_zh": "地域ID",
                      "example": "cn-qingdao",
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "type": "array"
              }
            },
            "type": "object"
          }
        },
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Queries the regions where Function Compute 3.0 is available",
  "description_zh": "查询FC 3.0产品支持的地域信息",
  "kebab_example": "aliyun fc describe-regions --accept-language zh-CN --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeRegions",
  "operation": {
    "action": "DescribeRegions",
    "api_style": "ROA",
    "api_version": "2023-03-30",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/2023-03-30/regions"
  },
  "parameters": [
    {
      "enum": [
        "zh-CN",
        "en-US",
        "ja"
      ],
      "example": "zh-CN",
      "help_en": "The language in which the list of regions is returned. For more information, see RFC 7231. Valid values:\n\nzh-CN: Simplified Chinese.\nen-US: English.\nja: Japanese\n\nDefault value: en-US",
      "help_zh": "用户指定返回中文、英文等语言的地域列表信息。更多详情，请参见RFC 7231。取值范围：\n\nzh-CN：简体中文。\n\nen-US：英文。\n\nja：日文\n\n默认值：zh-CN",
      "location": "query",
      "name": "accept_language",
      "options": [
        "--accept-language"
      ],
      "raw_name": "AcceptLanguage",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/2023-03-30/regions",
  "product_code": "FC",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "$ref": "#/components/schemas/DescribeRegionsOutput",
        "description_en": "The response parameters",
        "description_zh": "地域信息",
        "parseType": "struct"
      }
    }
  }
}
