{
  "camel_example": "aliyun onsmqtt InactivateCaCertificate --region cn-hangzhou --MqttInstanceId 'post-cn-7mz2d000000' --Sn 007269004887000000",
  "cmd_full_name": "onsmqtt inactivate-ca-certificate",
  "cmd_name": "inactivate-ca-certificate",
  "deprecated": false,
  "description_en": "ApsaraMQ for MQTT supports X.509 certificate-based device verification. Clients use device certificates for connection authentication. A Certificate Authority (CA) certificate is used to issue device certificates to clients and to verify the validity of those certificates. If you do not need to use a CA certificate for a period of time, call this operation to unregister it. This changes the status of the CA certificate to inactive. When you need to use the CA certificate again, you can call the ActiveCaCertificate operation to reactivate it",
  "description_zh": "微消息队列MQTT版支持X.509证书认证，客户端可通过设备证书完成接入认证。CA证书用于为客户端签发设备证书，并验证设备证书的正确性。\n若您暂时不需要使用某个CA证书，可通过本接口注销CA证书，将CA证书状态变成未激活。当您需要继续使用该CA证书时，可通过ActiveCaCertificate接口将CA证书重新激活",
  "kebab_example": "aliyun onsmqtt inactivate-ca-certificate --mqtt-instance-id post-cn-7mz2d000000 --sn 007269004887000000 --api-version 2020-04-20 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "InactivateCaCertificate",
  "operation": {
    "action": "InactivateCaCertificate",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "post-cn-7mz2d******",
      "help_en": "The ID of the ApsaraMQ for MQTT instance to which the CA certificate is attached",
      "help_zh": "CA证书所绑定的实例ID，即云消息队列 MQTT 版的实例ID",
      "location": "query",
      "name": "mqtt_instance_id",
      "options": [
        "--mqtt-instance-id"
      ],
      "raw_name": "MqttInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "007269004887******",
      "help_en": "The serial number (SN) of the CA certificate that you want to unregister. The SN uniquely identifies a CA certificate.\n\nThe value can be up to 128 bytes long",
      "help_zh": "待注销CA证书的SN序列号，用于唯一标识一个CA证书。\n\n取值范围：不超过128 Byte",
      "location": "query",
      "name": "sn",
      "options": [
        "--sn"
      ],
      "raw_name": "Sn",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The data returned",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request. This ID is unique for each request. You can use it to troubleshoot and locate issues",
            "description_zh": "公共参数，每个请求的ID都是唯一的，可用于排查和定位问题",
            "example": "020F6A43-19E6-4B6E-B846-44EB31DF****",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          },
          "Sn": {
            "backendName": "data",
            "description_en": "The serial number (SN) of the unregistered CA certificate. The SN uniquely identifies a CA certificate",
            "description_zh": "已注销CA证书的SN序列号，用于唯一标识一个CA证书",
            "example": "007269004887******",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
