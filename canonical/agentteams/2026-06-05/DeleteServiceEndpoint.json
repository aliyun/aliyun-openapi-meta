{
  "camel_example": "",
  "cmd_full_name": "agentteams delete-service-endpoint",
  "cmd_name": "delete-service-endpoint",
  "deprecated": false,
  "description_en": "Deletes an endpoint from a specified AgentTeams instance and cleans up related resources",
  "description_zh": "用于删除指定AgentTeams实例下的endpoint，并清理相关资源",
  "kebab_example": "aliyun agentteams delete-service-endpoint --endpoint-id ep-xxx --instance-id agentteams-demo --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteServiceEndpoint",
  "operation": {
    "action": "DeleteServiceEndpoint",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "delete",
    "url": "/"
  },
  "parameters": [
    {
      "example": "ep-xxx",
      "help_en": "Endpoint ID",
      "help_zh": "Endpoint ID",
      "location": "query",
      "name": "endpoint_id",
      "options": [
        "--endpoint-id"
      ],
      "raw_name": "EndpointId",
      "required": true,
      "type": "string"
    },
    {
      "example": "agentteams-demo",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "description_en": "删除成功",
      "description_zh": "删除成功",
      "schema": {
        "description_en": "The API response",
        "description_zh": "API 响应结果",
        "example": "{}",
        "properties": {
          "Code": {
            "backendName": "code",
            "description_en": "The business response code. The value is `SUCCESS` upon success",
            "description_zh": "业务响应码，成功为 `SUCCESS`",
            "example": "SUCCESS",
            "title_en": "业务响应码，成功为 `SUCCESS`",
            "title_zh": "业务响应码，成功为 `SUCCESS`",
            "type": "string"
          },
          "Data": {
            "backendName": "data",
            "description_en": "The value is `true` when the deletion is successful",
            "description_zh": "删除成功时为 `true`",
            "example": "true",
            "title_en": "删除成功时为 `true`",
            "title_zh": "删除成功时为 `true`",
            "type": "boolean"
          },
          "Message": {
            "backendName": "message",
            "description_en": "Response message, which is fixed to `success` upon success",
            "description_zh": "响应信息，成功固定为 `success`",
            "example": "success",
            "title_en": "响应信息，成功固定为 `success`",
            "title_zh": "响应信息，成功固定为 `success`",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "Request ID, which preferentially uses the `requestId` from the request context, and then the `traceId`",
            "description_zh": "请求 ID，优先取请求上下文 `requestId`，其次 `traceId`",
            "example": "xxx",
            "title_en": "请求 ID，优先取请求上下文 `requestId`，其次 `traceId`",
            "title_zh": "请求 ID，优先取请求上下文 `requestId`，其次 `traceId`",
            "type": "string"
          },
          "Success": {
            "backendName": "success",
            "description_en": "Indicates whether the operation is successful",
            "description_zh": "是否成功",
            "example": "true",
            "title_en": "是否成功",
            "title_zh": "是否成功",
            "type": "boolean"
          }
        },
        "type": "object"
      }
    }
  }
}
