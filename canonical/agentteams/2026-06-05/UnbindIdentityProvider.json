{
  "camel_example": "",
  "cmd_full_name": "agentteams unbind-identity-provider",
  "cmd_name": "unbind-identity-provider",
  "deprecated": false,
  "description_en": "Unbinds the upstream identity provider from a specified instance, dissociates the identity federation relationship, and cleans up associated user identities and data",
  "description_zh": "用于解绑指定实例的上游身份提供商，解除身份集成关系并清理关联的用户身份与数据",
  "kebab_example": "aliyun agentteams unbind-identity-provider --instance-id agentteams-xxx --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UnbindIdentityProvider",
  "operation": {
    "action": "UnbindIdentityProvider",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "agentteams-xxx",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "DingTalk",
      "help_en": "The identity provider type. Currently, only DingTalk is supported. Specify this parameter to locate the binding to be unbound",
      "help_zh": "身份提供商类型，目前仅支持钉钉，调用时需提供该参数以定位待解绑的绑定",
      "location": "query",
      "name": "identity_provider_type",
      "options": [
        "--identity-provider-type"
      ],
      "raw_name": "IdentityProviderType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "ApiDataResponse<Void>",
        "description_zh": "ApiDataResponse<Void>",
        "example": "{}",
        "properties": {
          "Code": {
            "backendName": "code",
            "description_en": "The response code",
            "description_zh": "响应代码",
            "example": "SUCCESS",
            "type": "string"
          },
          "HttpStatusCode": {
            "backendName": "httpStatusCode",
            "description_en": "The HTTP status code",
            "description_zh": "HTTP 状态码",
            "example": "200",
            "format": "int32",
            "type": "integer"
          },
          "Message": {
            "backendName": "message",
            "description_en": "The response message",
            "description_zh": "响应消息",
            "example": "success",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 ID",
            "example": "req-xxx",
            "type": "string"
          },
          "Success": {
            "backendName": "success",
            "description_en": "Indicates whether the call is successful",
            "description_zh": "是否成功",
            "example": "true",
            "type": "boolean"
          }
        },
        "title_en": "ApiDataResponse<Void>",
        "title_zh": "ApiDataResponse<Void>",
        "type": "object"
      }
    }
  }
}
