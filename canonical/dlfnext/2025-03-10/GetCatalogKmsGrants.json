{
  "camel_example": "",
  "cmd_full_name": "dlfnext get-catalog-kms-grants",
  "cmd_name": "get-catalog-kms-grants",
  "deprecated": false,
  "description_en": "Retrieves the DLF data access role ARN and KMS key policy authorization statement required to configure BYOK SSE KMS for a catalog. This operation returns the DLF data access role and KMS key policy authorization statement required to configure BYOK SSE KMS. The customer master key is configured at the catalog level through oss.sse.kms.key-id. When creating a table, you can enable SSE KMS for the table by setting the table property oss.sse.kms.enabled=true. Tables with KMS encryption enabled and tables without encryption can coexist under the same catalog",
  "description_zh": "获取为 Catalog 配置 BYOK SSE KMS 所需的 DLF 数据访问角色 ARN 和 KMS 密钥策略授权语句。本接口返回配置 BYOK SSE KMS 所需的 DLF 数据访问角色和 KMS 密钥策略授权语句。客户主密钥通过 oss.sse.kms.key-id 配置在 Catalog 级别。创建表时，可以通过表属性 oss.sse.kms.enabled=true 选择为该表启用 SSE KMS。同一 Catalog 下可以同时存在启 用 KMS 加密的表和未启用加密的表",
  "kebab_example": "aliyun dlfnext get-catalog-kms-grants --catalog my_catalog --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "GetCatalogKmsGrants",
  "operation": {
    "action": "GetCatalogKmsGrants",
    "api_style": "ROA",
    "api_version": "2025-03-10",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/dlf/v1/catalogs/{catalog}/kms/grants"
  },
  "parameters": [
    {
      "example": "my_catalog",
      "help_en": "The catalog name",
      "help_zh": "  Catalog名称",
      "location": "path",
      "name": "catalog",
      "options": [
        "--catalog"
      ],
      "raw_name": "catalog",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/dlf/v1/catalogs/[catalog]/kms/grants",
  "product_code": "DlfNext",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "dataAccessRoleArn": {
            "description_en": "The ARN of the RAM role used by DLF to access catalog data. When configuring the KMS key policy, you must grant this role permissions to use the customer master key",
            "description_zh": "  DLF用于访问Catalog数据的RAM角色ARN。配置KMS密钥策略时需要向该角色授予使用客户主密钥的权限",
            "example": "acs:ram::123456789012****:role/AliyunDlfNextDataAccessRole",
            "type": "string"
          },
          "keyPolicyStatement": {
            "description_en": "The authorization statement that must be added to the customer master key policy. This statement grants the DLF data access role corresponding to dataAccessRoleArn the KMS permissions required for data encryption and decryption",
            "description_zh": " 需要添加到客户主密钥策略中的授权语句，用于向dataAccessRoleArn对应的DLF数据访问角色授予数据加密和解密所需的KMS权限",
            "example": "  {\"Sid\":\"AllowDLFDataAccess\",\"Effect\":\"Allow\",\"Principal\":{\"RAM\":[\"acs:ram::123456789012****:role/\n  AliyunDlfNextDataAccessRole\"]},\"Action\":[\"kms:Decrypt\",\"kms:GenerateDataKey\"],\"Resource\":[\"*\"]}",
            "type": "string"
          },
          "region": {
            "description_en": "The region ID to which the catalog belongs",
            "description_zh": " Catalog所属的地域ID",
            "example": "cn-hangzhou",
            "type": "string"
          },
          "workflowRoleArn": {
            "description_en": "The DLF workflow role ARN. In the current version, the workflow role is not granted customer master key access permissions based on the least privilege principle. Therefore, this field returns an empty value",
            "description_zh": "  DLF工作流角色ARN。当前版本按最小权限原则不向工作流角色授予客户主密钥访问权限，因此该字段返回空值",
            "example": "null",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    },
    "403": {
      "schema": {
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "resourceName": {
            "type": "string"
          },
          "resourceType": {
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "404": {
      "schema": {
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "resourceName": {
            "type": "string"
          },
          "resourceType": {
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "500": {
      "schema": {
        "properties": {
          "code": {
            "backendName": "code",
            "type": "string"
          },
          "message": {
            "backendName": "message",
            "type": "string"
          },
          "resourceName": {
            "backendName": "resourceName",
            "type": "string"
          },
          "resourceType": {
            "backendName": "resourceType",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
