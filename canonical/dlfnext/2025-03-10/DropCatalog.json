{
  "camel_example": "",
  "cmd_full_name": "dlfnext drop-catalog",
  "cmd_name": "drop-catalog",
  "components": {
    "schemas": {
      "ErrorResponse": {
        "properties": {
          "code": {
            "format": "int32",
            "type": "integer"
          },
          "message": {
            "type": "string"
          },
          "resourceName": {
            "backendName": "resourceName",
            "type": "string"
          },
          "resourceType": {
            "backendName": "resourceType",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "deprecated": false,
  "description_en": "Deletes a data lake data catalog. The following conditions must be met, otherwise the deletion will fail: all tables and user-created databases under the catalog have been deleted; the databases and tables have been deleted for at least 24 hours",
  "description_zh": "删除数据湖数据目录。需满足以下条件，否则删除将失败：Catalog下所有表及自建数据库已删除；库表已删除24小时",
  "kebab_example": "aliyun dlfnext drop-catalog --catalog catalog_name --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "DropCatalog",
  "operation": {
    "action": "DropCatalog",
    "api_style": "ROA",
    "api_version": "2025-03-10",
    "method": "DELETE",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/dlf/v1/catalogs/{catalog}"
  },
  "parameters": [
    {
      "example": "catalog_name",
      "help_en": "The name of the data catalog",
      "help_zh": "数据目录名称",
      "location": "path",
      "name": "catalog",
      "options": [
        "--catalog"
      ],
      "raw_name": "catalog",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/dlf/v1/catalogs/[catalog]",
  "product_code": "DlfNext",
  "protocol": "HTTPS",
  "responses": {
    "200": {},
    "401": {
      "schema": {
        "$ref": "#/components/schemas/ErrorResponse",
        "parseType": "struct"
      }
    }
  }
}
