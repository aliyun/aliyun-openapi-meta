{
  "camel_example": "",
  "cmd_full_name": "dlfnext verify-catalog-kms",
  "cmd_name": "verify-catalog-kms",
  "deprecated": false,
  "description_en": "Validates the BYOK SSE-KMS configuration of a Catalog: performs an SSE-KMS write probe on the storage bucket using the issued temporary credentials. This operation only validates whether the specified customer master key (CMK) can be used for BYOK SSE-KMS of the Catalog. It does not automatically enable encryption for all tables in the Catalog. The CMK is configured at the Catalog level through oss.sse.kms.key-id. When creating a table, set the table property oss.sse.kms.enabled=true to enable SSE-KMS for the specified table",
  "description_zh": "校验 Catalog 的 BYOK SSE-KMS 配置:用下发的临时凭证对存储桶做一次 SSE-KMS 写探针并校验。本接口仅校验指定客户主密钥能否用于 Catalog 的 BYOK SSE KMS，不会自动为 Catalog 下的所有表启用加密。客户主密钥通过 oss.sse.kms.key-id 配置在 Catalog 级别。创建表时，需要通过表属性 oss.sse.kms.enabled=true 为指定表启用 SSE KMS",
  "kebab_example": "aliyun dlfnext verify-catalog-kms --catalog my_catalog --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "VerifyCatalogKms",
  "operation": {
    "action": "VerifyCatalogKms",
    "api_style": "ROA",
    "api_version": "2025-03-10",
    "body_mapping": {
      "kms_key_id": "kmsKeyId"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/dlf/v1/catalogs/{catalog}/kms/verify"
  },
  "parameters": [
    {
      "example": "my_catalog",
      "help_en": "The name of the Catalog whose BYOK SSE-KMS configuration you want to validate",
      "help_zh": "待校验 BYOK SSE-KMS 配置的 Catalog 名称。   ",
      "location": "path",
      "name": "catalog",
      "options": [
        "--catalog"
      ],
      "raw_name": "catalog",
      "required": true,
      "type": "string"
    },
    {
      "example": "key-1234567890abcdef",
      "help_en": "The ID of the KMS customer master key (CMK) to be validated. The server uses this key to perform an SSE-KMS write probe",
      "help_zh": "待验证的 KMS 客户主密钥（CMK）ID。服务端将使用该密钥执行 SSE-KMS 写探针",
      "location": "body",
      "name": "kms_key_id",
      "options": [
        "--kms-key-id"
      ],
      "param_style": "json",
      "raw_name": "kmsKeyId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/dlf/v1/catalogs/[catalog]/kms/verify",
  "product_code": "DlfNext",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "errorCode": {
            "description_en": "The error code returned when the validation fails. An empty string is returned when the validation is successful",
            "description_zh": "校验失败时返回的错误码；校验成功时为空字     KeyNotFound\n                           符串",
            "example": "KeyNotFound",
            "type": "string"
          },
          "hint": {
            "description_en": "The description of the failure cause and remediation suggestions returned when the validation fails. An empty string is returned when the validation is successful",
            "description_zh": "校验失败时返回的原因说明及处理建议；校验     The specified parameter KMS keyId is not found.\n                           成功时为空字符串",
            "example": "The specified parameter KMS keyId is not found.",
            "type": "string"
          },
          "kmsKeyId": {
            "description_en": "The KMS key identifier actually used by the probe object. When the validation is successful, this corresponds to the customer master key (CMK) specified in the request",
            "description_zh": "探针对象实际使用的 KMS 密钥标识。校验成功    key-1234567890abcdef\n                           时与请求指定的客户主密钥相对应",
            "example": " key-1234567890abcdef",
            "type": "string"
          },
          "serverSideEncryption": {
            "description_en": "The server-side encryption method actually used by the probe object. Returns KMS when the validation is successful",
            "description_zh": "探针对象实际使用的服务端加密方式。校验成     KMS\n                           功时返回 KMS",
            "example": "KMS",
            "type": "string"
          },
          "success": {
            "description_en": "Indicates whether the validation is successful. A value of true indicates that the write probe succeeded and the SSE-KMS configuration of the object meets expectations. A value of false indicates that the validation failed",
            "description_zh": " 校验是否成功。true 表示写探针成功且对象的    true\n                           SSE-KMS 配置符合预期；false 表示校验失\n                           败",
            "example": "true",
            "type": "boolean"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    },
    "403": {
      "schema": {
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "resourceName": {
            "type": "string"
          },
          "resourceType": {
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "404": {
      "schema": {
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "resourceName": {
            "type": "string"
          },
          "resourceType": {
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "500": {
      "schema": {
        "properties": {
          "code": {
            "backendName": "code",
            "type": "string"
          },
          "message": {
            "backendName": "message",
            "type": "string"
          },
          "resourceName": {
            "backendName": "resourceName",
            "type": "string"
          },
          "resourceType": {
            "backendName": "resourceType",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "v1_body_parameters": [
    {
      "help_en": "The validation request body, which contains the KMS customer master key (CMK) key ID to authenticate",
      "help_zh": "校验请求体，包含待验证的 KMS 客户主密钥 ID",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
