{
  "camel_example": "aliyun amqp-open CreateExchange --region cn-hangzhou --InstanceId 'amqp-cn-v0h1kb9nu000' --Internal false --VirtualHost test --AutoDeleteState false --ExchangeName DemoExchange --ExchangeType DIRECT",
  "cmd_full_name": "amqp-open create-exchange",
  "cmd_name": "create-exchange",
  "deprecated": false,
  "description_en": "A producer sends a message to an exchange. The exchange then routes the message to one or more queues based on the routing key and the binding key, or discards the message",
  "description_zh": "Producer将消息发送到Exchange，由Exchange将消息路由到一个或多个Queue中（或者丢弃）。Exchange根据Routing Key和Binding Key将消息路由到Queue",
  "kebab_example": "aliyun amqp-open create-exchange --instance-id amqp-cn-v0h1kb9nu000 --virtual-host test --exchange-name DemoExchange --exchange-type DIRECT --auto-delete-state false --internal false --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "CreateExchange",
  "operation": {
    "action": "CreateExchange",
    "api_style": "RPC",
    "api_version": "2019-12-12",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "amqp-cn-v0h1kb9nu***",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "test",
      "help_en": "The name of the vhost to which the exchange belongs",
      "help_zh": "Exchange所在的Vhost名称",
      "location": "formData",
      "name": "virtual_host",
      "options": [
        "--virtual-host"
      ],
      "raw_name": "VirtualHost",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "DemoExchange",
      "help_en": "The name of the exchange. Note:\n\nThe name can contain only letters, digits, hyphens (-), underscores (_), periods (.), number signs (#), forward slashes (/), and at signs (@). The name must be 1 to 255 characters in length.\n\nThe name of an exchange cannot be changed after the exchange is created. To change the name, delete the exchange and create a new one",
      "help_zh": "Exchange名称。说明：\n\nExchange名称只能包含字母、数字、短划线（-）、下划线（_）、半角句号（.）、井号（#）、正斜线（/）、at 符号（@），长度限制1~255 字符。\n\nExchange创建后，名称不支持修改，只能删除后重建",
      "location": "formData",
      "name": "exchange_name",
      "options": [
        "--exchange-name"
      ],
      "raw_name": "ExchangeName",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "enum": [
        "DIRECT",
        "TOPIC",
        "FANOUT",
        "HEADERS",
        "X_JMS_TOPIC",
        "X_DELAYED_MESSAGE",
        "X_CONSISTENT_HASH"
      ],
      "example": "DIRECT",
      "help_en": "The type of the exchange. Valid values:\n\nDIRECT: This routing rule type routes messages to a queue whose binding key exactly matches the routing key of the message.\n\nTOPIC: This type is similar to the DIRECT type. It routes messages to bound queues using routing key pattern matching and string comparison.\n\nFANOUT: This routing rule type is simple. It routes all messages sent to the exchange to all queues that are bound to the exchange. This works like a broadcast feature.\n\nHEADERS: This type is similar to the DIRECT type. It uses header properties instead of a routing key for routing. When a queue is bound to a headers exchange, key-value pairs are defined for the binding. When a message is sent to the exchange, key-value pairs are defined in the message header. The exchange routes the message by comparing the key-value pairs in the header with the key-value pairs of the binding",
      "help_zh": "Exchange类型。取值：\n\nDIRECT：该类型路由规则会将消息路由到Binding Key与Routing Key完全匹配的Queue中。\n\nTOPIC：该类型与DIRECT类型相似，使用Routing Key模式匹配和字符串比较的方式将消息路由至绑定的Queue。\n\nFANOUT：该类型路由规则非常简单，会把所有发送到该Exchange的消息路由到所有与它绑定的Queue中，相当于广播功能。\n\nHEADERS：该类型与DIRECT类型相似。Headers Exchange使用Headers属性代替Routing Key进行路由匹配，在绑定Headers Exchange和Queue时，设置绑定属性的键值对；在向Headers Exchange发送消息时，设置消息的Headers属性键值对，使用消息Headers属性键值对和绑定属性键值对比较的方式将消息路由至绑定的Queue",
      "location": "formData",
      "name": "exchange_type",
      "options": [
        "--exchange-type"
      ],
      "raw_name": "ExchangeType",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "DIRECT",
        "TOPIC",
        "FANOUT",
        "HEADERS",
        "X_JMS_TOPIC"
      ],
      "example": "DIRECT",
      "help_en": "An x-delayed-message exchange lets you use the x-delay header property to specify a delivery delay for a message in milliseconds. The routing rule for the delayed message is determined by the exchange type that you specify for the XDelayedType parameter. This parameter sets the actual exchange type to which the message is delivered after the delay. Valid values:\n\nDIRECT: Delivers the delayed message to the specified queue that is bound to a DIRECT exchange.\n\nTOPIC: Delivers the delayed message to queues that are bound to a TOPIC exchange.\n\nFANOUT: Delivers the delayed message to queues that are bound to a FANOUT exchange.\n\nHEADERS: Delivers the delayed message to queues that are bound to a HEADERS exchange.\n\nX-JMS-TOPIC: Delivers the delayed message to queues that are bound to an X-JMS-TOPIC exchange",
      "help_zh": "x-delayed-message 类型的 Exchange 可以自定义消息的 Header 属性， x-delay用来指定消息延时投递的时间段，单位为毫秒。该类 Exchange 的路由规则取决于 x-delayed-type 参数中指定的 Exchange 类型，用于指定延迟消息最终将被投递到的实际 Exchange 类型。取值：\n\nDIRECT：将延迟消息投递到和 DIRECT 类型 Exchange 绑定的指定队列。\nTOPIC：将延迟消息投递到和 TOPIC 类型 Exchange 绑定的队列。\n\nFANOUT：将延迟消息投递到和 FANOUT 类型 Exchange 绑定的队列。\n\nHEADERS：将延迟消息投递到和 HEADERS 类型 Exchange 绑定的队列。\n\nX-JMS-TOPIC：将延迟消息投递到和X-JMS-TOPIC绑定的队列",
      "location": "formData",
      "name": "x_delayed_type",
      "options": [
        "--x-delayed-type"
      ],
      "raw_name": "XDelayedType",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "false",
      "help_en": "Specifies whether to automatically delete the exchange. Valid values:\n\ntrue: Yes. The exchange is automatically deleted after the last queue is unbound from it.\n\nfalse: No. The exchange is not automatically deleted after the last queue is unbound from it",
      "help_zh": "是否自动删除。取值：\n\ntrue：是。如果绑定到该Exchange的最后一个Queue解除绑定，该Exchange会自动删除。\n\nfalse：否。如果绑定到该Exchange的最后一个Queue解除绑定，该Exchange不会自动删除",
      "location": "formData",
      "name": "auto_delete_state",
      "options": [
        "--auto-delete-state"
      ],
      "raw_name": "AutoDeleteState",
      "required": true,
      "type": "bool"
    },
    {
      "doc_required": true,
      "example": "false",
      "help_en": "Specifies whether the exchange is an internal exchange. Valid values:\n\nfalse: No\n\ntrue: Yes",
      "help_zh": "是否为内部Exchange。取值：\n\nfalse：否\n\ntrue：是",
      "location": "formData",
      "name": "internal",
      "options": [
        "--internal"
      ],
      "raw_name": "Internal",
      "required": true,
      "type": "bool"
    },
    {
      "example": "DemoAE",
      "help_en": "The alternate exchange. Configure an alternate exchange to receive messages that fail to be routed",
      "help_zh": "备份Exchange。配置备份Exchange用于接收Exchange路由失败的消息",
      "location": "formData",
      "name": "alternate_exchange",
      "options": [
        "--alternate-exchange"
      ],
      "raw_name": "AlternateExchange",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "amqp-open",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "021788F6-E50C-4BD6-9F80-66B0A19A****",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
