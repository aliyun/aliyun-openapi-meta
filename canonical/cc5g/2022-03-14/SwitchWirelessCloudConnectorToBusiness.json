{
  "camel_example": "",
  "cmd_full_name": "cc5g switch-wireless-cloud-connector-to-business",
  "cmd_name": "switch-wireless-cloud-connector-to-business",
  "deprecated": false,
  "description_en": "Switches a 5G Cloud service test instance to a commercial instance.  ",
  "description_zh": "用于把5G上云服务测试实例切换到商业实例",
  "kebab_example": "aliyun cc5g switch-wireless-cloud-connector-to-business --wireless-cloud-connector-id cc5g-xxxx --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "SwitchWirelessCloudConnectorToBusiness",
  "operation": {
    "action": "SwitchWirelessCloudConnectorToBusiness",
    "api_style": "RPC",
    "api_version": "2022-03-14",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cc5g-xxxx",
      "help_en": "ID of the 5G Cloud instance.  ",
      "help_zh": "5G高速上云实例id",
      "location": "query",
      "name": "wireless_cloud_connector_id",
      "options": [
        "--wireless-cloud-connector-id"
      ],
      "raw_name": "WirelessCloudConnectorId",
      "required": true,
      "type": "string"
    },
    {
      "example": "TF-******-1633255280-43c94bf7-2dd3-4c14-8",
      "help_en": "Ensures idempotence of the request. Generate a unique value from your client for this parameter across different requests. Only ASCII characters are supported, and the length cannot exceed 64 characters.  \nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request.  ",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。只支持ASCII字符，且不能超过64个字符。\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform a dry run of the request. Valid values:  \ntrue: Sends a dry run request without deleting the instance. Checks include whether required parameters are provided, whether the request format is valid, and whether business limits are met. If the check fails, an error is returned. If the check passes, the error code `DryRunOperation` is returned.  \nfalse (default): Sends a normal request. If the check passes, an HTTP 2xx status code is returned and the instance is deleted.  ",
      "help_zh": "是否只预检此次请求，取值：\ntrue：发送检查请求，不会删除实例。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并删除实例",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CC5G",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Response object  ",
        "description_zh": "响应对象",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "Id of the request",
            "description_zh": "Id of the request",
            "example": "74ADDCF3-E3FE-5200-9290-1320125D71DC",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
