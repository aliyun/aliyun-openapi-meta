{
  "camel_example": "",
  "cmd_full_name": "cc5g resume-cards",
  "cmd_name": "resume-cards",
  "deprecated": false,
  "description_en": "Resumes one or more SIM cards with the carrier. Batch operations are supported",
  "description_zh": "激活单卡，或恢复运营商侧卡状态，支持批量操作",
  "kebab_example": "aliyun cc5g resume-cards --iccids 890912389123 --biz-region-id cn-hangzhou --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "ResumeCards",
  "operation": {
    "action": "ResumeCards",
    "api_style": "RPC",
    "api_version": "2022-03-14",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "type": "string"
      },
      "help_en": "IoT SIM card ICCID. Maximum 500 ICCIDs per request",
      "help_zh": "物联网SIM卡卡号，每次最大500条",
      "location": "query",
      "name": "iccids",
      "options": [
        "--iccids"
      ],
      "param_style": "repeatList",
      "raw_name": "Iccids",
      "required": true,
      "type": "array"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "Region ID",
      "help_zh": "地域id",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "TF-******-1633255280-43c94bf7-2dd3-4c14-8",
      "help_en": "Ensures idempotence. Generate a unique value on your client for each request. Only ASCII characters are supported, and the value must not exceed 64 characters.\n\nIf you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. Each API request may have a different RequestId",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。只支持ASCII字符，且不能超过64个字符。\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Indicates whether to perform only a dry run. Valid values:\n\ntrue: Performs a check without activating or resuming the SIM card. Checks include required parameters, request format, and business limits. If the check fails, the corresponding error code returns. If the check passes, the error code `DryRunOperation` returns.\n\nfalse (default): Performs the actual operation. Activates or resumes the SIM card after passing the check",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会激活/恢复卡状态。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后激活/恢复卡状态",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CC5G",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Response object",
        "description_zh": "响应对象",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "Request ID",
            "description_zh": "请求id",
            "example": "1651FBB6-4FBF-49FF-A9F5-DF5D696C7EC6",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
