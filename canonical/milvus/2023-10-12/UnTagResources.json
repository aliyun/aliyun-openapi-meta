{
  "camel_example": "",
  "cmd_full_name": "milvus un-tag-resources",
  "cmd_name": "un-tag-resources",
  "deprecated": false,
  "description_en": "Remove resource tags",
  "description_zh": "删除标签",
  "kebab_example": "aliyun milvus un-tag-resources --biz-region-id cn-hangzhou --resource-type instance --resource-id c-xx --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "UnTagResources",
  "operation": {
    "action": "UnTagResources",
    "api_style": "ROA",
    "api_version": "2023-10-12",
    "method": "DELETE",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": "/webapi/tags"
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID",
      "help_zh": "地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "instance",
      "help_en": "The resource type",
      "help_zh": "资源类型",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The resource IDs. A maximum of 50 resource IDs are supported",
      "help_zh": "资源ID,最多 50个子项",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "json",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The tag keys to remove. A maximum of 20 tag keys are supported",
      "help_zh": "对应标签键列表，最多包含20个子项",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "json",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags from the resources. This parameter takes effect only when the TagKey parameter is left empty. Valid values: true and false (default)",
      "help_zh": "是否全部删除，只针对tags为空时有效。取值范围： true  false True False   默认是 false",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/webapi/tags",
  "product_code": "milvus",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "ABCD-1234-5678-EFGH",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
