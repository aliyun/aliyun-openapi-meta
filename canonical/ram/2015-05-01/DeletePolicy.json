{
  "camel_example": "aliyun ram DeletePolicy --region cn-hangzhou --CascadingDelete true --PolicyName 'OSS-Administrator'",
  "cmd_full_name": "ram delete-policy",
  "cmd_name": "delete-policy",
  "deprecated": false,
  "description_en": "Deletes a policy",
  "description_zh": "调用DeletePolicy接口删除指定的权限策略",
  "kebab_example": "aliyun ram delete-policy --policy-name OSS-Administrator --cascading-delete true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeletePolicy",
  "operation": {
    "action": "DeletePolicy",
    "api_style": "RPC",
    "api_version": "2015-05-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "OSS-Administrator",
      "help_en": "The name of the policy",
      "help_zh": "指定权限策略名称",
      "location": "query",
      "name": "policy_name",
      "options": [
        "--policy-name"
      ],
      "raw_name": "PolicyName",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to delete all versions of the policy. Valid values:\n\ntrue: deletes all versions of the policy.\n\nfalse: does not delete all versions of the policy. If you set the parameter to false, the non-default versions of the policy are not deleted. Before you delete the policy, you must manually delete all non-default versions of the policy",
      "help_zh": "是否级联删除权限策略的所有版本。取值：\n\ntrue：会级联删除所有版本。\nfalse：不会级联删除，需要您在删除权限策略前，手动删除权限策略非默认版本",
      "location": "query",
      "name": "cascading_delete",
      "options": [
        "--cascading-delete"
      ],
      "raw_name": "CascadingDelete",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ram",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回结果",
        "properties": {
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "898FAB24-7509-43EE-A287-086FE4C44394",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
