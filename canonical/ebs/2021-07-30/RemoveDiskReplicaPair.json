{
  "camel_example": "aliyun ebs RemoveDiskReplicaPair --region cn-hangzhou --RegionId 'cn-hangzhou' --ReplicaPairId 'pair-cn-dsa0000' --ReplicaGroupId 'pg-myreplica0000'",
  "cmd_full_name": "ebs remove-disk-replica-pair",
  "cmd_name": "remove-disk-replica-pair",
  "deprecated": false,
  "description_en": "Removes a replication pair from a replication pair-consistent group. After a replication pair is removed from a replication pair-consistent group, the pair is disassociated from the group but is not deleted",
  "description_zh": "移除一致性复制组中的复制对。移除复制对只是解除了复制对和一致性复制组的绑定关系，不会删除复制对",
  "kebab_example": "aliyun ebs remove-disk-replica-pair --biz-region-id cn-hangzhou --replica-group-id pg-myreplica0000 --replica-pair-id pair-cn-dsa0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RemoveDiskReplicaPair",
  "operation": {
    "action": "RemoveDiskReplicaPair",
    "api_style": "RPC",
    "api_version": "2021-07-30",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID of the replication pair-consistent group",
      "help_zh": "一致性复制组所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pg-myreplica****",
      "help_en": "The ID of the replication pair-consistent group.\n\nYou can call the https://help.aliyun.com/document_detail/426614.html operation to query the IDs of replication pair-consistent groups",
      "help_zh": "一致性复制组ID。\n\n您可以调用https://help.aliyun.com/document_detail/426614.html查询一致性复制组ID",
      "location": "query",
      "name": "replica_group_id",
      "options": [
        "--replica-group-id"
      ],
      "raw_name": "ReplicaGroupId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pair-cn-dsa****",
      "help_en": "The ID of the replication pair",
      "help_zh": "复制对ID",
      "location": "query",
      "name": "replica_pair_id",
      "options": [
        "--replica-pair-id"
      ],
      "raw_name": "ReplicaPairId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-42665544****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ebs",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "C123F94F-4E38-19AE-942A-A8D6F44F****",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
