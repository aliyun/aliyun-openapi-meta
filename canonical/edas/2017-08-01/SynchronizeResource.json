{
  "camel_example": "aliyun edas GET /pop/v5/resource/pop_sync_resource --region cn-hangzhou --Type ecs --header \"Content-Type=application/json\"",
  "cmd_full_name": "edas synchronize-resource",
  "cmd_name": "synchronize-resource",
  "deprecated": false,
  "description_en": "Synchronizes the basic Alibaba Cloud resources that belong to your account to Enterprise Distributed Application Service (EDAS). This operation is applicable to Elastic Compute Service (ECS) clusters",
  "description_zh": "同步用户下的阿里云基础资源到EDAS。适用于ECS集群",
  "kebab_example": "aliyun edas synchronize-resource --type ecs --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "SynchronizeResource",
  "operation": {
    "action": "SynchronizeResource",
    "api_style": "ROA",
    "api_version": "2017-08-01",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/pop/v5/resource/pop_sync_resource"
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "ecs",
      "help_en": "The type of the resource to be synchronized. Valid values: `ecs, slb, vpc, and all`. These values are case-sensitive",
      "help_zh": "同步的资源类型，该参数可选的参数值为：`ecs、slb、vpc、all`其中的一个，不支持其他格式的参数值",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    },
    {
      "example": "i-bp17c***5q8x,i-bp1**5q8x",
      "help_en": "The ID of the resource. This parameter is required only when you set the Type parameter to `ecs`. If you specify multiple IDs, separate them with commas (,). You can synchronize up to 50 resources at a time",
      "help_zh": "指定的资源ID，仅当Type为`ecs`时支持填写此参数，如有多个ID，需要以逗号分隔，一次最多同步50个资源",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/pop/v5/resource/pop_sync_resource",
  "product_code": "Edas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Code": {
            "backendName": "code",
            "description_en": "The HTTP status code that is returned",
            "description_zh": "接口请求返回的状态码",
            "example": "200",
            "format": "int32",
            "type": "integer",
            "visibility": "Public"
          },
          "Data": {
            "backendName": "data",
            "description_en": "The data returned for the request",
            "description_zh": "接口请求返回的数据",
            "example": "PopSyncResource success",
            "type": "string",
            "visibility": "Public"
          },
          "Message": {
            "backendName": "message",
            "description_en": "The message that is returned",
            "description_zh": "接口请求返回的消息",
            "example": "success",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "接口请求唯一标识符",
            "example": "F8DFGED-K98***************",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          },
          "Success": {
            "backendName": "success",
            "description_en": "Indicates whether the resources are synchronized. Valid values:\n\ntrue: The resources are synchronized.\n\nfalse: The resources fail to be synchronized",
            "description_zh": "接口请求结果，取值如下：\n\ntrue：请求成功。\n\nfalse：请求失败",
            "example": "true",
            "type": "boolean",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
