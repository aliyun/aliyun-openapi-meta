{
  "camel_example": "aliyun edas POST /pop/v5/account/authorize_role --region cn-hangzhou --RoleIds 1_2 --TargetUserId 'test@133000000' --header \"Content-Type=application/json\"",
  "cmd_full_name": "edas authorize-role",
  "cmd_name": "authorize-role",
  "deprecated": false,
  "description_en": "Grant permissions to RAM roles",
  "description_zh": "授权角色",
  "kebab_example": "aliyun edas authorize-role --target-user-id test@133000000 --role-ids 1_2 --region cn-hangzhou",
  "method": "DELETE|POST|PUT",
  "multi_version": false,
  "name": "AuthorizeRole",
  "operation": {
    "action": "AuthorizeRole",
    "api_style": "ROA",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/pop/v5/account/authorize_role"
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "test@133******",
      "help_en": "The ID of the RAM user to which you want to assign the roles",
      "help_zh": "待授权子账号 ID",
      "location": "query",
      "name": "target_user_id",
      "options": [
        "--target-user-id"
      ],
      "raw_name": "TargetUserId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "1;2",
      "help_en": "The ID of the role to be assigned. If you want to assign multiple roles to the specified RAM user, separate the IDs of the roles with semicolons (;). If you leave this parameter empty, the roles assigned to the specified RAM user are revoked",
      "help_zh": "角色 ID，用“;”号分割，null 代表取消角色授权",
      "location": "query",
      "name": "role_ids",
      "options": [
        "--role-ids"
      ],
      "raw_name": "RoleIds",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/pop/v5/account/authorize_role",
  "product_code": "Edas",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Code": {
            "backendName": "code",
            "description_en": "The HTTP status code that is returned",
            "description_zh": "状态码",
            "example": "200",
            "format": "int32",
            "type": "integer",
            "visibility": "Public"
          },
          "Message": {
            "backendName": "message",
            "description_en": "The additional information that is returned",
            "description_zh": "附加信息",
            "example": "success",
            "type": "string",
            "visibility": "Public"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "57609587-DFA2-*******",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
