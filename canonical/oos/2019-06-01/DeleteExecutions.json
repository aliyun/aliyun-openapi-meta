{
  "camel_example": "aliyun oos DeleteExecutions --region cn-hangzhou --RegionId 'cn-hangzhou' --ExecutionIds 'exec-xxx'",
  "cmd_full_name": "oos delete-executions",
  "cmd_name": "delete-executions",
  "deprecated": false,
  "description_en": "Deletes an execution",
  "description_zh": "删除执行",
  "kebab_example": "aliyun oos delete-executions --biz-region-id cn-hangzhou --execution-ids exec-xxx --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteExecutions",
  "operation": {
    "action": "DeleteExecutions",
    "api_style": "RPC",
    "api_version": "2019-06-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域 ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "[\"exec-xxx\"]",
      "help_en": "The execution IDs. The value is a JSON array of IDs in the format of `[\"xxxxxxxxx\", \"yyyyyyyyy\", … \"zzzzzzzzz\"]`. A maximum of 100 IDs are supported. Separate multiple IDs with commas (,)",
      "help_zh": "执行 ID（列表）。\n可以由多个 ID 组成一个 JSON 数组，格式为`[\"xxxxxxxxx\", \"yyyyyyyyy\", … \"zzzzzzzzz\"]`，最多支持 100 个 ID，ID `之间用半角逗号（,）隔开",
      "location": "query",
      "name": "execution_ids",
      "options": [
        "--execution-ids"
      ],
      "raw_name": "ExecutionIds",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to force delete a task that is being executed. Default value: false",
      "help_zh": "是否强制删除执行中的任务，默认为false",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "oos",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "491DF8C2-34C9-4679-9DB3-4C0F49B129AC",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
