{
  "camel_example": "aliyun oos DeleteParameter --region cn-hangzhou --RegionId 'cn-hangzhou' --Name MyParameter",
  "cmd_full_name": "oos delete-parameter",
  "cmd_name": "delete-parameter",
  "deprecated": false,
  "description_en": "Deletes a common parameter",
  "description_zh": "删除普通参数",
  "kebab_example": "aliyun oos delete-parameter --biz-region-id cn-hangzhou --name MyParameter --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteParameter",
  "operation": {
    "action": "DeleteParameter",
    "api_style": "RPC",
    "api_version": "2019-06-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "MyParameter",
      "help_en": "The parameter name. The name can contain letters, digits, hyphens (-), and underscores (_). The name cannot exceed 180 characters in length and cannot start with aliyun, acs, alibaba, alicloud, or oos",
      "help_zh": "参数名称。取值范围：内容限制为字母、数字、中划线、下划线，长度不能超过180字符，且不能以aliyun、acs、alibaba、alicloud、oos开头",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "oos",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "RequestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "C0D02BDF-77F6-49F2-95C9-8E87121D2979",
            "nullToEmpty": false,
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
