{
  "camel_example": "aliyun gpdb UntagResources --region cn-hangzhou --ResourceId.1 'gp-j6cdjo680513l0000' --ResourceType instance --RegionId 'cn-hangzhou'",
  "cmd_full_name": "gpdb untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from AnalyticDB for PostgreSQL instances. If the tags that you remove are not added to other instances, the tags are automatically deleted",
  "description_zh": "为指定的AnalyticDB PostgreSQL实例列表统一解绑标签。解绑后，如果该标签没有绑定其他任何实例，会被自动删除",
  "kebab_example": "aliyun gpdb untag-resources --biz-region-id cn-hangzhou --resource-type instance --resource-id gp-j6cdjo680513l0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2016-05-03",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "Region ID, you can view available region IDs through the https://help.aliyun.com/document_detail/86912.html interface",
      "help_zh": "地域ID，您可通过接口https://help.aliyun.com/document_detail/86912.html查看可用的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "instance",
      "help_en": "Resource type. The value range is:\n`instance`: Reserved mode instance.\n`ALIYUN::GPDB::INSTANCE`: Elastic mode instance",
      "help_zh": "资源类型。取值范围：\n`instance`：预留模式实例。\n`ALIYUN::GPDB::INSTANCE`：弹性模式实例",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Whether to untag all tags on the instance. This parameter is only effective when TagKey.N is not set in the request. The value range is:\n\ntrue\nfalse\n\nDefault value: false",
      "help_zh": "是否解绑实例上全部的标签。当请求中未设置TagKey.N时，该参数才有效。取值范围：\n\ntrue\nfalse\n\n默认值：false",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "Instance ID. N's value range: 1~50",
      "help_zh": "实例ID。N的取值范围：1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "Resource tag key. N's value range: 1~20",
      "help_zh": "资源的标签键。N的取值范围：1~20",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "gpdb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "Request ID",
            "description_zh": "请求ID",
            "example": "5414A4E5-4C36-4461-95FC-23757A20B5F8",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
