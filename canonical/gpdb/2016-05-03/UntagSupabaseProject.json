{
  "camel_example": "aliyun gpdb UntagSupabaseProject --region cn-hangzhou --All false --ResourceId.1 'gp-2zev51nt170740000' --TagKey.1 TestKey --ResourceType instance --RegionId 'cn-hangzhou'",
  "cmd_full_name": "gpdb untag-supabase-project",
  "cmd_name": "untag-supabase-project",
  "deprecated": false,
  "description_en": "Unbinds tags from a Supabase instance",
  "description_zh": "解绑 Supabase 实例的标签",
  "kebab_example": "aliyun gpdb untag-supabase-project --biz-region-id cn-hangzhou --resource-type instance --all false --resource-id gp-2zev51nt170740000 --tag-key TestKey --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "UntagSupabaseProject",
  "operation": {
    "action": "UntagSupabaseProject",
    "api_style": "RPC",
    "api_version": "2016-05-03",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the instance",
      "help_zh": "实例所在的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "instance",
      "help_en": "The resource type. Valid values:\n`instance`: reserved mode instance.\n`ALIYUN::GPDB::INSTANCE`: elastic mode instance",
      "help_zh": "资源类型。取值范围：\n`instance`：预留模式实例。\n`ALIYUN::GPDB::INSTANCE`：弹性模式实例",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to unbind all tags from the instance. This parameter takes effect only when TagKey.N is not specified in the request. Valid values:\n\ntrue\nfalse\n\nDefault value: false",
      "help_zh": "是否解绑实例上全部的标签。当请求中未设置TagKey.N时，该参数才有效。取值范围：\n\ntrue\nfalse\n\n默认值：false",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The instance ID. Valid values of N: 1 to 50",
      "help_zh": "实例ID。N的取值范围：1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": false,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The tag key of the resource. Valid values of N: 1 to 20",
      "help_zh": "资源的标签键。N的取值范围：1~20",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "gpdb",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "Id of the request",
            "description_zh": "Id of the request",
            "example": "34b32a0a-08ef-4a87-b6be-cdd9f56fc3ad",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
