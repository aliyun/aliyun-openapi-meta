{
  "camel_example": "aliyun gpdb GetBranchSchema --region cn-hangzhou --ProjectId 'spb-xxxx' --DBName neondb --RegionId 'cn-hangzhou' --BranchId 'br-xxxx'",
  "cmd_full_name": "gpdb get-branch-schema",
  "cmd_name": "get-branch-schema",
  "deprecated": false,
  "description_en": "Retrieves the database schema of a Supabase branch",
  "description_zh": "获取 Supabase 分支的数据库 Schema",
  "kebab_example": "aliyun gpdb get-branch-schema --biz-region-id cn-hangzhou --project-id spb-xxxx --branch-id br-xxxx --db-name neondb --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetBranchSchema",
  "operation": {
    "action": "GetBranchSchema",
    "api_style": "RPC",
    "api_version": "2016-05-03",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID. This parameter is required when you create a primary branch. When you create a sub-branch, the region of the primary branch is inherited by default",
      "help_zh": "地域 ID。创建主分支时必须指定，创建子分支时默认继承主分支所在地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "spb-xxxx",
      "help_en": "The Supabase project ID that corresponds to the primary branch",
      "help_zh": "主分支对应的 Supabase 项目 ID",
      "location": "query",
      "name": "project_id",
      "options": [
        "--project-id"
      ],
      "raw_name": "ProjectId",
      "required": true,
      "type": "string"
    },
    {
      "example": "br-xxxx",
      "help_en": "The branch ID that uniquely identifies a Supabase branch",
      "help_zh": "分支 ID，用于唯一标识一个 Supabase 分支",
      "location": "query",
      "name": "branch_id",
      "options": [
        "--branch-id"
      ],
      "raw_name": "BranchId",
      "required": true,
      "type": "string"
    },
    {
      "example": "neondb",
      "help_en": "The database name. The system databases postgres, template0, and template1 do not support schema queries",
      "help_zh": "数据库名称。系统库 postgres、template0、template1 不支持查询 Schema",
      "location": "query",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "raw_name": "DBName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "gpdb",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response object",
        "description_zh": "返回数据对象",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 ID",
            "example": "ABB39CC3-4488-4857-905D-2E4A051D****",
            "type": "string"
          },
          "Sql": {
            "backendName": "data.sql",
            "description_en": "The schema SQL content",
            "description_zh": "Schema SQL 内容",
            "example": "CREATE TABLE public.example(id int);",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
