{
  "camel_example": "aliyun cdn RollbackStagingConfig --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "cdn rollback-staging-config",
  "cmd_name": "rollback-staging-config",
  "deprecated": false,
  "description_en": "Rolls back configurations in the staging environment. After you call this operation, all configurations in the staging environment are cleared",
  "description_zh": "调用RollbackStagingConfig可以起到回滚模拟环境配置的作用，配置回滚后的模拟环境将被还原为初始状态，模拟环境下的所有配置都将被清空",
  "kebab_example": "aliyun cdn rollback-staging-config --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RollbackStagingConfig",
  "operation": {
    "action": "RollbackStagingConfig",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name. You can specify only one domain name in each request",
      "help_zh": "加速域名，仅支持单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "04F0F334-1335-436C-A1D7-6C044FE73368",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
