{
  "camel_example": "aliyun cdn PublishStagingConfigToProduction --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "cdn publish-staging-config-to-production",
  "cmd_name": "publish-staging-config-to-production",
  "deprecated": false,
  "description_en": "Publishes the configurations of the staging environment to the production environment",
  "description_zh": "调用PublishStagingConfigToProduction将模拟环境配置发布为生产环境配置",
  "kebab_example": "aliyun cdn publish-staging-config-to-production --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "PublishStagingConfigToProduction",
  "operation": {
    "action": "PublishStagingConfigToProduction",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name. You can specify only one domain name in each call",
      "help_zh": "加速域名，仅支持单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "04F0F334-1335-436C-A1D7-6C044FE73368",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
