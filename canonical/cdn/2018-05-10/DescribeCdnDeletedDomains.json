{
  "camel_example": "aliyun cdn DescribeCdnDeletedDomains --region cn-hangzhou --PageSize 5 --PageNumber 1",
  "cmd_full_name": "cdn describe-cdn-deleted-domains",
  "cmd_name": "describe-cdn-deleted-domains",
  "deprecated": false,
  "description_en": "Queries the domain names that are deleted from your account",
  "description_zh": "调用DescribeCdnDeletedDomains查询您名下已删除的域名",
  "kebab_example": "aliyun cdn describe-cdn-deleted-domains --page-number 1 --page-size 5 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeCdnDeletedDomains",
  "operation": {
    "action": "DescribeCdnDeletedDomains",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "1",
      "help_en": "The number of the page to return. Valid values: 1 to 100000. Default value: 1",
      "help_zh": "当前页码，默认值1，取值范围：1~100000",
      "location": "query",
      "maximum": "100000",
      "minimum": "1",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "5",
      "help_en": "The number of domain names to return per page. Valid values: an integer between 1 and 500. Default value: 20",
      "help_zh": "每页显示的域名个数，默认值20，取值范围：1~500之前的任意整数",
      "location": "query",
      "maximum": "500",
      "minimum": "1",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Domains": {
            "backendName": "data.content.data",
            "itemName": "PageData",
            "items": {
              "properties": {
                "DomainName": {
                  "backendName": "resourceName",
                  "type": "string"
                },
                "GmtModified": {
                  "backendName": "updateTime",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "nullToEmpty": true,
            "type": "array"
          },
          "PageNumber": {
            "backendName": "data.pageIndex",
            "description_en": "The page number of the returned page, which is the same as the PageNumber parameter in request parameters",
            "description_zh": "当前页码，同请求参数的PageNumber",
            "example": "1",
            "format": "int64",
            "type": "integer"
          },
          "PageSize": {
            "backendName": "data.pageSize",
            "description_en": "The number of domain names returned per page, which is the same as the PageSize parameter in request parameters",
            "description_zh": "每页显示的域名个数，同请求参数的PageSize",
            "example": "5",
            "format": "int64",
            "type": "integer"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "AA75AADB-5E25-4970-B480-EAA1F5658483",
            "type": "string"
          },
          "TotalCount": {
            "backendName": "data.content.total",
            "description_en": "The total number of domain names returned",
            "description_zh": "查询到的域名总数",
            "example": "16",
            "format": "int64",
            "type": "integer"
          }
        },
        "type": "object"
      }
    }
  }
}
