{
  "camel_example": "aliyun cdn VerifyDomainOwner --region cn-hangzhou --DomainName 'example.com' --VerifyType dnsCheck",
  "cmd_full_name": "cdn verify-domain-owner",
  "cmd_name": "verify-domain-owner",
  "deprecated": false,
  "description_en": "Verifies the ownership of a specified domain name",
  "description_zh": "调用VerifyDomainOwner对域名归属权进行校验",
  "kebab_example": "aliyun cdn verify-domain-owner --domain-name example.com --verify-type dnsCheck --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "VerifyDomainOwner",
  "operation": {
    "action": "VerifyDomainOwner",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The domain name of which you want to verify the ownership. You can specify only one domain name",
      "help_zh": "只支持单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    },
    {
      "example": "dnsCheck",
      "help_en": "The verification method. Valid values:\n\ndnsCheck: by DNS record\n\nfileCheck: by verification file",
      "help_zh": "校验方式，取值：\n\ndnsCheck：DNS验证。\nfileCheck：文件验证",
      "location": "query",
      "name": "verify_type",
      "options": [
        "--verify-type"
      ],
      "raw_name": "VerifyType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Content": {
            "backendName": "data.content",
            "description_en": "The verification result.\n\nThis parameter is returned only if the operation fails",
            "description_zh": "校验内容。\n\n调用成功不返回该参数，调用失败返回校验内容",
            "example": "verify_dffeb6610035dcb77b413a59c32c****",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "34AB41F1-04A5-496F-8C8D-634BDBE6A9FB",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
