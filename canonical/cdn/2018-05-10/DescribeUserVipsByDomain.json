{
  "camel_example": "aliyun cdn DescribeUserVipsByDomain --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "cdn describe-user-vips-by-domain",
  "cmd_name": "describe-user-vips-by-domain",
  "deprecated": false,
  "description_en": "Queries virtual IP addresses (VIPs) of CDN points of presence (POPs) by domain name",
  "description_zh": "调用DescribeUserVipsByDomain查询域名的IP列表",
  "kebab_example": "aliyun cdn describe-user-vips-by-domain --domain-name example.com --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeUserVipsByDomain",
  "operation": {
    "action": "DescribeUserVipsByDomain",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name. You can specify only one domain name",
      "help_zh": "域名，只支持单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "on",
        "off"
      ],
      "example": "on",
      "help_en": "Specifies whether to query the virtual IP addresses of only healthy CDN POPs. Valid values:\n\non: healthy CDN edge nodes.\n\noff: all CDN edge nodes",
      "help_zh": "是否查询健康VIP。取值：\n\non：健康VIP。\noff：全部VIP",
      "location": "query",
      "name": "available",
      "options": [
        "--available"
      ],
      "raw_name": "Available",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "DomainName": {
            "backendName": "data.content.domainName",
            "description_en": "The domain name",
            "description_zh": "域名",
            "example": "example.com",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "820E7900-5CA9-4AEF-B0DD-20ED5F64BE55",
            "type": "string"
          },
          "Vips": {
            "backendName": "data.content.vips",
            "itemName": "Vip",
            "items": {
              "backendName": "data.content.vips",
              "nullToEmpty": true,
              "type": "string"
            },
            "nullToEmpty": true,
            "parseType": "null",
            "type": "array"
          }
        },
        "type": "object"
      }
    }
  }
}
