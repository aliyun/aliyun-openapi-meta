{
  "camel_example": "aliyun cdn DescribeDomainCname --region cn-hangzhou --DomainName 'example.com,example.org'",
  "cmd_full_name": "cdn describe-domain-cname",
  "cmd_name": "describe-domain-cname",
  "deprecated": false,
  "description_en": "Performs a CNAME resolution check on an accelerated domain name to determine whether the CNAME configuration is complete",
  "description_zh": "调用接口对加速域名 Cname 进行解析检测，查看解析结果判断是否完成Cname配置",
  "kebab_example": "aliyun cdn describe-domain-cname --domain-name example.com,example.org --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDomainCname",
  "operation": {
    "action": "DescribeDomainCname",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com,example.org",
      "help_en": "The accelerated domain names to query. Separate multiple domain names with commas (,). You can specify up to 30 domain names in a single request",
      "help_zh": "需要查询的加速域名，多个用逗号（,）分隔，单次查询最多支持30个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The check result",
        "description_zh": "检测结果",
        "properties": {
          "CnameDatas": {
            "backendName": "data.content",
            "itemName": "Data",
            "items": {
              "properties": {
                "Cname": {
                  "backendName": "cname",
                  "type": "string"
                },
                "Domain": {
                  "backendName": "domain",
                  "type": "string"
                },
                "ErrMsg": {
                  "backendName": "errMsg",
                  "type": "string"
                },
                "Passed": {
                  "backendName": "passed",
                  "type": "string"
                },
                "Status": {
                  "backendName": "status",
                  "format": "int32",
                  "type": "integer"
                }
              },
              "type": "object"
            },
            "nullToEmpty": true,
            "type": "array"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "06D29681-B7CD-4034-A8CC-28AFFA213539",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
