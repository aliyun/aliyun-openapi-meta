{
  "camel_example": "aliyun cdn DescribeVerifyContent --region cn-hangzhou --DomainName 'example.com'",
  "cmd_full_name": "cdn describe-verify-content",
  "cmd_name": "describe-verify-content",
  "deprecated": false,
  "description_en": "Queries the ownership verification content of an accelerated domain name",
  "description_zh": "调用DescribeVerifyContent查询归属校验内容",
  "kebab_example": "aliyun cdn describe-verify-content --domain-name example.com --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeVerifyContent",
  "operation": {
    "action": "DescribeVerifyContent",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The domain name of which the ownership was verified. You can specify only one domain name",
      "help_zh": "只支持单个域名",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Content": {
            "backendName": "data.content",
            "description_en": "The verification content",
            "description_zh": "校验内容",
            "example": "verify_dffeb6610035dcb77b413a59c32cd91f",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "该条任务请求ID",
            "example": "34AB41F1-04A5-496F-8C8D-634BDBE6A9FB",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
