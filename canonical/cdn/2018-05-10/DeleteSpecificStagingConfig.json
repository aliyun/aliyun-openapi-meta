{
  "camel_example": "aliyun cdn DeleteSpecificStagingConfig --region cn-hangzhou --DomainName 'example.com' --ConfigId 2317",
  "cmd_full_name": "cdn delete-specific-staging-config",
  "cmd_name": "delete-specific-staging-config",
  "deprecated": false,
  "description_en": "Deletes a specified configuration of the staging environment",
  "description_zh": "调用DeleteSpecificStagingConfig删除模拟环境中的指定配置",
  "kebab_example": "aliyun cdn delete-specific-staging-config --domain-name example.com --config-id 2317 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteSpecificStagingConfig",
  "operation": {
    "action": "DeleteSpecificStagingConfig",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "example.com",
      "help_en": "The accelerated domain name. You can specify up to 50 domain names in each request. Separate multiple domain names with commas (,)",
      "help_zh": "您的加速域名，多个域名使用半角逗号（,）分隔，一次最多支持50个域名查询",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": true,
      "type": "string"
    },
    {
      "example": "2317",
      "help_en": "The configuration IDs. Separate configuration IDs with commas (,). For more information about ConfigId, see https://help.aliyun.com/document_detail/388994.html",
      "help_zh": "功能配置ID，多个ID用半角逗号（,）分隔。ConfigId查询和使用，请参见https://help.aliyun.com/document_detail/388994.html",
      "location": "query",
      "name": "config_id",
      "options": [
        "--config-id"
      ],
      "raw_name": "ConfigId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "04F0F334-1335-436C-A1D7-6C044FE73368",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
