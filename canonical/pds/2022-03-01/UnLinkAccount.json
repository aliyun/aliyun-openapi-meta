{
  "camel_example": "",
  "cmd_full_name": "pds un-link-account",
  "cmd_name": "un-link-account",
  "deprecated": false,
  "description_en": "Unlinks a specified identity from a user account",
  "description_zh": "取消账号绑定",
  "kebab_example": "aliyun pds un-link-account --user-id uid1 --type mobile --identity 1390000 --extra 1 --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "UnLinkAccount",
  "operation": {
    "action": "UnLinkAccount",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "extra": "extra",
      "identity": "identity",
      "type": "type",
      "user_id": "user_id"
    },
    "content_type": "application/json",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/v2/account/unlink"
  },
  "parameters": [
    {
      "example": "1",
      "help_en": "Additional information about the identity. For `mobile` accounts, this parameter specifies the country code. The default value is `86` (mainland China)",
      "help_zh": "账号唯一标识的附加信息，比如当账号为手机号时，此字段填手机的区域编码，比如中国大陆为86，不填写默认为86",
      "location": "body",
      "name": "extra",
      "options": [
        "--extra"
      ],
      "param_style": "json",
      "raw_name": "extra",
      "required": false,
      "type": "string"
    },
    {
      "example": "139****\n",
      "help_en": "The account's unique identifier, such as a mobile number",
      "help_zh": "账号的唯一标识，比如手机号",
      "location": "body",
      "name": "identity",
      "options": [
        "--identity"
      ],
      "param_style": "json",
      "raw_name": "identity",
      "required": true,
      "type": "string"
    },
    {
      "example": "mobile",
      "help_en": "The account type. Valid values:\n\n`mobile`: Mobile number\n\n`email`: Email address\n\n`ding`: DingTalk account\n\n`ram`: Alibaba Cloud RAM sub-account\n\n`wechat`: WeCom account\n\n`ldap`: LDAP account\n\n`custom`: Custom account",
      "help_zh": "账号类型\n\nmobile：手机号\n\nemail：邮箱\n\nding：钉钉\n\nram：阿里云RAM子账号\n\nwechat：企业微信\n\nldap：LDAP账号\n\ncustom：自定义账号",
      "location": "body",
      "name": "type",
      "options": [
        "--type"
      ],
      "param_style": "json",
      "raw_name": "type",
      "required": true,
      "type": "string"
    },
    {
      "example": "uid1",
      "help_en": "The user ID",
      "help_zh": "用户标识",
      "location": "body",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "param_style": "json",
      "raw_name": "user_id",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/account/unlink",
  "product_code": "pds",
  "protocol": "HTTPS",
  "responses": {
    "204": {}
  },
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The domain ID",
      "help_zh": "A short description of struct",
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
