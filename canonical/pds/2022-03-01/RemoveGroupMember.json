{
  "camel_example": "",
  "cmd_full_name": "pds remove-group-member",
  "cmd_name": "remove-group-member",
  "deprecated": false,
  "description_en": "Removes a member from a group",
  "description_zh": "从群组移除成员",
  "kebab_example": "aliyun pds remove-group-member --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "RemoveGroupMember",
  "operation": {
    "action": "RemoveGroupMember",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "group_id": "group_id",
      "member_id": "member_id",
      "member_type": "member_type"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "url": "/v2/group/remove_member"
  },
  "parameters": [
    {
      "example": "3e5***2c2",
      "help_en": "The ID of the group to remove the member from",
      "help_zh": "目标群组ID，表示从哪个群组下移除成员",
      "location": "body",
      "name": "group_id",
      "options": [
        "--group-id"
      ],
      "param_style": "json",
      "raw_name": "group_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "2e4***1b1",
      "help_en": "The member ID. If `member_type` is `user`, this is the `userID`",
      "help_zh": "成员ID， 当member_type为user时，此字段填对应的userID",
      "location": "body",
      "name": "member_id",
      "options": [
        "--member-id"
      ],
      "param_style": "json",
      "raw_name": "member_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "user",
      "help_en": "The member type. This operation only removes users. To remove a member that is a group, delete the group instead.\n\nuser\n\nNote: A group can be a member of only one other group. A user can be a member of multiple groups",
      "help_zh": "成员类型，当前只能移除用户，群组移除可以通过删除群组完成\n\nuser（用户）\n\n注意：群组只能作为一个群组的成员，不能同时成为多个群组的成员。 用户可以同时成为多个群组的成员",
      "location": "body",
      "name": "member_type",
      "options": [
        "--member-type"
      ],
      "param_style": "json",
      "raw_name": "member_type",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/group/remove_member",
  "product_code": "pds",
  "protocol": "HTTPS",
  "responses": {
    "204": {
      "headers": {},
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {},
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  },
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "Request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "Request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The domain ID",
      "help_zh": "A short description of struct",
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
