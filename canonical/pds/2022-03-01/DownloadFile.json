{
  "camel_example": "",
  "cmd_full_name": "pds download-file",
  "cmd_name": "download-file",
  "deprecated": false,
  "description_en": "Downloads a file",
  "description_zh": "下载文件",
  "kebab_example": "aliyun pds download-file --file-id 9520943DC264 --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DownloadFile",
  "operation": {
    "action": "DownloadFile",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "method": "GET",
    "protocol": "HTTPS",
    "response_format": "binary",
    "system_operation_type": "get",
    "url": "/v2/file/download"
  },
  "parameters": [
    {
      "example": "7JQX1FswpQ8",
      "help_en": "The ID of the share. If you access a file by using a share link, you must authenticate with the `x-share-token` header and specify this parameter. In this case, the `drive_id` parameter is not applicable. Otherwise, you must authenticate by using `AK/SK` or an `access_token` and specify the `drive_id` parameter. You must specify either `share_id` or `drive_id`",
      "help_zh": "分享id。 如果通过分享操作文件，请携带`x-share-token header`鉴权并传递此字段，此时`drive_id`无效。否则请使用`aksk`或者`access_token`方式鉴权，并传递`drive_id`字段。 `share_id`和`drive_id`两者必须有一个字段赋值",
      "location": "query",
      "name": "share_id",
      "options": [
        "--share-id"
      ],
      "raw_name": "share_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The ID of the drive",
      "help_zh": "空间id",
      "location": "query",
      "name": "drive_id",
      "options": [
        "--drive-id"
      ],
      "raw_name": "drive_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "9520943DC264",
      "help_en": "The ID of the file",
      "help_zh": "文件id",
      "location": "query",
      "name": "file_id",
      "options": [
        "--file-id"
      ],
      "raw_name": "file_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "image/resize,m_fill,h_128,w_128,limit_0",
      "help_en": "The image processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "图片处理样式，设置后会跳转至处理后的缩略图地址",
      "location": "query",
      "name": "image_thumbnail_process",
      "options": [
        "--image-thumbnail-process"
      ],
      "raw_name": "image_thumbnail_process",
      "required": false,
      "type": "string"
    },
    {
      "example": "video/snapshot,t_7000,f_jpg,w_800,h_600,m_fast",
      "help_en": "The video processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "视频处理样式，设置后会跳转至处理后的缩略图地址",
      "location": "query",
      "name": "video_thumbnail_process",
      "options": [
        "--video-thumbnail-process"
      ],
      "raw_name": "video_thumbnail_process",
      "required": false,
      "type": "string"
    },
    {
      "example": "image/resize,m_lfit,w_256,limit_0/format,jpg|image/format,webp",
      "help_en": "The document processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "文档处理样式，设置后会跳转至处理后的缩略图地址",
      "location": "query",
      "name": "office_thumbnail_process",
      "options": [
        "--office-thumbnail-process"
      ],
      "raw_name": "office_thumbnail_process",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/file/download",
  "product_code": "pds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "302": {
      "headers": {
        "location": {
          "schema": {
            "title_en": "下载地址",
            "title_zh": "下载地址",
            "type": "string"
          }
        }
      }
    }
  },
  "tags": [
    "api"
  ],
  "v1_parameters": [
    {
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The ID of the drive",
      "help_zh": "空间id",
      "name": "drive_id",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the file",
      "help_zh": "文件id",
      "name": "file_id",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The image processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "图片处理样式，设置后会跳转至处理后的缩略图地址",
      "name": "image_thumbnail_process",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The document processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "文档处理样式，设置后会跳转至处理后的缩略图地址",
      "name": "office_thumbnail_process",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the share. If you access a file by using a share link, you must authenticate with the `x-share-token` header and specify this parameter. In this case, the `drive_id` parameter is not applicable. Otherwise, you must authenticate by using `AK/SK` or an `access_token` and specify the `drive_id` parameter. You must specify either `share_id` or `drive_id`",
      "help_zh": "分享id。 如果通过分享操作文件，请携带`x-share-token header`鉴权并传递此字段，此时`drive_id`无效。否则请使用`aksk`或者`access_token`方式鉴权，并传递`drive_id`字段。 `share_id`和`drive_id`两者必须有一个字段赋值",
      "name": "share_id",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The video processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail",
      "help_zh": "视频处理样式，设置后会跳转至处理后的缩略图地址",
      "name": "video_thumbnail_process",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
