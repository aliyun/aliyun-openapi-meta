{
  "camel_example": "",
  "cmd_full_name": "pds delete-group",
  "cmd_name": "delete-group",
  "deprecated": false,
  "description_en": "Deletes a group. Before deleting a group, ensure it contains no other groups or users. Otherwise, the deletion fails",
  "description_zh": "删除群组接口， 在删除群组前，需保证群组下没有其他群组或用户，否则会导致删除失败",
  "kebab_example": "aliyun pds delete-group --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "DeleteGroup",
  "operation": {
    "action": "DeleteGroup",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "group_id": "group_id"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "url": "/v2/group/delete"
  },
  "parameters": [
    {
      "example": "g123",
      "help_en": "The ID of the group to delete",
      "help_zh": "待删除的群组ID",
      "location": "body",
      "name": "group_id",
      "options": [
        "--group-id"
      ],
      "param_style": "json",
      "raw_name": "group_id",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/group/delete",
  "product_code": "pds",
  "protocol": "HTTPS",
  "responses": {
    "204": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {},
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  },
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
