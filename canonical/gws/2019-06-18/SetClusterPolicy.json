{
  "camel_example": "",
  "cmd_full_name": "gws set-cluster-policy",
  "cmd_name": "set-cluster-policy",
  "deprecated": false,
  "description_en": "Sets the policy for a cluster",
  "description_zh": "设置集群策略",
  "kebab_example": "aliyun gws set-cluster-policy --cluster-id gws-xxx --usb-redirect on --watermark off --local-drive readwrite --clipboard read --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SetClusterPolicy",
  "operation": {
    "action": "SetClusterPolicy",
    "api_style": "RPC",
    "api_version": "2019-06-18",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "gws-xxx",
      "help_en": "The cluster ID",
      "help_zh": "集群 id",
      "location": "query",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "on"
      ],
      "example": "on",
      "help_en": "The USB redirection policy. Valid values:\noff: disabled.\non: enabled",
      "help_zh": "USB 重定向：\noff：关闭\non：打开",
      "location": "query",
      "name": "usb_redirect",
      "options": [
        "--usb-redirect"
      ],
      "raw_name": "UsbRedirect",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "on"
      ],
      "example": "off",
      "help_en": "The watermark policy. Valid values:\noff: disabled.\non: enabled. The watermark consists of two lines of semi-transparent text in a star pattern that covers the screen. The text displays cluster information and host information in the following format:\n    * gws-xxx@gws\n    * c0-i00-xx-t6",
      "help_zh": "水印：\noff：关闭\non：打开。水印为星状覆盖屏幕的两行半透明文字，格式为集群信息与主机信息，类似\n    * gws-xxx@gws\n    * c0-i00-xx-t6",
      "location": "query",
      "name": "watermark",
      "options": [
        "--watermark"
      ],
      "raw_name": "Watermark",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "read",
        "readwrite"
      ],
      "example": "readwrite",
      "help_en": "The local drive mapping policy. Valid values:\noff: disabled.\nread: read-only.\nreadwrite: read and write",
      "help_zh": "本地磁盘映射：\noff：关闭\nread：只可读\nreadwrite：可读写",
      "location": "query",
      "name": "local_drive",
      "options": [
        "--local-drive"
      ],
      "raw_name": "LocalDrive",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "read",
        "readwrite",
        "write"
      ],
      "example": "read",
      "help_en": "The clipboard policy. Valid values:\noff: disabled.\nread: read-only.\nwrite: write-only.\nreadwrite: read and write",
      "help_zh": "剪贴板：\noff：关闭\nread：只可读\nwrite：只可写\nreadwrite：可读写",
      "location": "query",
      "name": "clipboard",
      "options": [
        "--clipboard"
      ],
      "raw_name": "Clipboard",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "on"
      ],
      "example": "off",
      "help_en": "The UDP port policy. Valid values:\non (default): enabled. We recommend that you set this parameter to on for better bandwidth usage and network stability.\noff: disabled. To troubleshoot UDP-related network issues, set this parameter to off. After this parameter is set to off, the inbound security group rules for ports 1494 and 2598 are removed",
      "help_zh": "udp 端口：\non（默认）：打开。推荐配置为 on，带宽占用与网络稳定性更好。\noff：关闭。定位 udp 相关的网络问题时，有时需要在这里设置 off，设置后安全组入方向的 1494 / 2598 的规则会被移除",
      "location": "query",
      "name": "udp_port",
      "options": [
        "--udp-port"
      ],
      "raw_name": "UdpPort",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "off",
        "on"
      ],
      "example": "on",
      "help_en": "The audio policy. Valid values:\non (default): enabled. The client can hear audio from the Cloud Desktop.\noff: disabled. The client cannot hear audio from the Cloud Desktop",
      "help_zh": "音频：\non（默认）：打开，客户端可以听到云桌面内部的声音。\noff：关闭，客户端听不到云桌面内部的声音",
      "location": "query",
      "name": "audio",
      "options": [
        "--audio"
      ],
      "raw_name": "Audio",
      "required": false,
      "type": "string"
    },
    {
      "example": "*.abc.com,*.xyz.com",
      "help_en": "The domain name whitelist.\nIf the whitelist is empty, access to all domain names is allowed.\nIf the whitelist is not empty, only domain names in the list are accessible.\nThe `*` wildcard is supported.\nSeparate multiple domain name rules with commas (,).\nThe settings take effect immediately on all instances in the current cluster",
      "help_zh": "域名白名单\n白名单为空时，允许访问所有域名\n白名单非空时，仅允许访问列表中的域名\n支持 `*` 通配符\n多条域名规则之间用逗号分隔\n设置后对当前集群的所有实例即时生效",
      "location": "query",
      "name": "domain_list",
      "options": [
        "--domain-list"
      ],
      "raw_name": "DomainList",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to use asynchronous mode. Valid values:\nfalse (default): The API returns a response only after the command is executed.\ntrue: The command is executed in the background, and the API returns a response immediately. Record the TaskId from the response, and then call DescribeClusterPolicy to query the execution status of the command",
      "help_zh": "异步模式调用：\nfalse（默认）：命令执行完成后，api 才会返回\ntrue：命令后台执行，api 立即返回。调用方需要记录返回值中的 TaskId，然后来 DescribeClusterPolicy 查询命令的执行状态",
      "location": "query",
      "name": "async_mode",
      "options": [
        "--async-mode"
      ],
      "raw_name": "AsyncMode",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "gws",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 id",
            "example": "XXXX",
            "nullToEmpty": true,
            "type": "string"
          },
          "TaskId": {
            "backendName": "taskId",
            "description_en": "This value is valid only when `AsyncMode` is set to `true`.\n\nYou can use this value to call DescribeClusterPolicy to query the execution status of the command",
            "description_zh": "此值只在 `AsyncMode` 为 `true` 时有效\n\n调用方可使用此值，用 DescribeClusterPolicy 来查询命令的执行状态",
            "example": "t-xxx",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
