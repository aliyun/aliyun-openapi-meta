{
  "camel_example": "",
  "cmd_full_name": "gws create-cluster",
  "cmd_name": "create-cluster",
  "deprecated": false,
  "description_en": "Creates a cluster",
  "description_zh": "创建集群",
  "kebab_example": "aliyun gws create-cluster --vpc-id vpc-xxx --cluster-type gws.s1.standard --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateCluster",
  "operation": {
    "action": "CreateCluster",
    "api_style": "RPC",
    "api_version": "2019-06-18",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "vpc-xxx",
      "help_en": "The VPC ID",
      "help_zh": "VPC id",
      "location": "query",
      "name": "vpc_id",
      "options": [
        "--vpc-id"
      ],
      "raw_name": "VpcId",
      "required": true,
      "type": "string"
    },
    {
      "example": "gws.s1.standard",
      "help_en": "The cluster type. Valid values:\ngws.s1.standard",
      "help_zh": "集群类型：\ngws.s1.standard",
      "location": "query",
      "name": "cluster_type",
      "options": [
        "--cluster-type"
      ],
      "raw_name": "ClusterType",
      "required": true,
      "type": "string"
    },
    {
      "example": "vsw-xxx",
      "help_en": "The vSwitch ID used to create cluster servers. If this parameter is not specified, the system automatically finds the latest active zone with inventory based on the current cluster specifications (for example, China North 2 Zone H). If the zone:\nalready has a vSwitch, the existing vSwitch is used.\ndoes not have a vSwitch, the system attempts automatic creation",
      "help_zh": "VSWitch id，用于集群服务器的创建。当不指定时，后台会根据当前集群规格，找到有库存的最新可用区（例如华北 2 可用区 H ），如果此可用区\n已有交换机，则直接选用\n没有交换机，则会尝试自动创建",
      "location": "query",
      "name": "vswitch_id",
      "options": [
        "--vswitch-id"
      ],
      "raw_name": "VSwitchId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "gws",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "ClusterId": {
            "backendName": "clusterId",
            "description_en": "The cluster ID",
            "description_zh": "集群 id",
            "example": "gws-xxx",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 id",
            "example": "XXXX",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
