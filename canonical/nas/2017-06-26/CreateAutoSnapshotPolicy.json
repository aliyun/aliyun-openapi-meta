{
  "camel_example": "aliyun nas CreateAutoSnapshotPolicy --region cn-hangzhou --TimePoints '0,1,…,23' --FileSystemType extreme --RepeatWeekdays '1,2,3'",
  "cmd_full_name": "nas create-auto-snapshot-policy",
  "cmd_name": "create-auto-snapshot-policy",
  "deprecated": false,
  "description_en": "Creates an automatic snapshot policy",
  "description_zh": "创建一条自动快照策略",
  "kebab_example": "aliyun nas create-auto-snapshot-policy --repeat-weekdays 1,2,3 --time-points '0,1,…,23' --file-system-type extreme --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateAutoSnapshotPolicy",
  "operation": {
    "action": "CreateAutoSnapshotPolicy",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "1,2,3",
      "help_en": "The days of the week on which automatic snapshots are created.\n\nCycle: week.\n\nValid values: 1 to 7, which represent Monday to Sunday.\n\nTo create automatic snapshots on multiple days in a week, specify multiple values separated by commas (,). You can specify up to 7 values",
      "help_zh": "自动快照的重复日期。\n\n周期：星期\n\n取值：1~7，依次表示周一至周日。\n\n如果一星期内需要创建多次自动快照时，可设置多个时间点，多个时间点用半角逗号（,）隔开，最多设置7个时间点",
      "location": "query",
      "name": "repeat_weekdays",
      "options": [
        "--repeat-weekdays"
      ],
      "raw_name": "RepeatWeekdays",
      "required": true,
      "type": "string"
    },
    {
      "example": "0,1,…,23",
      "help_en": "The time points at which automatic snapshots are created.\n\nUnit: hours.\n\nValid values: 0 to 23, which represent the 24 time points from 00:00 to 23:00. For example, 1 indicates 01:00.\n\nTo create multiple automatic snapshots within a day, specify multiple time points separated by commas (,). You can specify up to 24 time points",
      "help_zh": "自动快照的创建时间点。\n\n单位：小时\n\n取值：0~23，代表00:00至23:00共24个时间点，例如：1表示01:00。\n\n当一天内需要创建多次自动快照时，可以设置多个时间点，多个时间点用半角逗号（,）隔开，最多支持设置24个时间点",
      "location": "query",
      "name": "time_points",
      "options": [
        "--time-points"
      ],
      "raw_name": "TimePoints",
      "required": true,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The retention period of automatic snapshots.\n\nUnit: days.\n\nValid values:\n\n-1 (default): Automatic snapshots are permanently retained. When the snapshot quota is reached, the earliest automatic snapshots are automatically deleted.\n1 to 65536: Automatic snapshots are retained for the specified number of days. Snapshots undergo automatic release after the retention period expires",
      "help_zh": "自动快照的保留时间。\n\n单位：天\n\n取值：\n\n-1（默认值）：永久保存，当快照数量达到额度上限后被自动删除。\n1~65536：指定保存天数，保留时间到期后快照会被自动释放",
      "location": "query",
      "maximum": "65536",
      "minimum": "-1",
      "name": "retention_days",
      "options": [
        "--retention-days"
      ],
      "raw_name": "RetentionDays",
      "required": false,
      "type": "int"
    },
    {
      "example": "FinanceJoshua",
      "help_en": "The name of the automatic snapshot policy.\n\nLimits:\n\nThe name must be 2 to 128 characters in length.\nThe name must start with a letter or a Chinese character.\nThe name can contain digits, colons (:), underscores (_), or hyphens (-). It cannot start with `http://` or `https://`.\nDefault value: empty",
      "help_zh": "自动快照策略的名称。\n\n限制：\n\n长度为2~128个英文或中文字符。\n必须以大小写字母或中文开头。\n可包含数字、半角冒号（:）、下划线（_）或短划线（-），不能以`http://`和`https://`开头。\n默认为空",
      "location": "query",
      "name": "auto_snapshot_policy_name",
      "options": [
        "--auto-snapshot-policy-name"
      ],
      "raw_name": "AutoSnapshotPolicyName",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "extreme"
      ],
      "example": "extreme",
      "help_en": "The type of the file system.\n\nValid values: extreme (Extreme NAS file system)",
      "help_zh": "文件系统类型。\n\n取值：extreme（极速型NAS）",
      "location": "query",
      "name": "file_system_type",
      "options": [
        "--file-system-type"
      ],
      "raw_name": "FileSystemType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "",
        "description_zh": "",
        "properties": {
          "AutoSnapshotPolicyId": {
            "description_en": "The ID of the automatic snapshot policy",
            "description_zh": "自动快照策略ID",
            "example": "sp-extreme-233e6****",
            "type": "string"
          },
          "RequestId": {
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
