{
  "camel_example": "aliyun nas CreateAccessRule --region cn-hangzhou --AccessGroupName 'vpc-test'",
  "cmd_full_name": "nas create-access-rule",
  "cmd_name": "create-access-rule",
  "deprecated": false,
  "description_en": "Creates a permission rule for a permission group",
  "description_zh": "创建权限组规则",
  "kebab_example": "aliyun nas create-access-rule --access-group-name vpc-test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateAccessRule",
  "operation": {
    "action": "CreateAccessRule",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "vpc-test",
      "help_en": "The name of the permission group",
      "help_zh": "权限组名称",
      "location": "query",
      "name": "access_group_name",
      "options": [
        "--access-group-name"
      ],
      "raw_name": "AccessGroupName",
      "required": true,
      "type": "string"
    },
    {
      "example": "192.0.2.0/16",
      "help_en": "The IP address or CIDR block of the authorized address.\n\nThe value must be a single IP address or a CIDR block.\nPermission groups of the classic network type support only IP addresses",
      "help_zh": "授权对象的IP地址或网段。\n\n格式必须为单一的IP地址或者CIDR网段。\n经典网络类型的权限组只支持IP地址",
      "location": "query",
      "name": "source_cidr_ip",
      "options": [
        "--source-cidr-ip"
      ],
      "raw_name": "SourceCidrIp",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "RDWR",
        "RDONLY"
      ],
      "example": "RDWR",
      "help_en": "The read and write permissions of the authorized address on the file system.\n\nValid values:\nRDWR (default): read and write.\nRDONLY: read-only",
      "help_zh": "授权对象对文件系统的读写权限。\n\n取值：\nRDWR（默认值）：读写。\nRDONLY：只读",
      "location": "query",
      "name": "rw_access_type",
      "options": [
        "--rw-access-type"
      ],
      "raw_name": "RWAccessType",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "no_squash",
        "root_squash",
        "all_squash"
      ],
      "example": "no_squash",
      "help_en": "The access permissions of the system user of the authorized address on the file system.\n\nValid values:\nno_squash (default): allows access to the file system as the root user.\nroot_squash: maps the root user to the nobody user when the root user accesses the file system.\nall_squash: maps all users to the nobody user regardless of the user identity.\n\nThe nobody user is a default user in Linux. The nobody user can access only public content on the server and has low privileges and high security. Authorization is required for the system user to access the file system",
      "help_zh": "授权对象的系统用户对文件系统的访问权限。\n\n取值：\nno_squash（默认值）：允许使用root用户访问文件系统。\nroot_squash：以root用户身份访问时，映射nobody用户。\nall_squash：无论以何种用户身份访问，均映射为nobody用户。\n\nnobody用户是Linux系统的默认用户，只能访问服务器上的公共内容，具有低权限、高安全性的特点",
      "location": "query",
      "name": "user_access_type",
      "options": [
        "--user-access-type"
      ],
      "raw_name": "UserAccessType",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The priority of the permission rule.\n\nIf an authorized address matches multiple rules, the rule with the highest priority takes effect.\n\nValid values: 1 to 100. The value 1 indicates the highest priority",
      "help_zh": "权限组规则优先级。\n\n当同一个授权对象匹配到多条规则时，高优先级规则生效。\n\n取值范围：1~100，1为最高优先级",
      "location": "query",
      "maximum": "100",
      "minimum": "1",
      "name": "priority",
      "options": [
        "--priority"
      ],
      "raw_name": "Priority",
      "required": false,
      "type": "int"
    },
    {
      "enum": [
        "standard",
        "extreme"
      ],
      "example": "standard",
      "help_en": "The type of the file system.\n\nValid values:\n\nstandard (default): General-purpose NAS.\nextreme: Extreme NAS",
      "help_zh": "文件系统类型。\n\n取值：\n\nstandard（默认值）：通用型NAS。\nextreme：极速型NAS",
      "location": "query",
      "name": "file_system_type",
      "options": [
        "--file-system-type"
      ],
      "raw_name": "FileSystemType",
      "required": false,
      "type": "string"
    },
    {
      "example": "2001:250:6000::***",
      "help_en": "The source IPv6 CIDR block.\n\nThe value supports CIDR format and IPv6 format address range.\n\nThe IPv6 feature is supported only by Extreme NAS file systems in regions in the Chinese mainland, and IPv6 must be enabled for the file system.\nOnly VPC networks are supported.\nIPv4 and IPv6 are mutually exclusive",
      "help_zh": "源端IPv6 CIDR地址段。\n\n支持CIDR格式和IPv6格式的IP地址范围。\n\n当前仅极速型NAS中国内地各地域支持IPv6功能，且该文件系统需要开启IPv6功能。\n只支持VPC网络。\nIPv4和IPv6互斥",
      "location": "query",
      "name": "ipv6_source_cidr_ip",
      "options": [
        "--ipv6-source-cidr-ip"
      ],
      "raw_name": "Ipv6SourceCidrIp",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "",
        "description_zh": "",
        "properties": {
          "AccessRuleId": {
            "description_en": "The ID of the permission rule",
            "description_zh": "权限组规则ID",
            "example": "1",
            "type": "string"
          },
          "RequestId": {
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "A323836B-5BC6-45A6-8048-60675C23****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
