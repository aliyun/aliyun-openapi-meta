{
  "camel_example": "aliyun nas ModifyDataFlow --region cn-hangzhou --FileSystemId 'cpfs-099394bd928c0000' --DataFlowId 'df-194433a5be310000'",
  "cmd_full_name": "nas modify-data-flow",
  "cmd_name": "modify-data-flow",
  "deprecated": false,
  "description_en": "Modifies the attributes of a data flow",
  "description_zh": "修改数据流动的属性",
  "kebab_example": "aliyun nas modify-data-flow --file-system-id cpfs-099394bd928c0000 --data-flow-id df-194433a5be310000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyDataFlow",
  "operation": {
    "action": "ModifyDataFlow",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cpfs-099394bd928c****",
      "help_en": "The file system ID.\n\nCPFS: The ID must start with `cpfs-`, such as cpfs-125487\\\\.\n\nCPFS for Lingjun: The ID must start with `bmcpfs-`, such as bmcpfs-0015\\\\",
      "help_zh": "文件系统ID。\n\nCPFS：必须以`cpfs-`开头，例如cpfs-125487\\\\。\n\nCPFS智算版：必须以`bmcpfs-`开头，例如bmcpfs-0015\\\\",
      "location": "query",
      "name": "file_system_id",
      "options": [
        "--file-system-id"
      ],
      "raw_name": "FileSystemId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "df-194433a5be31****",
      "help_en": "The data flow ID",
      "help_zh": "数据流动ID",
      "location": "query",
      "name": "data_flow_id",
      "options": [
        "--data-flow-id"
      ],
      "raw_name": "DataFlowId",
      "required": true,
      "type": "string"
    },
    {
      "example": "Bucket01数据流动",
      "help_en": "The description of the data flow. \n\nLimits:\n\nThe description must be 2 to 128 characters in length.\nThe description must start with a letter and cannot start with http:// or https://.\nThe description can contain digits, colons (:), underscores (_), or hyphens (-)",
      "help_zh": "数据流动的描述。 \n\n限制：\n\n长度为2~128个英文或中文字符。\n必须以大小写字母或中文开头，不能以http://和https://开头。\n可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "600",
        "1200",
        "1500",
        "2000"
      ],
      "example": "600",
      "help_en": "The maximum transmission bandwidth of the data flow. Unit: MB/s.\nValid values: \n\n600\n1200\n1500\n\nThe transmission bandwidth of the data flow must be less than the I/O bandwidth of the file system. This parameter is required when the file system type is CPFS",
      "help_zh": "数据流动的传输带宽上限，单位：MB/s 。\n取值： \n\n600\n1200\n1500\n\n数据流动的传输带宽必须小于文件系统的IO带宽。当文件系统类型为CPFS时，该参数必填",
      "location": "query",
      "name": "throughput",
      "options": [
        "--throughput"
      ],
      "raw_name": "Throughput",
      "required": false,
      "type": "int"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run for this request.\n\nA dry run checks parameter validity and resource availability without actually creating the instance or incurring fees.\n\nValid values:\n\ntrue: Sends a check request without creating the instance. The check items include whether required parameters are specified, the request format, business limits, and NAS inventory. If the check fails, the corresponding error is returned. If the check passes, HTTP status code 200 is returned, but FileSystemId is empty.\nfalse (default): Sends a normal request. After the check passes, the instance is directly created",
      "help_zh": "是否对此次创建请求执行预检。\n\n预检操作会帮助您检查参数有效性、校验库存等，并不会实际创建实例，也不会产生费用。\n\n取值：\n\ntrue：发送检查请求，不会创建实例。检查项包括是否填写了必需参数、请求格式、业务限制和NAS库存。如果检查不通过，则返回对应错误。如果检查通过，则返回200 HttpCode，但FileSystemId为空。\nfalse（默认）：发送正常请求，通过检查后直接创建实例",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "123e4567-e89b-12d3-a456-42665544****",
      "help_en": "Ensures the idempotence of the request. Generate a parameter value from your client to ensure that the value is unique across different requests.\n\nClientToken supports only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request",
      "help_zh": "保证请求幂等性，从您的客户端生成一个参数值，确保不同请求间该参数值唯一。\n\nClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html。\n\n若用户未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "max_length": "64",
      "min_length": "1",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "",
        "description_zh": "",
        "properties": {
          "RequestId": {
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "98696EF0-1607-4E9D-B01D-F20930B6****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
