{
  "camel_example": "aliyun slb CreateLoadBalancerHTTPListener --region cn-hangzhou --ListenerPort 80 --HealthCheck on --LoadBalancerId 'lb-bp1c9vixxjh92q83tw00000' --RegionId 'cn-hangzhou'",
  "cmd_full_name": "slb create-load-balancer-http-listener",
  "cmd_name": "create-load-balancer-http-listener",
  "deprecated": false,
  "description_en": "Creates an HTTP listener for a Classic Load Balancer (CLB) instance",
  "description_zh": "创建HTTP监听",
  "kebab_example": "aliyun slb create-load-balancer-http-listener --biz-region-id cn-hangzhou --load-balancer-id lb-bp1c9vixxjh92q83tw00000 --listener-port 80 --health-check on --api-version 2014-05-15 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateLoadBalancerHTTPListener",
  "operation": {
    "action": "CreateLoadBalancerHTTPListener",
    "api_style": "RPC",
    "api_version": "2014-05-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the CLB instance is deployed.\n\nYou can query the region ID from the https://help.aliyun.com/document_detail/40654.html list or by calling the https://help.aliyun.com/document_detail/27584.html operation",
      "help_zh": "传统型负载均衡实例的地域ID。\n\n您可以从https://help.aliyun.com/document_detail/40654.html列表或通过调用https://help.aliyun.com/document_detail/27584.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "lb-bp1c9vixxjh92q83tw*****",
      "help_en": "The ID of the CLB instance",
      "help_zh": "传统型负载均衡实例ID",
      "location": "query",
      "name": "load_balancer_id",
      "options": [
        "--load-balancer-id"
      ],
      "raw_name": "LoadBalancerId",
      "required": true,
      "type": "string"
    },
    {
      "example": "-1",
      "help_en": "The maximum bandwidth of the listener. Unit: Mbit/s. Valid values:\n\n-1: specifies that the bandwidth of the listener is unlimited",
      "help_zh": "监听的带宽峰值，单位：Mbps。取值：\n\n<props=\"intl\">-1：不限制带宽峰值。</props>\n\n<props=\"china\">\n\n-1：不限制带宽峰值。\n\n1~5120：监听的带宽峰值，所有监听的带宽峰值之和不能超过实例的带宽峰值。\n\n</props>",
      "location": "query",
      "maximum": "5120",
      "minimum": "-1",
      "name": "bandwidth",
      "options": [
        "--bandwidth"
      ],
      "raw_name": "Bandwidth",
      "required": false,
      "type": "int"
    },
    {
      "doc_required": true,
      "example": "80",
      "help_en": "The frontend port that is used by the CLB instance.\n\nValid values: 1 to 65535",
      "help_zh": "实例前端使用的端口。\n\n取值：1~65535",
      "location": "query",
      "maximum": "65535",
      "minimum": "1",
      "name": "listener_port",
      "options": [
        "--listener-port"
      ],
      "raw_name": "ListenerPort",
      "required": true,
      "type": "int"
    },
    {
      "example": "80",
      "help_en": "The backend port that is used by the CLB instance.\n\nValid values: 1 to 65535.\n\nIf the VServerGroupId parameter is not specified, this parameter is required",
      "help_zh": "实例后端使用的端口。\n\n取值：1~65535。\n\n如果不使用虚拟服务器组（不指定VServerGroupId参数），则该参数必选",
      "location": "query",
      "maximum": "65535",
      "minimum": "1",
      "name": "backend_server_port",
      "options": [
        "--backend-server-port"
      ],
      "raw_name": "BackendServerPort",
      "required": false,
      "type": "int"
    },
    {
      "example": "on",
      "help_en": "Specifies whether to use the `X-Forwarded-For` header to retrieve client IP addresses. Valid values:\n\non (default)\n\noff",
      "help_zh": "是否开启通过`X-Forwarded-For`头字段获取来访客户端IP。取值：\non（默认值）：是。\noff：否",
      "location": "query",
      "name": "xforwarded_for",
      "options": [
        "--xforwarded-for"
      ],
      "raw_name": "XForwardedFor",
      "required": false,
      "type": "string"
    },
    {
      "example": "wrr",
      "help_en": "The scheduling algorithm. Valid values:\n\nwrr (default): Backend servers with higher weights receive more requests than those with lower weights.\n\nrr: Requests are distributed to backend servers in sequence",
      "help_zh": "调度算法。取值：\n\nwrr（默认值）：权重值越高的后端服务器，被轮询到的概率也越高。\n\nrr：按照访问顺序依次将外部请求依序分发到后端服务器",
      "location": "query",
      "name": "scheduler",
      "options": [
        "--scheduler"
      ],
      "raw_name": "Scheduler",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "off",
      "help_en": "Specifies whether to enable session persistence. Valid values:\n\non\n\noff (default)",
      "help_zh": "是否开启会话保持。取值：\n\non：是。\n\noff（默认值）：否",
      "location": "query",
      "name": "sticky_session",
      "options": [
        "--sticky-session"
      ],
      "raw_name": "StickySession",
      "required": false,
      "type": "string"
    },
    {
      "example": "insert",
      "help_en": "The method that is used to handle cookies. Valid values:\n\ninsert: inserts a cookie.\n\n  The first time a client accesses CLB, CLB inserts a cookie into the response packet. Subsequent requests from the client that carry the cookie are distributed to the same backend server as the first request.\n\nserver: rewrites the original cookie.\n\n  CLB rewrites the custom cookies in requests from a client. Subsequent requests from the client that carry the new cookie are forwarded to the same backend server as the first request.\n\nThis parameter is required if the StickySession parameter is set to on",
      "help_zh": "Cookie处理方式。取值：\n\ninsert：植入Cookie。\n\n    客户端第一次访问时，负载均衡会在返回请求中植入Cookie，下次客户端携带此Cookie访问，负载均衡服务会将请求定向转发给之前记录到的后端服务器上。\n\nserver：重写Cookie。\n\n  负载均衡发现用户自定义了Cookie，将会对原来的Cookie进行重写，下次客户端携带新的Cookie访问，负载均衡服务会将请求定向转发给之前记录到的后端服务器。\n\n当StickySession值为on时，必须指定该参数",
      "location": "query",
      "name": "sticky_session_type",
      "options": [
        "--sticky-session-type"
      ],
      "raw_name": "StickySessionType",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "TestKey",
            "help_en": "The tag key of the bastion host. Valid values of N: 1 to 20. The tag key cannot be an empty string.\n\nThe tag key can be at most 64 characters in length, and cannot contain `http://` or `https://`. It must not start with `aliyun` or `acs:`",
            "help_zh": "监听的标签键。N的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "TestValue",
            "help_en": "The tag value. Valid values of N: 1 to 20. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `acs:` or `aliyun`. The tag value cannot contain `http://` or `https://`",
            "help_zh": "监听的标签值。N的取值范围：1~20。一旦传入该值，可以为空字符串。最多支持128个字符，不能以`acs:`开头，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tags",
      "help_zh": "标签列表",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "example": "500",
      "help_en": "The timeout period of a cookie. Unit: seconds.\n\nValid values: 1 to 86400.\n\nIf StickySession is set to on and StickySessionType is set to insert, this parameter is required",
      "help_zh": "Cookie超时时间。单位：秒。\n\n取值：1~86400。\n\n当StickySession为on且StickySessionType为insert时，该参数必选",
      "location": "query",
      "maximum": "86400",
      "minimum": "1",
      "name": "cookie_timeout",
      "options": [
        "--cookie-timeout"
      ],
      "raw_name": "CookieTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "testCookie",
      "help_en": "The cookie configured for the server.\n\nThe cookie must be 1 to 200 characters in length, and can contain only ASCII letters and digits. It cannot contain commas (,), semicolons (;), space characters, or start with a dollar sign ($).\n\nThis parameter is required when the StickySession parameter is set to on and the StickySessionType parameter is set to server",
      "help_zh": "服务器上配置的Cookie。\n\n长度为1~200个字符，只能包含ASCII英文字母和数字字符，不能包含半角逗号（,）、半角分号（;）或空格，也不能以美元符号（$）开头。\n\n当StickySession为on且StickySessionType为server时，该参数必选",
      "location": "query",
      "name": "cookie",
      "options": [
        "--cookie"
      ],
      "raw_name": "Cookie",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "on",
      "help_en": "Specifies whether to enable the health check feature. Valid values:\n\non: yes\n\noff: no",
      "help_zh": "是否开启健康检查。取值：\n\non：开启健康检查。\n\noff：不开启健康检查",
      "location": "query",
      "name": "health_check",
      "options": [
        "--health-check"
      ],
      "raw_name": "HealthCheck",
      "required": true,
      "type": "string"
    },
    {
      "example": "get",
      "help_en": "The health check method used in HTTP health checks. Valid values: head and get.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "监听HTTP类型健康检查的健康检查方法。取值：head或get。\n\nHealthCheck值为on时才会有效",
      "location": "query",
      "name": "health_check_method",
      "options": [
        "--health-check-method"
      ],
      "raw_name": "HealthCheckMethod",
      "required": false,
      "type": "string"
    },
    {
      "example": "172.16.*.*/12",
      "help_en": "The domain name that is used for health checks. Valid values:\n\n$_ip: the private IP address of a backend server. If an IP address is specified, or this parameter is not specified, the CLB instance uses the private IP address of each backend server as the domain name for health checks.\n\ndomain: The domain name must be 1 to 80 characters in length, and can contain letters, digits, periods (.), and hyphens (-).\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "用于健康检查的域名，取值：\n\n$_ip： 后端服务器的私网IP。当指定了IP或该参数未指定时，负载均衡会使用各后端服务器的私网IP当做健康检查使用的域名。\n\ndomain：域名长度为1~80字符，只能包含字母、数字、半角句号（.）和短划线（-）。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "name": "health_check_domain",
      "options": [
        "--health-check-domain"
      ],
      "raw_name": "HealthCheckDomain",
      "required": false,
      "type": "string"
    },
    {
      "example": "/test/index.html",
      "help_en": "The URI that is used for health checks.\n\nThe URI must be 1 to 80 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), percent signs (%), question marks (?), number signs (#), and ampersands (&). The URI must start with a forward slash (/) but cannot be a single forward slash (/).\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "用于健康检查的URI。\n\n长度限制为1~80个字符，只能使用字母、数字和短划线（-）、正斜线（/）、半角句号（.）、百分号（%）、半角问号（?）、井号（#）和and（&）这些字符。URI不能只为正斜线（/），但必须以正斜线（/）开头。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "name": "health_check_uri",
      "options": [
        "--health-check-uri"
      ],
      "raw_name": "HealthCheckURI",
      "required": false,
      "type": "string"
    },
    {
      "example": "4",
      "help_en": "The number of times that an unhealthy backend server must consecutively pass health checks before it is declared healthy. In this case, the health status is changed from fail to success.\n\nValid values: 2 to 10.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由失败判定为成功。\n\n取值：2~10。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "maximum": "10",
      "minimum": "1",
      "name": "healthy_threshold",
      "options": [
        "--healthy-threshold"
      ],
      "raw_name": "HealthyThreshold",
      "required": false,
      "type": "int"
    },
    {
      "example": "4",
      "help_en": "The number of times that a healthy backend server must consecutively fail health checks before it is declared unhealthy. In this case, the health status is changed from success to fail.\n\nValid values: 2 to 10.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由成功判定为失败。\n\n取值：2~10。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "maximum": "10",
      "minimum": "1",
      "name": "unhealthy_threshold",
      "options": [
        "--unhealthy-threshold"
      ],
      "raw_name": "UnhealthyThreshold",
      "required": false,
      "type": "int"
    },
    {
      "example": "3",
      "help_en": "The timeout period of a health check response. If a backend ECS instance does not respond within the specified timeout period, the ECS instance fails the health check. Unit: seconds\n\nValid values: 1 to 300.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "接收来自运行状况检查的响应需要等待的时间。如果后端ECS在指定的时间内没有正确响应，则判定为健康检查失败。单位：秒。\n\n取值：1~300。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "maximum": "300",
      "minimum": "1",
      "name": "health_check_timeout",
      "options": [
        "--health-check-timeout"
      ],
      "raw_name": "HealthCheckTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "80",
      "help_en": "The backend port that is used for health checks.\n\nValid values: 1 to 65535.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "健康检查的后端服务器的端口。\n\n取值： 1~65535。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "maximum": "65535",
      "minimum": "-520",
      "name": "health_check_connect_port",
      "options": [
        "--health-check-connect-port"
      ],
      "raw_name": "HealthCheckConnectPort",
      "required": false,
      "type": "int"
    },
    {
      "example": "5",
      "help_en": "The interval between two consecutive health checks. Unit: seconds.\n\nValid values: 1 to 50.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "健康检查的时间间隔。单位：秒。\n\n取值： 1~50。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "maximum": "50",
      "minimum": "1",
      "name": "health_check_interval",
      "options": [
        "--health-check-interval"
      ],
      "raw_name": "HealthCheckInterval",
      "required": false,
      "type": "int"
    },
    {
      "example": "http_2xx",
      "help_en": "The HTTP status code for a successful health check. Separate multiple HTTP status codes with commas (,).\n\nValid values: http_2xx (default), http_3xx, http_4xx, and http_5xx.\n\nThis parameter takes effect only if the HealthCheck parameter is set to on",
      "help_zh": "健康检查正常的HTTP状态码，多个状态码用逗号分隔。\n\n取值：http_2xx（默认值）、http_3xx、http_4xx或http_5xx。\n\n在HealthCheck值为on时才会有效",
      "location": "query",
      "name": "health_check_http_code",
      "options": [
        "--health-check-http-code"
      ],
      "raw_name": "HealthCheckHttpCode",
      "required": false,
      "type": "string"
    },
    {
      "example": "rsp-cige6j*****",
      "help_en": "The ID of the vServer group",
      "help_zh": "虚拟服务器组ID",
      "location": "query",
      "name": "vserver_group_id",
      "options": [
        "--vserver-group-id"
      ],
      "raw_name": "VServerGroupId",
      "required": false,
      "type": "string"
    },
    {
      "example": "on",
      "help_en": "Specifies whether to use the `SLB-IP` header to retrieve the virtual IP address of the client. Valid values:\n\non\n\noff (default)",
      "help_zh": "是否通过`SLB-IP`头字段获取客户端请求的VIP（Virtual IP address）。取值：\non：是。\noff（默认值）：否",
      "location": "query",
      "name": "xforwarded_for_slb_ip",
      "options": [
        "--xforwarded-for-slb-ip"
      ],
      "raw_name": "XForwardedFor_SLBIP",
      "required": false,
      "type": "string"
    },
    {
      "example": "on",
      "help_en": "Specifies whether to use the `SLB-ID` header to retrieve the ID of the CLB instance. Valid values:\n\non\n\noff (default)",
      "help_zh": "是否通过`SLB-ID`头字段获取负载均衡实例ID。取值：\non：是。\noff（默认值）：否",
      "location": "query",
      "name": "xforwarded_for_slb_id",
      "options": [
        "--xforwarded-for-slb-id"
      ],
      "raw_name": "XForwardedFor_SLBID",
      "required": false,
      "type": "string"
    },
    {
      "example": "on",
      "help_en": "Specifies whether to use the `X-Forwarded-Proto` header to retrieve the listener protocol. Valid values:\n\non\n\noff (default)",
      "help_zh": "是否通过`X-Forwarded-Proto`头字段获取负载均衡实例的监听协议。取值：\non：是。\noff（默认值）：否",
      "location": "query",
      "name": "xforwarded_for_proto",
      "options": [
        "--xforwarded-for-proto"
      ],
      "raw_name": "XForwardedFor_proto",
      "required": false,
      "type": "string"
    },
    {
      "example": "on",
      "help_en": "Specifies whether to enable `GZIP` compression to compress specific types of files. Valid values:\n\non (default)\n\noff",
      "help_zh": "是否开启`Gzip`压缩，对特定文件类型进行压缩。取值：\non（默认值）：是。\noff：否",
      "location": "query",
      "name": "gzip",
      "options": [
        "--gzip"
      ],
      "raw_name": "Gzip",
      "required": false,
      "type": "string"
    },
    {
      "example": "acl-uf60jw******",
      "help_en": "The ID of the network access control list (ACL) that is associated with the listener.\n\nIf AclStatus is set to on, this parameter is required",
      "help_zh": "监听绑定的访问策略组ID。\n\n当AclStatus参数的值为on时，该参数必选",
      "location": "query",
      "name": "acl_id",
      "options": [
        "--acl-id"
      ],
      "raw_name": "AclId",
      "required": false,
      "type": "string"
    },
    {
      "example": "white",
      "help_en": "The type of access control. Valid values:\n\nwhite: Only requests from IP addresses and CIDR blocks on the whitelist are forwarded by the listener. Your service may be adversely affected if the whitelist is not properly configured. If a whitelist is configured, the listener forwards only requests from IP addresses that are added to the whitelist.\n\n  If you configure a whitelist but do not add an IP address to the whitelist, the listener forwards all requests.\n\nblack: Requests from the IP addresses and CIDR blocks on the blacklist are blocked.\n\n  If you configure a blacklist but do not add an IP address to the blacklist, the listener forwards all requests.\n\nWhen AclStatus is set to on, this parameter takes effect",
      "help_zh": "访问控制类型。取值：\n\nwhite： 仅转发来自所选访问控制策略组中设置的IP地址或地址段的请求，白名单适用于只允许特定IP访问的场景。设置白名单存在一定业务风险。一旦设置白名单，就只有白名单中的IP可以访问负载均衡监听。\n \n  如果开启了白名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\nblack： 来自所选访问控制策略组中设置的IP地址或地址段的所有请求都不会转发，黑名单适用于只限制某些特定IP访问的场景。\n\n  如果开启了黑名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\n当AclStatus参数的值为on时，该参数有效",
      "location": "query",
      "name": "acl_type",
      "options": [
        "--acl-type"
      ],
      "raw_name": "AclType",
      "required": false,
      "type": "string"
    },
    {
      "example": "off",
      "help_en": "Specifies whether to enable access control. Valid values:\n\non: yes\n\noff (default): no",
      "help_zh": "是否开启访问控制功能。取值：\non：是。\noff（默认值）：否",
      "location": "query",
      "name": "acl_status",
      "options": [
        "--acl-status"
      ],
      "raw_name": "AclStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "HTTP_443",
      "help_en": "The name of the listener.\n\nThe name must be 1 to 256 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), and underscores (_)",
      "help_zh": "自定义监听名称。\n\n长度限制为1~256个字符，支持中文、字母、数字、短划线（-）、正斜线（/）、半角句号（.）和下划线（_）",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "off",
      "help_en": "Specifies whether to enable HTTP-to-HTTPS redirection. Valid values:\n\non: yes\n\noff (default): no",
      "help_zh": "是否开启HTTP至HTTPS的转发。取值：\non：是。\noff（默认值）：否",
      "location": "query",
      "name": "listener_forward",
      "options": [
        "--listener-forward"
      ],
      "raw_name": "ListenerForward",
      "required": false,
      "type": "string"
    },
    {
      "example": "443",
      "help_en": "The listening port that is used to redirect HTTP requests to HTTPS",
      "help_zh": "HTTP至HTTPS的监听转发端口",
      "location": "query",
      "maximum": "65535",
      "minimum": "1",
      "name": "forward_port",
      "options": [
        "--forward-port"
      ],
      "raw_name": "ForwardPort",
      "required": false,
      "type": "int"
    },
    {
      "example": "3",
      "help_en": "The timeout period of an idle connection. Unit: seconds.\n\nDefault value: 15. Valid values: 1 to 60.\n\nIf no request is received within the specified timeout period, SLB closes the connection. When a request is received, SLB establishes a new connection",
      "help_zh": "指定连接空闲超时时间。单位：秒。\n\n默认值为15，取值范围：1~60。\n\n在超时时间内一直没有访问请求，负载均衡会暂时中断当前连接，直到一下次请求来临时重新建立新的连接",
      "location": "query",
      "maximum": "9999",
      "minimum": "1",
      "name": "idle_timeout",
      "options": [
        "--idle-timeout"
      ],
      "raw_name": "IdleTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "6",
      "help_en": "The timeout period of a request. Unit: seconds.\n\nDefault value: 60. Valid values: 1 to 180.\n\nIf no response is received from the backend server within the specified timeout period, CLB sends an `HTTP 504` error code to the client",
      "help_zh": "指定请求超时时间。单位：秒。\n\n默认值为60，取值范围为1~180。\n\n在超时时间内后端服务器一直没有响应，负载均衡将放弃等待，给客户端返回` HTTP 504 `错误码",
      "location": "query",
      "maximum": "9999",
      "minimum": "1",
      "name": "request_timeout",
      "options": [
        "--request-timeout"
      ],
      "raw_name": "RequestTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "off",
      "help_en": "Specifies whether to use the `XForwardedFor_SLBPORT` header to retrieve the listener port of the CLB instance. Valid values:\n\non\n\noff",
      "help_zh": "是否通过`XForwardedFor_SLBPORT`头字段获取负载均衡实例的监听端口。取值：\non：是。\noff：否",
      "location": "query",
      "name": "xforwarded_for_slbport",
      "options": [
        "--xforwarded-for-slbport"
      ],
      "raw_name": "XForwardedFor_SLBPORT",
      "required": false,
      "type": "string"
    },
    {
      "example": "off",
      "help_en": "Specifies whether to use the `XForwardedFor_ClientSrcPort` header to retrieve the client port. Valid values:\n\non\n\noff",
      "help_zh": "是否通过`XForwardedFor_ClientSrcPort`头字段获取访问负载均衡实例客户端的端口。取值：\non：是。\noff：否",
      "location": "query",
      "name": "xforwarded_for_client_src_port",
      "options": [
        "--xforwarded-for-client-src-port"
      ],
      "raw_name": "XForwardedFor_ClientSrcPort",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Slb",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "CEF72CEB-54B6-4AE8-B225-F876FF7BA984",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
