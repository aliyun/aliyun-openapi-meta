{
  "camel_example": "aliyun cs DELETE /cluster/c5b5e80b0b64a4bf6939d2d8fbbc50000/user/2xxxxxxxxxxxxxxxxxx/permissions --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "cs clean-cluster-user-permissions",
  "cmd_name": "clean-cluster-user-permissions",
  "deprecated": false,
  "description_en": "Cleans up KubeConfig credentials and RBAC permissions that have been issued to a specified user in a specified cluster. You can call this operation to revoke authorization for KubeConfig credentials that pose security risks",
  "description_zh": "如果您想针对部分有安全风险的KubeConfig进行清理和解除授权，您可以调用CleanClusterUserPermissions清理指定用户在指定集群已下发的KubeConfig凭据和RBAC权限",
  "kebab_example": "aliyun cs clean-cluster-user-permissions --cluster-id c5b5e80b0b64a4bf6939d2d8fbbc50000 --uid 2xxxxxxxxxxxxxxxxxx --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "CleanClusterUserPermissions",
  "operation": {
    "action": "CleanClusterUserPermissions",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "method": "DELETE",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/cluster/{ClusterId}/user/{Uid}/permissions"
  },
  "parameters": [
    {
      "example": "c5b5e80b0b64a4bf6939d2d8fbbc5****\n",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "path",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2xxxxxxxxxxxxxxxxxx\n",
      "help_en": "The ID of the specified Resource Access Management (RAM) user or RAM role within the Alibaba Cloud account",
      "help_zh": "阿里云账号内指定RAM用户或RAM角色ID",
      "location": "path",
      "name": "uid",
      "options": [
        "--uid"
      ],
      "raw_name": "Uid",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to force delete the specified KubeConfig. Valid values:\nfalse (default): Before deleting the KubeConfig, the system checks whether cluster access records exist within the last seven days. If access records exist or cannot be retrieved, the deletion is not allowed.\ntrue: Force deletes the KubeConfig without checking cluster access records",
      "help_zh": "是否强制删除指定的KubeConfig，取值：\nfalse（默认值）：删除KubeConfig前校验是否在七天内存在相关的集群访问记录，如果存在或无法查找集群访问记录，则不允许删除。\ntrue：强制删除KubeConfig，不检查集群访问记录",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/cluster/[ClusterId]/user/[Uid]/permissions",
  "product_code": "CS",
  "protocol": "HTTPS",
  "responses": {
    "200": {}
  }
}
