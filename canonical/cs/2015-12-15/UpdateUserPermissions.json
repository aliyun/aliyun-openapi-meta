{
  "camel_example": "aliyun cs POST /permissions/users/23670000/update --region cn-hangzhou --mode apply --header \"Content-Type=application/json\" --body '[{\"role_name\":\"ops\",\"cluster\":\"c796c60000\",\"role_type\":\"cluster\",\"namespace\":\"test\",\"is_custom\":false,\"is_ram_role\":false}]'",
  "cmd_full_name": "cs update-user-permissions",
  "cmd_name": "update-user-permissions",
  "deprecated": false,
  "description_en": "In an ACK cluster, non-cluster creators, Resource Access Management (RAM) users, and RAM roles have no RBAC permissions by default. You can invoke the UpdateUserPermissions operation to update the RBAC access permissions of a RAM user or role, including accessible resources, permission scope, and preset role types, to better manage cluster management and secure access control",
  "description_zh": "在ACK集群中，非集群创建者、RAM用户以及RAM角色在集群中默认没有任何RBAC权限。您可以调用UpdateUserPermissions接口，更新RAM用户或角色拥有的RBAC访问权限，包括可访问的资源、权限的作用范围、预置的角色类型等，从而更好地管理集群的安全访问控制",
  "kebab_example": "aliyun cs update-user-permissions --uid 23670000 --biz-body role_name=ops cluster=c796c60000 role_type=cluster namespace=test is_custom=false is_ram_role=false --biz-mode apply --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "UpdateUserPermissions",
  "operation": {
    "action": "UpdateUserPermissions",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "content_type": "application/json",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/permissions/users/{uid}/update"
  },
  "parameters": [
    {
      "example": "2367****",
      "help_en": "The ID of the Resource Access Management (RAM) user or RAM role",
      "help_zh": "RAM用户或RAM角色的ID",
      "location": "path",
      "name": "uid",
      "options": [
        "--uid"
      ],
      "raw_name": "uid",
      "required": true,
      "type": "string"
    },
    {
      "direct_body": true,
      "element": {
        "fields": [
          {
            "example": "c796c60***",
            "help_en": "The ID of the target cluster for authorization.\n\nIf the `role_type` parameter is set to `all-clusters`, you do not need to specify this parameter",
            "help_zh": "授权目标集群ID。\n\n当`role_type`参数选择为`all-clusters`时，该参数无需填写",
            "name": "cluster",
            "options": [
              "--cluster"
            ],
            "raw_name": "cluster",
            "required": false,
            "type": "string"
          },
          {
            "example": "false",
            "help_en": "Specifies whether the authorization is a custom authorization (the `role_name` uses a custom ClusterRole name).\n\ntrue: The authorized role is a custom cluster role.\n\nfalse: The authorized role is a cluster preset role",
            "help_zh": "该授权是否是自定义授权（`role_name`使用自定义的ClusterRole名称）。\n\ntrue：授权角色为自定义的集群角色。\n\nfalse：授权角色集群预置角色",
            "name": "is_custom",
            "options": [
              "--is-custom"
            ],
            "raw_name": "is_custom",
            "required": false,
            "type": "bool"
          },
          {
            "example": "false",
            "help_en": "Specifies whether the authorization is for a RAM role.\n\ntrue: The authorization is for a RAM role.\n\nfalse: The authorization is for a Resource Access Management (RAM) user",
            "help_zh": "是否是RAM角色授权。\n\ntrue：为RAM角色授权。\n\nfalse：为RAM用户授权",
            "name": "is_ram_role",
            "options": [
              "--is-ram-role"
            ],
            "raw_name": "is_ram_role",
            "required": false,
            "type": "bool"
          },
          {
            "example": "test",
            "help_en": "The namespace name. This parameter is empty by default for cluster-level authorization",
            "help_zh": "命名空间名称，集群维度授权时默认为空",
            "name": "namespace",
            "options": [
              "--namespace"
            ],
            "raw_name": "namespace",
            "required": false,
            "type": "string"
          },
          {
            "example": "ops",
            "help_en": "The name of the preset role. Valid values:\n\n`admin`: administrator.\n`admin-view`: read-only administrator.\n`ops`: O&M engineer.\n`dev`: developer.\n`restricted`: restricted user.\nA custom ClusterRole name.\n\n\n`admin`, `admin-view`, `ops`: These roles cannot be granted at the namespace level.\n`admin-view`: This role cannot be granted at the all-clusters level",
            "help_zh": "预置的角色名称，取值：\n\n`admin`: 管理员。\n`admin-view`: 只读管理员。\n`ops`：运维人员。\n`dev`：开发人员。\n`restricted`: 受限用户。\n自定义的ClusterRole名称。\n\n\n`admin`、`admin-view`、`ops`：不支持命名空间维度的授予。\n`admin-view`： 暂不支持所有集群维度的授予",
            "name": "role_name",
            "options": [
              "--role-name"
            ],
            "raw_name": "role_name",
            "required": false,
            "type": "string"
          },
          {
            "example": "cluster",
            "help_en": "The authorization type. Valid values:\n\n`cluster`: cluster level.\n`namespace`: namespace level.\n`all-clusters`: all-clusters level",
            "help_zh": "授权类型，取值：\n\n`cluster`：集群维度。\n`namespace`: 命名空间维度。\n`all-clusters`: 所有集群维度",
            "name": "role_type",
            "options": [
              "--role-type"
            ],
            "raw_name": "role_type",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The request body parameters",
      "help_zh": "请求体参数",
      "location": "body",
      "name": "body",
      "options": [
        "--biz-body"
      ],
      "param_style": "json",
      "raw_name": "body",
      "required": false,
      "type": "array"
    },
    {
      "enum": [
        "delete",
        "patch",
        "apply"
      ],
      "example": "apply",
      "help_en": "The authorization mode. Valid values:\n\n`apply`: full update. A full update overwrites all existing cluster permissions of the target RAM user or RAM role. The request must include all permission configurations that you want to grant to the target RAM user or RAM role.\n`delete`: delete permissions. Only the cluster authorization information included in the request is deleted. Other cluster Resource Access Management (RAM) user or RAM role are not affected.\n`patch`: add permissions. Only the cluster authorization information included in the request is added. Other cluster Resource Access Management (RAM) user or RAM role are not affected.\n\nDefault value: `apply`",
      "help_zh": "授权方式，取值：\n\n`apply`：全量更新权限，全量更新RAM用户或RAM角色集群授权信息操作会覆盖目标RAM用户或RAM角色已有的集群权限，请求时需要包含想要授权给目标RAM用户或RAM角色的所有权限配置。\n`delete`: 删除权限，只会删除请求时包含的集群授权信息，不影响RAM用户或RAM角色的其他集群权限。\n`patch`: 新增权限，只会新增请求时包含的集群授权信息，不影响RAM用户或RAM角色的其他集群权限。\n\n默认值：`apply`",
      "location": "query",
      "name": "mode",
      "options": [
        "--biz-mode"
      ],
      "raw_name": "mode",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/permissions/users/[uid]/update",
  "product_code": "CS",
  "protocol": "HTTPS",
  "responses": {
    "200": {}
  },
  "v1_parameters": [
    {
      "help_en": "The request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "sub_parameters": [
        {
          "name": "cluster",
          "position": "body",
          "required": false,
          "type": "string"
        },
        {
          "name": "is_custom",
          "position": "body",
          "required": false,
          "type": "bool"
        },
        {
          "name": "is_ram_role",
          "position": "body",
          "required": false,
          "type": "bool"
        },
        {
          "name": "namespace",
          "position": "body",
          "required": false,
          "type": "string"
        },
        {
          "name": "role_name",
          "position": "body",
          "required": false,
          "type": "string"
        },
        {
          "name": "role_type",
          "position": "body",
          "required": false,
          "type": "string"
        }
      ],
      "type": "string"
    },
    {
      "help_en": "The authorization mode. Valid values:\n\n`apply`: full update. A full update overwrites all existing cluster permissions of the target RAM user or RAM role. The request must include all permission configurations that you want to grant to the target RAM user or RAM role.\n`delete`: delete permissions. Only the cluster authorization information included in the request is deleted. Other cluster Resource Access Management (RAM) user or RAM role are not affected.\n`patch`: add permissions. Only the cluster authorization information included in the request is added. Other cluster Resource Access Management (RAM) user or RAM role are not affected.\n\nDefault value: `apply`",
      "help_zh": "授权方式，取值：\n\n`apply`：全量更新权限，全量更新RAM用户或RAM角色集群授权信息操作会覆盖目标RAM用户或RAM角色已有的集群权限，请求时需要包含想要授权给目标RAM用户或RAM角色的所有权限配置。\n`delete`: 删除权限，只会删除请求时包含的集群授权信息，不影响RAM用户或RAM角色的其他集群权限。\n`patch`: 新增权限，只会新增请求时包含的集群授权信息，不影响RAM用户或RAM角色的其他集群权限。\n\n默认值：`apply`",
      "name": "mode",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the Resource Access Management (RAM) user or RAM role",
      "help_zh": "RAM用户或RAM角色的ID",
      "name": "uid",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
