{
  "camel_example": "aliyun cs DELETE /users/265624438516500000/permissions --region cn-hangzhou --header \"Content-Type=application/json\"",
  "cmd_full_name": "cs clean-user-permissions",
  "cmd_name": "clean-user-permissions",
  "deprecated": false,
  "description_en": "Cleans up KubeConfig credentials and revokes RBAC permissions for a specified user. If you want to clean up KubeConfig credentials and revoke authorization for risky users such as those who have left the organization or whose accounts have been frozen, call CleanUserPermissions to clean up the issued KubeConfig credentials and RBAC permissions for the specified user",
  "description_zh": "如果您想针对部分风险用户（已离职、账号冻结等）的KubeConfig进行清理并解除授权，您可以调用CleanUserPermissions清理指定用户已下发的KubeConfig凭据和RBAC权限",
  "kebab_example": "aliyun cs clean-user-permissions --uid 265624438516500000 --region cn-hangzhou",
  "method": "DELETE",
  "multi_version": false,
  "name": "CleanUserPermissions",
  "operation": {
    "action": "CleanUserPermissions",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "method": "DELETE",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/users/{Uid}/permissions"
  },
  "parameters": [
    {
      "example": "26562443851650****",
      "help_en": "The ID of the specified Resource Access Management (RAM) user or RAM role within the Alibaba Cloud account",
      "help_zh": "阿里云账号内指定RAM用户或RAM角色ID",
      "location": "path",
      "name": "uid",
      "options": [
        "--uid"
      ],
      "raw_name": "Uid",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to force delete the specified KubeConfig. Valid values:\nfalse (default): Before deleting the KubeConfig, the system checks whether cluster access records exist within the last seven days. If access records exist or cannot be retrieved, the deletion is not allowed.\ntrue: Force deletes the KubeConfig without checking cluster access records",
      "help_zh": "是否强制删除指定的KubeConfig，取值：\nfalse（默认值）：删除KubeConfig前校验是否在七天内存在相关的集群访问记录，如果存在或无法查找集群访问记录，则不允许删除。\ntrue：强制删除KubeConfig，不检查集群访问记录",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of cluster IDs. If this list is specified, only the KubeConfig credentials and RBAC permissions of the current user in the specified clusters are cleaned up",
      "help_zh": "集群ID列表。如指定了该列表，则仅清除当前用户在列表内集群的KubeConfig和RBAC权限",
      "location": "query",
      "name": "cluster_ids",
      "options": [
        "--cluster-ids"
      ],
      "param_style": "simple",
      "raw_name": "ClusterIds",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "/users/[Uid]/permissions",
  "product_code": "CS",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "request_id": {
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "687C5BAA-D103-4993-884B-C35E4314****",
            "type": "string"
          },
          "task_id": {
            "description_en": "The task ID",
            "description_zh": "任务ID",
            "example": "clean-user-permissions-2085266204********-6694c16e6ae07***********",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
