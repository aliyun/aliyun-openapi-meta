{
  "camel_example": "",
  "cmd_full_name": "hsm config-audit-log",
  "cmd_name": "config-audit-log",
  "deprecated": false,
  "description_en": "Enables or disables the audit log feature and delivers audit logs to buckets",
  "description_zh": "开通或关闭审计日志、配置审计日志投递Bucket",
  "kebab_example": "aliyun hsm config-audit-log --biz-region-id cn-hangzhou --audit-action enable --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ConfigAuditLog",
  "operation": {
    "action": "ConfigAuditLog",
    "api_style": "RPC",
    "api_version": "2023-11-13",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "hsm-log",
      "help_en": "The bucket to which audit logs are delivered",
      "help_zh": "审计日志投递Bucket",
      "location": "query",
      "name": "audit_oss_bucket",
      "options": [
        "--audit-oss-bucket"
      ],
      "raw_name": "AuditOssBucket",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "disable",
        "enable"
      ],
      "example": "enable",
      "help_en": "Specifies whether to enable the audit log feature. Valid values:\n\nenable\n\ndisable",
      "help_zh": "审计日志开通状态。\nenable：启用 \ndisable：关闭",
      "location": "query",
      "name": "audit_action",
      "options": [
        "--audit-action"
      ],
      "raw_name": "AuditAction",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "hsm",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response",
        "description_zh": "响应信息",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "本次调用请求的ID，是由阿里云为该请求生成的唯一标识符，可用于排查和定位问题",
            "example": "42B118FB-16A6-56FB-B877-D58637EEC6AF",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
