{
  "camel_example": "aliyun drds CheckDrdsDbName --region cn-hangzhou --DrdsInstanceId drds00000000 --DbName drds_test",
  "cmd_full_name": "drds check-drds-db-name",
  "cmd_name": "check-drds-db-name",
  "deprecated": false,
  "description_en": "Checks whether a DRDS database name is valid",
  "description_zh": "调用CheckDrdsDbName接口检查DRDS的数据库名是否合法",
  "kebab_example": "aliyun drds check-drds-db-name --drds-instance-id drds00000000 --db-name drds_test --api-version 2019-01-23 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "CheckDrdsDbName",
  "operation": {
    "action": "CheckDrdsDbName",
    "api_style": "RPC",
    "api_version": "2019-01-23",
    "method": "POST",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "drds********",
      "help_en": "The ID of the DRDS instance",
      "help_zh": "DRDS 实例 ID",
      "location": "query",
      "name": "drds_instance_id",
      "options": [
        "--drds-instance-id"
      ],
      "raw_name": "DrdsInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "drds_test",
      "help_en": "The name of the DRDS database",
      "help_zh": "DRDS 数据库名",
      "location": "query",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "raw_name": "DbName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Drds",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求 ID",
            "example": "CF38538C-68BD-4278-B58F-EDE96F******",
            "type": "string"
          },
          "Result": {
            "backendName": "data",
            "description_en": "The check result. A value of true indicates that the database name is valid. A value of false indicates that the database name is invalid",
            "description_zh": "检查结果。true：数据库名合法， false：数据库名不合法",
            "example": "true",
            "type": "boolean"
          },
          "Success": {
            "backendName": "success",
            "description_en": "Indicates whether the request was successful",
            "description_zh": "是否成功",
            "example": "true",
            "type": "boolean"
          }
        },
        "type": "object"
      }
    }
  }
}
