{
  "camel_example": "aliyun smc UntagResources --region cn-hangzhou --ResourceId.1 's-bp12tueadp5ndleg0000' --ResourceType sourceserver",
  "cmd_full_name": "smc untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Detaches and deletes tags from specified SMC resources, such as migration sources and migration tasks",
  "description_zh": "为指定的SMC资源（迁移源和迁移任务）统一解绑并删除标签",
  "kebab_example": "aliyun smc untag-resources --resource-type sourceserver --resource-id s-bp12tueadp5ndleg0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2019-06-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "sourceserver",
      "help_en": "The type of the SMC resource. Valid values:\n\nsourceserver: a migration source.\n\nreplicationjob: a migration task",
      "help_zh": "SMC资源类型。取值范围：\n\nsourceserver：迁移源。\n\nreplicationjob：迁移任务",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags from the SMC resources. This parameter takes effect only if you do not specify the `TagKey.N` parameter in the request. Valid values:\n\ntrue: Removes all tags from the specified SMC resources. If no tags are attached to the SMC resources, no operation is performed.\n\nfalse: Does not remove any tags from the specified SMC resources.\n\nDefault value: false",
      "help_zh": "是否删除SMC资源绑定的全部标签。仅当请求中未设置`TagKey.N`时该参数有效。取值范围：\n\ntrue：删除指定SMC资源绑定的全部标签。若指定的SMC资源未绑定任何标签，则不做处理。\nfalse：不删除指定SMC资源绑定的任何标签。\n\n默认值：false",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "s-bw526m1vi6x20c6g****",
      "help_en": "The IDs of the SMC resources. You can specify 1 to 50 resource IDs. SMC resources include migration sources and migration tasks",
      "help_zh": "SMC资源ID。SMC资源包括迁移源和迁移任务。N的取值范围：1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "TestKey",
      "help_en": "The keys of the tags to remove. You can specify 1 to 20 tag keys. Tag keys are case-sensitive",
      "help_zh": "SMC资源的标签键。标签键大小写敏感。N的取值范围：1~20",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "smc",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "2D69A58F-345C-4FDE-88E4-BF518948****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
