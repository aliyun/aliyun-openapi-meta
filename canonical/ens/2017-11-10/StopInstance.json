{
  "camel_example": "aliyun ens StopInstance --region cn-hangzhou --InstanceId 'i-instanceid0000'",
  "cmd_full_name": "ens stop-instance",
  "cmd_name": "stop-instance",
  "deprecated": false,
  "description_en": "Stops an instance",
  "description_zh": "调用StopInstance停止运行一台实例",
  "kebab_example": "aliyun ens stop-instance --instance-id i-instanceid0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "StopInstance",
  "operation": {
    "action": "StopInstance",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "i-instanceid****",
      "help_en": "The ID of the instance that you want to stop. You can specify only one instance ID",
      "help_zh": "要停止运行的实例的ID，仅支持停止单个",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "true",
        "false"
      ],
      "example": "true",
      "help_en": "Specifies whether to forcibly stop the instance.\n\ntrue\n\nfalse (default)",
      "help_zh": "停止实例时是否强制关机策略。\n\ntrue：强制关机。\n\nfalse（默认）：正常关机流程",
      "location": "query",
      "name": "force_stop",
      "options": [
        "--force-stop"
      ],
      "raw_name": "ForceStop",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "Code": {
            "backendName": "code",
            "description_en": "The returned service code. 0 indicates that the request was successful",
            "description_zh": "返回的业务码，正常成功为0",
            "example": "0",
            "format": "int32",
            "type": "integer"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
