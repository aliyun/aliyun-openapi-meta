{
  "camel_example": "aliyun ens UnAssociateEnsEipAddress --region cn-hangzhou --AllocationId 'eip-5sqa431nx3vee8heqxfxp0000'",
  "cmd_full_name": "ens un-associate-ens-eip-address",
  "cmd_name": "un-associate-ens-eip-address",
  "deprecated": false,
  "description_en": "Disassociates an elastic IP address (EIP) from the cloud resource to which it is bound",
  "description_zh": "调用UnAssociateEnsEipAddress将弹性公网IP（EIP）从绑定的云产品上解绑",
  "kebab_example": "aliyun ens un-associate-ens-eip-address --allocation-id eip-5sqa431nx3vee8heqxfxp0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UnAssociateEnsEipAddress",
  "operation": {
    "action": "UnAssociateEnsEipAddress",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "eip-5sqa431nx3vee8heqxfxp****",
      "help_en": "The instance ID of the edge elastic IP address (EIP).\n\nIf the EIP is referenced by an SNAT entry, invoke DeleteSnatIpForSnatEntry to remove it first. If the EIP is referenced by a DNAT entry, invoke DeleteForwardEntry to delete it first, or set Force to true under Settings as needed",
      "help_zh": "边缘弹性公网IP的实例ID。\n\n如果 EIP 被 SNAT 条目引用，请先调用 DeleteSnatIpForSnatEntry 移除；如果被 DNAT 条目引用，请先调用 DeleteForwardEntry 删除，或按需设置 Force=true",
      "location": "query",
      "name": "allocation_id",
      "options": [
        "--allocation-id"
      ],
      "raw_name": "AllocationId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to forcibly disassociate the EIP when the EIP is bound to a NAT gateway that has DNAT or SNAT entries. Valid values:\n\nfalse (default): does not forcibly disassociate the EIP.\ntrue: forcibly disassociates the EIP",
      "help_zh": "当EIP绑定了NAT网关，且NAT网关添加了DNAT或SNAT条目时，是否强制解绑EIP，取值：\nfalse（默认值）：不强制解绑EIP。\ntrue：强制解绑EIP",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "4A431388-2D4B-46F4-A96B-D4E6BD0688C1",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
