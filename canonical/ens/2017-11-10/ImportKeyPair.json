{
  "camel_example": "aliyun ens ImportKeyPair --region cn-hangzhou --KeyPairName TestKeyPairName --PublicKeyBody 'ssh-rsa_AAAAB0000'",
  "cmd_full_name": "ens import-key-pair",
  "cmd_name": "import-key-pair",
  "deprecated": false,
  "description_en": "Imports the public key of a Rivest–Shamir–Adleman (RSA)-encrypted key pair that is generated by a third-party tool",
  "description_zh": "调用ImportKeyPair导入由其他工具产生的RSA密钥对的公钥部分",
  "kebab_example": "aliyun ens import-key-pair --biz-key-pair-name TestKeyPairName --public-key-body ssh-rsa_AAAAB0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ImportKeyPair",
  "operation": {
    "action": "ImportKeyPair",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "TestKeyPairName",
      "help_en": "The name of the key pair. The name must conform to the following naming conventions:\n\nThe name must be 2 to 128 characters in length.\n\nThe name must start with a letter but cannot start with `http://` or `https://`.\n\nThe name can contain letters, digits, colons (:), underscores (_), and hyphens (-).\n\nYou can specify the name of only one key pair",
      "help_zh": "密钥对名称。命名规则如下所示：\n\n长度为2~128个英文或中文字符。\n必须以大小字母或中文开头，不能以`http://`和`https://`开头。\n可以包含数字、半角冒号（:）、下划线（_）、或者连字符（-）。\n\n仅支持传单个密钥对",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": true,
      "type": "string"
    },
    {
      "example": "ssh-rsa AAAAB****",
      "help_en": "The public key of the key pair. You can specify only one public key",
      "help_zh": "密钥对的公钥内容。仅支持传单个内容",
      "location": "query",
      "name": "public_key_body",
      "options": [
        "--public-key-body"
      ],
      "raw_name": "PublicKeyBody",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "KeyPairFingerPrint": {
            "backendName": "data.keyPairFingerPrint",
            "description_en": "The fingerprint of the key pair. The message-digest algorithm 5 (MD5) is used based on the public key fingerprint format defined in RFC 4716",
            "description_zh": "密钥对的指纹。根据RFC4716定义的公钥指纹格式，采用MD5信息摘要算法",
            "example": "fdaf8ff7a756ef843814fc****",
            "type": "string"
          },
          "KeyPairName": {
            "backendName": "data.keyPairName",
            "description_en": "The name of the key pair",
            "description_zh": "密钥对名称",
            "example": "TestKeyPairName",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
