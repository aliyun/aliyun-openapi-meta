{
  "camel_example": "aliyun ens PrepareUpload --region cn-hangzhou --BucketName test",
  "cmd_full_name": "ens prepare-upload",
  "cmd_name": "prepare-upload",
  "deprecated": false,
  "description_en": "Prepares for an upload and retrieves information about the physical storage location",
  "description_zh": "准备上传，获取物理存储位置信息",
  "kebab_example": "aliyun ens prepare-upload --bucket-name test --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "PrepareUpload",
  "operation": {
    "action": "PrepareUpload",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "test",
      "help_en": "The bucket name",
      "help_zh": "bucket名称",
      "location": "query",
      "name": "bucket_name",
      "options": [
        "--bucket-name"
      ],
      "raw_name": "BucketName",
      "required": true,
      "type": "string"
    },
    {
      "example": "180.166.XX.XX",
      "help_en": "The client's IP address. Use this parameter when the client's IP address differs from the IP address used to call the API. For example, a server obtains authorization and then sends it to a client",
      "help_zh": "指定IP，适用于用户IP与接口调用IP不一致的情况，如服务端获取授权后下发给客户端的场景",
      "location": "query",
      "name": "client_ip",
      "options": [
        "--client-ip"
      ],
      "raw_name": "ClientIp",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "BucketName": {
            "backendName": "content.bucketName",
            "description_en": "The name of the physical storage. Pass this value when you use the OSS SDK",
            "description_zh": "物理存储名称，使用OSS SDK时传入",
            "example": "test-xxxxxx",
            "mappingResourcePropertyName": "$.BucketName",
            "title_en": "物理存储名称，使用oss sdk时传入",
            "title_zh": "物理存储名称，使用oss sdk时传入",
            "type": "string"
          },
          "Endpoint": {
            "backendName": "content.endpoint",
            "description_en": "The endpoint. Pass this value when you use the OSS SDK",
            "description_zh": "endpoint，使用OSS SDK时传入",
            "example": "eos.aliyuncs.com",
            "mappingResourcePropertyName": "$.Endpoint",
            "title_en": "endpoint，使用oss sdk时传入",
            "title_zh": "endpoint，使用oss sdk时传入",
            "type": "string"
          },
          "RequestId": {
            "backendName": "content.requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "6666C5A5-75ED-422E-A022-7121FA18C968",
            "title_en": "请求ID",
            "title_zh": "请求ID",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
