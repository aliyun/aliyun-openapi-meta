{
  "camel_example": "aliyun ens ReleaseAICInstance --region cn-hangzhou --ServerId 'cas-instance0000'",
  "cmd_full_name": "ens release-aic-instance",
  "cmd_name": "release-aic-instance",
  "deprecated": false,
  "description_en": "Releases an Android in Container (AIC) instance from the server",
  "description_zh": "调用ReleaseAicInstance释放板卡上的AIC",
  "kebab_example": "aliyun ens release-aic-instance --server-id cas-instance0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ReleaseAICInstance",
  "operation": {
    "action": "ReleaseAICInstance",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "cas-instance****",
      "help_en": "The ID of the server",
      "help_zh": "服务器实例ID",
      "location": "query",
      "name": "server_id",
      "options": [
        "--server-id"
      ],
      "raw_name": "ServerId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
