{
  "camel_example": "aliyun ens AssociateHaVip --region cn-hangzhou --InstanceId 'i-50c40000' --HaVipId 'havip-5p14t0000'",
  "cmd_full_name": "ens associate-ha-vip",
  "cmd_name": "associate-ha-vip",
  "deprecated": false,
  "description_en": "Associates a high-availability virtual IP address (HAVIP) with an Edge Node Service (ENS) instance or elastic network interface (ENI)",
  "description_zh": "调用AssociateHaVip接口将HaVip绑定到ENS实例或ENS弹性网卡上",
  "kebab_example": "aliyun ens associate-ha-vip --ha-vip-id havip-5p14t0000 --instance-id i-50c40000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "AssociateHaVip",
  "operation": {
    "action": "AssociateHaVip",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "havip-5p14t****",
      "help_en": "The ID of the HAVIP",
      "help_zh": "要绑定的HaVip实例ID",
      "location": "query",
      "name": "ha_vip_id",
      "options": [
        "--ha-vip-id"
      ],
      "raw_name": "HaVipId",
      "required": true,
      "type": "string"
    },
    {
      "example": "i-50c4****",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "EnsInstance",
        "NetworkInterface"
      ],
      "example": "EnsInstance",
      "help_en": "The type of the instance to be associated with the HAVIP. Valid values:\n\nEnsInstance (default): ENS instance\n\nNetworkInterface: elastic network interface (ENI)",
      "help_zh": "要绑定HaVip的实例类型，取值：\nEnsInstance：云服务器ENS实例（默认）。\nNetworkInterface：弹性网卡实例，当绑定HaVip的实例为弹性网卡，该参数必须填写",
      "location": "query",
      "name": "instance_type",
      "options": [
        "--instance-type"
      ],
      "raw_name": "InstanceType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "Schema of Response",
        "description_zh": "Schema of Response",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "CEF72CEB-54B6-4AE8-B225-F876FF7BA984",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
