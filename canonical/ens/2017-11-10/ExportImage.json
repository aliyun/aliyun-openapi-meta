{
  "camel_example": "aliyun ens ExportImage --region cn-hangzhou --ImageId 'm-5ragaz3s74b7go8ks7jp90000' --OSSRegionId 'cn-beijing' --OSSBucket whxyl0000",
  "cmd_full_name": "ens export-image",
  "cmd_name": "export-image",
  "deprecated": false,
  "description_en": "Exports a custom image to an Object Storage Service (OSS) bucket in the same region",
  "description_zh": "调用ExportImage导出一份自定义镜像到与该自定义镜像同一地域的OSS Bucket里",
  "kebab_example": "aliyun ens export-image --image-id m-5ragaz3s74b7go8ks7jp90000 --oss-bucket whxyl0000 --oss-region-id cn-beijing --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ExportImage",
  "operation": {
    "action": "ExportImage",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "m-5ragaz3s74b7go8ks7jp9****",
      "help_en": "The ID of the image",
      "help_zh": "镜像ID",
      "location": "query",
      "name": "image_id",
      "options": [
        "--image-id"
      ],
      "raw_name": "ImageId",
      "required": true,
      "type": "string"
    },
    {
      "example": "whxyl****",
      "help_en": "The OSS bucket to which you want to export the image",
      "help_zh": "保存导出镜像的OSS Bucket",
      "location": "query",
      "name": "oss_bucket",
      "options": [
        "--oss-bucket"
      ],
      "raw_name": "OSSBucket",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "cn-beijing"
      ],
      "example": "cn-beijing",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "oss_region_id",
      "options": [
        "--oss-region-id"
      ],
      "raw_name": "OSSRegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "dataCenter/972****/",
      "help_en": "The prefix of the object as which you want to store the image in the OSS bucket. The prefix must be 1 to 30 characters in length and can contain digits and letters",
      "help_zh": "您的OSS Object的前缀。可以由数字或者字母组成，字符长度为1~30",
      "location": "query",
      "max_length": "30",
      "name": "oss_prefix",
      "options": [
        "--oss-prefix"
      ],
      "raw_name": "OSSPrefix",
      "required": false,
      "type": "string"
    },
    {
      "example": "AliyunMNSLoggingRole",
      "help_en": "The name of the Resource Access Management (RAM) role",
      "help_zh": "授权角色名",
      "location": "query",
      "name": "role_name",
      "options": [
        "--role-name"
      ],
      "raw_name": "RoleName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "ExportedImageURL": {
            "backendName": "data.ExportedImageURL",
            "description_en": "The URL that points to the exported image",
            "description_zh": "导出的镜像地址",
            "example": "http://oss.url",
            "nullToEmpty": true,
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "43A426AD-3F2E-5DD9-9C08-D4DBDCA48D85",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
