{
  "camel_example": "aliyun ens RollbackApplication --region cn-hangzhou --AppId '474bdef0-d149-4695-abfb-52912d910000' --FromAppVersion 'v2-1'",
  "cmd_full_name": "ens rollback-application",
  "cmd_name": "rollback-application",
  "deprecated": false,
  "description_en": "Rolls back the container version of a specific application",
  "description_zh": "调用RollbackApplication回滚应用下边缘资源的容器版本，回退到历史版本",
  "kebab_example": "aliyun ens rollback-application --app-id 474bdef0-d149-4695-abfb-52912d910000 --from-app-version v2-1 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RollbackApplication",
  "operation": {
    "action": "RollbackApplication",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "474bdef0-d149-4695-abfb-52912d91****",
      "help_en": "The ID of the application",
      "help_zh": "应用ID",
      "location": "query",
      "name": "app_id",
      "options": [
        "--app-id"
      ],
      "raw_name": "AppId",
      "required": true,
      "type": "string"
    },
    {
      "example": "v2-1",
      "help_en": "The current version number",
      "help_zh": "待回滚的版本号",
      "location": "query",
      "name": "from_app_version",
      "options": [
        "--from-app-version"
      ],
      "raw_name": "FromAppVersion",
      "required": true,
      "type": "string"
    },
    {
      "example": "v2",
      "help_en": "The target version number. By default, the system automatically rolls back the container version to the previous version",
      "help_zh": "目标版本号。默认自动回滚到上一版本",
      "location": "query",
      "name": "to_app_version",
      "options": [
        "--to-app-version"
      ],
      "raw_name": "ToAppVersion",
      "required": false,
      "type": "string"
    },
    {
      "example": "1800",
      "help_en": "The timeout period of the asynchronous rollback operation. Unit: seconds. Default value: 300",
      "help_zh": "回滚的异步操作超时时间。单位：秒。默认值：300",
      "location": "query",
      "name": "timeout",
      "options": [
        "--timeout"
      ],
      "raw_name": "Timeout",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The ID of the request",
            "description_zh": "请求ID",
            "example": "C0003E8B-B930-4F59-ADC0-0E209A9012A8",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
