{
  "camel_example": "aliyun ens DeleteKeyPairs --region cn-hangzhou --KeyPairName TestKeyPairName --KeyPairId 'ssh-5lywanlkih1zo9yl8eg0000'",
  "cmd_full_name": "ens delete-key-pairs",
  "cmd_name": "delete-key-pairs",
  "deprecated": false,
  "description_en": "Deletes SSH key pairs",
  "description_zh": "调用DeleteKeyPairs删除一对SSH密钥对",
  "kebab_example": "aliyun ens delete-key-pairs --biz-key-pair-name TestKeyPairName --key-pair-id ssh-5lywanlkih1zo9yl8eg0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteKeyPairs",
  "operation": {
    "action": "DeleteKeyPairs",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "TestKeyPairName",
      "help_en": "The name of the key pair. The name must conform to the following naming conventions:\n\nThe name must be 2 to 128 characters in length.\n\nThe name must start with a letter and cannot start with `http://` or `https://`.\n\nThe name can contain letters, digits, colons (:), underscores (_), and hyphens (-).\n\nBefore you delete a key pair, you can call the DescribeKeyPairs operation to query existing key pairs",
      "help_zh": "密钥对名称。命名规则如下所示：\n\n长度为2~128个英文或中文字符。\n必须以大小字母或中文开头，不能以`http://`和`https://`开头。\n可以包含数字、半角冒号（:）、下划线（_）、或者连字符（-）。\n\n在进行删除密钥对前，您可以调用DescribeKeyPairs查询已有密钥对",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": false,
      "type": "string"
    },
    {
      "example": "ssh-5lywanlkih1zo9yl8eg****",
      "help_en": "The ID of the SSH key pair",
      "help_zh": "SSH密钥对的ID",
      "location": "query",
      "name": "key_pair_id",
      "options": [
        "--key-pair-id"
      ],
      "raw_name": "KeyPairId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "473469C7-AA6F-4DC5-B3DB-A3DC0DE3C83E\t",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
