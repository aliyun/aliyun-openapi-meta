{
  "camel_example": "aliyun vod SetAuditSecurityIp --region cn-hangzhou --Ips '192.168.0.1'",
  "cmd_full_name": "vod set-audit-security-ip",
  "cmd_name": "set-audit-security-ip",
  "deprecated": false,
  "description_en": "Sets the review security IP addresses",
  "description_zh": "调用本接口设置审核安全IP",
  "kebab_example": "aliyun vod set-audit-security-ip --ips 192.168.0.1 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SetAuditSecurityIp",
  "operation": {
    "action": "SetAuditSecurityIp",
    "api_style": "RPC",
    "api_version": "2017-03-21",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "Default",
      "help_en": "The name of the security group for review. Default value: Default. A maximum of 10 security groups are supported",
      "help_zh": "审核安全组名称。默认值为：Default。最多支持10个安全分组",
      "location": "query",
      "name": "security_group_name",
      "options": [
        "--security-group-name"
      ],
      "raw_name": "SecurityGroupName",
      "required": false,
      "type": "string"
    },
    {
      "example": "192.168.0.1",
      "help_en": "The list of security IP addresses for review. Each group supports a maximum of 100 IP addresses. Separate multiple IP addresses with commas (,). The following formats are supported:\n\nExact IP address: 192.168.0.1\nCIDR block: 192.168.0.1/24 (Classless Inter-Domain Routing. /24 specifies the length of the prefix in the address. Valid values: `[1,32]`.)",
      "help_zh": "审核安全IP列表。每个分组最多支持100个，多个IP使用英文逗号（,）隔开，格式如下：\n\n精确IP：192.168.0.1\nCIDR模式：192.168.0.1/24（无类域间路由，/24表示了地址中前缀的长度，范围：`[1,32]`）",
      "location": "query",
      "name": "ips",
      "options": [
        "--ips"
      ],
      "raw_name": "Ips",
      "required": true,
      "type": "string"
    },
    {
      "example": "Cover",
      "help_en": "The operation mode. Valid values:\n\nAppend: default value. Appends IP addresses to the IP address whitelist.\nCover: overwrites the existing IP address whitelist.\nDelete: deletes IP addresses from the IP address whitelist.\nIf the specified value is not within the valid values, the default value (Append) is used",
      "help_zh": "操作方式，取值范围：\n\nAppend：默认值，追加IP白名单。\nCover：覆盖原IP白名单。\nDelete：删除IP白名单。\n当输入的参数取值不在上述取值范围内时，会使用默认值调用(即Append)",
      "location": "query",
      "name": "operate_mode",
      "options": [
        "--operate-mode"
      ],
      "raw_name": "OperateMode",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "vod",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回结果",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "25818875-5F78-4AF6-D7393642CA58****",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
