{
  "camel_example": "aliyun vod DeleteCategory --region cn-hangzhou --CateId 33000000",
  "cmd_full_name": "vod delete-category",
  "cmd_name": "delete-category",
  "deprecated": false,
  "description_en": "Deletes a category and its subcategories",
  "description_zh": "调用本接口删除分类及其子分类",
  "kebab_example": "aliyun vod delete-category --cate-id 33000000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteCategory",
  "operation": {
    "action": "DeleteCategory",
    "api_style": "RPC",
    "api_version": "2017-03-21",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "3300****",
      "help_en": "The category ID. Only a single category ID is supported. You can obtain the category ID by using the following methods:\nLog on to the ApsaraVideo VOD console (https://vod.console.aliyun.com) and choose Configuration Management > Media Asset Management Configuration > Category Management to view the category ID.\nObtain the category ID from the response of the [AddCategory](~~AddCategory~~) operation when you create a category.\n\nIf the specified category ID is the ID of a parent category, the parent category and all its subcategories are deleted. Proceed with caution",
      "help_zh": "分类ID，仅支持传入单个分类ID。可通过以下方式获取：\n登录视频点播控制台 (https://vod.console.aliyun.com)，选择配置管理 > 媒资管理配置 > 分类管理查看分类ID。\n通过[AddCategory](~~AddCategory~~)接口创建分类时会返回。\n\n若传入的分类ID为父分类的ID，则会同时删除该分类及其所有子分类，请谨慎操作",
      "location": "query",
      "name": "cate_id",
      "options": [
        "--cate-id"
      ],
      "raw_name": "CateId",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "vod",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response parameters",
        "description_zh": "返回结果",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "25818875-5F78-4AF6-D7393642CA58****",
            "type": "string",
            "visibility": "Public"
          }
        },
        "type": "object"
      }
    }
  }
}
