{
  "cmd_full_name": "green verify-custom-ocr-template",
  "cmd_name": "verify-custom-ocr-template",
  "deprecated": false,
  "description_en": "Tests an OCR template",
  "description_zh": "ocr模板测试",
  "kebab_example": "aliyun green verify-custom-ocr-template --id 969 --test-img-url http://xxx.jpg --api-version 2017-08-23 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "VerifyCustomOcrTemplate",
  "operation": {
    "action": "VerifyCustomOcrTemplate",
    "api_style": "RPC",
    "api_version": "2017-08-23",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "969",
      "help_en": "The primary key ID",
      "help_zh": "主键ID",
      "location": "query",
      "name": "id",
      "options": [
        "--id"
      ],
      "raw_name": "Id",
      "required": true,
      "type": "int"
    },
    {
      "example": "http://xxx.jpg",
      "help_en": "The URL of the test template image. The value must be a relative path (in the format of customize_image_temp/{uid}/{filename}.png) uploaded to Object Storage Service (OSS) by using the upload credentials obtained from DescribeImageUploadInfo. Full HTTP or HTTPS URLs are not supported",
      "help_zh": "测试模板图片url,测试模板图片 URL。须为通过 DescribeImageUploadInfo 获取上传凭证后上传至 OSS 的相对路径（形如 customize_image_temp/{uid}/{filename}.png），不支持完整 HTTP/HTTPS URL",
      "location": "query",
      "name": "test_img_url",
      "options": [
        "--test-img-url"
      ],
      "raw_name": "TestImgUrl",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Green",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "properties": {
          "ImageUrl": {
            "backendName": "recognizeImageUrl",
            "description_en": "The image URL",
            "description_zh": "图片地址",
            "example": "http://xxx.jpg\n",
            "type": "string"
          },
          "RecognizeInfo": {
            "backendName": "recognizeInfo",
            "description_en": "The recognition result",
            "description_zh": "认证结果",
            "example": "{}",
            "type": "string"
          },
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "AAAAAA-BBBB-CCCCC-DDDD-EEEEEEEE****\n",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  }
}
