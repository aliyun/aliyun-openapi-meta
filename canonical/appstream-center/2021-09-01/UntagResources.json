{
  "cmd_full_name": "appstream-center untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Unbinds tags from specified Wuying delivery groups in compliance with Alibaba Cloud tagging standards. You can unbind specific tags by tag key or unbind all custom tags from a resource at once by setting `All=true` without specifying tag keys. Currently, only the delivery group (`APPINSTANCEGROUP`) resource type is supported",
  "description_zh": "按阿里云标签规范为指定的无影交付组解绑标签。可按标签键解绑指定标签，也可以在不传标签键时通过 `All=true` 一次解绑资源上的全部自定义标签。当前仅支持交付组（`APPINSTANCEGROUP`）资源类型",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2021-09-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. This parameter is required. Set this parameter to the ID of the region where the delivery group resides, such as `cn-hangzhou`",
      "help_zh": "地域 ID，必填。传入交付组所在地域的 ID，例如 `cn-hangzhou`",
      "location": "formData",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "APPINSTANCEGROUP",
      "help_en": "The resource type. This parameter is required. Currently, only delivery groups are supported. The value is case-insensitive. We recommend that you use uppercase.\n\nValid values:\n\n`APPINSTANCEGROUP`: Wuying delivery group.\n\nIf you specify other values, the error code `InvalidResourceType.Invalid` is returned",
      "help_zh": "资源类型，必填。当前仅支持交付组，取值不区分大小写，建议使用大写形式：\n\n`APPINSTANCEGROUP`：无影交付组。\n\n传入其他取值时返回错误码 `InvalidResourceType.Invalid`",
      "location": "formData",
      "max_length": "64",
      "min_length": "1",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of resource IDs from which you want to unbind tags. This parameter is required. Specify delivery group IDs. You can specify up to 50 IDs at a time. Duplicate IDs are automatically deduplicated.\n\nAll IDs must correspond to existing delivery groups under the current Alibaba Cloud account. If any ID does not exist or does not belong to the current account, the entire request fails with the error code `InvalidAppInstanceGroup.NotFound`, and no tags are unbound from any resource",
      "help_zh": "需要解绑标签的资源 ID 列表，必填。传入交付组 ID，单次最多 50 个，重复的 ID 会自动去重。\n\n所有 ID 必须是当前阿里云账号下已存在的交付组。 任一 ID 不存在或不属于当前账号时，整个请求失败并返回错误码 `InvalidAppInstanceGroup.NotFound`，不会为任何资源解绑标签",
      "location": "formData",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of tag keys to unbind. You can specify up to 20 tag keys at a time. Specify at least one of `TagKey.N` and `All`. If neither is specified, the error code `InvalidParameter.TagKeyListOrAll` is returned.\n\nIf `TagKey.N` is specified, only the tags that correspond to the specified tag keys are unbound. The `All` parameter is ignored.\nIf a specified tag key does not exist on the resource, the tag key is skipped and no error is returned.\nIf `TagKey.N` is not specified, set `All` to `true` to unbind all custom tags from the resource.\n\nTag keys that start with `System/` are Wuying system tags. Only the following values are supported:\n\n`System/Scheduler/GRAYSCALE`: the canary release tag for the delivery group.\n`System/Scheduler/STOP_NEW_USER_CONNECTION`: prevents newly bound users from establishing connections to the delivery group.\n\nIf you specify other tag keys that start with `System/`, the error code `InvalidTagPolicy.KeyInvalid` or `InvalidTag.SystemKeyNotAllow` is returned",
      "help_zh": "要解绑的标签键列表，单次最多 20 个。`TagKey.N` 与 `All` 至少指定一个，两者都不传时返回错误码 `InvalidParameter.TagKeyListOrAll`。\n\n传入 `TagKey.N` 时，仅解绑这些标签键对应的标签，`All` 参数被忽略。\n资源上不存在指定的标签键时，该标签键会被跳过，不会返回错误。\n不传 `TagKey.N` 时，需要将 `All` 设置为 `true` 以解绑资源上的全部自定义标签。\n\n以 `System/` 开头的标签键为无影系统标签，仅支持以下取值：\n\n`System/Scheduler/GRAYSCALE`：交付组灰度标签。\n`System/Scheduler/STOP_NEW_USER_CONNECTION`：阻止交付组新绑定用户建连。\n\n传入其他以 `System/` 开头的标签键时，返回错误码 `InvalidTagPolicy.KeyInvalid` 或 `InvalidTag.SystemKeyNotAllow`",
      "location": "formData",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to unbind all custom tags from the resource. This parameter takes effect only when `TagKey.N` is not specified. If `TagKey.N` is specified, this parameter is ignored. Valid values:\n\n`true`: Unbinds all custom tags from the resource, including Wuying system tags that start with `System/` and were bound by calling [TagResources](~~TagResources~~).\n`false` (default): Does not perform a full unbinding. If `TagKey.N` is also not specified, the error code `InvalidParameter.TagKeyListOrAll` is returned",
      "help_zh": "是否解绑资源上的全部自定义标签。仅在未传入 `TagKey.N` 时生效；传入了 `TagKey.N` 时该参数被忽略。取值：\n\n`true`：解绑资源上的全部自定义标签，包括通过 [TagResources](~~TagResources~~) 绑定的 `System/` 开头的无影系统标签。\n`false`（默认值）：不做全量解绑。此时若也未传入 `TagKey.N`，返回错误码 `InvalidParameter.TagKeyListOrAll`",
      "location": "formData",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "appstream-center",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response of this request. On success, only the request ID is returned",
        "description_zh": "本次请求的返回结果。解绑成功时仅返回请求 ID",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID, which can be used to troubleshoot issues",
            "description_zh": "请求 ID，可用于定位请求问题",
            "example": "1CBAFFAB-B697-4049-A9B1-67E1FC5F****",
            "title_en": "Id of the request",
            "title_zh": "Id of the request",
            "type": "string"
          }
        },
        "title_en": "Schema of Response",
        "title_zh": "Schema of Response",
        "type": "object"
      }
    }
  }
}
