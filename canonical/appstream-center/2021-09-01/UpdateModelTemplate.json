{
  "cmd_full_name": "appstream-center update-model-template",
  "cmd_name": "update-model-template",
  "deprecated": false,
  "description_en": "Updates a model template",
  "description_zh": "更新模型模板",
  "kebab_example": "aliyun appstream-center update-model-template --model-template-id mt-xxxx --api-version 2021-09-01 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "UpdateModelTemplate",
  "operation": {
    "action": "UpdateModelTemplate",
    "api_style": "RPC",
    "api_version": "2021-09-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "mt-xxxx",
      "help_en": "The model group ID",
      "help_zh": "模型分组ID",
      "location": "query",
      "name": "model_template_id",
      "options": [
        "--model-template-id"
      ],
      "raw_name": "ModelTemplateId",
      "required": true,
      "type": "string"
    },
    {
      "example": "model-template-001",
      "help_en": "The template group name",
      "help_zh": "模板分组名称",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "测试模型分组",
      "help_en": "The template group description",
      "help_zh": "模板分组描述",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "OpenClaw：\n{\"defaults\":{\"model\":{\"primary\":\"bailian/qwen3.5-plus\"}}}\n\nHermesAgent：\n{\"model\":{\"provider\":\"bailian\",\"default\":\"qwen3.5-plus\"}}",
      "help_en": "The model group configuration JSON object. You can use this field to modify the default model. The configuration format varies depending on the agent provider (AgentProvider):\n\nOpenClaw / AgenticComputer scenarios:\nSet the default model by using the `defaults.model.primary` field in the format of `provider name/model code`.\n\nHermesAgent scenarios:\nSpecify the provider name by using `model.provider` and the model code by using `model.default`.\n\nNote:\nWhen you modify the default model, the system verifies whether the specified provider and model code already exist in the model group",
      "help_zh": "模型分组配置 JSON 对象，可通过该字段修改默认模型。配置格式因智能体提供商（AgentProvider）不同而异：\n\nOpenClaw / AgenticComputer 场景：\n通过 `defaults.model.primary` 字段设置默认模型，格式为 `服务商名称/模型编码`。\n\nHermesAgent 场景：\n通过 `model.provider` 指定服务商名称，`model.default` 指定模型编码。\n\n：\n修改默认模型时，系统将校验指定的服务商与模型编码是否已存在于该模型分组中",
      "location": "query",
      "name": "config",
      "options": [
        "--config"
      ],
      "raw_name": "Config",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The authorization scope. This parameter is optional and can be modified only for Common model groups. Valid values: ALL_USER and USER_MIXED",
      "help_zh": "授权范围（可选，仅 Common 模型分组支持修改）：ALL_USER / USER_MIXED",
      "location": "query",
      "name": "ref_scope",
      "options": [
        "--ref-scope"
      ],
      "raw_name": "RefScope",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "appstream-center",
  "protocol": "HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "The response",
        "description_zh": "请求结果",
        "properties": {
          "RequestId": {
            "backendName": "requestId",
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "1CBAFFAB-B697-4049-A9B1-67E1FC5F****",
            "title_en": "请求ID，用于标识请求的唯一 ID",
            "title_zh": "请求ID，用于标识请求的唯一 ID",
            "type": "string"
          }
        },
        "title_en": "BaseResult",
        "title_zh": "BaseResult",
        "type": "object"
      }
    }
  }
}
