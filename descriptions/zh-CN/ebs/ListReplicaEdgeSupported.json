{
  "parameters": [
    {
      "name": "Azone",
      "description": "可用区ID。"
    },
    {
      "name": "MaxResults",
      "description": "分页查询时每页的最大条目数。取值范围：1~500。该参数暂未启用。\n\n默认值：10。"
    },
    {
      "name": "NextToken",
      "description": "查询凭证（Token）。取值为上一次调用该接口返回的NextToken参数值，初次调用接口时无需设置该参数。如果设置了NextToken，则请求参数PageSize和PageNumber将失效，且返回数据中的TotalCount无效。该参数暂未启用。"
    },
    {
      "name": "RegionId",
      "description": "地域ID。"
    }
  ],
  "deprecated": false
}
