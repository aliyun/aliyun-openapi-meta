{
  "parameters": [
    {
      "name": "AcceleratorId",
      "description": "全球加速实例ID。"
    },
    {
      "name": "AssociatedResourceId",
      "description": "关联资源ID。"
    },
    {
      "name": "AssociatedResourceRegionId",
      "description": "关联资源所在的地域ID。"
    },
    {
      "name": "AssociatedResourceType",
      "description": "关联的周边资源类型。"
    },
    {
      "name": "DryRun",
      "description": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会解绑资源。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回HTTP 2xx状态码。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接创建终端节点组。"
    },
    {
      "name": "RegionId",
      "description": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。"
    }
  ],
  "deprecated": false
}
