{
  "parameters": [
    {
      "name": "PreCheck",
      "description": "是否执行预检查。默认false。为true时，本次调用只会检查当前网格是否满足升级条件，并不会真正执行升级。"
    },
    {
      "name": "ServiceMeshId",
      "description": "ASM实例ID"
    }
  ],
  "deprecated": false
}
