{
  "parameters": [
    {
      "name": "LabelIds",
      "description": "标签 ID 的集合，最多支持 5 个。"
    },
    {
      "name": "NeedRelatedInfo",
      "description": "是否需要查询任务关联信息（数据源连接信息、传输实例信息、任务步骤信息等），默认值为 true。"
    },
    {
      "name": "Order",
      "description": "排列顺序，包括 `asc`（升序）和 `descend`（降序）。默认值为 `descend`。"
    },
    {
      "name": "PageNumber",
      "description": "页序号，分页查询时生效。"
    },
    {
      "name": "PageSize",
      "description": "页大小，分页查询时生效。"
    },
    {
      "name": "SearchKey",
      "description": "数据迁移或数据同步任务的名称/ID。"
    },
    {
      "name": "SinkEndpointTypes",
      "description": "目标端数据源类型的集合。"
    },
    {
      "name": "SortField",
      "description": "排序字段，包括 `gmtCreate` 和 `gmtModified`，默认值为 `gmtCreate`。"
    },
    {
      "name": "SourceEndpointTypes",
      "description": "源端数据源类型的集合。"
    },
    {
      "name": "Status",
      "description": "数据迁移或数据同步任务的状态集合，包括 INIT（未启动）, RUNNING（运行中）、SUSPEND（已暂停）、FAILED（失败）、FINISHED（已完成）、RELEASING（释放中）、RELEASED（已释放）、DELETED（已删除）、RUNNING_MODIFYING（修改中）、RUNNING_MERGING（合并中）、RUNNING_ALARMING（告警中）和 UNCONFIGURED（待配置）。"
    },
    {
      "name": "Type",
      "description": "任务的类型，包括 `MIGRATION`（表示数据迁移任务）和 `SYNC`（表示数据同步任务）。\n><notice> Type 参数为必填项。></notice>"
    },
    {
      "name": "VisibleSubProject",
      "description": "是否展示子任务。"
    }
  ],
  "deprecated": false
}
