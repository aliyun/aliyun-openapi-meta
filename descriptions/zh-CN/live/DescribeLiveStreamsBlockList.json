{
  "parameters": [
    {
      "name": "DomainName",
      "description": "主播流域名。"
    },
    {
      "name": "PageNum",
      "description": "当前页码。默认值为**1**。"
    },
    {
      "name": "PageSize",
      "description": "每页大小。取值范围：**1~3000**，默认值为**2000**。"
    }
  ]
}
