{
  "parameters": [
    {
      "name": "PageNumber",
      "description": "分页页码，从1开始，默认值为1。"
    },
    {
      "name": "PageSize",
      "description": "分页大小，最小值为1，最大值为99，默认值为10。"
    }
  ],
  "deprecated": false
}
