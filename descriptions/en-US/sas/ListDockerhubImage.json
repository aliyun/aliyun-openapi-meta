{
  "parameters": [
    {
      "name": "Query",
      "description": "The query condition for images. You can query images in the `[namespace/]repoName[:version]` format. Conditions in `[]` are optional."
    }
  ]
}
