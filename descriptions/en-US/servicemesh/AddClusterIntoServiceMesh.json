{
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The ID of the cluster to be added."
    },
    {
      "name": "DiscoveryOnly",
      "description": "Indicates whether to add the cluster to an ASM instance in only service discovery mode."
    },
    {
      "name": "IgnoreNamespaceCheck",
      "description": "Specifies whether to check that the cluster you want to add to the ASM instance belongs to the istio-system namespace. This parameter is often used in scenarios where you migrate traffic from self-managed open source Istio to ASM. Valid values: true and false."
    },
    {
      "name": "Kubeconfig",
      "description": "The cluster certificate."
    },
    {
      "name": "ServiceMeshId",
      "description": "The ID of the Service Mesh (ASM) instance."
    }
  ],
  "deprecated": false
}
