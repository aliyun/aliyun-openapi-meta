{
  "name": "DescribeRegions",
  "protocol": "HTTPS|HTTP",
  "method": "GET",
  "pathPattern": "/openapi/regions",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Host",
      "type": "String",
      "required": false
    }
  ]
}