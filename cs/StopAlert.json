{
  "name": "StopAlert",
  "protocol": "HTTPS|HTTP",
  "method": "POST",
  "pathPattern": "/alert/[ClusterId]/alert_rule/stop",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}