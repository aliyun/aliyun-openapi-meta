{
  "name": "DescribeTemplates",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/templates",
  "parameters": [
    {
      "name": "template_type",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}