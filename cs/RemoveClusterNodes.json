{
  "name": "RemoveClusterNodes",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/clusters/[ClusterId]/nodes/remove",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}