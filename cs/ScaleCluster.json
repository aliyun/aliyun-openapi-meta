{
  "name": "ScaleCluster",
  "protocol": "HTTPS|HTTP",
  "method": "PUT",
  "pathPattern": "/clusters/[ClusterId]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}