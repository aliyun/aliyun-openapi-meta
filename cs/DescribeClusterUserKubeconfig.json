{
  "name": "DescribeClusterUserKubeconfig",
  "protocol": "HTTPS|HTTP",
  "method": "GET",
  "pathPattern": "/k8s/[ClusterId]/user_config",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": "true"
    },
    {
      "name": "PrivateIpAddress",
      "position": "Query",
      "type": "Boolean",
      "required": "false"
    }
  ]
}