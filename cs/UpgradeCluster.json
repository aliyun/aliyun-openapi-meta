{
  "name": "UpgradeCluster",
  "protocol": "HTTPS|HTTP",
  "method": "POST",
  "pathPattern": "/api/v2/clusters/[ClusterId]/upgrade",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}