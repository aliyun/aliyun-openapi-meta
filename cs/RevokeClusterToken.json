{
  "name": "RevokeClusterToken",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/token/[Token]/revoke",
  "parameters": [
    {
      "name": "Token",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}