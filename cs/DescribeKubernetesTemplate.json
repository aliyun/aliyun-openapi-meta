{
  "name": "DescribeKubernetesTemplate",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/k8s/templates/[ClusterId]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}