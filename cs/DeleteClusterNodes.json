{
  "name": "DeleteClusterNodes",
  "protocol": "HTTP|HTTPS",
  "method": "POST|DELETE",
  "pathPattern": "/clusters/[ClusterId]/nodes",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}