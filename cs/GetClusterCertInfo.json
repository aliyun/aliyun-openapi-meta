{
  "name": "GetClusterCertInfo",
  "protocol": "HTTPS|HTTP",
  "method": "GET",
  "pathPattern": "/clusters/[ClusterId]/hosts/certs",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}