{
  "name": "DescribeAgilityTunnelCerts",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/agility/[Token]/agent_certs",
  "parameters": [
    {
      "name": "Token",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}