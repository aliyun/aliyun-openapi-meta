{
  "name": "CreateAutoscalingConfig",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/cluster/[ClusterId]/autoscale/config/",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}