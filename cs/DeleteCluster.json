{
  "name": "DeleteCluster",
  "protocol": "HTTPS|HTTP",
  "method": "DELETE",
  "pathPattern": "/clusters/[ClusterId]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": "true"
    }
  ]
}