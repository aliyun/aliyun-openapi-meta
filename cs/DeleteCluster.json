{
  "name": "DeleteCluster",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/clusters/[ClusterId]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}