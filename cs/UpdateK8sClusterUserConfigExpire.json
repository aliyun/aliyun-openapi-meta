{
  "name": "UpdateK8sClusterUserConfigExpire",
  "protocol": "HTTPS|HTTP",
  "method": "POST",
  "pathPattern": "/k8s/[ClusterId]/user_config/expire",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "hidden": false
    }
  ]
}