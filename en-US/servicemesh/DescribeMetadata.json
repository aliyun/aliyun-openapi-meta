{
  "name": "DescribeMetadata",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun servicemesh describe-metadata --region cn-hangzhou",
    "legacyCli": "aliyun servicemesh DescribeMetadata --region cn-hangzhou"
  }
}
