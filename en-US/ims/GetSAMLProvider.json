{
  "name": "GetSAMLProvider",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SAMLProviderName",
      "description": "The name of the SAML provider.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims get-saml-provider --saml-provider-name test-provider --region cn-hangzhou",
    "legacyCli": "aliyun ims GetSAMLProvider --region cn-hangzhou --SAMLProviderName 'test-provider'"
  }
}
