{
  "name": "DeleteOIDCProvider",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OIDCProviderName",
      "description": "The name of the OIDC IdP.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims delete-oidc-provider --oidc-provider-name TestOIDCProvider --region cn-hangzhou",
    "legacyCli": "aliyun ims DeleteOIDCProvider --region cn-hangzhou --OIDCProviderName TestOIDCProvider"
  }
}
