{
  "name": "GetPasswordPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun ims get-password-policy --region cn-hangzhou",
    "legacyCli": "aliyun ims GetPasswordPolicy --region cn-hangzhou"
  }
}
