{
  "name": "GetUser",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserAccessKeyId",
      "description": "The AccessKey ID of the RAM user.\n\n> You must specify only one of the following parameters: `UserPrincipalName`, `UserId`, and `UserAccessKeyId`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserId",
      "description": "The ID of the RAM user.\n\n> You must specify only one of the following parameters: `UserPrincipalName`, `UserId`, and `UserAccessKeyId`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserPrincipalName",
      "description": "The logon name of the RAM user.\n\nThe name is in the format of `<username>@<AccountAlias>.onaliyun.com`. `<username>` indicates the name of the RAM user. `<AccountAlias>.onaliyun.com` indicates the default domain name.\n\nThe value of `UserPrincipalName` must be `1 to 128` characters in length and can contain letters, digits, periods (.), hyphens (-), and underscores (\\_). The value of `<username>` must be `1 to 64` characters in length.\n\n> You must specify only one of the following parameters: `UserPrincipalName`, `UserId`, and `UserAccessKeyId`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims get-user --user-principal-name test@example.onaliyun.com --user-id 207329002493920000 --user-access-key-id LTAI0000000000000000000 --region cn-hangzhou",
    "legacyCli": "aliyun ims GetUser --region cn-hangzhou --UserAccessKeyId LTAI0000000000000000000 --UserId 207329002493920000 --UserPrincipalName 'test@example.onaliyun.com'"
  }
}
