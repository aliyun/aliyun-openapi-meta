{
  "name": "ListPredefinedScopes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppType",
      "description": "The type of the application. Valid values:\n\n- WebApp\n\n- NativeApp\n\n- ServerApp\n\nIf this parameter is empty, the permissions on all types of applications are queried.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims list-predefined-scopes --app-type WebApp --region cn-hangzhou",
    "legacyCli": "aliyun ims ListPredefinedScopes --region cn-hangzhou --AppType WebApp"
  }
}
