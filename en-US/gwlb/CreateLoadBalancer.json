{
  "name": "CreateLoadBalancer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddressIpVersion",
      "description": "The IP version. Valid values:\n\n*   **Ipv4** (default): IPv4",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "LoadBalancerName",
      "description": "The GWLB instance name.\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The resource group ID.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags that are added to the instance.",
      "position": "Body",
      "type": "Array",
      "required": false
    },
    {
      "name": "VpcId",
      "description": "The virtual private cloud (VPC) ID.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ZoneMappings",
      "description": "The mappings between zones and vSwitches. You must specify at least one zone. You can specify at most 20 zones. If the region supports two or more zones, specify at least two zones.",
      "position": "Body",
      "type": "Array",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun gwlb create-load-balancer --address-ip-version Ipv4 --load-balancer-name testGwlbName --vpc-id vpc-uf6qcgpv22ttrnnjh0000 --zone-mappings ZoneId=cn-hangzhou-a VSwitchId=vsw-2f0eb0200000 --tag Value=testTagValue Key=testTagKey --resource-group-id rg-acfmwbufq6q30000 --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun gwlb CreateLoadBalancer --region cn-hangzhou --LoadBalancerName testGwlbName --Tag.1.Key testTagKey --AddressIpVersion Ipv4 --VpcId 'vpc-uf6qcgpv22ttrnnjh0000' --ResourceGroupId 'rg-acfmwbufq6q30000' --Tag.1.Value testTagValue --ZoneMappings.1.ZoneId 'cn-hangzhou-a' --DryRun false --ZoneMappings.1.VSwitchId 'vsw-2f0eb0200000' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --method POST --force"
  }
}
