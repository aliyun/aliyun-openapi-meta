{
  "name": "StartDiskReplicaGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The ClientToken value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OneShot",
      "description": "Specifies whether to immediately synchronize data once. Valid values:\n\n*   true: immediately synchronizes data once.\n*   false: synchronizes data based on the RPO of the replication pair-consistent group.\n\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The ID of the replication pair-consistent group.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ReplicaGroupId",
      "description": "The ID of the replication pair-consistent group. You can call the [DescribeDiskReplicaGroups](~~426614~~) operation to query the IDs of replication pair-consistent groups.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ebs start-disk-replica-group --biz-region-id cn-hangzhou --replica-group-id pg-myreplica0000 --client-token 123e4567-e89b-12d3-a456-426655440000 --one-shot false --region cn-hangzhou",
    "legacyCli": "aliyun ebs StartDiskReplicaGroup --region cn-hangzhou --OneShot false --RegionId 'cn-hangzhou' --ReplicaGroupId 'pg-myreplica0000' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}
