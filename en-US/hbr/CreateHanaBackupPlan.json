{
  "name": "CreateHanaBackupPlan",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupPrefix",
      "description": "The backup prefix.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BackupType",
      "description": "The backup type. Valid values:\n\n*   COMPLETE: full backup\n*   INCREMENTAL: incremental backup\n*   DIFFERENTIAL: differential backup",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClusterId",
      "description": "The ID of the SAP HANA instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DatabaseName",
      "description": "The name of the database.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PlanName",
      "description": "The name of the backup plan.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Schedule",
      "description": "The backup policy. Format: `I|{startTime}|{interval}`. The system runs the first backup job at a point in time that is specified in the {startTime} parameter and the subsequent backup jobs at an interval that is specified in the {interval} parameter. The system does not run a backup job before the specified point in time. Each backup job, except the first one, starts only after the previous backup job is completed. For example, `I|1631685600|P1D` specifies that the system runs the first backup job at 14:00:00 on September 15, 2021 and the subsequent backup jobs once a day.\n\n*   startTime: the time at which the system starts to run a backup job. The time must follow the UNIX time format. Unit: seconds.\n*   interval: the interval at which the system runs a backup job. The interval must follow the ISO 8601 standard. For example, PT1H specifies an interval of one hour. P1D specifies an interval of one day.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VaultId",
      "description": "The ID of the backup vault.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun hbr create-hana-backup-plan --vault-id v-0002pcwhdn000000wmi --cluster-id cl-00024vyjj9000000v --plan-name plan-20220110-113108 --schedule I_1602673264_P1D --backup-type COMPLETE --database-name SYSTEMDB --backup-prefix DIFF_DATA_BACKUP --resource-group-id rg-acfmvnf22m7itha --region cn-hangzhou",
    "legacyCli": "aliyun hbr CreateHanaBackupPlan --region cn-hangzhou --ResourceGroupId 'rg-acfmvnf22m7itha' --ClusterId 'cl-00024vyjj9000000v' --BackupPrefix DIFF_DATA_BACKUP --Schedule I_1602673264_P1D --DatabaseName SYSTEMDB --BackupType COMPLETE --VaultId 'v-0002pcwhdn000000wmi' --PlanName 'plan-20220110-113108'"
  }
}
