{
  "name": "DeleteIngestProcessor",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/ingestprocessors/[processorName]",
  "parameters": [
    {
      "name": "processorName",
      "description": "The identifier of the ingest processor.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-ingest-processor --project ali-test-project --processor-name parse-nginx-log --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /ingestprocessors/parse-nginx-log --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
