{
  "name": "ConsumerGroupHeartBeat",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/logstores/[logstore]/consumergroups/[consumerGroup]?type=heartbeat",
  "parameters": [
    {
      "name": "body",
      "description": "The list of shard IDs that are being consumed.",
      "position": "Body",
      "type": "Json",
      "required": true
    },
    {
      "name": "consumer",
      "description": "The consumer.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "consumerGroup",
      "description": "The name of the consumer group. The name must be unique within a project.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "logstore",
      "description": "The name of the Logstore.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls consumer-group-heart-beat --project ali-test-project --logstore ali-test-logstore --consumer-group consumer_group_test --consumer consumer_1 --biz-body 0 --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /logstores/ali-test-logstore/consumergroups/consumer_group_test?type=heartbeat --project ali-test-project --region cn-hangzhou --consumer consumer_1 --header \"Content-Type=application/json\" --body [0]"
  }
}
