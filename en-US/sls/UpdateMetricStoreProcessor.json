{
  "name": "UpdateMetricStoreProcessor",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/metricstores/[metricstore]/processor",
  "parameters": [
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "metricstore",
      "description": "The name of the Metricstore.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls update-metric-store-processor --project ali-test-project --metricstore ali-test-metricstore --processor-name filter-metrics --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /metricstores/ali-test-metricstore/processor --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"processorName\":\"filter-metrics\"}"
  }
}
