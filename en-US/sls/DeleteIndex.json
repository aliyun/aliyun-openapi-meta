{
  "name": "DeleteIndex",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/logstores/[logstore]/index",
  "parameters": [
    {
      "name": "logstore",
      "description": "The name of the Logstore.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-index --logstore ali-test-logstore --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /logstores/ali-test-logstore/index --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
