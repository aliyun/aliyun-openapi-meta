{
  "name": "UpdateSqlInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/sqlinstance",
  "parameters": [
    {
      "name": "body",
      "description": "The request struct.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "project",
      "description": "The name of the project in which to enable Dedicated SQL.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls update-sql-instance --project test-project --cu 2 --use-as-default true --region cn-hangzhou",
    "legacyCli": "aliyun sls PUT /sqlinstance --project test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"cu\":2,\"useAsDefault\":true}"
  }
}
