{
  "name": "GetCursor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/logstores/[logstore]/shards/[shardId]?type=cursor",
  "parameters": [
    {
      "name": "from",
      "description": "A point in time. This can be a UNIX timestamp or the string `begin` or `end`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "logstore",
      "description": "The logstore name.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The project name.",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "shardId",
      "description": "The shard ID.",
      "position": "Path",
      "type": "Integer",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-cursor --project ali-test-project --logstore sls-test-logstore --shard-id 1 --from begin --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logstores/sls-test-logstore/shards/1?type=cursor --project ali-test-project --region cn-hangzhou --from begin --header \"Content-Type=application/json\""
  }
}
