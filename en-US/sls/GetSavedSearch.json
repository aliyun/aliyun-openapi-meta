{
  "name": "GetSavedSearch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/savedsearches/[savedsearchName]",
  "parameters": [
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "savedsearchName",
      "description": "The name of the saved search.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-saved-search --project aliyun-test-project --savedsearch-name savedsearch-name --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /savedsearches/savedsearch-name --project aliyun-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
