{
  "name": "ListTagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/tags",
  "parameters": [
    {
      "name": "resourceId",
      "description": "The IDs of the resources whose tags you want to query. You must specify at least one of resourceId and tags.",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "resourceType",
      "description": "The type of the resource. Valid values:\n\n*   project\n*   logstore\n*   dashboard\n*   machinegroup\n*   logtailconfig",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "tags",
      "description": "The tags that you want to use to filter resources based on exact match. Each tag is a key-value pair. You must specify at least one of resourceId and tags.\n\nYou can enter up to 20 tags.",
      "position": "Query",
      "type": "Json",
      "required": false,
      "sub_parameters": [
        {
          "name": "key",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "value",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls list-tag-resources --resource-type project --resource-id ali-test-project --tags value=value1 key=key1 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /tags --region cn-hangzhou --resourceId '[\"ali-test-project\"]' --resourceType project --tags '[{\"value\":\"value1\",\"key\":\"key1\"}]' --header \"Content-Type=application/json\""
  }
}
