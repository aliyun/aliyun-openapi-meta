{
  "name": "DeleteSavedSearch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/savedsearches/[savedsearchName]",
  "parameters": [
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "savedsearchName",
      "description": "The name of the saved search to be deleted.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-saved-search --project ali-test-project --savedsearch-name test-savedsearch-name --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /savedsearches/test-savedsearch-name --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
