{
  "name": "GetLogStoreMeteringMode",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/logstores/[logstore]/meteringmode",
  "parameters": [
    {
      "name": "logstore",
      "description": "The name of the Logstore.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-log-store-metering-mode --project projectName --logstore logstoreName --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logstores/logstoreName/meteringmode --project projectName --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
