{
  "name": "ListConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/configs",
  "parameters": [
    {
      "name": "configName",
      "description": "The name of the Logtail configuration, which is used for fuzzy match.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "logstoreName",
      "description": "The name of the Logstore.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "offset",
      "description": "The line from which the query starts. Default value: 0.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "size",
      "description": "The number of entries per page. Maximum value: 500.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls list-config --project ali-test-project --logstore-name ali-test-logstore --offset 0 --size 10 --config-name logtail-config-sample --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /configs --project ali-test-project --region cn-hangzhou --configName 'logtail-config-sample' --offset 0 --size 10 --logstoreName 'ali-test-logstore' --header \"Content-Type=application/json\""
  }
}
