{
  "name": "GetConsumeProcessor",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/consumeprocessors/[processorName]",
  "parameters": [
    {
      "name": "processorName",
      "description": "The identifier of the consume processor.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-consume-processor --project test-project --processor-name consume-processor-1 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /consumeprocessors/consume-processor-1 --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
