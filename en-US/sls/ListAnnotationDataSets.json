{
  "name": "ListAnnotationDataSets",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/ml/annotationdataset",
  "parameters": [
    {
      "name": "offset",
      "description": "The line from which to start the query.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "size",
      "description": "The number of entries to return on each page for a paged query.",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls list-annotation-datasets --offset 0 --size 100 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /ml/annotationdataset --region cn-hangzhou --offset 0 --size 100 --header \"Content-Type=application/json\""
  }
}
