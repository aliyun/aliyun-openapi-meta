{
  "name": "DeleteMachineGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/machinegroups/[machineGroup]",
  "parameters": [
    {
      "name": "machineGroup",
      "description": "The name of the machine group.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-machine-group --project ali-test-project --machine-group test-machine-group-4 --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /machinegroups/test-machine-group-4 --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
