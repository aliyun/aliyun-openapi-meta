{
  "name": "GetAppliedConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/machinegroups/[machineGroup]/configs",
  "parameters": [
    {
      "name": "machineGroup",
      "description": "The name of the machine group.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-applied-configs --project ali-test-project --machine-group test-machine-group --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /machinegroups/test-machine-group/configs --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
