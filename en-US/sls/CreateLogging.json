{
  "name": "CreateLogging",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/logging",
  "parameters": [
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-logging --project ali-test-project --logging-project my-project --logging-details type=consumergroup_log logstore=my-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /logging --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"loggingDetails\":[{\"type\":\"consumergroup_log\",\"logstore\":\"my-logstore\"}],\"loggingProject\":\"my-project\"}"
  }
}
