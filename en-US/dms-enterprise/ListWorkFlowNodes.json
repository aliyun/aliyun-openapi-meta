{
  "name": "ListWorkFlowNodes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SearchName",
      "description": "The name that is used to search for approval nodes.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tid",
      "description": "The ID of the tenant.\n\n> : To view the ID of the tenant, log on to the Data Management (DMS) console and move the pointer over the profile picture in the upper-right corner. For more information, see [Manage DMS tenants](~~181330~~).",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dms-enterprise list-work-flow-nodes --tid -1 --search-name admin --region cn-hangzhou",
    "legacyCli": "aliyun dms-enterprise ListWorkFlowNodes --region cn-hangzhou --SearchName admin --Tid '-1'"
  }
}
