{
  "name": "ListStandardGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Tid",
      "description": "The ID of the tenant.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dms-enterprise list-standard-groups --tid 3000 --region cn-hangzhou",
    "legacyCli": "aliyun dms-enterprise ListStandardGroups --region cn-hangzhou --Tid 3000"
  }
}
