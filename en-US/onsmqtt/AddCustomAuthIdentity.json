{
  "name": "AddCustomAuthIdentity",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientId",
      "description": "The client ID if you set IdentityType to CLIENT.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "IdentityType",
      "description": "The identity type. Valid values:\n\n*   USER\n*   CLIENT",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceId",
      "description": "The ID of the Message Queue for MQTT instance.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "Secret",
      "description": "The AccessKey secret.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "SignMode",
      "description": "The signature verification mode. ORIGIN: compares the password and the AccessKey secret. SIGNED: uses the HMAC_SHA1 algorithm to sign the client ID to obtain a password and then compares the password.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "Username",
      "description": "The username.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun onsmqtt add-custom-auth-identity --instance-id mqtt-cn-xxxx --username test --secret xxxxx --identity-type USER --client-id GID_test@@@test --sign-mode SIGNED --api-version 2020-04-20 --region cn-hangzhou",
    "legacyCli": "aliyun onsmqtt AddCustomAuthIdentity --region cn-hangzhou --Secret xxxxx --IdentityType USER --InstanceId 'mqtt-cn-xxxx' --Username test --SignMode SIGNED --ClientId 'GID_test@@@test'"
  }
}
