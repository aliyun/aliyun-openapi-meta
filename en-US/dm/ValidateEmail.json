{
  "name": "ValidateEmail",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CheckGraylist",
      "description": "Specifies whether to check the graylist. The default value is false. The result is sent through an asynchronous notification message from EventBridge.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Email",
      "description": "The email address to validate.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Timeout",
      "description": "The timeout period. The default value is 60 seconds.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dm validate-email --email xxx@yyy.com --timeout 20 --check-graylist true --probe-type FULL --api-version 2015-11-23 --region cn-hangzhou",
    "legacyCli": "aliyun dm ValidateEmail --region cn-hangzhou --Email 'xxx@yyy.com' --CheckGraylist true --ProbeType FULL --Timeout 20"
  }
}
