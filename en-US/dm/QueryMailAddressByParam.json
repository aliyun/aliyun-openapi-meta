{
  "name": "QueryMailAddressByParam",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyWord",
      "description": "The email address. The length is 1 to 60 characters. It supports digits, letters, periods (.), hyphens (-), and at signs (@).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNo",
      "description": "The current page number. The default value is 1.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page. The default value is 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Sendtype",
      "description": "The type of the sender address. Valid values:\n\n- batch: batch emails\n\n- trigger: triggered emails",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dm query-mail-address-by-param --page-no 1 --page-size 10 --key-word Account+@+domain --sendtype batch --api-version 2015-11-23 --region cn-hangzhou",
    "legacyCli": "aliyun dm QueryMailAddressByParam --region cn-hangzhou --Sendtype batch --PageSize 10 --KeyWord 'Account+@+domain' --PageNo 1"
  }
}
