{
  "name": "DeleteTemplate",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "TemplateId",
      "description": "The ID of the custom transcoding template that is deleted.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}