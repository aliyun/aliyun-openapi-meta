{
  "name": "ImportKeyPair",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyPairName",
      "description": "The name of the key pair. The name must be unique. It must be 2 to 128 characters in length. It must start with a letter and cannot start with http:// or https://. It can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PublicKeyBody",
      "description": "The public key of the key pair.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the enterprise resource group to which the SSH key pair belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags to add to the key pair.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ],
  "example": {
    "unifiedCli": "aliyun ecs import-key-pair --biz-region-id cn-hangzhou --biz-key-pair-name testKeyPairName --public-key-body ABC1234567 --resource-group-id rg-bp67acfmxazb4p0000 --tag Value=TestValue Key=TestKey --region cn-hangzhou",
    "legacyCli": "aliyun ecs ImportKeyPair --region cn-hangzhou --KeyPairName testKeyPairName --Tag.1.Key TestKey --ResourceGroupId 'rg-bp67acfmxazb4p0000' --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --PublicKeyBody ABC1234567"
  }
}
