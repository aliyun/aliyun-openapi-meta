{
  "name": "EndTerminalSession",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "The region ID of the session.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SessionId",
      "description": "The session ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ecs end-terminal-session --biz-region-id cn-hangzhou --session-id s-hz023od0x90000 --region cn-hangzhou",
    "legacyCli": "aliyun ecs EndTerminalSession --region cn-hangzhou --RegionId 'cn-hangzhou' --SessionId 's-hz023od0x90000'"
  }
}
