{
  "name": "ListLayers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/2023-03-30/layers",
  "parameters": [
    {
      "name": "limit",
      "description": "The number of layers that are returned",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "nextToken",
      "description": "The pagination token that is used in the next request to retrieve a new page of results.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "official",
      "description": "Specifies whether the layer is official. Valid values: true and false.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "prefix",
      "description": "The name prefix of the layer.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "public",
      "description": "Specifies whether the layer is public. Valid values: true and false.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun fc list-layers --prefix my-layer --next-token MTIzNCNhYmM= --limit 10 --public true --official true --region cn-hangzhou",
    "legacyCli": "aliyun fc GET /2023-03-30/layers --region cn-hangzhou --public true --prefix 'my-layer' --nextToken 'MTIzNCNhYmM=' --limit 10 --official true --header \"Content-Type=application/json\""
  }
}
