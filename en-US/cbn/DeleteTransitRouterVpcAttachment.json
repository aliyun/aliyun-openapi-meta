{
  "name": "DeleteTransitRouterVpcAttachment",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among all requests. The token can contain only ASCII characters.\n\n> If you do not set this parameter, **ClientToken** is set to the value of **RequestId**. The value of **RequestId** for each API request may be different.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Default values:\n\n*   **false** (default): performs a dry run and sends the request.\n*   **true**: performs a dry run. The system checks the required parameters and request syntax. If the request fails the dry run, an error message is returned. If the request passes the dry run, the system returns the ID of the request.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Force",
      "description": "Specifies whether to forcefully delete the VPC connection. Valid values:\n\n*   **false** (default): checks resources such as associated forwarding correlations and route learning policies that are related to the VPC connection before it is deleted. If such a resource exists, the VPC connection is not deleted and an error message is returned.\n*   **true**: deletes the VPC connection and all resources that are related to the VPC connection.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "TransitRouterAttachmentId",
      "description": "The ID of the VPC connection.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cbn delete-transit-router-vpc-attachment --client-token 02fb3da4-130e-11e9-8e44-0010000 --transit-router-attachment-id tr-attach-ia340z7xis7t5s0000 --dry-run false --biz-force false --region cn-hangzhou",
    "legacyCli": "aliyun cbn DeleteTransitRouterVpcAttachment --region cn-hangzhou --TransitRouterAttachmentId 'tr-attach-ia340z7xis7t5s0000' --Force false --DryRun false --ClientToken '02fb3da4-130e-11e9-8e44-0010000'"
  }
}
