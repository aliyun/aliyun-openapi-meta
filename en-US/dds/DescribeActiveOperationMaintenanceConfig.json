{
  "name": "DescribeActiveOperationMaintenanceConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun dds describe-active-operation-maintenance-config --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeActiveOperationMaintenanceConfig --region cn-hangzhou"
  }
}
