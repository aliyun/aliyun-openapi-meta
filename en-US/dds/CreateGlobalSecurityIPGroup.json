{
  "name": "CreateGlobalSecurityIPGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GIpList",
      "description": "The IP addresses in the whitelist template.\n\n> Separate multiple IP addresses with commas (,). You can create up to 1,000 IP addresses or CIDR blocks for all IP address whitelists.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "GlobalIgName",
      "description": "The name of the IP whitelist template. The name must meet the following requirements:\n\n- It can contain lowercase letters, digits, and underscores (\\_).\n\n- It must start with a letter and end with a letter or digit.\n\n- It must be 2 to 120 characters in length.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance. You can call the [DescribeRegions](~~61933~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dds create-global-security-ip-group --biz-region-id cn-hangzhou --global-ig-name aizy --gip-list 27.18.86.18,183.92.40.129 --region cn-hangzhou",
    "legacyCli": "aliyun dds CreateGlobalSecurityIPGroup --region cn-hangzhou --GlobalIgName aizy --GIpList '27.18.86.18,183.92.40.129' --RegionId 'cn-hangzhou'"
  }
}
