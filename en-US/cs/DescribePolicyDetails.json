{
  "name": "DescribePolicyDetails",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/policies/[policy_name]",
  "parameters": [
    {
      "name": "policy_name",
      "description": "The name of the policy.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}