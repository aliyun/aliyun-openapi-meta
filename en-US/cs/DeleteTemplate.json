{
  "name": "DeleteTemplate",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/templates/[TemplateId]",
  "parameters": [
    {
      "name": "TemplateId",
      "description": "The ID of the orchestration template.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}