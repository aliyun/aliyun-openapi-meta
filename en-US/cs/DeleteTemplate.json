{
  "name": "DeleteTemplate",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/templates/[TemplateId]",
  "parameters": [
    {
      "name": "TemplateId",
      "description": "The orchestration template ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}