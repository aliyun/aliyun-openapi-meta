{
  "name": "UpdateContactGroupForAlert",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/alert/[ClusterId]/alert_rule/contact_groups",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The cluster ID. You can call the ListCluster operation to query the cluster ID.",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ]
}