{
  "name": "GetClusterAuditProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[clusterid]/audit",
  "parameters": [
    {
      "name": "clusterid",
      "description": "Cluster ID",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}