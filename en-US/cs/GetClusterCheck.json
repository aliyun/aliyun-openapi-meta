{
  "name": "GetClusterCheck",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[cluster_id]/checks/[check_id]",
  "parameters": [
    {
      "name": "check_id",
      "description": "The ID of the cluster check task. You can call the [RunClusterCheck](~~2667902~~) operation to obtain the check_id.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "cluster_id",
      "description": "The cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}