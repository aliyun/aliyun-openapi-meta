{
  "name": "CreateKubernetesTrigger",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/triggers",
  "parameters": [
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "Struct",
      "required": false
    }
  ]
}