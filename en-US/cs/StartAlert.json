{
  "name": "StartAlert",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/alert/[ClusterId]/alert_rule/start",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The cluster ID. You can call the ListClusters operation to query the cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}