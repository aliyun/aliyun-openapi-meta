{
  "name": "PauseTask",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/tasks/[task_id]/pause",
  "parameters": [
    {
      "name": "task_id",
      "description": "The task ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}