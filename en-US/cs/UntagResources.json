{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/tags",
  "parameters": [
    {
      "name": "all",
      "description": "Specifies whether to remove all custom labels. This parameter takes effect only when `tag_keys` is left empty. Valid values:\n\n*   `true`: Remove all custom labels.\n*   `false`: Do not remove all custom labels.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "region_id",
      "description": "The region ID of the resources.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "resource_ids",
      "description": "The list of resource IDs.",
      "position": "Query",
      "type": "Json",
      "required": true
    },
    {
      "name": "resource_type",
      "description": "The type of resource. Set the value to `CLUSTER`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "tag_keys",
      "description": "The list of keys of the labels that you want to remove.",
      "position": "Query",
      "type": "Json",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs untag-resources --biz-region-id cn-hangzhou --resource-ids c1a7bb282fc0d48b593000 --resource-type CLUSTER --tag-keys TestKey1 --all true --region cn-hangzhou",
    "legacyCli": "aliyun cs DELETE /tags --region cn-hangzhou --all true --region_id 'cn-hangzhou' --resource_type CLUSTER --resource_ids '[\"c1a7bb282fc0d48b593000\"]' --tag_keys '[\"TestKey1\"]' --header \"Content-Type=application/json\""
  }
}
