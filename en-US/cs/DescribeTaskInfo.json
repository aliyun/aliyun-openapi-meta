{
  "name": "DescribeTaskInfo",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/tasks/[task_id]",
  "parameters": [
    {
      "name": "task_id",
      "description": "The task ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}