{
  "name": "CreateLogConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CreateSlr",
      "description": "Specifies to create a service-linked role.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "LogType",
      "description": "The log type.\n\nValid values:\n\n*   PROVIDER",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SlsLogStore",
      "description": "slslogstore",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SlsProject",
      "description": "The name of the Log Service project.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cloudapi create-log-config --sls-project rec-lq-sls --sls-log-store api-gateway --log-type PROVIDER --create-slr true --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi CreateLogConfig --region cn-hangzhou --SlsLogStore 'api-gateway' --SlsProject 'rec-lq-sls' --CreateSlr true --LogType PROVIDER"
  }
}
