{
  "name": "ListBackupJobs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupMode",
      "description": "The backup mode. Valid values:\n\n*   Automated\n*   Manual\n\nIf you do not specify this parameter, all backup sets are returned.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBInstanceId",
      "description": "The instance ID.\n\n>  You can call the [DescribeDBInstances](~~86911~~) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun gpdb list-backup-jobs --db-instance-id gp-xxxxxxxxx --backup-mode Automated --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ListBackupJobs --region cn-hangzhou --DBInstanceId 'gp-xxxxxxxxx' --BackupMode Automated"
  }
}
