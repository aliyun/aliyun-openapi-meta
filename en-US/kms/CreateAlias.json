{
  "name": "CreateAlias",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "description": "The alias of the CMK.\n\nThe alias must be 1 to 255 characters in length and must contain the prefix `alias/`. The alias cannot be prefixed with the reserved word `alias/acs`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "KeyId",
      "description": "The ID of the CMK. The ID must be globally unique.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms create-alias --key-id 7906979c-8e06-46a2-be2d-68e3ccbc0000 --alias-name alias/example --region cn-hangzhou",
    "legacyCli": "aliyun kms CreateAlias --region cn-hangzhou --AliasName 'alias/example' --KeyId '7906979c-8e06-46a2-be2d-68e3ccbc0000'"
  }
}
