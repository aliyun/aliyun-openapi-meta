{
  "name": "DeleteNetworkRule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "The name of the network access rule that you want to delete.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms delete-network-rule --name networkrule_test --region cn-hangzhou",
    "legacyCli": "aliyun kms DeleteNetworkRule --region cn-hangzhou --Name networkrule_test"
  }
}
