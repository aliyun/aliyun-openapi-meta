{
  "name": "RestoreSecret",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SecretName",
      "description": "The name of the secret you want to restore.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms restore-secret --secret-name secret001 --region cn-hangzhou",
    "legacyCli": "aliyun kms RestoreSecret --region cn-hangzhou --SecretName secret001"
  }
}
