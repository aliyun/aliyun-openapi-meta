{
  "name": "DescribeNetworkRule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "The name of the access control rule that you want to query.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms describe-network-rule --name networkrule_test --region cn-hangzhou",
    "legacyCli": "aliyun kms DescribeNetworkRule --region cn-hangzhou --Name networkrule_test"
  }
}
