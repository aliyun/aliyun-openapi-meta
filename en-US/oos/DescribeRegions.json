{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The supported natural language. Valid values:\n\n*   zh-CN: Chinese\n*   en-US: English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun oos describe-regions --biz-region-id cn-hangzhou --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun oos DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN' --RegionId 'cn-hangzhou'"
  }
}
