{
  "name": "DeleteTemplates",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoDeleteExecutions",
      "description": "Specifies whether to delete the related executions when a template is deleted.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TemplateNames",
      "description": "The names of the templates to be deleted.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun oos delete-templates --biz-region-id cn-hangzhou --template-names t1_,_t2 --auto-delete-executions false --region cn-hangzhou",
    "legacyCli": "aliyun oos DeleteTemplates --region cn-hangzhou --TemplateNames 't1_,_t2' --AutoDeleteExecutions false --RegionId 'cn-hangzhou'"
  }
}
