{
  "name": "CreateBackupPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupRetentionPeriod",
      "description": "The number of days for which you can retain the backup data.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBInstanceId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PreferredBackupPeriod",
      "description": "The backup cycle, which indicates the day of the week when the system regularly backs up data. Separate multiple dates with commas (`,`).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PreferredBackupTime",
      "description": "The backup time window within which the backup task is performed. The time is displayed in `UTC`. For example, `12:00Z-13:00Z` indicates that the backup time window ranges from `12:00` (UTC) to `13:00` `(UTC)`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun clickhouse create-backup-policy --db-instance-id cc-2ze0eb0w182xh8549 --biz-region-id cn-hangzhou --preferred-backup-time 10:00Z-11:00Z --preferred-backup-period Monday,Friday --backup-retention-period 8 --api-version 2023-05-22 --region cn-hangzhou",
    "legacyCli": "aliyun clickhouse CreateBackupPolicy --region cn-hangzhou --PreferredBackupPeriod 'Monday,Friday' --DBInstanceId 'cc-2ze0eb0w182xh8549' --PreferredBackupTime '10:00Z-11:00Z' --RegionId 'cn-hangzhou' --BackupRetentionPeriod 8"
  }
}
