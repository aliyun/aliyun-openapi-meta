{
  "name": "UpdateCnameFlattening",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FlattenMode",
      "description": "The CNAME flattening mode. Valid values:\n\n*   flatten_all: flattens all CNAMEs.\n*   flatten_at_root: flattens only the root domain. Default: flatten_at_root",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SiteId",
      "description": "The website ID, which can be obtained by calling the [ListSites](~~2850189~~) operation.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun esa update-cname-flattening --site-id 1234560000 --flatten-mode flatten_all --region cn-hangzhou",
    "legacyCli": "aliyun esa UpdateCnameFlattening --region cn-hangzhou --SiteId 1234560000 --FlattenMode flatten_all"
  }
}
