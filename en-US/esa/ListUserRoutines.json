{
  "name": "ListUserRoutines",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "description": "The page number of the returned page. Default value: 1. Valid values: 1 to 10.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page. Valid values: 1 to 20.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "SearchKeyWord",
      "description": "The keyword used for fuzzy search.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun esa list-user-routines --page-number 1 --page-size 20 --search-key-word hello --region cn-hangzhou",
    "legacyCli": "aliyun esa ListUserRoutines --region cn-hangzhou --PageSize 20 --PageNumber 1 --SearchKeyWord hello"
  }
}
