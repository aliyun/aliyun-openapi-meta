{
  "name": "DescribeCertificateInfoByID",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertId",
      "description": "The ID of the certificate. You can query only one certificate in each call.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-certificate-info-by-id --cert-id 1644xx --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCertificateInfoByID --region cn-hangzhou --CertId 1644xx"
  }
}
