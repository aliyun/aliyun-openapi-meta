{
  "name": "DescribeStagingIp",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun cdn describe-staging-ip --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeStagingIp --region cn-hangzhou"
  }
}
