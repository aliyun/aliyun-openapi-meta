{
  "name": "DescribeDomainCertificateInfo",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-certificate-info --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainCertificateInfo --region cn-hangzhou --DomainName 'example.com'"
  }
}
