{
  "name": "DeleteFCTrigger",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "TriggerARN",
      "description": "The trigger that corresponds to the Function Compute service.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}