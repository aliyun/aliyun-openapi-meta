{
  "name": "DeleteSpecificConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigId",
      "description": "The ID of the configuration. Separate multiple configuration IDs with commas (,). For more information about ConfigId, see [Usage notes on ConfigId](~~388994~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn delete-specific-config --domain-name example.com --config-id 2317 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DeleteSpecificConfig --region cn-hangzhou --DomainName 'example.com' --ConfigId 2317"
  }
}
