{
  "name": "PublishStagingConfigToProduction",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each call.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn publish-staging-config-to-production --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn PublishStagingConfigToProduction --region cn-hangzhou --DomainName 'example.com'"
  }
}
