{
  "name": "DescribeCdnHttpsDomainList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "description": "The keyword that is used to search for certificates.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The number of the page to return. Valid values: **1** to **100000**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Default value: **20**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-https-domain-list --page-number 5 --page-size 20 --keyword com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnHttpsDomainList --region cn-hangzhou --PageSize 20 --Keyword com --PageNumber 5"
  }
}
