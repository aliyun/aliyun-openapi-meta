{
  "name": "DescribeCdnFullDomainsBlockIPConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IPList",
      "description": "The IP address or CIDR block to query. Separate multiple values with commas (,). You can specify up to 50 IP addresses or CIDR blocks.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-full-domains-block-ip-config --ip-list 1.XXX.XXX.1,2.XXX.XXX.2 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnFullDomainsBlockIPConfig --region cn-hangzhou --IPList '1.XXX.XXX.1,2.XXX.XXX.2'"
  }
}
