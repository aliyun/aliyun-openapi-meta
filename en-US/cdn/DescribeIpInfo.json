{
  "name": "DescribeIpInfo",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IP",
      "description": "The IP address. You can specify only one IP address.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-ip-info --ip 192.168.0.1 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeIpInfo --region cn-hangzhou --IP '192.168.0.1'"
  }
}
