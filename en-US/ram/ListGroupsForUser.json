{
  "name": "ListGroupsForUser",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "description": "The name of the RAM user.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram list-groups-for-user --user-name Alice --region cn-hangzhou",
    "legacyCli": "aliyun ram ListGroupsForUser --region cn-hangzhou --UserName Alice"
  }
}
