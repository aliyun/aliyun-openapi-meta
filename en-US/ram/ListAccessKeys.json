{
  "name": "ListAccessKeys",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "description": "The name of the RAM user. If a RAM user calls this operation and does not specify this parameter, the AccessKey pairs of the RAM user are returned.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram list-access-keys --user-name zhangq0000 --region cn-hangzhou",
    "legacyCli": "aliyun ram ListAccessKeys --region cn-hangzhou --UserName zhangq0000"
  }
}
