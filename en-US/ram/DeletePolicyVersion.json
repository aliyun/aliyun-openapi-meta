{
  "name": "DeletePolicyVersion",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "description": "The name of the policy.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VersionId",
      "description": "The ID of the policy version that you want to delete.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram delete-policy-version --policy-name OSS-Administrator --version-id v3 --region cn-hangzhou",
    "legacyCli": "aliyun ram DeletePolicyVersion --region cn-hangzhou --VersionId v3 --PolicyName 'OSS-Administrator'"
  }
}
