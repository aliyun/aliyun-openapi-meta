{
  "name": "CreateLoginProfile",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MFABindRequired",
      "description": "Specifies whether the RAM user must bind a multi-factor authentication (MFA) device upon the next logon. Default value: `false`.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Password",
      "description": "The logon password of the RAM user. The password must meet the password strength requirements. For more information, see [GetPasswordPolicy](~~2337691~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PasswordResetRequired",
      "description": "Specifies whether the RAM user has to change the password upon logon. Default value: `false`.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "UserName",
      "description": "The name of the RAM user.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram create-login-profile --user-name zhangq0000 --password mypassword --password-reset-required false --mfa-bind-required false --region cn-hangzhou",
    "legacyCli": "aliyun ram CreateLoginProfile --region cn-hangzhou --PasswordResetRequired false --UserName zhangq0000 --MFABindRequired false --Password mypassword"
  }
}
