{
  "name": "DeleteDcdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name to be deleted. You can specify only one domain name.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn delete-dcdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteDcdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
