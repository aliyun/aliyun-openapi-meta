{
  "name": "ModifyDcdnWafPolicy",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyId",
      "description": "The ID of the protection policy that you want to modify. You can specify only one ID in each request.",
      "position": "Body",
      "type": "Long",
      "required": true
    },
    {
      "name": "PolicyName",
      "description": "The new name of the protection policy.\n\n> You must specify PolicyName or PolicyStatus.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "PolicyStatus",
      "description": "The new status of the protection policy. Valid values:\n\n*   **on**\n*   **off**\n\n> You must specify PolicyName or PolicyStatus.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn modify-dcdn-waf-policy --policy-id 1000001 --policy-name policy_test --policy-status on --region cn-hangzhou",
    "legacyCli": "aliyun dcdn ModifyDcdnWafPolicy --region cn-hangzhou --PolicyName policy_test --PolicyStatus on --PolicyId 1000001"
  }
}
