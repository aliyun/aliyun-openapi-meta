{
  "name": "DeleteDcdnIpaSpecificConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigId",
      "description": "The ID of the configuration. You can call the [DescribeDcdnDomainConfigs](~~130625~~) operation to query configuration IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn delete-dcdn-ipa-specific-config --domain-name example.com --config-id 5003500 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteDcdnIpaSpecificConfig --region cn-hangzhou --DomainName 'example.com' --ConfigId 5003500"
  }
}
