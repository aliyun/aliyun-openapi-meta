{
  "name": "PublishDcdnStagingConfigToProduction",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "FunctionName",
      "description": "The name of the feature.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn publish-dcdn-staging-config-to-production --domain-name example.com --function-name aliauth --region cn-hangzhou",
    "legacyCli": "aliyun dcdn PublishDcdnStagingConfigToProduction --region cn-hangzhou --FunctionName aliauth --DomainName 'example.com'"
  }
}
