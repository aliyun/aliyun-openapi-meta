{
  "name": "DescribeDcdnDomainConfigs",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigId",
      "description": "The ID of the configuration.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "FunctionNames",
      "description": "The names of the features to query. Separate features with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-configs --domain-name example.com --function-names filetype_based_ttl_set,set_req_host_header --config-id 5003576 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainConfigs --region cn-hangzhou --DomainName 'example.com' --ConfigId 5003576 --FunctionNames 'filetype_based_ttl_set,set_req_host_header'"
  }
}
