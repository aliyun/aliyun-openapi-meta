{
  "name": "DescribeDcdnDeliverList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeliverId",
      "description": "The ID of the tracking task that you want to query. If you do not specify an ID, all tracking tasks are queried.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-deliver-list --deliver-id 92 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDeliverList --region cn-hangzhou --DeliverId 92"
  }
}
