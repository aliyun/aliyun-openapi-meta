{
  "name": "DescribeDcdnDomainRealTimeByteHitRateData",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The domain name. Separate multiple domain names with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "EndTime",
      "description": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe end time must be later than the start time.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-real-time-byte-hit-rate-data --domain-name example.com --start-time 2017-12-10T20:00:00Z --end-time 2017-12-10T21:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainRealTimeByteHitRateData --region cn-hangzhou --EndTime '2017-12-10T21:00:00Z' --DomainName 'example.com' --StartTime '2017-12-10T20:00:00Z'"
  }
}
