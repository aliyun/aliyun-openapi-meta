{
  "name": "DeleteDcdnIpaDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name that you want to delete. You can specify only one accelerated domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn delete-dcdn-ipa-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteDcdnIpaDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
