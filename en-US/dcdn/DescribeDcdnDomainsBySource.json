{
  "name": "DescribeDcdnDomainsBySource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Sources",
      "description": "The list of origin servers. Separate origin servers with commas (,). You can specify a maximum of 20 origin servers. Fuzzy match is not supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domains-by-source --sources example.org --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainsBySource --region cn-hangzhou --Sources 'example.org'"
  }
}
