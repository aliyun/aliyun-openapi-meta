{
  "name": "ReleasePrePaidInstance",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the instance to be deleted. You can specify only one instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens release-pre-paid-instance --instance-id i-instance0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens ReleasePrePaidInstance --region cn-hangzhou --InstanceId 'i-instance0000'"
  }
}
