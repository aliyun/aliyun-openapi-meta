{
  "name": "DeleteBucket",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "The name of the bucket that you want to delete. You can delete only one bucket at a time.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens delete-bucket --bucket-name test --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteBucket --region cn-hangzhou --BucketName test"
  }
}
