{
  "name": "AccosicateNetworkAcl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "NetworkAclId",
      "description": "The ID of the network ACL.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Resource",
      "description": "The type of resource with which you want to associate the network ACL.",
      "position": "Query",
      "type": "Array",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens accosicate-network-acl --network-acl-id nacl-a2do9e413e0sp0000 --resource ResourceId=n-50000 ResourceType=Network --region cn-hangzhou",
    "legacyCli": "aliyun ens AccosicateNetworkAcl --region cn-hangzhou --Resource.1.ResourceId 'n-50000' --NetworkAclId 'nacl-a2do9e413e0sp0000' --Resource.1.ResourceType Network --method POST --force"
  }
}
