{
  "name": "RemoveInstanceSDG",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceIds",
      "description": "The IDs of the instances. The value is a JSON array that consists of up to 100 IDs.",
      "position": "Query",
      "type": "Json",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens remove-instance-sdg --instance-ids aic-instance0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens RemoveInstanceSDG --region cn-hangzhou --InstanceIds '[\"aic-instance0000\"]'"
  }
}
