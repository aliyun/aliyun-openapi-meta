{
  "name": "ModifyVSwitchAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "description": "The description of the listener.\n\n*   The description must be 2 to 256 characters in length.\n*   It must start with a letter but cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VSwitchId",
      "description": "The ID of the vSwitch.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VSwitchName",
      "description": "The name of the vSwitch.\n\n*   The name must be 2 to 128 characters in length and can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).\n*   It must start with a letter but cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens modify-vswitch-attribute --vswitch-id n-0000 --vswitch-name Test-switch --description this_is_my_first_vswitch --enable-ipv6 true --ipv6-cidr-block 2408:XXXX:0:a600::/110 --region cn-hangzhou",
    "legacyCli": "aliyun ens ModifyVSwitchAttribute --region cn-hangzhou --Description this_is_my_first_vswitch --VSwitchId 'n-0000' --VSwitchName 'Test-switch' --Ipv6CidrBlock '2408:XXXX:0:a600::/110' --EnableIPv6 true"
  }
}
