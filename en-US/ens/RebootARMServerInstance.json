{
  "name": "RebootARMServerInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ServerId",
      "description": "The ID of the server.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens reboot-arm-server-instance --server-id yourInstance_ID --region cn-hangzhou",
    "legacyCli": "aliyun ens RebootARMServerInstance --region cn-hangzhou --ServerId yourInstance_ID"
  }
}
