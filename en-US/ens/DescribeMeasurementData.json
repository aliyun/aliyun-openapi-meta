{
  "name": "DescribeMeasurementData",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndDate",
      "description": "The end of the time range to query. Specify the time in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StartDate",
      "description": "The beginning of the time range to query. Specify the time in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens describe-measurement-data --start-date 2019-06-01T00:00:00Z --end-date 2019-08-30T00:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeMeasurementData --region cn-hangzhou --StartDate '2019-06-01T00:00:00Z' --EndDate '2019-08-30T00:00:00Z'"
  }
}
