{
  "name": "DescribeSDGs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceIds",
      "description": "The AIC instance ID to be queried.",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "SDGIds",
      "description": "The IDs of SDGs that you want to query. By default, all SDGs are queried.",
      "position": "Query",
      "type": "Json",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens describe-sdgs --sdg-ids sdg-1 --instance-ids aic-1 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeSDGs --region cn-hangzhou --SDGIds '[\"sdg-1\"]' --InstanceIds '[\"aic-1\"]'"
  }
}
