{
  "name": "GetOssStorageAndAccByBuckets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketList",
      "description": "The information about the bucket.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens get-oss-storage-and-acc-by-buckets --bucket-list my-bucket --region cn-hangzhou",
    "legacyCli": "aliyun ens GetOssStorageAndAccByBuckets --region cn-hangzhou --BucketList 'my-bucket'"
  }
}
