{
  "name": "ReleaseAICInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ServerId",
      "description": "The ID of the server.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens release-aic-instance --server-id cas-instance0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens ReleaseAICInstance --region cn-hangzhou --ServerId 'cas-instance0000'"
  }
}
