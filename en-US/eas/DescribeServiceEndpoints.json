{
  "name": "DescribeServiceEndpoints",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v2/services/[ClusterId]/[ServiceName]/endpoints",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The region ID.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "ServiceName",
      "description": "The service name. For more information about how to obtain the service name, see [ListServices](~~412109~~).",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eas describe-service-endpoints --cluster-id cn-hangzhou --service-name service_name --region cn-hangzhou",
    "legacyCli": "aliyun eas GET /api/v2/services/cn-hangzhou/service_name/endpoints --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
