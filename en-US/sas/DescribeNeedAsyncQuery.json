{
  "name": "DescribeNeedAsyncQuery",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Type",
      "description": "The type of the query. Valid values:\n\n*   **suspicious**: alerts",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas describe-need-async-query --type suspicious --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeNeedAsyncQuery --region cn-hangzhou --Type suspicious"
  }
}
