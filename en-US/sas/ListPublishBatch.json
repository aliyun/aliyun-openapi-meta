{
  "name": "ListPublishBatch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BatchName",
      "description": "The name of the release batch.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CurrentPage",
      "description": "The page number.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "UpgradeVersion",
      "description": "The destination version of the Security Center agent.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas list-publish-batch --upgrade-version 0.0.9 --current-page 1 --page-size 20 --batch-name '批次1' --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListPublishBatch --region cn-hangzhou --BatchName '批次1' --PageSize 20 --CurrentPage 1 --UpgradeVersion '0.0.9'"
  }
}
