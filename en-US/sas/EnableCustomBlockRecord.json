{
  "name": "EnableCustomBlockRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BlockIp",
      "description": "The IP address that is specified in the policy.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Bound",
      "description": "The traffic direction that is specified in the policy. Valid values:\n\n*   **in**: inbound\n*   **out**: outbound",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas enable-custom-block-record --block-ip 43.248.XX.XX --bound in --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas EnableCustomBlockRecord --region cn-hangzhou --BlockIp '43.248.XX.XX' --Bound in"
  }
}
