{
  "name": "DescribeScanTaskStatistics",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Levels",
      "description": "The severities of the alert events handled by the virus detection task. Separate multiple severities with commas (,). The severities decrease in descending order. Valid values:\n\n*   **serious**\n*   **suspicious**\n*   **remind**",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas describe-scan-task-statistics --levels serious,suspicious,remind --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeScanTaskStatistics --region cn-hangzhou --Levels 'serious,suspicious,remind'"
  }
}
