{
  "name": "DescribeBackupClients",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SupportRegionId",
      "description": "The region in which the anti-ransomware feature is supported.\n> You can call the [DescribeSupportRegion](~~DescribeSupportRegion~~) operation to query the regions in which the anti-ransomware feature is supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas describe-backup-clients --support-region-id cn-hangzhou --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeBackupClients --region cn-hangzhou --SupportRegionId 'cn-hangzhou'"
  }
}
