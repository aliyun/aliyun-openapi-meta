{
  "name": "ModifySoarStrategySubscribe",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "StrategyId",
      "description": "The ID of the policy.\n\n>  You can call the [DescribeSoarStrategies](~~DescribeSoarStrategies~~) operation to obtain the ID.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "SubscribeStatus",
      "description": "Specifies whether to create the policy template. Valid values:\n\n*   true: creates the policy template\n*   false: deletes the policy template",
      "position": "Query",
      "type": "Boolean",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas modify-soar-strategy-subscribe --strategy-id 8000 --subscribe-status true --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ModifySoarStrategySubscribe --region cn-hangzhou --SubscribeStatus true --StrategyId 8000"
  }
}
