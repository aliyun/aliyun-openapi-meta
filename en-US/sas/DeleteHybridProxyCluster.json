{
  "name": "DeleteHybridProxyCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "description": "The name of the proxy cluster.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas delete-hybrid-proxy-cluster --cluster-name proxy --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DeleteHybridProxyCluster --region cn-hangzhou --ClusterName proxy"
  }
}
