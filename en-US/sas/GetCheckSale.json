{
  "name": "GetCheckSale",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "The region in which the Security Center instance is deployed. Valid values:\n\n*   **cn-hangzhou:** inside China\n*   Global **ap-southeast-1:** outside China",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas get-check-sale --biz-region-id cn-hangzhou --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas GetCheckSale --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
