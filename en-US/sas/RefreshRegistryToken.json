{
  "name": "RefreshRegistryToken",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegistryId",
      "description": "The ID of the image repository.\n\n>  You can call the [PageImageRegistry](~~PageImageRegistry~~) operation to obtain the ID.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas refresh-registry-token --registry-id 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas RefreshRegistryToken --region cn-hangzhou --RegistryId 1"
  }
}
