{
  "name": "AddContainerPluginRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "description": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Mode",
      "description": "The action that you want to specify for the rule. Valid values:\n\n*   **1**: triggers alerts.\n*   **2**: blocks escapes.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "RuleName",
      "description": "The name of the rule. The name must be 1 to 80 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), and underscores (\\_). The names of rules that are created for the same user must be unique.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RuleTemplateId",
      "description": "The ID of the rule template. You can call the ListSystemClientRules operation to query the ID of the rule template.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "RuleType",
      "description": "The type of the rule. Valid values:\n\n*   **0**: custom rule\n*   **1**: system rule",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SelectedPolicy",
      "description": "The check items that are enabled for the rule.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "WhiteImages",
      "description": "The images that are added to the whitelist.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas add-container-plugin-rule --lang zh --rule-name tyest111 --rule-template-id 86863 --biz-mode 1 --selected-policy system_triggered_task --rule-type 0 --white-images centos --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas AddContainerPluginRule --region cn-hangzhou --WhiteImages.1 centos --RuleTemplateId 86863 --Mode 1 --Lang zh --SelectedPolicy.1 system_triggered_task --RuleType 0 --RuleName tyest111"
  }
}
