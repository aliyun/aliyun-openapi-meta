{
  "name": "GetSecurityScoreRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CalType",
      "description": "The old or new version of the security score rule. If you set this parameter to **home_security_score**, the new version of the security score rule is returned. Otherwise, the old version of the security score rule is returned by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Lang",
      "description": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese.\n*   **en**: English.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas get-security-score-rule --lang zh --cal-type home_security_score --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas GetSecurityScoreRule --region cn-hangzhou --CalType home_security_score --Lang zh"
  }
}
