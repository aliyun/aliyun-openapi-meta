{
  "name": "GetBinding",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DatasetName",
      "description": "The name of the dataset. You can obtain the name of the dataset from the response of the [CreateDataset](~~478160~~) operation.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProjectName",
      "description": "The name of the project. You can obtain the name of the project from the response of the [CreateProject](~~478153~~) operation.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "URI",
      "description": "The URI of the OSS bucket to which you bind the dataset.\n\nSpecify the value in the oss://${Bucket} format. `${Bucket}` specifies the name of the OSS bucket that resides in the same region as the current project.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm get-binding --project-name test-project --dataset-name test-dataset --uri oss://test-bucket --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm GetBinding --region cn-hangzhou --ProjectName 'test-project' --DatasetName 'test-dataset' --URI 'oss://test-bucket'"
  }
}
