{
  "name": "CreateVideoModerationTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CredentialConfig",
      "description": "The authorization chain settings. For more information, see [Use authorization chains to access resources of other entities](~~465340~~).",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "Interval",
      "description": "The interval of capturing video frames. Unit: seconds. Valid values: 1 to 600. Default value: 1.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "MaxFrames",
      "description": "The maximum number of frames that can be captured from the video. Valid values: 5 to 3600. Default value: 200.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "Notification",
      "description": "The notification settings. For information about the asynchronous notification format, see [Asynchronous message examples](~~2743997~~).",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "ProjectName",
      "description": "The name of the project.[](~~478153~~)",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Scenes",
      "description": "The scenarios of video moderation.",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "SourceURI",
      "description": "The OSS URI of the video.\n\nSpecify the OSS URI in the oss://${Bucket}/${Object} format, where `${Bucket}` is the name of the bucket in the same region as the current project and `${Object}` is the path of the object with the extension included.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Tags",
      "description": "The custom tags. The custom tags help you retrieve the task.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserData",
      "description": "The custom information, which is returned in an asynchronous notification and facilitates notification management. The maximum length of the value is 2,048 bytes.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm create-video-moderation-task --project-name immtest --user-data ID_:_user1_,_Name_:_test-user1_,_Avatar_:_http://example.com0id=user1 --source-uri oss://test-bucket/test-object --tags test=val1 --credential-config example-string --scenes porn --interval 1 --max-frames 200 --notification example-string --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm CreateVideoModerationTask --region cn-hangzhou --MaxFrames 200 --Scenes '[\"porn\"]' --ProjectName immtest --UserData 'ID_:_user1_,_Name_:_test-user1_,_Avatar_:_http://example.com0id=user1' --CredentialConfig 'example-string' --SourceURI 'oss://test-bucket/test-object' --Tags '{\"test\":\"val1\"}' --Notification 'example-string' --Interval 1"
  }
}
