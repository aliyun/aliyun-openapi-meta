{
  "name": "DeleteFileMeta",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DatasetName",
      "description": "The name of the dataset. For more information, see [Create a dataset](~~478160~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ProjectName",
      "description": "The name of the project. For more information, see [CreateProject](~~478153~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "URI",
      "description": "The URI of the file in OSS.\n\nSpecify the value in the oss://${Bucket}/${Object} format. `${Bucket}` specifies the name of the OSS bucket that resides in the same region as the current project. `${Object}` specifies the path of the object with the extension included.\n\nThe URI of the file in Photo and Drive Service must be in the pds://domains/${domain}/drives/${drive}/files/${file}/revisions/${revision} format.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm delete-file-meta --project-name test-project --dataset-name test-datset --uri oss://test-bucket/test-object --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm DeleteFileMeta --region cn-hangzhou --ProjectName 'test-project' --DatasetName 'test-datset' --URI 'oss://test-bucket/test-object'"
  }
}
