{
  "name": "DeleteDBInstanceEndpoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotency of requests. You can use the client to generate the value, but you must ensure that it is unique among different requests. The token can only contain ASCII characters and cannot exceed 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBInstanceEndpointId",
      "description": "The endpoint ID of the instance. You can call the DescribeDBInstanceEndpoints operation to query the endpoint ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBInstanceId",
      "description": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds delete-db-instance-endpoint --client-token 0c593ea1-3bea-11e9-b96b-880000 --db-instance-id rm-0000 --db-instance-endpoint-id ep-0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DeleteDBInstanceEndpoint --region cn-hangzhou --DBInstanceId 'rm-0000' --ClientToken '0c593ea1-3bea-11e9-b96b-880000' --DBInstanceEndpointId 'ep-0000'"
  }
}
