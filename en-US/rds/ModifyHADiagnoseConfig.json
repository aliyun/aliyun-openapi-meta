{
  "name": "ModifyHADiagnoseConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "description": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID. You can call the DescribeRegions operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TcpConnectionType",
      "description": "The availability check method of the instance. Valid values:\n\n*   **SHORT**: Alibaba Cloud uses short-lived connections to check the availability of the instance.\n*   **LONG**: Alibaba Cloud uses persistent connections to check the availability of the instance.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds modify-ha-diagnose-config --db-instance-id rm-uf6wjk50000 --tcp-connection-type SHORT --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds ModifyHADiagnoseConfig --region cn-hangzhou --DBInstanceId 'rm-uf6wjk50000' --TcpConnectionType SHORT --RegionId 'cn-hangzhou'"
  }
}
