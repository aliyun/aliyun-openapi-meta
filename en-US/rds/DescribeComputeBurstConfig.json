{
  "name": "DescribeComputeBurstConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of requests and prevent repeated requests from being submitted. You can use the client to generate the value, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBInstanceId",
      "description": "The instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The ID of the region in which the instance resides.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The resource group ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds describe-compute-burst-config --client-token ETnLKlblzczshOTUbOCzxxxxxxxxxx --db-instance-id pgm-2ze63v2p3o3k0000 --resource-group-id rg-acfmy00000 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeComputeBurstConfig --region cn-hangzhou --ResourceGroupId 'rg-acfmy00000' --DBInstanceId 'pgm-2ze63v2p3o3k0000' --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCzxxxxxxxxxx"
  }
}
