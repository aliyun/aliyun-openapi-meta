{
  "name": "DescribeIngress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/ingress/Ingress",
  "parameters": [
    {
      "name": "IngressId",
      "description": "The ID of the routing rule to be queried.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sae describe-ingress --ingress-id 16263 --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/ingress/Ingress --region cn-hangzhou --IngressId 16263 --header \"Content-Type=application/json\""
  }
}
