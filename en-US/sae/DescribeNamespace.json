{
  "name": "DescribeNamespace",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/paas/namespace",
  "parameters": [
    {
      "name": "NameSpaceShortId",
      "description": "The short ID of the namespace. You do not need to specify a region ID. The value of this parameter can be up to 20 characters in length and can contain only lowercase letters and digits.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NamespaceId",
      "description": "The ID of the namespace. The information about the default namespace cannot be queried or modified. The default namespace cannot be deleted.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sae describe-namespace --namespace-id cn-beijing:test --name-space-short-id test --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/paas/namespace --region cn-hangzhou --NameSpaceShortId test --NamespaceId 'cn-beijing:test' --header \"Content-Type=application/json\""
  }
}
