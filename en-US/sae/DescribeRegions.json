{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/paas/regionConfig",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun sae describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/paas/regionConfig --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
