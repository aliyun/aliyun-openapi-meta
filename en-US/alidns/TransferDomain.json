{
  "name": "TransferDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "description": "The domain names. Separate multiple domain names with commas (,). Only domain names registered with Alibaba Cloud are supported.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Lang",
      "description": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Remark",
      "description": "The description of the domain name.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TargetUserId",
      "description": "The destination user ID. The domain names and their Domain Name System (DNS) records are transferred to the destination user ID.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns transfer-domain --lang en --domain-names example.com,example.net --remark test_domain_transfer --target-user-id 120000000 --region cn-hangzhou",
    "legacyCli": "aliyun alidns TransferDomain --region cn-hangzhou --Lang en --DomainNames 'example.com,example.net' --Remark test_domain_transfer --TargetUserId 120000000"
  }
}
