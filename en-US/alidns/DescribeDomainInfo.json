{
  "name": "DescribeDomainInfo",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The domain name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Lang",
      "description": "The language type.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NeedDetailAttributes",
      "description": "Specifies whether detailed attributes are required. Default value: **false**, which indicates that detailed attributes are not returned.\n\nIf you set this parameter to **true**, the values of the following parameters are returned: LineType, MinTtl, RecordLineTreeJson, RecordLines, LineCode, LineDisplayName, LineName, RegionLines, and SlaveDns.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns describe-domain-info --lang en --domain-name example.com --need-detail-attributes true --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeDomainInfo --region cn-hangzhou --NeedDetailAttributes true --DomainName 'example.com' --Lang en"
  }
}
