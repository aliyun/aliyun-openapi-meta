{
  "name": "DeleteLiveEdgeTransfer",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The ingest domain. Only one stream relay configuration can be set for an ingest domain.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}