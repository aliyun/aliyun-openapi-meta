{
  "name": "DescribeLiveStreamsBlockList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PageNum",
      "description": "The number of the page to return. Default value: **1**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Valid values: 1 to 3000. Default value: 2000.",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live describe-live-streams-block-list --domain-name example.com --page-num 2 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveStreamsBlockList --region cn-hangzhou --PageNum 2 --PageSize 10 --DomainName 'example.com'"
  }
}
