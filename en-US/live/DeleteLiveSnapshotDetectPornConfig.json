{
  "name": "DeleteLiveSnapshotDetectPornConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "The name of the application to which the live stream belongs. You can call the [DescribeLiveSnapshotDetectPornConfig](~~2847918~~) operation to query the application name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-snapshot-detect-porn-config --domain-name example.com --app-name liveApp0000 --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveSnapshotDetectPornConfig --region cn-hangzhou --DomainName 'example.com' --AppName liveApp0000"
  }
}
