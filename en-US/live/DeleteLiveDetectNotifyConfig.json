{
  "name": "DeleteLiveDetectNotifyConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-detect-notify-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveDetectNotifyConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}
