{
  "name": "AddLiveDetectNotifyConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NotifyUrl",
      "description": "The callback URL that is used to receive callback notifications about violations, such as pornographic content and politically sensitive content, detected in live streams.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live add-live-detect-notify-config --domain-name example.com --notify-url http://demo.aliyundoc.com/examplecallback.action --region cn-hangzhou",
    "legacyCli": "aliyun live AddLiveDetectNotifyConfig --region cn-hangzhou --DomainName 'example.com' --NotifyUrl 'http://demo.aliyundoc.com/examplecallback.action'"
  }
}
