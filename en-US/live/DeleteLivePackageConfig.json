{
  "name": "DeleteLivePackageConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "App name, `*` matches all names.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "Live streaming domain (primary playback domain).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StreamName",
      "description": "Live stream name, `*` matches all streams under AppName.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-package-config --biz-region-id cn-hangzhou --domain-name example.com --app-name AppName --stream-name StreamName --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLivePackageConfig --region cn-hangzhou --StreamName StreamName --DomainName 'example.com' --RegionId 'cn-hangzhou' --AppName AppName"
  }
}
