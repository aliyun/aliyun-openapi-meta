{
  "name": "DeleteLivePrivateLine",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccelerationType",
      "description": "The acceleration type. Valid values:\n\n*   play: streaming acceleration\n*   publish: stream ingest acceleration",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AppName",
      "description": "The name of the application.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StreamName",
      "description": "The name of the live stream.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-private-line --biz-region-id cn-hangzhou --domain-name demo.aliyundoc.com --app-name live --stream-name testStream --acceleration-type play --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLivePrivateLine --region cn-hangzhou --StreamName testStream --DomainName 'demo.aliyundoc.com' --AccelerationType play --RegionId 'cn-hangzhou' --AppName live"
  }
}
