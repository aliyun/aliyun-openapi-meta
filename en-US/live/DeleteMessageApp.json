{
  "name": "DeleteMessageApp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "The ID of the interactive messaging application that you want to delete. You can specify only one ID.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-message-app --app-id VKL3000 --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteMessageApp --region cn-hangzhou --AppId VKL3000"
  }
}
