{
  "name": "EnableServiceAccess",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun ros enable-service-access --region cn-hangzhou",
    "legacyCli": "aliyun ros EnableServiceAccess --region cn-hangzhou"
  }
}
