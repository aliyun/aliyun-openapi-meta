{
  "name": "DescribeZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The supported language. Valid values:\n\n*   **zh-CN** (default): Chinese\n*   **en-US**: English\n*   **ja**: Japanese",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb describe-zones --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun alb DescribeZones --region cn-hangzhou --AcceptLanguage 'zh-CN'"
  }
}
