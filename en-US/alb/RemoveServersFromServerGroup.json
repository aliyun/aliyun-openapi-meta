{
  "name": "RemoveServersFromServerGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ServerGroupId",
      "description": "The server group ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Servers",
      "description": "The backend servers to be removed. You can specify up to 200 backend servers.",
      "position": "Query",
      "type": "Array",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb remove-servers-from-server-group --server-group-id sgp-atstuj3rtop0000 --servers ServerType=Ecs ServerId=i-bp1f9kdprbgy9uiu0000 Port=80 ServerIp=192.168.1.1 --client-token 593B0448-D13E-4C56-AC0D-FDF0FDE0E9A3 --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun alb RemoveServersFromServerGroup --region cn-hangzhou --Servers.1.ServerType Ecs --Servers.1.ServerId 'i-bp1f9kdprbgy9uiu0000' --DryRun false --Servers.1.Port 80 --ServerGroupId 'sgp-atstuj3rtop0000' --Servers.1.ServerIp '192.168.1.1' --ClientToken '593B0448-D13E-4C56-AC0D-FDF0FDE0E9A3' --method POST --force"
  }
}
