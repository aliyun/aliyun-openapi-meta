{
  "name": "AssociateAdditionalCertificatesWithListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Certificates",
      "description": "The extended validation certificates that you want to add to the listener.",
      "position": "Query",
      "type": "Array",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, a `2xx HTTP` status code is returned and the operation is performed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ListenerId",
      "description": "The listener ID. This parameter is supported only by HTTPS and QUIC listeners.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb associate-additional-certificates-with-listener --listener-id lsn-5qnirjhpt000000 --certificates CertificateId=10110000-cn-hangzhou --client-token 5A2CFF0E-5718-45B5-9D4D-70B3F000000 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun alb AssociateAdditionalCertificatesWithListener --region cn-hangzhou --DryRun true --Certificates.1.CertificateId '10110000-cn-hangzhou' --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3F000000' --ListenerId 'lsn-5qnirjhpt000000' --method POST --force"
  }
}
