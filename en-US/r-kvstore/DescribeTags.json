{
  "name": "DescribeTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "NextToken",
      "description": "The pagination token that is used in the next request to retrieve a new page of results.\n\n> This parameter is not required for the first request. If not all results are returned in one query, you can specify the token that is obtained from the previous query as the value of **NextToken**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "The resource type. Valid values: **INSTANCE** or **instance**.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-tags --biz-region-id cn-hangzhou --resource-type INSTANCE --next-token 212db86sca4384811e0b5e8707ec20000 --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeTags --region cn-hangzhou --NextToken 212db86sca4384811e0b5e8707ec20000 --ResourceType INSTANCE --RegionId 'cn-hangzhou'"
  }
}
