{
  "name": "DescribeZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The display language of the zone names to return. Valid values:\n\n*   **zh-CN** (default): Chinese\n*   **en-US**: English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-zones --biz-region-id cn-hangzhou --accept-language en-US --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeZones --region cn-hangzhou --AcceptLanguage 'en-US' --RegionId 'cn-hangzhou'"
  }
}
