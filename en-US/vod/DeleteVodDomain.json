{
  "name": "DeleteVodDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The domain name for CDN that you want to delete.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun vod delete-vod-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun vod DeleteVodDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
