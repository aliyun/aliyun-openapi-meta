{
  "name": "BatchStopVodDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "description": "The accelerated domain name. Separate multiple domain names with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun vod batch-stop-vod-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun vod BatchStopVodDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
