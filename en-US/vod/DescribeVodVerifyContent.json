{
  "name": "DescribeVodVerifyContent",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The domain name for which you want to verify the ownership. You can specify only one domain name in each call.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun vod describe-vod-verify-content --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun vod DescribeVodVerifyContent --region cn-hangzhou --DomainName 'example.com'"
  }
}
