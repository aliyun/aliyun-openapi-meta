{
  "name": "DescribeVodRefreshQuota",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun vod describe-vod-refresh-quota --region cn-hangzhou",
    "legacyCli": "aliyun vod DescribeVodRefreshQuota --region cn-hangzhou"
  }
}
