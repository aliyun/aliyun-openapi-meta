{
  "name": "SetVodDomainCertificate",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertName",
      "description": "The name of the certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "The domain name that is secured by the certificate. The domain name must use HTTPS acceleration.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SSLPri",
      "description": "The private key. This parameter is required only if you enable the SSL certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SSLProtocol",
      "description": "Specifies whether to enable the SSL certificate. Default value: off. Valid values:\n\n*   **on**\n*   **off**",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SSLPub",
      "description": "The content of the certificate. This parameter is required only if you enable the SSL certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun vod set-vod-domain-certificate --domain-name example.com --cert-name cert_name --ssl-protocol off --ssl-pub 0000 --ssl-pri 0000 --region cn-hangzhou",
    "legacyCli": "aliyun vod SetVodDomainCertificate --region cn-hangzhou --DomainName 'example.com' --SSLPub 0000 --CertName cert_name --SSLPri 0000 --SSLProtocol off"
  }
}
