{
  "name": "SetVodDomainSSLCertificate",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertId",
      "description": "The ID of the certificate.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "CertName",
      "description": "The name of the certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CertRegion",
      "description": "The region of the certificate. Valid values:\n\n*   **ap-southeast-1**: Singapore\n*   **cn-hangzhou**: China (Hangzhou)\n\nDefault value: **cn-hangzhou**",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CertType",
      "description": "The type of the certificate.\n\n*   **upload**: a user-uploaded SSL certificate.\n*   **cas**: a certificate that is acquired through Certificate Management Service.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "VOD acceleration domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Env",
      "description": "Specifies whether the certificate is issued in canary releases. If you set this parameter to **staging**, the certificate is issued in canary releases. If you do not specify this parameter or set this parameter to other values, the certificate is officially issued.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SSLPri",
      "description": "The private key. This parameter is required only if you enable the certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SSLProtocol",
      "description": "Specifies whether to enable the SSL certificate. Default value: off. Valid values:\n\n*   **on**\n*   **off**",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SSLPub",
      "description": "The content of the certificate. This parameter is required only if you enable the SSL certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun vod set-vod-domain-ssl-certificate --domain-name example.com --cert-name cert_name --cert-id 12342707 --cert-type cas --ssl-protocol off --ssl-pub 0000 --ssl-pri 0000 --cert-region cn-hangzhou --env staging --region cn-hangzhou",
    "legacyCli": "aliyun vod SetVodDomainSSLCertificate --region cn-hangzhou --DomainName 'example.com' --CertId 12342707 --SSLPub 0000 --CertRegion 'cn-hangzhou' --CertType cas --CertName cert_name --SSLPri 0000 --Env staging --SSLProtocol off"
  }
}
