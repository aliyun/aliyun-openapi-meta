{
  "name": "ListUserDefinedEventSources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EventBusName",
      "description": "The name of the event bus.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Limit",
      "description": "The maximum number of entries to be returned in a call. You can use this parameter and NextToken to implement paging. Note: Up to 100 entries can be returned in a call.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NamePrefix",
      "description": "The name of the event source.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "If you configure Limit and excess return values exist, this parameter is returned.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge list-user-defined-event-sources --name-prefix testName --event-bus-name testBus --limit 50 --next-token 100 --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge ListUserDefinedEventSources --region cn-hangzhou --NamePrefix testName --EventBusName testBus --NextToken 100 --Limit 50"
  }
}
