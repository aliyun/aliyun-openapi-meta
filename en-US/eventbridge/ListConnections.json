{
  "name": "ListConnections",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionNamePrefix",
      "description": "The key word that you specify to query connections. Connections can be queried by prefixes.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The maximum number of entries to be returned in a single call. You can use this parameter and the NextToken parameter to implement paging.\n\n*   Default value: 10.",
      "position": "Body",
      "type": "Long",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "If you set the Limit parameter and excess return values exist, this parameter is returned.\n\n*   Default value: 0.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge list-connections --connection-name-prefix connection-name --type Http --max-results 10 --next-token 0 --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge ListConnections --region cn-hangzhou --Type Http --NextToken 0 --MaxResults 10 --ConnectionNamePrefix 'connection-name'"
  }
}
