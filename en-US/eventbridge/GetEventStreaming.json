{
  "name": "GetEventStreaming",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EventStreamingName",
      "description": "The name of the event stream whose details you want to query.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge get-event-streaming --event-streaming-name myeventstreaming --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge GetEventStreaming --region cn-hangzhou --EventStreamingName myeventstreaming"
  }
}
