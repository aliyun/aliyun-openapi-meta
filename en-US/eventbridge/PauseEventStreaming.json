{
  "name": "PauseEventStreaming",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EventStreamingName",
      "description": "The name of the event stream that you want to stop.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge pause-event-streaming --event-streaming-name rocketmq-sync --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge PauseEventStreaming --region cn-hangzhou --EventStreamingName 'rocketmq-sync'"
  }
}
