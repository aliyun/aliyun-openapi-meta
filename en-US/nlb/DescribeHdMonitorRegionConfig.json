{
  "name": "DescribeHdMonitorRegionConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "The ID of the region where the resources are deployed.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun nlb describe-hd-monitor-region-config --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun nlb DescribeHdMonitorRegionConfig --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
