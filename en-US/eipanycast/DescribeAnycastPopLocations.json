{
  "name": "DescribeAnycastPopLocations",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ServiceLocation",
      "description": "The access area of the Anycast elastic IP address (EIP).\n\nSet the value to **international**, which specifies the areas outside the Chinese mainland.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun eipanycast describe-anycast-pop-locations --service-location international --region cn-hangzhou",
    "legacyCli": "aliyun eipanycast DescribeAnycastPopLocations --region cn-hangzhou --ServiceLocation international"
  }
}
