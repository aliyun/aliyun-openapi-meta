{
  "name": "UpdateInstanceIpWhiteList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Delete",
      "description": "Specifies whether to clear the whitelist.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "GroupName",
      "description": "The name of the whitelist group. If you leave this parameter empty, the default value user is used.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the instance. Call the [GetLindormInstanceList](~~426069~~) operation to obtain the ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecurityIpList",
      "description": "The IP addresses to add to the whitelist.\n\n> Set this parameter to 127.0.0.1 to deny access from all IP addresses. For example, 192.168.0.0/24 allows all IP addresses in the 192.168.0.0/24 CIDR block to access the Lindorm instance. Separate multiple IP addresses or CIDR blocks with a comma (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}