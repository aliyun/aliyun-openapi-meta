{
  "name": "DeleteConfigTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/pop/v5/config_template",
  "parameters": [
    {
      "name": "Id",
      "description": "The ID of the configuration template.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun edas delete-config-template --id 555 --region cn-hangzhou",
    "legacyCli": "aliyun edas DELETE /pop/v5/config_template --region cn-hangzhou --Id 555 --header \"Content-Type=application/json\""
  }
}
