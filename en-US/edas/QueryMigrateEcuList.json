{
  "name": "QueryMigrateEcuList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v5/resource/migrate_ecu_list",
  "parameters": [
    {
      "name": "LogicalRegionId",
      "description": "The ID of the namespace.\n\n*   The ID of a custom namespace is in the `region ID:namespace identifier` format. Example: `cn-beijing:test`.\n*   The ID of the default namespace is in the `region ID` format. Example: `cn-beijing`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun edas query-migrate-ecu-list --logical-region-id cn-hangzhou_or_cn-hangzhou:test --region cn-hangzhou",
    "legacyCli": "aliyun edas GET /pop/v5/resource/migrate_ecu_list --region cn-hangzhou --LogicalRegionId 'cn-hangzhou_or_cn-hangzhou:test' --header \"Content-Type=application/json\""
  }
}
