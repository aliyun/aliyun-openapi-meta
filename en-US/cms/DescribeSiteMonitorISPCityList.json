{
  "name": "DescribeSiteMonitorISPCityList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "City",
      "description": "The name or ID of the city.\n\n> City names support fuzzy match.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "IPV4",
      "description": "Specifies whether to query IPv4 probes. Valid values:\n\n*   true (default): IPv4 probes are queried.\n*   false: IPv4 probes are not queried.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IPV6",
      "description": "Specifies whether to query IPv6 probes. Valid values:\n\n*   true (default): IPv6 probes are queried.\n*   false: IPv6 probes are not queried.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Isp",
      "description": "The name or ID of the carrier.\n\n> Carrier names support fuzzy match.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ViewAll",
      "description": "Specifies whether to return all detection points. Valid values:\n\n*   true (default): returns all detection points.\n*   false: returns only available detection points.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cms describe-site-monitor-isp-city-list --isp China_Unicom --city Guiyang --ipv6 true --ipv4 true --view-all true --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms DescribeSiteMonitorISPCityList --region cn-hangzhou --IPV6 true --Isp China_Unicom --IPV4 true --ViewAll true --City Guiyang"
  }
}
