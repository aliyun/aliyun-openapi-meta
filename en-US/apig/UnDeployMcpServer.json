{
  "name": "UnDeployMcpServer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/mcp-servers/[mcpServerId]/undeploy",
  "parameters": [
    {
      "name": "mcpServerId",
      "description": "The ID of the MCP server.",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun apig un-deploy-mcp-server --mcp-server-id mcp-adfef2334fa --region cn-hangzhou",
    "legacyCli": "aliyun apig POST /v1/mcp-servers/mcp-adfef2334fa/undeploy --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
