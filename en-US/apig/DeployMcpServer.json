{
  "name": "DeployMcpServer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/mcp-servers/[mcpServerId]/deploy",
  "parameters": [
    {
      "name": "mcpServerId",
      "description": "The MCP server ID.",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ]
}