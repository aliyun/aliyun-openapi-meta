{
  "name": "GetHybridClusterConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The ID of the cluster.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Node",
      "description": "The name of the on-premises compute node. You can call this operation to query the configurations of the on-premises compute node.\n\nBy default, the operation queries the configurations of a cluster.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ehpc get-hybrid-cluster-config --cluster-id ehpc-hz-FYUr320000 --node testNode --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc GetHybridClusterConfig --region cn-hangzhou --ClusterId 'ehpc-hz-FYUr320000' --Node testNode"
  }
}
