{
  "name": "CreateBackup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun polardb create-backup --db-cluster-id pc-0000000000000 --client-token 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb CreateBackup --region cn-hangzhou --Comment 'example-string' --DBClusterId 'pc-0000000000000' --ClientToken 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000"
  }
}
