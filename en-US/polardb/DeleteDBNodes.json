{
  "name": "DeleteDBNodes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CloudProvider",
      "description": "",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The ID of the cluster.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBNodeId",
      "description": "The IDs of the nodes.\n\n> You can call the [DescribeDBClusters](~~185342~~) operation to query the details of all clusters that belong to your Alibaba Cloud account, such as the cluster ID.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "DBNodeType",
      "description": "The node type. Valid values:\n\n*   RO\n*   STANDBY\n*   DLNode\n\nEnumerated values:\n\n*   DLNode: AI node\n*   STANDBY: standby node\n*   RO: read-only node",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun polardb delete-db-nodes --db-cluster-id pc-00000000000000 --client-token 6000170000591aed949d0f54a343f1a4200000000000 --db-node-type RO --db-node-id pi-0000000000000000 --cloud-provider ENS --region cn-hangzhou",
    "legacyCli": "aliyun polardb DeleteDBNodes --region cn-hangzhou --DBNodeId.1 'pi-0000000000000000' --CloudProvider ENS --DBClusterId 'pc-00000000000000' --DBNodeType RO --ClientToken 6000170000591aed949d0f54a343f1a4200000000000"
  }
}
