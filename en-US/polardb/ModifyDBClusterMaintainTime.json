{
  "name": "ModifyDBClusterMaintainTime",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MaintainTime",
      "description": "The maintenance window of the cluster. Specify the maintenance window in the `HH:mmZ-HH:mmZ` format. For example, the value `16:00Z-17:00Z` indicates that the cluster can be maintained from 00:00 to 01:00 (UTC+8).\n \n>  The maintenance window must start on the hour and last for an hour.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun polardb modify-db-cluster-maintain-time --db-cluster-id pc-000000000000000 --maintain-time 02:00Z-03:00Z --region cn-hangzhou",
    "legacyCli": "aliyun polardb ModifyDBClusterMaintainTime --region cn-hangzhou --DBClusterId 'pc-000000000000000' --MaintainTime '02:00Z-03:00Z'"
  }
}
