{
  "name": "StartMmsJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]/jobs/[jobId]/start",
  "parameters": [
    {
      "name": "jobId",
      "description": "The ID of the migration job.",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "sourceId",
      "description": "The ID of the data source.",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute start-mms-job --source-id 2000015 --job-id 88 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute POST /api/v1/mms/datasources/2000015/jobs/88/start --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
