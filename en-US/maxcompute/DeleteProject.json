{
  "name": "DeleteProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/projects/[projectName]",
  "parameters": [
    {
      "name": "isLogical",
      "description": "Specifies whether to perform a logical deletion. The default value is true. A value of false indicates a physical deletion.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "projectName",
      "description": "The name of the project.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute delete-project --project-name odps_project --is-logical true --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/projects/odps_project --region cn-hangzhou --isLogical true --header \"Content-Type=application/json\""
  }
}
