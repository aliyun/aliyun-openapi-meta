{
  "name": "GetComputeEffectivePlan",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/quotas/[nickname]/computeEffectivePlan/",
  "parameters": [
    {
      "name": "nickname",
      "description": "The nickname of level-1 compute quota.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute get-compute-effective-plan --nickname os_ComputeQuota_p --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/quotas/os_ComputeQuota_p/computeEffectivePlan/ --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
