{
  "name": "RetryMmsJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]/jobs/[jobId]/retry",
  "parameters": [
    {
      "name": "jobId",
      "description": "The ID of the migration job.",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "sourceId",
      "description": "The ID of the data source.",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute retry-mms-job --source-id 2000015 --job-id 78 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute POST /api/v1/mms/datasources/2000015/jobs/78/retry --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
