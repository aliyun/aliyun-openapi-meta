{
  "name": "UpdateProjectModelTier",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/projects/[projectName]/modelTier",
  "parameters": [
    {
      "name": "projectName",
      "description": "The name of the project.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute update-project-model-tier --project-name projectA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute PUT /api/v1/projects/projectA/modelTier --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
