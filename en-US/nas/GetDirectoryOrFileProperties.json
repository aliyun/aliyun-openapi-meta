{
  "name": "GetDirectoryOrFileProperties",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemId",
      "description": "The ID of the file system.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Path",
      "description": "The absolute path of the directory.\n\nThe path must start with a forward slash (/) and must be a path that exists in the mount target.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun nas get-directory-or-file-properties --file-system-id 31a8e40000 --path /pathway/to/folder --region cn-hangzhou",
    "legacyCli": "aliyun nas GetDirectoryOrFileProperties --region cn-hangzhou --Path '/pathway/to/folder' --FileSystemId 31a8e40000"
  }
}
