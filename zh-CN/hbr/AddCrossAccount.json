{
  "name": "AddCrossAccount",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Alias",
      "description": "别名，最长为32位。基于资源目录配置跨账号备份时，不需要填写。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CrossAccountRoleName",
      "description": "基于 RAM 角色扮演配置跨账号备份时，配置需备份账号的 RAM 角色名。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CrossAccountType",
      "description": "跨账号备份类型。支持：\n- **CROSS_ACCOUNT**：基于RAM角色扮演配置跨账号备份\n- **CROSS_ACCOUNT_BY_RD**：基于资源目录配置跨账号备份",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CrossAccountUserId",
      "description": "需备份账号 UID。",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun hbr add-cross-account --cross-account-user-id 1589753625 --cross-account-role-name hbrcrossrole --cross-account-type CROSS_ACCOUNT --alias Source_account_1 --region cn-hangzhou",
    "legacyCli": "aliyun hbr AddCrossAccount --region cn-hangzhou --CrossAccountUserId 1589753625 --CrossAccountRoleName hbrcrossrole --Alias Source_account_1 --CrossAccountType CROSS_ACCOUNT"
  }
}
