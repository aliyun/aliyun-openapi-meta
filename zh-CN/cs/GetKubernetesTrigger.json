{
  "name": "GetKubernetesTrigger",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/triggers/[ClusterId]",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "集群ID。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "Name",
      "description": "应用名称。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Namespace",
      "description": "命名空间名称。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Type",
      "description": "触发器类型。取值：\n\n- `deployment`：针对无状态应用的触发器。 \n\n- `application`：针对应用中心应用的触发器。\n\n默认值：`deployment`。\n\n当不指定触发器类型时，查询结果将不过滤触发器类型。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "action",
      "description": "触发器行为，取值：\n\n`redeploy`：重新部署`project_id`中定义的资源。\n\n当不指定触发器行为时，查询结果将不过滤触发器行为。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs get-kubernetes-trigger --cluster-id c5cdf7e3938bc4f8eb0e44b21a80f0000 --namespace default --type deployment --name web-server --action redeploy --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /triggers/c5cdf7e3938bc4f8eb0e44b21a80f0000 --region cn-hangzhou --Type deployment --action redeploy --Namespace default --Name 'web-server' --header \"Content-Type=application/json\""
  }
}
