{
  "name": "DeleteAlertContact",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/alert/contacts",
  "parameters": [
    {
      "name": "contact_ids",
      "description": "报警联系人的 ID 列表。",
      "position": "Query",
      "type": "Json",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs delete-alert-contact --contact-ids 12345 --region cn-hangzhou",
    "legacyCli": "aliyun cs DELETE /alert/contacts --region cn-hangzhou --contact_ids '[12345]' --header \"Content-Type=application/json\""
  }
}
