{
  "name": "CreateKubernetesTrigger",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/triggers",
  "parameters": [
    {
      "name": "body",
      "description": "请求体参数。",
      "position": "Body",
      "type": "Struct",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs create-kubernetes-trigger --region cn-hangzhou",
    "legacyCli": "aliyun cs POST /triggers --region cn-hangzhou --header \"Content-Type=application/json\" --body \"cn-beijing\""
  }
}
