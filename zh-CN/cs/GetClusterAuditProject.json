{
  "name": "GetClusterAuditProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[clusterid]/audit",
  "parameters": [
    {
      "name": "clusterid",
      "description": "ACK集群ID。",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}