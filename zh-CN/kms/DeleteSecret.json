{
  "name": "DeleteSecret",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceDeleteWithoutRecovery",
      "description": "是否强制删除凭据，且不允许恢复。\n\n取值范围：\n- **true**\n- **false**（默认值）",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RecoveryWindowInDays",
      "description": "按照可恢复的方式删除凭据，且指定可恢复的窗口（天数）。默认值：30。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SecretName",
      "description": "凭据名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}