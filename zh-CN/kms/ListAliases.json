{
  "name": "ListAliases",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "description": "当前页数。取值范围：大于0的整数。\n\n默认值：1。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "每页返回的结果个数。取值范围：0~100。\n\n默认值：20。",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms list-aliases --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun kms ListAliases --region cn-hangzhou --PageSize 10 --PageNumber 1"
  }
}
