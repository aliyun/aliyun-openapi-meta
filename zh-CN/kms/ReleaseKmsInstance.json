{
  "name": "ReleaseKmsInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceDeleteWithoutBackup",
      "description": "KMS实例未备份的场景下是否强制释放。\n\n- true：强制释放。\n- false（默认值）：禁止释放。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KmsInstanceId",
      "description": "KMS实例ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms release-kms-instance --kms-instance-id kst-hzz60000 --force-delete-without-backup false --region cn-hangzhou",
    "legacyCli": "aliyun kms ReleaseKmsInstance --region cn-hangzhou --KmsInstanceId 'kst-hzz60000' --ForceDeleteWithoutBackup false"
  }
}
