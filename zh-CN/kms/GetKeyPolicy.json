{
  "name": "GetKeyPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyId",
      "description": "密钥ID或密钥资源名称（ARN）。\n>访问其他阿里云账号下的密钥时，必须输入密钥ARN。密钥ARN的格式为`acs:kms:${region}:${account}:key/${keyid}`。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PolicyName",
      "description": "密钥策略名称。仅支持固定值default。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun kms get-key-policy --key-id key-hzz630494463ejqjx0000 --policy-name default --region cn-hangzhou",
    "legacyCli": "aliyun kms GetKeyPolicy --region cn-hangzhou --PolicyName default --KeyId 'key-hzz630494463ejqjx0000'"
  }
}
