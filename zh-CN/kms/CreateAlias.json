{
  "name": "CreateAlias",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "description": "CMK的别名名称。  \n长度为1~255个字符，必须包含前缀`alias/`，但不能以`alias/acs`保留字为前缀。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "KeyId",
      "description": "CMK的全局唯一标识符。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}