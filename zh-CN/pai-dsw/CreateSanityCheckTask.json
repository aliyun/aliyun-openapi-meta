{
  "name": "CreateSanityCheckTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/sanitychecks/[CheckType]",
  "parameters": [
    {
      "name": "CheckType",
      "description": "检查项类型。取值：\n\n- **SSH**：用于触发实例SSH健康检查",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "请求body。",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun pai-dsw create-sanity-check-task --check-type SSH --instance-id dsw-730xxxxxxxxxx --api-version 2022-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun pai-dsw POST /api/v2/sanitychecks/SSH --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"InstanceId\":\"dsw-730xxxxxxxxxx\"}"
  }
}
