{
  "name": "StopInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v2/instances/[InstanceId]/stop",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "实例ID。如何获取实例ID，请参见[ListInstances](~~470439~~)。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "SaveImage",
      "description": "是否保存环境后再关闭实例。\n-  打开开关：保存环境后再关闭实例。\n- 关闭开关：不保存环境数据，直接关闭实例。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun pai-dsw stop-instance --instance-id dsw-730xxxxxxxxxx --save-image false --api-version 2022-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun pai-dsw PUT /api/v2/instances/dsw-730xxxxxxxxxx/stop --region cn-hangzhou --SaveImage false --header \"Content-Type=application/json\""
  }
}
