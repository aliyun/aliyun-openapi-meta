{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/ha3/regions",
  "parameters": [
    {
      "name": "acceptLanguage",
      "description": "指定返回的语言，默认值为zh-cn。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun searchengine describe-regions --accept-language zh-cn --region cn-hangzhou",
    "legacyCli": "aliyun searchengine GET /openapi/ha3/regions --region cn-hangzhou --acceptLanguage 'zh-cn' --header \"Content-Type=application/json\""
  }
}
