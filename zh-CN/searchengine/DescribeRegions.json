{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/ha3/regions",
  "parameters": [
    {
      "name": "acceptLanguage",
      "description": "指定返回的语言，默认值为zh-cn。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}