{
  "name": "DescribeRegions",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "指定返回参数**RegionName**和**ZoneName**的语言，取值说明：\n- **zh**：默认值，中文。\n- **en**：英文。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "地域ID。\n>如果不填写，默认查询所有支持的地域。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}