{
  "name": "GetInvalidDomainCount",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ga get-invalid-domain-count --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ga GetInvalidDomainCount --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
