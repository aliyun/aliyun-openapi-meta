{
  "name": "GetComputeQuotaSchedule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/quotas/[nickname]/computeQuotaSchedule",
  "parameters": [
    {
      "name": "displayTimezone",
      "description": "展示时区。\n\n> 默认为: UTC+8。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nickname",
      "description": "一级Quota昵称。",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute get-compute-quota-schedule --nickname os_ComputeQuota_p --display-timezone UTC+8 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/quotas/os_ComputeQuota_p/computeQuotaSchedule --region cn-hangzhou --displayTimezone 'UTC+8' --header \"Content-Type=application/json\""
  }
}
