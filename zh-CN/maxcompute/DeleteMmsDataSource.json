{
  "name": "DeleteMmsDataSource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]",
  "parameters": [
    {
      "name": "sourceId",
      "description": "数据源ID。",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute delete-mms-data-source --source-id 2000015 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/mms/datasources/2000015 --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
