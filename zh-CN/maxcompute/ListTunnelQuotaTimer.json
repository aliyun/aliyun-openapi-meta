{
  "name": "ListTunnelQuotaTimer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/tunnel/[nickname]/timers",
  "parameters": [
    {
      "name": "nickname",
      "description": "一级数据传输服务独享资源组（Tunnel Quota）昵称。",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute list-tunnel-quota-timer --nickname ot_tunnel_quota_p --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/tunnel/ot_tunnel_quota_p/timers --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
