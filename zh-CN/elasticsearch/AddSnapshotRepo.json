{
  "name": "AddSnapshotRepo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/snapshot-repos",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "目标端实例ID，需要设置跨集群OSS仓库的实例ID。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "指定跨集群备份信息，即指定引用实例。",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}