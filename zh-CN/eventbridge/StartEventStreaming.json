{
  "name": "StartEventStreaming",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EventStreamingName",
      "description": "要启用的事件流的名称。",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge start-event-streaming --event-streaming-name rocketmq-sync --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge StartEventStreaming --region cn-hangzhou --EventStreamingName 'rocketmq-sync'"
  }
}
