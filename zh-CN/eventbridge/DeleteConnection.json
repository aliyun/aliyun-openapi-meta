{
  "name": "DeleteConnection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionName",
      "description": "需要删除的连接名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge delete-connection --connection-name connection-name --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge DeleteConnection --region cn-hangzhou --ConnectionName 'connection-name'"
  }
}
