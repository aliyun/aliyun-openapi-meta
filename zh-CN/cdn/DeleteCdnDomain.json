{
  "name": "DeleteCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "要删除的CDN的域名，仅支持删除单个域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn delete-cdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DeleteCdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
