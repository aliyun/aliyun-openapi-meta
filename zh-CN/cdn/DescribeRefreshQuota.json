{
  "name": "DescribeRefreshQuota",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun cdn describe-refresh-quota --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeRefreshQuota --region cn-hangzhou"
  }
}
