{
  "name": "DescribeCdnCertificateDetailById",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertId",
      "description": "证书ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "CertRegion",
      "description": "证书Region。取值：\n- **ap-southeast-1**（新加坡）\n-  **cn-hangzhou**（杭州）\n\n默认值为**cn-hangzhou**。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-certificate-detail-by-id --cert-id 12345 --cert-region cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnCertificateDetailById --region cn-hangzhou --CertId 12345 --CertRegion 'cn-hangzhou'"
  }
}
