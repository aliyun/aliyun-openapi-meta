{
  "name": "CheckCdnDomainExist",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn check-cdn-domain-exist --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn CheckCdnDomainExist --region cn-hangzhou --DomainName 'example.com'"
  }
}
