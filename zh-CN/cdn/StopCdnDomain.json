{
  "name": "StopCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名，仅支持停用单个。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn stop-cdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn StopCdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
