{
  "name": "EnableRealtimeLogDelivery",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "description": "开启实时日志投递服务域名，多个域名之间使用英文逗号（,）分隔。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}