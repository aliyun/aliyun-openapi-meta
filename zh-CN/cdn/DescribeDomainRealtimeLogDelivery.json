{
  "name": "DescribeDomainRealtimeLogDelivery",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "description": "开启实时日志投递服务域名，仅支持查询单个。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-realtime-log-delivery --domain example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainRealtimeLogDelivery --region cn-hangzhou --Domain 'example.com'"
  }
}
