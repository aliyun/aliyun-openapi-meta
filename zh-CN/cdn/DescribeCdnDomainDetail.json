{
  "name": "DescribeCdnDomainDetail",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名信息，仅支持单个查询。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-domain-detail --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDomainDetail --region cn-hangzhou --DomainName 'example.com'"
  }
}
