{
  "name": "DescribeCdnReportList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ReportId",
      "description": "待查询的报表ID。如果不传该参数，默认查询所有报表。",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-report-list --report-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnReportList --region cn-hangzhou --ReportId 1"
  }
}
