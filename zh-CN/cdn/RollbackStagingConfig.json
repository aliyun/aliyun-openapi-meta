{
  "name": "RollbackStagingConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名，仅支持单个域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn rollback-staging-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn RollbackStagingConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}
