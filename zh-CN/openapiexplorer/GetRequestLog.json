{
  "name": "GetRequestLog",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/getRequestLog",
  "parameters": [
    {
      "name": "logRequestId",
      "description": "需要查询日志的RequestId，\nAPI 请求的唯一标识符（UUID格式，且必须为大写）。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}