{
  "name": "GetCallerIdentity",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun sts get-caller-identity --region cn-hangzhou",
    "legacyCli": "aliyun sts GetCallerIdentity --region cn-hangzhou"
  }
}
