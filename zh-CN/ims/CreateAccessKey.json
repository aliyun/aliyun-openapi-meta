{
  "name": "CreateAccessKey",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserPrincipalName",
      "description": "RAM用户的登录名称。\n\n如果为空，默认为当前用户创建访问密钥。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}