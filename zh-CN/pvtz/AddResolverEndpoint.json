{
  "name": "AddResolverEndpoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IpConfig",
      "description": "出站流量源IP地址列表（至少添加2个，最多可添加6个）。\n\n>为了保证高可用，出站终端节点要求至少添加2个出站源IP地址，而且建议这2个IP地址分配在不同的可用区，出站终端节点允许添加的出站源IP地址最多为6个",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "AzId",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "CidrBlock",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Ip",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "VSwitchId",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    },
    {
      "name": "Lang",
      "description": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "终端节点名称，最长20个字符，超过限制会报错。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecurityGroupId",
      "description": "安全组ID，安全组里面的规则将应用于出站VPC。\n\n>为了避免误操作造成线上流量中断，创建出站终端节点之后，无法更改此值。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VpcId",
      "description": "出站专有网络ID（VPC ID），解析器所有出站的DNS查询流量都将经由此VPC进行流量转发。\n\n>为了避免误操作造成线上流量中断，创建出站终端节点之后，无法更改此值。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VpcRegionId",
      "description": "出站VPC所在的地域ID（regionID）。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}