{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "返回结果中地域名称（LocalName）的显示语言，取值：\n\n- **zh-CN**：中文，默认值。\n- **en-US**：英文。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}