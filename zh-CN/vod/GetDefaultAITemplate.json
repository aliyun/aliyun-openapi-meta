{
  "name": "GetDefaultAITemplate",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "TemplateType",
      "description": "AI模板类型。固定取值：**AIMediaAudit**（智能审核）。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}