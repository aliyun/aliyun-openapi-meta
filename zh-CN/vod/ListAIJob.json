{
  "name": "ListAIJob",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "作业ID列表。JobId可以通过[GetPlayInfo](~~56124~~)接口中返回的PlayInfo结构体中获取。\n\n> 多个ID之间用半角逗号（,）分隔，一次最多可以查询10个作业ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}