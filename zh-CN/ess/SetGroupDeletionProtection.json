{
  "name": "SetGroupDeletionProtection",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupDeletionProtection",
      "description": "是否为伸缩组开启删除保护。取值范围：\n\n- true：开启删除保护。开启删除保护后，不能在控制台或者通过调用API删除伸缩组。如果需要删除伸缩组，必须先关闭删除保护。\n- false：关闭删除保护。\n\n默认值：false。",
      "position": "Query",
      "type": "Boolean",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "伸缩组所属的地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ScalingGroupId",
      "description": "伸缩组的ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}