{
  "name": "UpdateClusterAutoRenew",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoRenewInstances",
      "description": "自动续费ECS实例列表。RenewAllInstances不为true时生效。",
      "position": "Query",
      "type": "Array",
      "required": false
    },
    {
      "name": "ClusterAutoRenew",
      "description": "集群是否开启自动续费。取值范围：\n\n- true：开启自动续费。\n\n- false：不开启自动续费。\n\n默认值：false。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ClusterAutoRenewDuration",
      "description": "集群自动续费时长。当 ClusterAutoRenew 取值为 true 时生效。\nClusterAutoRenewDurationUnit取值为 Month 时，取值：1、2、3、4、5、6、7、8、9、12、24、36。ClusterAutoRenewDurationUnit取值为 Year时，取值：1、2、3。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ClusterAutoRenewDurationUnit",
      "description": "集群续费时长单位。取值范围：\n\n- Month：月。\n\n- Year：年。\n\n默认值：Month。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClusterId",
      "description": "集群ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "区域ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RenewAllInstances",
      "description": "是否对集群中所有ECS实例配置自动续费。取值范围：\n\n- true：对集群中所有ECS实例配置自动续费，默认配置自动续费1个月。\n\n- false：不对集群中所有ECS实例配置自动续费。可在AutoRenewInstances中指定要配置自动续费的ECS实例。\n\n默认值：false。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}