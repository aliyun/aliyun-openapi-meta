{
  "name": "GetRole",
  "security": [
    "AK"
  ],
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RoleName",
      "description": "指定角色名。\n\n长度为1~64个字符，可包含英文字母、数字、点号（.）和短划线（-）。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}