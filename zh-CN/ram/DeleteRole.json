{
  "name": "DeleteRole",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RoleName",
      "description": "RAM角色名称。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram delete-role --role-name ECSAdmin --region cn-hangzhou",
    "legacyCli": "aliyun ram DeleteRole --region cn-hangzhou --RoleName ECSAdmin"
  }
}
