{
  "name": "ListEntitiesForPolicy",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "description": "权限策略名称。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PolicyType",
      "description": "指定权限策略的类型，取值为`System`或`Custom`。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram list-entities-for-policy --policy-type Custom --policy-name OSS-Administrator --region cn-hangzhou",
    "legacyCli": "aliyun ram ListEntitiesForPolicy --region cn-hangzhou --PolicyType Custom --PolicyName 'OSS-Administrator'"
  }
}
