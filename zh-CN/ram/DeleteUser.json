{
  "name": "DeleteUser",
  "security": [
    "AK"
  ],
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "description": "指定用户名。\n\n长度为1~64个字符，可包含英文字母、数字、半角句号（.）、短划线（-）和下划线（_）。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}