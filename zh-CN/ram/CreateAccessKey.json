{
  "name": "CreateAccessKey",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "description": "指定用户名，RAM用户调用此接口时，默认为自己创建访问密钥。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram create-access-key --user-name zhangq0000 --region cn-hangzhou",
    "legacyCli": "aliyun ram CreateAccessKey --region cn-hangzhou --UserName zhangq0000"
  }
}
