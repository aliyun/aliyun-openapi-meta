{
  "name": "ListPoliciesForRole",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RoleName",
      "description": "指定角色名。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ram list-policies-for-role --role-name AdminRole --region cn-hangzhou",
    "legacyCli": "aliyun ram ListPoliciesForRole --region cn-hangzhou --RoleName AdminRole"
  }
}
