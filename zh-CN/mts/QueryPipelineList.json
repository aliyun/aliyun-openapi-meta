{
  "name": "QueryPipelineList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PipelineIds",
      "description": "需要查询的管道ID列表，可通过**MPS控制台** > **全局设置** > **管道**获取。一次最多查询10个，用半角逗号（,）分隔。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}