{
  "name": "BindOutputBucket",
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bucket",
      "description": "需要绑定的输出OSS Bucket名称，支持中英文、数字、中划线（-），不能以特殊符号开头，不超过64个字节。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}