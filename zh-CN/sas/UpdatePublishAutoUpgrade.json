{
  "name": "UpdatePublishAutoUpgrade",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoUpgrade",
      "description": "是否开启自动升级。取值：\n\n- **1**：开启\n- **0**：关闭",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas update-publish-auto-upgrade --auto-upgrade 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas UpdatePublishAutoUpgrade --region cn-hangzhou --AutoUpgrade 1"
  }
}
