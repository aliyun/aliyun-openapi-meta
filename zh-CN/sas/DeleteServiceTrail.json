{
  "name": "DeleteServiceTrail",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "云安全中心实例地域。取值为：\n\n- **cn-hangzhou：**  中心\n- **ap-southeast-1：**  新加坡",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas delete-service-trail --biz-region-id cn-hangzhou --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DeleteServiceTrail --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
