{
  "name": "CreateServiceTrail",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "要查询的实例所在地域的ID。取值：\n\n- **cn-hangzhou**（默认值）：中国。\n- **ap-southeast-1**：全球（不含中国）。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}