{
  "name": "DisableCustomBlockRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BlockIp",
      "description": "防暴力破解IP地址。\n> 调用[DescribeCustomBlockRecords](~~DescribeCustomBlockRecords~~)接口获取该参数。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Bound",
      "description": "服务器暴力破解拦截策略阻断IP的方向。\n取值：\n- **in** ：入方向 \n- **out**： 出方向",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas disable-custom-block-record --block-ip 62.233.XX.XX --bound in --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DisableCustomBlockRecord --region cn-hangzhou --BlockIp '62.233.XX.XX' --Bound in"
  }
}
