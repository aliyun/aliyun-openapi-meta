{
  "name": "DescribeHybridProxyPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "description": "代理集群名称，用户可以通过控制台查看集群名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas describe-hybrid-proxy-policy --cluster-name test-idc --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeHybridProxyPolicy --region cn-hangzhou --ClusterName 'test-idc'"
  }
}
