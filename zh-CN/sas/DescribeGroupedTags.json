{
  "name": "DescribeGroupedTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MachineTypes",
      "description": "指定要查询的资产类型。不指定资产类型时，默认查询所有资产类型下的标签信息。取值：\n- **ecs**：服务器\n- **cloud_product**：云产品",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas describe-grouped-tags --machine-types ecs --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeGroupedTags --region cn-hangzhou --MachineTypes ecs"
  }
}
