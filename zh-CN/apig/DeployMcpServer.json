{
  "name": "DeployMcpServer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/mcp-servers/[mcpServerId]/deploy",
  "parameters": [
    {
      "name": "mcpServerId",
      "description": "MCP Server ID",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun apig deploy-mcp-server --mcp-server-id mcp-adfefz24afg --region cn-hangzhou",
    "legacyCli": "aliyun apig POST /v1/mcp-servers/mcp-adfefz24afg/deploy --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
