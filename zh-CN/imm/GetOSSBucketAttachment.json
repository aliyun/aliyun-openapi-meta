{
  "name": "GetOSSBucketAttachment",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OSSBucket",
      "description": "OSS Bucket名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm get-oss-bucket-attachment --oss-bucket examplebucket --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm GetOSSBucketAttachment --region cn-hangzhou --OSSBucket examplebucket"
  }
}
