{
  "name": "ListBindings",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DatasetName",
      "description": "数据集名称，获取方式请参见[创建数据集](~~478160~~)。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "- 返回绑定关系的最大个数，取值范围为0~200。\n- 不设置此参数或者设置为0时，则默认值为100。",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "- 当绑定关系总数大于设置的MaxResults时，用于翻页的token。\n- 从NextToken开始按字典序返回绑定关系信息列表。\n- 第一次调用此接口时，设置为空。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProjectName",
      "description": "项目名称，获取方式请参见[创建项目](~~478153~~)。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm list-bindings --project-name test-project --dataset-name test-dataset --max-results 20 --next-token immtest:dataset001:examplebucket01 --name imm --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm ListBindings --region cn-hangzhou --NextToken 'immtest:dataset001:examplebucket01' --ProjectName 'test-project' --DatasetName 'test-dataset' --MaxResults 20 --Name imm"
  }
}
