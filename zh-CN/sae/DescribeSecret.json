{
  "name": "DescribeSecret",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/secret/secret",
  "parameters": [
    {
      "name": "NamespaceId",
      "description": "Secret实例所在命名空间ID。如果所在命名空间为默认命名空间，则只需填写RegionId，例如`cn-beijing`。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecretId",
      "description": "查询的Secret实例ID。需要调用[ListSecrets](~~466929~~)接口查看。",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}