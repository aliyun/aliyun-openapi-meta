{
  "name": "GetAvailabilityMetric",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/getAvailabilityMetric",
  "parameters": [
    {
      "name": "AppSource",
      "description": "SAE应用类型。\n\n- **micro_service。**\n- **web。**\n- **job。**",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CpuStrategy",
      "description": "CPU分配策略。\n\n- **request**：仅在有请求时分配CPU。\n- **always**：始终分配固定CPU。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Limit",
      "description": "返回数据条数，取值范围[0,100]。",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sae get-availability-metric --biz-region-id cn-hangzhou --limit 10 --app-source micro_service --cpu-strategy always --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/getAvailabilityMetric --region cn-hangzhou --CpuStrategy always --RegionId 'cn-hangzhou' --Limit 10 --AppSource micro_service --header \"Content-Type=application/json\""
  }
}
