{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v2/regions",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun eas describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun eas GET /api/v2/regions --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
