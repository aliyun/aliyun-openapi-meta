{
  "name": "DescribeMachineSpec",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v2/public/instance_types",
  "parameters": [
    {
      "name": "ChargeType",
      "description": "付费方式，默认为按量付费。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceTypes",
      "description": "参数已废弃。",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "ResourceType",
      "description": "资源类型，默认为公共资源组。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun eas describe-machine-spec --resource-type Public --charge-type PostPaid --instance-types 无需填写 --region cn-hangzhou",
    "legacyCli": "aliyun eas GET /api/v2/public/instance_types --region cn-hangzhou --ChargeType PostPaid --ResourceType Public --InstanceTypes '无需填写' --header \"Content-Type=application/json\""
  }
}
