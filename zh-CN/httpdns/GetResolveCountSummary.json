{
  "name": "GetResolveCountSummary",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Granularity",
      "description": "统计数据粒度，可选值有**day**和**month**。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TimeSpan",
      "description": "统计时间跨度。",
      "position": "Query",
      "type": "Integer",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun httpdns get-resolve-count-summary --granularity day --time-span 7 --region cn-hangzhou",
    "legacyCli": "aliyun httpdns GetResolveCountSummary --region cn-hangzhou --TimeSpan 7 --Granularity day"
  }
}
