{
  "name": "DeleteProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "forceDelete",
      "description": "",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-project --project ali-project-test --force-delete true --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE / --project ali-project-test --region cn-hangzhou --forceDelete true --header \"Content-Type=application/json\""
  }
}
