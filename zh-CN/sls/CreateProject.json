{
  "name": "CreateProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "body",
      "description": "请求体参数。",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-project --region cn-hangzhou",
    "legacyCli": "aliyun sls POST / --region cn-hangzhou --header \"Content-Type=application/json\" --body \"test-project\""
  }
}
