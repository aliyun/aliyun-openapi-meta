{
  "name": "DeleteLogtailPipelineConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/pipelineconfigs/[configName]",
  "parameters": [
    {
      "name": "configName",
      "description": "Logtail流水线配置名称",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-logtail-pipeline-config --project test-project --config-name test-config --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /pipelineconfigs/test-config --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
