{
  "name": "GetSqlInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/sqlinstance",
  "parameters": [
    {
      "name": "project",
      "description": "project名称",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-sql-instance --project test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /sqlinstance --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
