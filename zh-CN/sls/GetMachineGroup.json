{
  "name": "GetMachineGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/machinegroups/[machineGroup]",
  "parameters": [
    {
      "name": "machineGroup",
      "description": "机器组名称。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-machine-group --project ali-test-project --machine-group test-machine-group --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /machinegroups/test-machine-group --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
