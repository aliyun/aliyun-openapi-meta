{
  "name": "PutIngestProcessor",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/ingestprocessors/[processorName]",
  "parameters": [
    {
      "name": "body",
      "description": "请求结构体。",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "processorName",
      "description": "写入处理器标识。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls put-ingest-processor --project ali-test-project --processor-name parse-nginx-log --display-name Nginx_log_parsing --description Extract_fields_from_Nginx_logs_using_regex. --configuration example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls PUT /ingestprocessors/parse-nginx-log --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"configuration\":\"example-string\",\"displayName\":\"Nginx_log_parsing\",\"description\":\"Extract_fields_from_Nginx_logs_using_regex.\"}"
  }
}
