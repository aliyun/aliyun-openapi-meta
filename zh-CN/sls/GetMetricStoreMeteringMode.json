{
  "name": "GetMetricStoreMeteringMode",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/metricstores/[metricStore]/meteringmode",
  "parameters": [
    {
      "name": "metricStore",
      "description": "时序库名称",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "日志项目名称",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-metric-store-metering-mode --project projectName --metric-store metricStoreName --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /metricstores/metricStoreName/meteringmode --project projectName --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
