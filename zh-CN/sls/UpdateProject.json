{
  "name": "UpdateProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "body",
      "description": "请求结构体。",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls update-project --description Description_of_my-project-test --recycle-bin-enabled true --project ali-project-test --region cn-hangzhou",
    "legacyCli": "aliyun sls PUT / --project ali-project-test --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"description\":\"Description_of_my-project-test\",\"recycleBinEnabled\":true}"
  }
}
