{
  "name": "CreateDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/domains",
  "parameters": [
    {
      "name": "body",
      "description": "请求消息体。",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-domain --project ali-test-project --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /domains --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"domainName\":\"example.com\"}"
  }
}
