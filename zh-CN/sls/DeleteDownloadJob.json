{
  "name": "DeleteDownloadJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/downloadjobs/[downloadJobName]",
  "parameters": [
    {
      "name": "downloadJobName",
      "description": "日志下载任务名称。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-download-job --download-job-name download-123 --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /downloadjobs/download-123 --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
