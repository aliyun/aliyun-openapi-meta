{
  "name": "CreateIndex",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/logstores/[logstore]/index",
  "parameters": [
    {
      "name": "body",
      "description": "请求消息体。",
      "position": "Body",
      "type": "Json",
      "required": false
    },
    {
      "name": "logstore",
      "description": "Logstore的名称。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-index --logstore ali-logstore-test --project ali-project-test --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /logstores/ali-logstore-test/index --project ali-project-test --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}
