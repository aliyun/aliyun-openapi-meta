{
  "name": "UpdateLogStoreProcessor",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/logstores/[logstore]/processor",
  "parameters": [
    {
      "name": "body",
      "description": "请求结构体。",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "logstore",
      "description": "LogStore名称。",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls update-log-store-processor --project ali-test-project --logstore ali-test-logstore --processor-name parse-nginx-log --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /logstores/ali-test-logstore/processor --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"processorName\":\"parse-nginx-log\"}"
  }
}
