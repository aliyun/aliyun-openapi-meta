{
  "name": "GetDownloadJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/downloadjobs/[downloadJobName]",
  "parameters": [
    {
      "name": "downloadJobName",
      "description": "日志下载任务名称",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "description": "Project名称",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-download-job --download-job-name download-123 --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /downloadjobs/download-123 --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
