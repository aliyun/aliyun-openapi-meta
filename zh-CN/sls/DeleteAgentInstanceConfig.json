{
  "name": "DeleteAgentInstanceConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/agentinstanceconfigs/[configType]",
  "parameters": [
    {
      "name": "attributes",
      "description": "进程级配置归属",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "configType",
      "description": "进程级配置类型",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-agent-instance-config --config-type apm_biz_trace --attributes key=example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /agentinstanceconfigs/apm_biz_trace --region cn-hangzhou --attributes '{\"key\":\"example-string\"}' --header \"Content-Type=application/json\""
  }
}
