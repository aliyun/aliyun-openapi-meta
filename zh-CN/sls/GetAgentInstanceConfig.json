{
  "name": "GetAgentInstanceConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/agentinstanceconfigs/[configType]",
  "parameters": [
    {
      "name": "attributes",
      "description": "进程级配置归属",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "configType",
      "description": "进程级配置类型",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-agent-instance-config --config-type apm_biz_trace --attributes key=example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /agentinstanceconfigs/apm_biz_trace --region cn-hangzhou --attributes '{\"key\":\"example-string\"}' --header \"Content-Type=application/json\""
  }
}
