{
  "name": "GetProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-project --project ali-project-test --cross-region true --region cn-hangzhou",
    "legacyCli": "aliyun sls GET / --project ali-project-test --region cn-hangzhou --crossRegion true --header \"Content-Type=application/json\""
  }
}
