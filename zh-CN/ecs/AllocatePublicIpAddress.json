{
  "name": "AllocatePublicIpAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "待分配IP地址的实例ID。\n\n>该实例带宽必须大于0。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IpAddress",
      "description": "实例的公网IP地址。如果该参数值为空，则默认由系统随机分配IP地址。\n\n>该参数只支持白名单用户指定，普通用户不支持指定IP地址。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VlanId",
      "description": "实例的VLAN ID。\n> 该参数即将被弃用，为提高兼容性，请尽量使用其他参数。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ecs allocate-public-ip-address --instance-id i-bp1gtjxuuvwj17zr0000 --ip-address 112.124.00.00 --vlan-id 720 --region cn-hangzhou",
    "legacyCli": "aliyun ecs AllocatePublicIpAddress --region cn-hangzhou --InstanceId 'i-bp1gtjxuuvwj17zr0000' --IpAddress '112.124.00.00' --VlanId 720"
  }
}
