{
  "name": "ListCiphers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CiphersGroup",
      "description": "密码套件组名称，可取值：all，strict，custom。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun esa list-ciphers --ciphers-group strict --region cn-hangzhou",
    "legacyCli": "aliyun esa ListCiphers --region cn-hangzhou --CiphersGroup strict"
  }
}
