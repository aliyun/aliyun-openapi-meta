{
  "name": "DeleteRoutine",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "Routine函数名, 同一个账号下命名唯一。",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn delete-routine --name test --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteRoutine --region cn-hangzhou --Name test"
  }
}
