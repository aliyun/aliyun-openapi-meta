{
  "name": "VerifyDcdnDomainOwner",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "待校验的域名，只支持传入单个域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VerifyType",
      "description": "校验方式，取值：\n\n- **dnsCheck**：DNS验证。\n- **fileCheck**：文件验证。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn verify-dcdn-domain-owner --domain-name 00example00.com --verify-type dnsCheck --region cn-hangzhou",
    "legacyCli": "aliyun dcdn VerifyDcdnDomainOwner --region cn-hangzhou --DomainName '00example00.com' --VerifyType dnsCheck"
  }
}
