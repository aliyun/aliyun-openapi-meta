{
  "name": "DescribeDcdnUserVipsByDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Available",
      "description": "是否查询健康VIP。取值：\n\n- **on**：健康VIP。\n\n- **off**：全部VIP。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "域名，只支持单个域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-user-vips-by-domain --domain-name example.com --available on --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnUserVipsByDomain --region cn-hangzhou --DomainName 'example.com' --Available on"
  }
}
