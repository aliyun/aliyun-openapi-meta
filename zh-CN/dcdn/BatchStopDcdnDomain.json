{
  "name": "BatchStopDcdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "description": "需要接入全站加速的域名，多个用半角逗号（,）分隔。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn batch-stop-dcdn-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn BatchStopDcdnDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
