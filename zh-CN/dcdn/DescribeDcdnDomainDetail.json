{
  "name": "DescribeDcdnDomainDetail",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名，仅支持单个域名查询。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-detail --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainDetail --region cn-hangzhou --DomainName 'example.com'"
  }
}
