{
  "name": "DescribeRoutine",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "边缘函数名, 同一个账号下命名唯一。",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn describe-routine --name test --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeRoutine --region cn-hangzhou --Name test"
  }
}
