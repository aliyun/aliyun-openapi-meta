{
  "name": "StartDcdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名，仅支持启用单个。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn start-dcdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn StartDcdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
