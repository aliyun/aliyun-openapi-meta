{
  "name": "RollbackDcdnStagingConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "加速域名，仅支持传单个域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn rollback-dcdn-staging-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn RollbackDcdnStagingConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}
