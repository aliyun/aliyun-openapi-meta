{
  "name": "BatchStartDcdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "description": "加速域名，多个域名用半角逗号（,）分隔。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun dcdn batch-start-dcdn-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn BatchStartDcdnDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
