{
  "name": "RemoveAuthPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "返回结果显示的语言。取值：zh-CN（默认值）：中文，en-US：英文",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PolicyId",
      "description": "策略ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun mse remove-auth-policy --policy-id 259 --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse RemoveAuthPolicy --region cn-hangzhou --AcceptLanguage zh --PolicyId 259"
  }
}
