{
  "name": "DeleteReplicationLink",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "description": "灾备实例ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PromoteToMaster",
      "description": "是否删除主实例和灾备实例间的数据同步链路，并提升灾备实例为主实例，取值：\n\n- **true**：是\n- **false**：否",
      "position": "Query",
      "type": "Boolean",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds delete-replication-link --db-instance-id m-2zecuz9tolf000000 --promote-to-master true --region cn-hangzhou",
    "legacyCli": "aliyun rds DeleteReplicationLink --region cn-hangzhou --DBInstanceId 'm-2zecuz9tolf000000' --PromoteToMaster true"
  }
}
