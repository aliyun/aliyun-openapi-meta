{
  "name": "CancelActiveOperationTasks",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ids",
      "description": "批量取消ID列表，多个ID需英文逗号（,）分隔，建议一次不超过25个。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}