{
  "name": "DescribeRegions",
  "deprecated": false,
  "security": [
    "AK"
  ],
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "指定返回的**LocalName**参数的语言。取值：\n* **zh-CN**：中文\n* **en-US**：英文\n\n默认值：**en-US**",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}