{
  "name": "ModifyRCInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "description": "是否自动支付。取值范围：\n- **true**（默认）：自动支付。您需要确保账户余额充足。\n- **false**：只生成订单不扣费。\n> 如果您的支付方式余额不足，可以将参数AutoPay设置为false，此时会生成未支付订单，您可以登录RDS管理控制台自行支付。\n>",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "AutoUseCoupon",
      "description": "是否自动使用优惠券，取值：\n* **true**（默认）：是。\n* **false**：否。\n\n> 使用优惠券后，若需要进行降配操作，由代金券抵扣的金额将不会进行退款。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Direction",
      "description": "实例变配类型，取值范围：\n> 该参数可无需上传，系统可自动判断升配还是降配；如要上传，请按照下面的逻辑规则操作。\n- **Up**（默认）：升配实例规格。请确保您的账户支付方式余额充足。\n- **Down**：降配实例规格。当InstanceType设置的实例规格低于当前实例规格时，设置Direction=down。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "是否对本次创建实例的操作执行预检查，取值范围：\n* **true**：执行预检查操作，不创建实例。检查项目包含请求参数、请求格式、业务限制和库存等。\n* **false**（默认）：发送正常请求，通过检查后直接创建实例。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "实例ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceType",
      "description": "需要变配的目标实例规格。RDS Custom实例支持的资源规格，请参见[RDS Custom实例规格列表](~~2844823~~)。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PromotionCode",
      "description": "优惠券Code。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RebootTime",
      "description": "实例的重启时间。\n\n- **RebootWhenFinished**设置为**false**且实例状态为**Running**时，**必须**设置48小时以内的重启时间。\n- 按照ISO 8601标准表示，使用UTC+0时间。格式为：`yyyy-MM-ddTHH:mmZ`。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RebootWhenFinished",
      "description": "实例变配结束后是否立即重启。取值范围：\n\n- **true**（默认）：是。\n- **false**：否。\n\n> 若实例处于**已暂停**状态，即使您设置了`RebootWhenFinished=true`，也会保持原状态不变，并不会执行重启操作。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "实例所在地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}