{
  "name": "DeleteSecretParameter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "加密参数名称。取值范围：内容限制为字母、数字、中划线、下划线，长度不能超过180字符，且不能以ALIYUN、ACS、ALIBABA、ALICLOUD、OOS开头。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun oos delete-secret-parameter --biz-region-id cn-hangzhou --name MySecretParameter --region cn-hangzhou",
    "legacyCli": "aliyun oos DeleteSecretParameter --region cn-hangzhou --RegionId 'cn-hangzhou' --Name MySecretParameter"
  }
}
