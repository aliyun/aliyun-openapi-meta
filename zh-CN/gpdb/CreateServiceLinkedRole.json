{
  "name": "CreateServiceLinkedRole",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "地域ID，您可以通过[DescribeRegions](~~86912~~)接口查看可用的地域ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun gpdb create-service-linked-role --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CreateServiceLinkedRole --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
