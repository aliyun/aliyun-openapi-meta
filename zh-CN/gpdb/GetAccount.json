{
  "name": "GetAccount",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "description": "初始账号名称，限制如下：\n\n- 由小写字母，数字，下划线组成。\n- 以小写字母开头，小写字母或数字结尾。\n- 不能以gp开头。\n- 长度为2~16个字符。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBInstanceId",
      "description": "实例ID。\n>支持传入最多30个实例ID进行批量操作，用英文逗号（,）隔开。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}