{
  "name": "DeleteLiveAppRecordConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "播流所属应用名称。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "主播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StreamName",
      "description": "播流名称。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-app-record-config --domain-name example.com --app-name liveApp0000 --stream-name liveStream0000 --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveAppRecordConfig --region cn-hangzhou --StreamName liveStream0000 --DomainName 'example.com' --AppName liveApp0000"
  }
}
