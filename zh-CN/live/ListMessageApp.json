{
  "name": "ListMessageApp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNum",
      "description": "页码，默认值：1。取值范围：1~100000。\n> 此页码为当前展示页。",
      "position": "Body",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "单页显示应用的数量，默认值：20。取值范围：1~50。",
      "position": "Body",
      "type": "Integer",
      "required": true
    },
    {
      "name": "SortType",
      "description": "排列方式，取值：\n\n- 0：时间递增顺序。\n\n- 1：时间递减顺序。",
      "position": "Body",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live list-message-app --sort-type 1 --page-num 10 --page-size 20 --region cn-hangzhou",
    "legacyCli": "aliyun live ListMessageApp --region cn-hangzhou --SortType 1 --PageNum 10 --PageSize 20"
  }
}
