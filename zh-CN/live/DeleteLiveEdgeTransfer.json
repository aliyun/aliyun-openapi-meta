{
  "name": "DeleteLiveEdgeTransfer",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "推流域名。一个域名只有一个直播转推配置。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-edge-transfer --biz-region-id cn-hangzhou --domain-name demo.aliyundoc.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveEdgeTransfer --region cn-hangzhou --DomainName 'demo.aliyundoc.com' --RegionId 'cn-hangzhou'"
  }
}
