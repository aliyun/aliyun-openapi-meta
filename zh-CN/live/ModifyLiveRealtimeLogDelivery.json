{
  "name": "ModifyLiveRealtimeLogDelivery",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "修改实时日志投递的主播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Logstore",
      "description": "实时投递日志服务（SLS）的LogStoreName。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Project",
      "description": "实时投递SLS的ProjectName。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Region",
      "description": "实时投递SLS的Region。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live modify-live-realtime-log-delivery --biz-region-id cn-hangzhou --project project_example --logstore logstore_example --biz-region cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live ModifyLiveRealtimeLogDelivery --region cn-hangzhou --Project project_example --DomainName 'example.com' --Logstore logstore_example --Region 'cn-hangzhou' --RegionId 'cn-hangzhou'"
  }
}
