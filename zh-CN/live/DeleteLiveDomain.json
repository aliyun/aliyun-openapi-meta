{
  "name": "DeleteLiveDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "需要删除的推流或播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-domain --domain-name demo.aliyundoc.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveDomain --region cn-hangzhou --DomainName 'demo.aliyundoc.com'"
  }
}
