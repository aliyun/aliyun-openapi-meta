{
  "name": "DescribeLiveDomainRealtimeLogDelivery",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live describe-live-domain-realtime-log-delivery --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDomainRealtimeLogDelivery --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}
