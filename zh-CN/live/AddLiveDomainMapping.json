{
  "name": "AddLiveDomainMapping",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PullDomain",
      "description": "您的播流域名。域名类型为：**liveVideo**。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PushDomain",
      "description": "您的推流域名。域名类型为：**liveEdge**。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live add-live-domain-mapping --push-domain demo.aliyundoc.com --pull-domain example.com --region cn-hangzhou",
    "legacyCli": "aliyun live AddLiveDomainMapping --region cn-hangzhou --PullDomain 'example.com' --PushDomain 'demo.aliyundoc.com'"
  }
}
