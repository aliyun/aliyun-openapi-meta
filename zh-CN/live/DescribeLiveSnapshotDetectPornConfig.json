{
  "name": "DescribeLiveSnapshotDetectPornConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "播流所属应用名称。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "主播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Order",
      "description": "按创建时间排序。取值：\n\n- **Asc**（默认值）：升序。\n- **Desc**：降序。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNum",
      "description": "分页的页码。默认值：**1**。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "每页大小。默认值：**10**，取值范围：**5~30**。",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live describe-live-snapshot-detect-porn-config --domain-name example.com --app-name liveApp0000 --page-num 1 --page-size 10 --order Asc --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveSnapshotDetectPornConfig --region cn-hangzhou --Order Asc --PageNum 1 --PageSize 10 --DomainName 'example.com' --AppName liveApp0000"
  }
}
