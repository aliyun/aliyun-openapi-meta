{
  "name": "DescribeLiveDomainMapping",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "您要查询的域名。域名可以是以下类型：\n\n- 推流域名。\n\n- 主播流域名。\n\n- 子播流域名。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live describe-live-domain-mapping --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDomainMapping --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}
