{
  "name": "TagLiveResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceId",
      "description": "资源ID。取值：直播域名。\nN的取值范围为**1~50**。",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "资源类型。固定值：**DOMAIN**。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Tag",
      "description": "标签列表。",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ],
  "example": {
    "unifiedCli": "aliyun live tag-live-resources --biz-region-id cn-hangzhou --resource-id example.com --tag Value=product Key=env --resource-type DOMAIN --region cn-hangzhou",
    "legacyCli": "aliyun live TagLiveResources --region cn-hangzhou --ResourceId.1 'example.com' --Tag.1.Key env --Tag.1.Value product --ResourceType DOMAIN --RegionId 'cn-hangzhou'"
  }
}
