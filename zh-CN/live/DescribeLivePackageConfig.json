{
  "name": "DescribeLivePackageConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "App名，置空表示匹配所有名称。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "直播域名（主播放域名）。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Order",
      "description": "排序。取值：\n\n- **asc**（默认值）：升序。\n\n- **desc**：降序。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNum",
      "description": "当前页码。默认值：1。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "每页大小。默认值：10，取值范围：5~30。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "地域ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StreamName",
      "description": "直播流名，置空表示匹配所有流名。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live describe-live-package-config --biz-region-id cn-hangzhou --domain-name example.com --app-name AppName --stream-name StreamName --page-num 1 --page-size 10 --order asc --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLivePackageConfig --region cn-hangzhou --Order asc --StreamName StreamName --PageNum 1 --PageSize 10 --DomainName 'example.com' --RegionId 'cn-hangzhou' --AppName AppName"
  }
}
