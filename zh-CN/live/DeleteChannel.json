{
  "name": "DeleteChannel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "应用ID，仅支持传单个ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ChannelId",
      "description": "已入会的频道ID，仅支持传单个ID。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-channel --app-id eo850000 --channel-id testid --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteChannel --region cn-hangzhou --AppId eo850000 --ChannelId testid"
  }
}
