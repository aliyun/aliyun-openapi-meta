{
  "name": "DeleteObject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "Bucket名称。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ObjectKey",
      "description": "文件名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens delete-object --object-key image5 --bucket-name tets --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteObject --region cn-hangzhou --BucketName tets --ObjectKey image5"
  }
}
