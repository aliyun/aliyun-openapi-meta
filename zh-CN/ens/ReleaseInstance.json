{
  "name": "ReleaseInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "EIP实例的ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens release-instance --instance-id eip-0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens ReleaseInstance --region cn-hangzhou --InstanceId 'eip-0000'"
  }
}
