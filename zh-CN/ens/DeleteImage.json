{
  "name": "DeleteImage",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageId",
      "description": "镜像ID，仅支持传单个ID。\n\n仅支持自定义镜像。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}