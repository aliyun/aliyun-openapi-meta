{
  "name": "GetBucketAcl",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "Bucket名称。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens get-bucket-acl --bucket-name test --region cn-hangzhou",
    "legacyCli": "aliyun ens GetBucketAcl --region cn-hangzhou --BucketName test"
  }
}
