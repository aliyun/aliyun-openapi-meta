{
  "name": "DescribeLoadBalancerSpec",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "LoadBalancerSpec",
      "description": "负载均衡实例的规格。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens describe-load-balancer-spec --load-balancer-spec elb.s2.small --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeLoadBalancerSpec --region cn-hangzhou --LoadBalancerSpec 'elb.s2.small'"
  }
}
