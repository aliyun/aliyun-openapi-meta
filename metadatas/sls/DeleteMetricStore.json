{
  "name": "DeleteMetricStore",
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/metricstores/[name]",
  "parameters": [
    {
      "name": "name",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-metric-store --project my-project --name my_metric_store --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /metricstores/my_metric_store --project my-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
