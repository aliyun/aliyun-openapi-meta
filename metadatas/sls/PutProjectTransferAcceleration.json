{
  "name": "PutProjectTransferAcceleration",
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/transferacceleration",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls put-project-transfer-acceleration --project test-project --enabled true --region cn-hangzhou",
    "legacyCli": "aliyun sls PUT /transferacceleration --project test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"enabled\":true}"
  }
}
