{
  "name": "GetLogging",
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/logging",
  "parameters": [
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-logging --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logging --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
