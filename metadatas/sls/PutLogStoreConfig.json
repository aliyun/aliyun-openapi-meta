{
  "name": "PutLogStoreConfig",
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/logstores/[logstore]/config",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "logstore",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls put-log-store-config --project ali-test-project --logstore ali-test-logstore --client-ip-headers X-Forwarded-For --region cn-hangzhou",
    "legacyCli": "aliyun sls PUT /logstores/ali-test-logstore/config --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"clientIpHeaders\":[\"X-Forwarded-For\"]}"
  }
}
