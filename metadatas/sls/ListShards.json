{
  "name": "ListShards",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/logstores/[logstore]/shards",
  "parameters": [
    {
      "name": "logstore",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls list-shards --project ali-test-project --logstore test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logstores/test-logstore/shards --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
