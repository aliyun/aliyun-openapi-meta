{
  "name": "GetLogStore",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/logstores/[logstore]",
  "parameters": [
    {
      "name": "logstore",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-log-store --logstore ali-test-logstore --project example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logstores/ali-test-logstore --project example-string --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
