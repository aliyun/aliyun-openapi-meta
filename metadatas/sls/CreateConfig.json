{
  "name": "CreateConfig",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/configs",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": false
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-config --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /configs --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}
