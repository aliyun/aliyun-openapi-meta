{
  "name": "DeleteProjectPolicy",
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/policy",
  "parameters": [
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-project-policy --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /policy --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
