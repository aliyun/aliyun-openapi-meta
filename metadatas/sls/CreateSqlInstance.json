{
  "name": "CreateSqlInstance",
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/sqlinstance",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-sql-instance --project test-project --cu 2 --use-as-default true --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /sqlinstance --project test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"cu\":2,\"useAsDefault\":true}"
  }
}
