{
  "name": "GetStoreViewIndex",
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/storeviews/[name]/index",
  "parameters": [
    {
      "name": "name",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-store-view-index --project example-project --name my_storeview --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /storeviews/my_storeview/index --project example-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
