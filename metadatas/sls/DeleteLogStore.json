{
  "name": "DeleteLogStore",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/logstores/[logstore]",
  "parameters": [
    {
      "name": "logstore",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-log-store --project ali-test-project --logstore aliyun-test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /logstores/aliyun-test-logstore --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
