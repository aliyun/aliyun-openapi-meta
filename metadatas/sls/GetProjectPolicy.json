{
  "name": "GetProjectPolicy",
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/policy",
  "parameters": [
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls get-project-policy --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /policy --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
