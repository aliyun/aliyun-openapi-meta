{
  "name": "CreateDashboard",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/dashboards",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls create-dashboard --project ali-test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /dashboards --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}
