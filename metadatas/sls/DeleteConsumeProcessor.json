{
  "name": "DeleteConsumeProcessor",
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/consumeprocessors/[processorName]",
  "parameters": [
    {
      "name": "processorName",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sls delete-consume-processor --project test-project --processor-name consume-processor-1 --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /consumeprocessors/consume-processor-1 --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
