{
  "name": "VerifyDomainOwner",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VerifyType",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn verify-domain-owner --domain-name example.com --verify-type dnsCheck --region cn-hangzhou",
    "legacyCli": "aliyun cdn VerifyDomainOwner --region cn-hangzhou --DomainName 'example.com' --VerifyType dnsCheck"
  }
}
