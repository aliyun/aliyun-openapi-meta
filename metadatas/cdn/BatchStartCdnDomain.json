{
  "name": "BatchStartCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn batch-start-cdn-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn BatchStartCdnDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
