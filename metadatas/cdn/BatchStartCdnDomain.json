{
  "name": "BatchStartCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，多个用半角逗号（,）分隔。",
        "en": "The accelerated domain names. You can specify one or more domain names. Separate multiple domain names with commas (,)."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn batch-start-cdn-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn BatchStartCdnDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
