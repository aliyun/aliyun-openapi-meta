{
  "name": "DescribeCdnDeletedDomains",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页码，默认值**1**，取值范围：**1**~**100000**。",
        "en": "The number of the page to return. Valid values: **1** to **100000**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示的域名个数，默认值**20**，取值范围：**1**~**500**之前的任意整数。",
        "en": "The number of domain names to return per page. Valid values: an integer between **1** and **500**. Default value: **20**."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-deleted-domains --page-number 1 --page-size 5 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDeletedDomains --region cn-hangzhou --PageSize 5 --PageNumber 1"
  }
}
