{
  "name": "DescribeDomainCname",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要查询的加速域名，多个用逗号（,）分隔。参数不可为空",
        "en": "The accelerated domain name that you want to query. Separate multiple domain names with commas (,). This parameter cannot be left empty."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-cname --domain-name example.com,example.org --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainCname --region cn-hangzhou --DomainName 'example.com,example.org'"
  }
}
