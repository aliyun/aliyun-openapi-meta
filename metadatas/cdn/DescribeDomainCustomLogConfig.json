{
  "name": "DescribeDomainCustomLogConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "域名，只支持单个查询。",
        "en": "The domain name. You can specify only one domain name."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-custom-log-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainCustomLogConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}
