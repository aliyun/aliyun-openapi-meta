{
  "name": "DescribeDomainCcActivityLog",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的加速域名。支持批量域名查询，多个域名用英文逗号（,）分隔。\n\n若参数为空，默认返回所有加速域名合并后的数据。",
        "en": "The accelerated domain name. You can specify multiple domain names and separate them with commas (,).\n\nIf you do not specify this parameter, data of all accelerated domain names under your account is queried."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据结束时间。日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n结束时间需大于起始时间。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe end time must be later than the start time."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码。默认值：**1** 。",
        "en": "The page number of the page to return. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "单页显示数量。默认值：**30**。",
        "en": "The number of entries to return on each page. Default value: **30**."
      }
    },
    {
      "name": "RuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义规则名。取值：\n- 正常模式规则：default_normal。\n- 紧急模式规则：default_attack。\n\n若该参数为空，则查询所有拦截事件。",
        "en": "A custom rule name. Valid values:\n\n*   default_normal: rule for the normal mode\n*   default_attack: rule for the emergency mode\n\nIf you leave this parameter empty, events that triggered rate limiting based on all rules are queried."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间。日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n最小数据粒度为5分钟。\n\n若该参数为空，默认读取过去24小时数据。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe minimum time granularity of data collection is 5 minutes.\n\nIf you leave this parameter empty, the data collected over the last 24 hours is queried."
      }
    },
    {
      "name": "TriggerObject",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "触发对象。\n\n若该参数为空，则查询所有拦截事件。",
        "en": "The trigger of rate limiting by which you want to query data.\n\nIf you leave this parameter empty, all events that triggered rate limiting are queried."
      }
    },
    {
      "name": "Value",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "触发对象值\n\n若该参数为空，则查询所有拦截事件。",
        "en": "The value of the trigger.\n\nIf you leave this parameter empty, all events recorded for the trigger are queried."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-cc-activity-log --domain-name example.com --start-time 2018-12-10T20:00:00Z --end-time 2018-12-10T21:00:00Z --trigger-object IP --value 1.2.3.4 --rule-name test2 --page-size 30 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainCcActivityLog --region cn-hangzhou --EndTime '2018-12-10T21:00:00Z' --PageSize 30 --DomainName 'example.com' --PageNumber 1 --StartTime '2018-12-10T20:00:00Z' --Value '1.2.3.4' --TriggerObject IP --RuleName test2"
  }
}
