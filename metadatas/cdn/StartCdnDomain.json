{
  "name": "StartCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn start-cdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn StartCdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}
