{
  "name": "DeleteSpecificConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "功能配置ID，多个用半角逗号（,）分隔。ConfigId查询和使用，请参见[ConfigId使用说明](~~388994~~)。",
        "en": "The ID of the configuration. Separate multiple configuration IDs with commas (,). For more information about ConfigId, see [Usage notes on ConfigId](~~388994~~)."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持单个删除。",
        "en": "The accelerated domain name. You can specify only one domain name."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn delete-specific-config --domain-name example.com --config-id 2317 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DeleteSpecificConfig --region cn-hangzhou --DomainName 'example.com' --ConfigId 2317"
  }
}
