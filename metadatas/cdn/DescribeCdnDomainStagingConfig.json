{
  "name": "DescribeCdnDomainStagingConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持单个查询。",
        "en": "The accelerated domain name. You can specify only one domain name in each request."
      }
    },
    {
      "name": "FunctionNames",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "功能名称列表，多个用半角逗号（,）分隔，可配置功能请参见[功能列表](~~388460~~)。",
        "en": "The list of feature names. Separate multiple values with commas (,). For more information, see [A list of features](~~388460~~)."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-domain-staging-config --domain-name example.com --function-names aliauth --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDomainStagingConfig --region cn-hangzhou --DomainName 'example.com' --FunctionNames aliauth"
  }
}
