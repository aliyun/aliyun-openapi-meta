{
  "name": "DescribeCdnSSLCertificateList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "加速域名，仅支持查询单个域名。",
        "en": "The accelerated domain name. You can specify only one domain name in each request."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码。默认值：**1** 。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小。\n取值：**1**~**1000**之间的任意整数。",
        "en": "The number of entries per page. Valid values: an integer from **1** to **1000**."
      }
    },
    {
      "name": "SearchKeyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书名称模糊查询。",
        "en": "The keyword that is used to query the certificate."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-ssl-certificate-list --domain-name www.example.com --page-number 1 --page-size 20 --search-keyword certabc --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnSSLCertificateList --region cn-hangzhou --PageSize 20 --DomainName 'www.example.com' --PageNumber 1 --SearchKeyword certabc"
  }
}
