{
  "name": "BatchStopCdnDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，多个用半角逗号（,）分隔。",
        "en": "The names of the accelerated domain names. You can specify one or more domain names in each request. Separate multiple domain names with commas (,)."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn batch-stop-cdn-domain --domain-names example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn BatchStopCdnDomain --region cn-hangzhou --DomainNames 'example.com'"
  }
}
