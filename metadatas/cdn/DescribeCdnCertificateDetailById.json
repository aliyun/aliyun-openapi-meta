{
  "name": "DescribeCdnCertificateDetailById",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "证书ID。",
        "en": "The ID of the certificate."
      }
    },
    {
      "name": "CertRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书Region。取值：\n- **ap-southeast-1**（新加坡）\n-  **cn-hangzhou**（杭州）\n\n默认值为**cn-hangzhou**。",
        "en": "The region of the certificate. Valid values:\n\n*   **ap-southeast-1**: Singapore\n*   **cn-hangzhou**: China (Hangzhou)\n\nDefault value: **cn-hangzhou**"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-certificate-detail-by-id --cert-id 12345 --cert-region cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnCertificateDetailById --region cn-hangzhou --CertId 12345 --CertRegion 'cn-hangzhou'"
  }
}
