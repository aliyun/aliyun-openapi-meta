{
  "name": "DescribeCdnHttpsDomainList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键字。",
        "en": "The keyword that is used to search for certificates."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "取得第几页，取值范围为：**1**~**100000**。",
        "en": "The number of the page to return. Valid values: **1** to **100000**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，默认**20**。",
        "en": "The number of entries to return on each page. Default value: **20**."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-https-domain-list --page-number 5 --page-size 20 --keyword com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnHttpsDomainList --region cn-hangzhou --PageSize 20 --Keyword com --PageNumber 5"
  }
}
