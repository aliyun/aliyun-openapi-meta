{
  "name": "DescribeCdnDeliverList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeliverId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "待查询的订阅任务ID。如果不传该参数，默认返回所有的订阅任务。",
        "en": "The ID of the tracking task that you want to query. If you do not specify an ID, all tracking tasks are queried."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-deliver-list --deliver-id 3 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDeliverList --region cn-hangzhou --DeliverId 3"
  }
}
