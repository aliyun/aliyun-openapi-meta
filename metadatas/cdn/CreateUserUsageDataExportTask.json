{
  "name": "CreateUserUsageDataExportTask",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "获取数据结束时间点，结束时间需大于起始时间。\n\n日期格式按照ISO8601表示法，并使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The end of the time range to query. The end time must be later than the start time.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "Language",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "导出文件的语言。\n\n- **zh-cn**（默认值）：简体中文。\n\n- **en-us**：英文。",
        "en": "The language in which you want to export the file. Default value: zh-cn. Valid values:\n\n*   **zh-cn**: Chinese\n*   **en-us**: English"
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "获取数据起始时间点，数据粒度为5分钟。\n\n日期格式按照ISO8601表示法，并使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The start of the time range to query. The data is collected every 5 minutes.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "TaskName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务名称。",
        "en": "The name of the task."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn create-user-usage-data-export-task --start-time 2015-12-10T20:00:00Z --end-time 2015-12-10T21:00:00Z --task-name Refresh --biz-language zh-cn --region cn-hangzhou",
    "legacyCli": "aliyun cdn CreateUserUsageDataExportTask --region cn-hangzhou --EndTime '2015-12-10T21:00:00Z' --Language 'zh-cn' --TaskName Refresh --StartTime '2015-12-10T20:00:00Z'"
  }
}
