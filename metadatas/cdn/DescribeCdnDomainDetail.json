{
  "name": "DescribeCdnDomainDetail",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名信息，仅支持单个查询。",
        "en": "The accelerated domain name. You can specify only one domain name."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-domain-detail --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDomainDetail --region cn-hangzhou --DomainName 'example.com'"
  }
}
