{
  "name": "DescribeCdnSMCertificateList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持查询单个域名。",
        "en": "The accelerated domain name. You can specify only one domain name in each request."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-sm-certificate-list --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnSMCertificateList --region cn-hangzhou --DomainName 'example.com'"
  }
}
