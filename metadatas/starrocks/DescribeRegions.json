{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/webapi/region/list",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun starrocks describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun starrocks POST /webapi/region/list --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
