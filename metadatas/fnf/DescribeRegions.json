{
  "name": "DescribeRegions",
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun fnf describe-regions --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun fnf DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN'"
  }
}
