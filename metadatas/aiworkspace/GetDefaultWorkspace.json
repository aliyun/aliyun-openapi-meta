{
  "name": "GetDefaultWorkspace",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/defaultWorkspaces",
  "parameters": [
    {
      "name": "Verbose",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否显示默认工作空间的详细信息，目前详细信息包含阶段列表详情（Conditions）。取值如下：\n- false（默认值）：不显示详细信息。\n- true：显示详细信息。",
        "en": "Specifies whether to show the details of the default workspace. The details include the conditions of the workspace in different phases. Valid values:\n\n*   false (default)\n*   true"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun aiworkspace get-default-workspace --verbose false --region cn-hangzhou",
    "legacyCli": "aliyun aiworkspace GET /api/v1/defaultWorkspaces --region cn-hangzhou --Verbose false --header \"Content-Type=application/json\""
  }
}
