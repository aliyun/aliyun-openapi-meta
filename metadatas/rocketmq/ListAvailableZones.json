{
  "name": "ListAvailableZones",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/zones",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun rocketmq list-available-zones --region cn-hangzhou",
    "legacyCli": "aliyun rocketmq GET /zones --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
