{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun dds describe-regions --biz-region-id cn-hangzhou --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeRegions --region cn-hangzhou --AcceptLanguage zh --RegionId 'cn-hangzhou'"
  }
}
