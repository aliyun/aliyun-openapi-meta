{
  "name": "DescribeServiceMeshes",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Tag",
      "position": "Query",
      "type": "Array",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun servicemesh describe-service-meshes --tag Value=yahaha Key=test --region cn-hangzhou",
    "legacyCli": "aliyun servicemesh DescribeServiceMeshes --region cn-hangzhou --Tag.1.Key test --Tag.1.Value yahaha --force"
  }
}
