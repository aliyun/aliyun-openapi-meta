{
  "name": "DescribeVpcs",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun servicemesh describe-vpcs --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun servicemesh DescribeVpcs --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
