{
  "name": "GetWafQuota",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Paths",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun esa get-waf-quota --paths page --region cn-hangzhou",
    "legacyCli": "aliyun esa GetWafQuota --region cn-hangzhou --Paths page"
  }
}
