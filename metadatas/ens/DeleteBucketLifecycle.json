{
  "name": "DeleteBucketLifecycle",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens delete-bucket-lifecycle --bucket-name test --rule-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteBucketLifecycle --region cn-hangzhou --BucketName test --RuleId 1"
  }
}
