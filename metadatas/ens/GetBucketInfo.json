{
  "name": "GetBucketInfo",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ens get-bucket-info --bucket-name test --region cn-hangzhou",
    "legacyCli": "aliyun ens GetBucketInfo --region cn-hangzhou --BucketName test"
  }
}
