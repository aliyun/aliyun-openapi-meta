{
  "name": "DeleteProject",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/projects/[projectName]",
  "parameters": [
    {
      "name": "isLogical",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute delete-project --project-name odps_project --is-logical true --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/projects/odps_project --region cn-hangzhou --isLogical true --header \"Content-Type=application/json\""
  }
}
