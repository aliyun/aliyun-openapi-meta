{
  "name": "ListPackages",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/projects/[projectName]/packages",
  "parameters": [
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute list-packages --project-name projectA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/projects/projectA/packages --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
