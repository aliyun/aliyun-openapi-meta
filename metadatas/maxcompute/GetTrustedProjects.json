{
  "name": "GetTrustedProjects",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/projects/[projectName]/trustedProjects",
  "parameters": [
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute get-trusted-projects --project-name odps_project --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/projects/odps_project/trustedProjects --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
