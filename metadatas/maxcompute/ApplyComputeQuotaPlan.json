{
  "name": "ApplyComputeQuotaPlan",
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/quotas/[nickname]/computeQuotaPlan/[planName]/apply",
  "parameters": [
    {
      "name": "nickname",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "planName",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun maxcompute apply-compute-quota-plan --nickname os_ComputeQuota_p --plan-name planA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute PUT /api/v1/quotas/os_ComputeQuota_p/computeQuotaPlan/planA/apply --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
