{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun hitsdb describe-regions --accept-language en-US --api-version 2020-06-15 --region cn-hangzhou",
    "legacyCli": "aliyun hitsdb DescribeRegions --region cn-hangzhou --AcceptLanguage 'en-US'"
  }
}
