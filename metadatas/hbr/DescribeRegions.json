{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun hbr describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun hbr DescribeRegions --region cn-hangzhou"
  }
}
