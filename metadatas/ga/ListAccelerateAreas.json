{
  "name": "ListAccelerateAreas",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ga list-accelerate-areas --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ga ListAccelerateAreas --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
