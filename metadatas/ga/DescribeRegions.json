{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ga describe-regions --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ga DescribeRegions --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
