{
  "name": "UntagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，仅当未传入tagkey.n时有效。取值范围：-true-false",
        "en": "Specifies whether to remove all tags. This parameter is valid only if the TagKey.n parameter is not set. Valid values: true and false."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源id列表",
        "en": "The IDs of resources."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型\ninstance 节省计划实例",
        "en": "The type of the resource. Specify the savings plan instance as the type of the resource."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键列表",
        "en": "The tag keys."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun bssopenapi untag-resources --resource-type instance --all true --resource-id spn-xxxxxx --tag-key codename --api-version 2017-12-14 --region cn-hangzhou",
    "legacyCli": "aliyun bssopenapi UntagResources --region cn-hangzhou --All true --ResourceId.1 'spn-xxxxxx' --TagKey.1 codename --ResourceType instance"
  }
}
