{
  "name": "DescribeCostBudgetsSummary",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BudgetName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算名称。模糊匹配",
        "en": "The name of the budget. Fuzzy match is supported."
      }
    },
    {
      "name": "BudgetStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算状态，枚举值：overdue（已过期）,notOverdue（未过期）。不填写则默认取全部",
        "en": "The status of the budget. Valid values: overdue and notOverdue. A value of overdue specifies to filter expired budgets. A value of notOverdue specifies to filter budgets that do not expire. By default, if you do not specify this parameter, information about all budgets is to be returned."
      }
    },
    {
      "name": "BudgetType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算类型。枚举值：cost(费用预算), byquantity（按量编制预算）, asset（使用率及覆盖率预算）。\n不填写则默认取全部",
        "en": "The type of the budget. Valid values: cost, byquantity, and asset. A value of cost specifies to filter expense budgets. A value of byquantity specifies to filter budgets calculated based on the resource usage. A value of asset specifies to filter usage or coverage budgets. By default, information about all budgets is returned if you do not specify this parameter."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "读取的最大数据记录数量。默认值：10，最大值：10，最小值：1。",
        "en": "The number of entries to return on each page. Default value: 10. Maximum value: 10. Minimum value: 1."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当前调用开始读取的位置，参数值必须为空或者使用返回结果中的NextToken设值，否则会报错，空则代表从头开始读取。默认：空",
        "en": "The position in which the query starts. You must set this parameter to null or the token that is obtained from the previous query. Otherwise, an error is returned. If you set the NextToken parameter to null, the query starts from the beginning. The default value is null."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun bssopenapi describe-cost-budgets-summary --budget-name '全年预算' --budget-status notOverdue --budget-type cost --next-token eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6NH0= --max-results 5 --api-version 2017-12-14 --region cn-hangzhou",
    "legacyCli": "aliyun bssopenapi DescribeCostBudgetsSummary --region cn-hangzhou --BudgetStatus notOverdue --NextToken 'eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6NH0=' --MaxResults 5 --BudgetName '全年预算' --BudgetType cost"
  }
}
