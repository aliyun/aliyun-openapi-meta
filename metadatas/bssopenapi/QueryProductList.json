{
  "name": "QueryProductList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "当前页码。",
        "en": "The page number of the page to return."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，最大300，默认为20。",
        "en": "The number of entries to return on each page. Default value: 20."
      }
    },
    {
      "name": "QueryTotalCount",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否返回产品总数目，默认为false。",
        "en": "Specifies whether to return the total number of services. Default value: false."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun bssopenapi query-product-list --query-total-count true --page-num 1 --page-size 10 --api-version 2017-12-14 --region cn-hangzhou",
    "legacyCli": "aliyun bssopenapi QueryProductList --region cn-hangzhou --PageNum 1 --PageSize 10 --QueryTotalCount true"
  }
}
