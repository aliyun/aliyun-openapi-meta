{
  "name": "DescribeUserPermissions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定RAM用户。",
        "en": "The ID of the RAM user that you want to query."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adcp describe-user-permissions --user-id 211750000 --region cn-hangzhou",
    "legacyCli": "aliyun adcp DescribeUserPermissions --region cn-hangzhou --UserId 211750000"
  }
}
