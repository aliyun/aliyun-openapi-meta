{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Language",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "语言。取值：\n- `zh`：中文。\n- `en`：英文。",
        "en": "The language. Valid values: zh and en."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adcp describe-regions --biz-language en --region cn-hangzhou",
    "legacyCli": "aliyun adcp DescribeRegions --region cn-hangzhou --Language en"
  }
}
