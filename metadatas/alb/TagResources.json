{
  "name": "TagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源ID。",
        "en": "The resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值：\n\n- **acl**：访问控制。\n\n- **loadbalancer**：应用型负载均衡实例。\n\n- **securitypolicy**：安全策略。\n\n- **servergroup**：服务器组。",
        "en": "The type of the resource. Valid values:\n\n*   **acl**: an access control list (ACL)\n*   **loadbalancer**: an Application Load Balancer (ALB) instance\n*   **securitypolicy**: a security policy\n*   **servergroup**: a server group"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "标签。",
        "en": "The tags."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb tag-resources --resource-type loadbalancer --resource-id alb-gws76zi000000 --tag Value=product Key=env --region cn-hangzhou",
    "legacyCli": "aliyun alb TagResources --region cn-hangzhou --ResourceId.1 'alb-gws76zi000000' --Tag.1.Key env --Tag.1.Value product --ResourceType loadbalancer --method POST --force"
  }
}
