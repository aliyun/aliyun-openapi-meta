{
  "name": "ListListenerCertificates",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertificateIds",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "证书ID列表",
        "en": "The certificates."
      }
    },
    {
      "name": "CertificateType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书类型。取值：**Ca**或**Server**。",
        "en": "The type of the certificate. Valid values: **Ca** and **Server**."
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "监听ID。HTTPS和QUIC监听有效。",
        "en": "The listener ID. You must specify the ID of an HTTPS listener or a QUIC listener."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次读取的最大数据记录。取值范围：**1~100**。入参为空时，默认值为**20**。",
        "en": "The number of entries to return in each call. Valid values: **1 to 100**. Default value: **20**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的**NextToken**值。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. Valid values:\n\n*   You do not need to specify this parameter for the first request.\n*   You must specify the token that is obtained from the previous query as the value of **NextToken**."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb list-listener-certificates --next-token FFmyTO70tTpLG6I3FmYAXGKPd0000 --max-results 50 --listener-id lsn-5qnirjhpt000000 --certificate-type Server --certificate-ids 14160000-cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun alb ListListenerCertificates --region cn-hangzhou --CertificateType Server --CertificateIds.1 '14160000-cn-hangzhou' --NextToken FFmyTO70tTpLG6I3FmYAXGKPd0000 --MaxResults 50 --ListenerId 'lsn-5qnirjhpt000000' --method POST --force"
  }
}
