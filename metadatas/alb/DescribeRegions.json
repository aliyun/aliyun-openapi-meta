{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb describe-regions --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun alb DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN'"
  }
}
