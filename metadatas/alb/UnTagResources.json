{
  "name": "UnTagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源的所有标签。取值：\n\n- **true**：解绑资源的所有标签。\n- **false**（默认值）：不解绑资源的所有标签。",
        "en": "Specifies whether to remove all tags from the specified resource. Valid values:\n\n*   **true**\n*   **false**"
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "要删除标签的资源ID列表。",
        "en": "The IDs of the resources from which you want to remove tags."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值：\n\n- **acl**：访问控制。\n\n- **loadbalancer**：应用型负载均衡实例。\n\n- **securitypolicy**：安全策略。\n\n- **servergroup**：服务器组。",
        "en": "The type of the resource. Valid values:\n\n*   **acl**: an access control list (ACL)\n*   **loadbalancer**: an Application Load Balancer (ALB) instance\n*   **securitypolicy**: a security policy\n*   **servergroup**: a server group"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "要删除的标签列表。",
        "en": "The tags that you want to remove."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "要删除标签的标签键列表。",
        "en": "The keys of the tags that you want to remove."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb un-tag-resources --resource-type loadbalancer --resource-id alb-gws76zij000000 --tag Value=product Key=env --tag-key env --all false --region cn-hangzhou",
    "legacyCli": "aliyun alb UnTagResources --region cn-hangzhou --All false --ResourceId.1 'alb-gws76zij000000' --Tag.1.Key env --Tag.1.Value product --TagKey.1 env --ResourceType loadbalancer --method POST --force"
  }
}
