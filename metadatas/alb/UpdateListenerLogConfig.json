{
  "name": "UpdateListenerLogConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessLogRecordCustomizedHeadersEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "访问日志是否开启携带自定义头字段。取值：\n\n- **true**：是。\n- **false**（默认值）：否。\n\n> 只有实例访问日志开关**AccessLogEnabled**打开时，才能设置此参数为**true**。",
        "en": "Specifies whether to record custom headers in the access log. Valid values:\n\n*   **true**\n*   **false** (default)\n\n> You can set this parameter to **true** only if the access log feature is enabled by specifying **AccessLogEnabled**."
      }
    },
    {
      "name": "AccessLogTracingConfig",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "Xtrace配置信息。",
        "en": "The configuration information about the Xtrace feature."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会更新监听的日志配置。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回**HTTP_2xx**状态码并直接进行操作。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false**: (default): performs a dry run and performs the actual request. If the request passes the dry run, a **2xx HTTP** status code is returned and the operation is performed."
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用型负载均衡实例监听ID。",
        "en": "The ID of the Application Load Balancer (ALB) listener."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alb update-listener-log-config --listener-id lsn-o4u54y73wq7b000000 --client-token 5A2CFF0E-5718-45B5-9D4D-70B000000 --dry-run true --access-log-record-customized-headers-enabled true --access-log-tracing-config TracingType=Zipkin TracingSample=100 TracingEnabled=true --region cn-hangzhou",
    "legacyCli": "aliyun alb UpdateListenerLogConfig --region cn-hangzhou --DryRun true --AccessLogRecordCustomizedHeadersEnabled true --AccessLogTracingConfig.TracingType Zipkin --AccessLogTracingConfig.TracingEnabled true --AccessLogTracingConfig.TracingSample 100 --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B000000' --ListenerId 'lsn-o4u54y73wq7b000000' --method POST --force"
  }
}
