{
  "name": "DeleteEventBridgeIntegration",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Body",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "必填，EventBridge通知集成ID，可通过**ListEventBridgeIntegrations**接口查询。",
        "en": "Required. The ID of the EventBridge notification integration. You can call the **ListEventBridgeIntegrations** operation to query the ID."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms delete-event-bridge-integration --id 123 --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms DeleteEventBridgeIntegration --region cn-hangzhou --Id 123"
  }
}
