{
  "name": "AddPrometheusInstance",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Remote Write实例类型的名称。",
        "en": "The name of the Prometheus instance for Remote Write."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例类型， 目前仅支持Remote Write类型， 简写RW。",
        "en": "The type of the Prometheus instance. Only Prometheus instances for Remote Write is supported. Set the value to RW."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms add-prometheus-instance --biz-region-id cn-hangzhou --name notificationpolicy_test --type RW --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms AddPrometheusInstance --region cn-hangzhou --Type RW --RegionId 'cn-hangzhou' --Name notificationpolicy_test"
  }
}
