{
  "name": "OpenVCluster",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群类型，云产品统一为`cloud-product-prometheus`。",
        "en": "The type of the cluster. For cloud services, set this parameter to `cloud-product-prometheus`."
      }
    },
    {
      "name": "Length",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "集群ID长度，默认为10。",
        "en": "The length of the cluster ID. Default value: 10."
      }
    },
    {
      "name": "Product",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云产品名称，当`clusterType=cloud-product-prometheus`时该字段必传。目前已经接入的云产品名为influxdb、mongodb、DLA，该参数不支持传入多个。",
        "en": "The name of the cloud service. This parameter must be specified when ClusterType is set to `cloud-product-prometheus`. Valid values: influxdb, mongodb, and DLA. You cannot specify multiple service names."
      }
    },
    {
      "name": "RecreateSwitch",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "创建或者查询虚拟集群开关参数，可以对老数据兼容控制。",
        "en": "Specifies whether to create or query a virtual cluster. This parameter provides backward compatibility."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID",
        "en": "The region ID."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms open-vcluster --biz-region-id cn-hangzhou --cluster-type cloud-product-prometheus --length 10 --product influxdb --recreate-switch false --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms OpenVCluster --region cn-hangzhou --Length 10 --Product influxdb --RegionId 'cn-hangzhou' --ClusterType 'cloud-product-prometheus' --RecreateSwitch false"
  }
}
