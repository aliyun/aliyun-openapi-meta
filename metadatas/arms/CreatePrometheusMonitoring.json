{
  "name": "CreatePrometheusMonitoring",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "prometheus实例ID",
        "en": "The ID of the Prometheus instance."
      }
    },
    {
      "name": "ConfigYaml",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "监控配置yaml串。",
        "en": "The monitoring configuration. Specify a YAML string."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监控配置状态：run, stop。\n为空时，默认是run。\nprobe暂不支持配置状态，此字段为空。",
        "en": "The status of the monitoring configuration. Valid values: run and stop. Default value: run. This parameter is not available if the Type parameter is set to Probe."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "容器服务类型的实例支持：serviceMonitor、podMonitor、customJob、probe。\necs类型的实例支持：customJob、probe。",
        "en": "The type of the monitoring configuration. \nValid values for a Prometheus instance for Container Service: serviceMonitor, podMonitor, customJob, and probe. \nValid values for a Prometheus instance for ECS: customJob and probe."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms create-prometheus-monitoring --biz-region-id cn-hangzhou --cluster-id cc7a37ee31aea4ed1a059eff8034b0000 --type serviceMonitor --status run --config-yaml '参见请求参数补充说明。' --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms CreatePrometheusMonitoring --region cn-hangzhou --Status run --Type serviceMonitor --ClusterId cc7a37ee31aea4ed1a059eff8034b0000 --ConfigYaml '参见请求参数补充说明。' --RegionId 'cn-hangzhou'"
  }
}
