{
  "name": "SearchAlertHistories",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AlertId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "报警规则ID，可调用SearchAlertRules接口获取（对应返回参数中的`Id`），详情请参见[SearchAlertRules](~~175825~~)。",
        "en": "The ID of the alert rule. You can call the SearchAlertRules operation and view the `Id` parameter in the response. For more information, see [SearchAlertRules](~~175825~~)."
      }
    },
    {
      "name": "AlertType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "报警规则类型：\n\n- `1`：基于下钻数据集的自定义监控报警规则。\n- `3`：基于平铺数据集的自定义监控报警规则。\n- `4`：前端监控报警规则，包含默认前端监控报警规则（AlertType=6）。\n- `5`：应用监控报警规则，包含默认应用监控报警规则（AlertType=7）。\n- `6`：默认前端监控报警规则。\n- `7`：默认应用监控报警规则。\n- `8`：链路追踪Tracing Analysis报警规则。\n- `101`：Prometheus报警规则。",
        "en": "The type of the alert rule. Valid values:\n\n*   `1`: a custom alert rule that is used to monitor drill-down data sets\n*   `3`: a custom alert rule that is used to monitor tiled data sets\n*   `4`: an alert rule that is used to monitor web pages, including the default alert rule for browser monitoring\n*   `5`: an alert rule that is used to monitor applications, including the default alert rule for application monitoring\n*   `6`: the default alert rule for browser monitoring\n*   `7`: the default alert rule for application monitoring\n*   `8`: a Tracing Analysis alert rule\n*   `101`: a Prometheus alert rule"
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询结果分页的页码。默认为`1`。",
        "en": "The number of the page to return. Default value: `1`."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询报警历史记录的结束时间的时间戳。格式为Unix Timestamp Long，单位为毫秒。默认为当前时间。",
        "en": "The end of the time range to query. The value is a UNIX timestamp of the LONG data type. Unit: milliseconds. The default value is the current time."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询结果分页的每页项目数量。默认为`10`。",
        "en": "The number of entries to return on each page. Default value: `10`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。默认为`cn-hangzhou`。",
        "en": "The ID of the region. Default value: `cn-hangzhou`."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询报警历史记录的开始时间的时间戳。格式为Unix Timestamp Long，单位为毫秒。默认为当前时间的前10分钟。",
        "en": "The beginning of the time range to query. The value is a UNIX timestamp of the LONG data type. Unit: milliseconds. The default value is 10 minutes before the current time."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms search-alert-histories --alert-id 123 --alert-type 4 --current-page 1 --page-size 10 --biz-region-id cn-hangzhou --start-time 1595568910000 --end-time 1579499626000 --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms SearchAlertHistories --region cn-hangzhou --AlertType 4 --AlertId 123 --PageSize 10 --EndTime 1579499626000 --CurrentPage 1 --StartTime 1595568910000 --RegionId 'cn-hangzhou'"
  }
}
