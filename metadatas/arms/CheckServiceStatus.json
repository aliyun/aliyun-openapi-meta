{
  "name": "CheckServiceStatus",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "SvcCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "阿里云ServiceCode。 Arms Prometheus对应的ServiceCode为prometheus。",
        "en": "The service code of an Alibaba Cloud service. The service code of Managed Service for Prometheus is prometheus."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun arms check-service-status --svc-code prometheus --biz-region-id cn-hangzhou --api-version 2019-08-08 --region cn-hangzhou",
    "legacyCli": "aliyun arms CheckServiceStatus --region cn-hangzhou --SvcCode prometheus --RegionId 'cn-hangzhou'"
  }
}
