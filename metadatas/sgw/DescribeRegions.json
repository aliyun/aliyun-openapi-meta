{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun sgw describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun sgw DescribeRegions --region cn-hangzhou"
  }
}
