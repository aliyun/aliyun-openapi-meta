{
  "name": "CreateBackup",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun gpdb create-backup --db-instance-id gp-xxxxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CreateBackup --region cn-hangzhou --DBInstanceId 'gp-xxxxxxxxx'"
  }
}
