{
  "name": "CheckServiceLinkedRole",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun gpdb check-service-linked-role --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CheckServiceLinkedRole --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
