{
  "name": "GetConnection",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun eventbridge get-connection --connection-name connection-name --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge GetConnection --region cn-hangzhou --ConnectionName 'connection-name'"
  }
}
