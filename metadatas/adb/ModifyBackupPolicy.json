{
  "name": "ModifyBackupPolicy",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupRetentionPeriod",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全量备份保留天数，取值范围为：7~730。\n> 本参数不填写，默认为7天。",
        "en": "The number of days for which to retain full backup files. Valid values: 7 to 730.\n\n>  If you do not specify this parameter, the default value 7 is used."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The ID of the cluster."
      }
    },
    {
      "name": "EnableBackupLog",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启日志（实时）备份。取值说明：\n- **Enable**：开启。\n- **Disable**：关闭。\n> 本参数不填写，默认开启。",
        "en": "Specifies whether to enable real-time log backup. Valid values:\n\n*   **Enable**\n\n*   **Disable**\n\n> If you leave this parameter empty, the default value Enable is used."
      }
    },
    {
      "name": "LogBackupRetentionPeriod",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "日志（实时）数据备份保留天数，取值范围为：7~730。\n> 本参数不填写，默认为7天。",
        "en": "The number of days for which to retain log backup files. Valid values: 7 to 730.\n\n>  If you do not specify this parameter, the default value 7 is used."
      }
    },
    {
      "name": "PreferredBackupPeriod",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全量备份周期，多个取值用英文逗号（,）隔开。取值说明：\n- **Monday**：周一。\n- **Tuesday**：周二。\n- **Wednesday**：周三。\n- **Thursday**：周四。\n- **Friday**：周五。\n- **Saturday**：周六。\n- **Sunday**：周日。\n\n> 为保证数据安全，最少选择两个。",
        "en": "The days of the week on which to perform full backup. Separate multiple values with commas (,). Valid values:\n\n*   **Monday**\n*   **Tuesday**\n*   **Wednesday**\n*   **Thursday**\n*   **Friday**\n*   **Saturday**\n*   **Sunday**\n\n>  To ensure data security, we recommend that you specify at least two values."
      }
    },
    {
      "name": "PreferredBackupTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "执行全量备份的开始时间，格式为HH:mmZ-HH:mmZ（UTC时间）。\n\n> 时间范围限制为1小时。",
        "en": "The start time of the full backup within a time range. Specify the time range in the HH:mmZ-HH:mmZ format. The time must be in UTC.\n\n>  The time range is 1 hour."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb modify-backup-policy --db-cluster-id am-bp1xxxxxxxx47 --preferred-backup-time 00:00Z-01:00Z --preferred-backup-period Monday,Wednesday,Friday,Sunday --backup-retention-period 30 --enable-backup-log Enable --log-backup-retention-period 30 --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb ModifyBackupPolicy --region cn-hangzhou --PreferredBackupPeriod 'Monday,Wednesday,Friday,Sunday' --LogBackupRetentionPeriod 30 --PreferredBackupTime '00:00Z-01:00Z' --DBClusterId 'am-bp1xxxxxxxx47' --EnableBackupLog Enable --BackupRetentionPeriod 30"
  }
}
