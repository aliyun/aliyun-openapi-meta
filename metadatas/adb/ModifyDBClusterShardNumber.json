{
  "name": "ModifyDBClusterShardNumber",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AnalyticDB MySQL数仓版集群ID。\n> 您可以调用[DescribeDBClusters](~~129857~~)接口查看目标地域下所有AnalyticDB MySQL数仓版集群的集群ID。",
        "en": "The ID of the AnalyticDB for MySQL Data Warehouse Edition cluster.\n\n>  You can call the [DescribeDBClusters](~~129857~~) operation to query the information about all AnalyticDB for MySQL Data Warehouse Edition clusters within a region, including cluster IDs."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求, 默认为 false。取值范围：\n- true：发送检查请求，检查实例是否满足修改分片数目前置条件和传入目标分片数目的合法性，并**不会**执行修改分片操作。\n- false（默认）：发送正常请求，通过检查后触发后台任务更改集群分片数目。",
        "en": "Specifies whether to perform only a dry run. Valid values:\n\n*   true: sends a request to check whether the cluster meets the prerequisites for changing the number of shards and whether the desired number of shards is valid, but **does not** perform the change operation.\n*   false (default): sends a request to perform a check and trigger the change operation."
      }
    },
    {
      "name": "NewShardNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "目标分片(Shard)数目",
        "en": "The desired number of shards."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n>您可以调用[DescribeRegions](~~143074~~)接口查看地域ID。",
        "en": "The region ID.\n\n>  You can call the [DescribeRegions](~~143074~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SwitchTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后台将在指定时间点执行目标待处理运维事件所对应的操作。格式为`yyyy-MM-ddTHH:mm:ssZ`（UTC时间）。",
        "en": "The point in time when you want the system to perform the change operation. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC."
      }
    },
    {
      "name": "SwitchTimeMode",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "修改参数的执行时间，取值：\n* **Immediate**：默认值，立即执行。\n* **MaintainTime**：实例可运维时间段内执行。可调用ModifyDBInstanceMaintainTime接口修改可运维时间段。\n* **ScheduleTime**：手动指定执行时间。传入该值需要同时传入**SwitchTime**参数。",
        "en": "The mode in which you want the change operation to be performed. Valid values:\n\n*   **Immediate** (default): immediately performs the change operation.\n*   **MaintainTime**: performs the change operation within the maintenance window of the cluster. You can call the ModifyDBInstanceMaintainTime operation to change the maintenance window.\n*   **ScheduleTime**: performs the change operation at the point in time that you specify. If you specify this value, you must also specify **SwitchTime**."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb modify-db-cluster-shard-number --biz-region-id cn-hangzhou --db-cluster-id amv-bp1xxxxxxxx47 --new-shard-number 256 --switch-time-mode 1 --switch-time 2021-07-09T22:00:00Z --dry-run false --is-rollback true --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb ModifyDBClusterShardNumber --region cn-hangzhou --IsRollback true --NewShardNumber 256 --SwitchTimeMode 1 --DBClusterId 'amv-bp1xxxxxxxx47' --DryRun false --RegionId 'cn-hangzhou' --SwitchTime '2021-07-09T22:00:00Z'"
  }
}
