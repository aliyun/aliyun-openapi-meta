{
  "name": "AllocateClusterPublicConnection",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionStringPrefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "公网连接地址的前缀。\n- 由小写字母开头，必须包含小写字母、数字和中划线（-）组成。\n- 长度不超过30个字符。\n- 默认使用集群名。",
        "en": "The prefix of the public endpoint.\n\n*   The prefix must contain lowercase letters, digits, and hyphens (-). It must start with a lowercase letter.\n*   The prefix can be up to 30 characters in length.\n*   By default, the cluster name is used as the prefix of the public endpoint."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数仓版集群ID。\n您可以调用[DescribeDBClusters](~~129857~~)接口查看目标地域下所有数仓版集群的集群ID。",
        "en": "The Data Warehouse Edition Cluster ID of AnalyticDB for MySQL. You can call the [DescribeDBClusters](~~129857~~) operation to retrieve all Data Warehouse Edition cluster IDs within the specified region."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb allocate-cluster-public-connection --db-cluster-id am-bp278jg90000 --connection-string-prefix am-bp278jg90000.ads.aliyuncs.com --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb AllocateClusterPublicConnection --region cn-hangzhou --DBClusterId 'am-bp278jg90000' --ConnectionStringPrefix 'am-bp278jg90000.ads.aliyuncs.com'"
  }
}
