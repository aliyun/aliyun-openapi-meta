{
  "name": "DescribeTablePartitionDiagnose",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。\n> 您可以调用[DescribeDBClusters](~~612241~~)接口查看目标地域下所有AnalyticDB MySQL数仓版集群的集群ID。",
        "en": "The ID of the cluster."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese.\n*   **en**: English."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "将查询结果按指定字段进行排序。格式为JOSN字符串，例如`[{\"Field\":\"TotalSize\",\"Type\":\"Desc\"}]`。其中：\n- `Field`表示排序字段。支持如下取值：\n    - `SchemaName`：表所属的数据库名。\n    - `TableName`：表名。\n    - `TotalSize`：总数据量。\n    -  `SpaceRatio`：空间占比。\n- `Type`表示排序方式。支持如下取值：\n    - `Asc`：升序排序。\n    - `Desc`： 降序排序。\n\n> 若不设置参数，默认按照目标表的总数据量倒序排列。",
        "en": "The order by which to sort query results. Specify the parameter value in the JSON string format. Example: `[{\"Field\":\"TotalSize\",\"Type\":\"Desc\"}]`.\n\n*   `Field` specifies the field by which to sort the query results. Valid values:\n\n    *   `SchemaName`: the name of the database to which the table belongs.\n    *   `TableName`: the name of the table.\n    *   `TotalSize`: the total data size of the table.\n    *   `SpaceRatio`: the storage percentage of the table.\n\n*   `Type` specifies the sorting order. Valid values:\n\n    *   `Asc`: ascending order.\n    *   `Desc`: descending order.\n\n>  If you do not specify this parameter, the query results are sorted by the TotalSize field in descending order."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值为：大于0且不超过Integer数据类型的最大值，默认值为1。",
        "en": "The page number. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值：\n\n- 30；\n- 50；\n- 100；\n\n默认值：30。",
        "en": "The number of entries per page. Valid values:\n\n*   30\n*   50\n*   100\n\nDefault value: 30."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb describe-table-partition-diagnose --db-cluster-id am-bpxxxxxxxx47 --page-size 30 --page-number 1 --order Field_:_TotalSize_,_Type_:_Desc --biz-region-id cn-hangzhou --lang zh --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb DescribeTablePartitionDiagnose --region cn-hangzhou --Order 'Field_:_TotalSize_,_Type_:_Desc' --PageSize 30 --PageNumber 1 --DBClusterId 'am-bpxxxxxxxx47' --Lang zh --RegionId 'cn-hangzhou'"
  }
}
