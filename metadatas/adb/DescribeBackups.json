{
  "name": "DescribeBackups",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份集ID。",
        "en": "The ID of the backup set."
      }
    },
    {
      "name": "CrossRole",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨阿里云账号主账号角色名，即备份集所属阿里云账号下的RAM角色。 \n>该参数仅在跨阿里云账号备份时填写。",
        "en": "The Resource Access Management (RAM) role of the Alibaba Cloud account to which the backup set belongs.\n\n>  This parameter must be specified only when cross-account backup operations are performed."
      }
    },
    {
      "name": "CrossUid",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨阿里云主账号，即备份集所属的阿里云主账号。 \n>该参数仅在跨阿里云账号备份时填写。",
        "en": "The Alibaba Cloud account to which the backup set belongs.\n\n>  This parameter must be specified only when cross-account backup operations are performed."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数仓版集群ID。\n> 您可以调用[DescribeDBClusters](~~129857~~)接口查询目标地域下数仓版集群ID。",
        "en": "The ID of the AnalyticDB for MySQL Data Warehouse Edition cluster.\n\n> You can call the [DescribeDBClusters](~~129857~~) operation to query the IDs of all AnalyticDB for MySQL Data Warehouse Edition clusters within a region."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询结束时间，需要大于查询开始时间，格式：yyyy-MM-ddTHH:mmZ。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC. The end time must be later than the start time."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值：大于0且不超过Integer数据类型最大值的正整数。默认值：1。",
        "en": "The page number. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值：\n- **30**（默认值）\n- **50**\n- **100**",
        "en": "The number of entries per page. Valid values:\n\n*   **30** (default)\n*   **50**\n*   **100**"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n> 您可以调用[DescribeRegions](~~143074~~)接口查看AnalyticDB MySQL版支持的地域和可用区信息，包括地域ID。",
        "en": "The region ID.\n\n>  You can call the [DescribeRegions](~~143074~~) operation to query the most recent region list."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询开始时间，格式：yyyy-MM-ddTHH:mmZ。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb describe-backups --db-cluster-id am-bp18934i73vb50000 --backup-id 327320000 --start-time 2022-06-01T16:00Z --end-time 2022-06-02T16:00Z --page-size 30 --page-number 1 --cross-uid 1950000 --cross-role role-for-xxx --biz-region-id cn-hangzhou --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb DescribeBackups --region cn-hangzhou --EndTime '2022-06-02T16:00Z' --PageSize 30 --PageNumber 1 --DBClusterId 'am-bp18934i73vb50000' --StartTime '2022-06-01T16:00Z' --BackupId 327320000 --CrossUid 1950000 --RegionId 'cn-hangzhou' --CrossRole 'role-for-xxx'"
  }
}
