{
  "name": "ModifyAccountDescription",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "修改账号备注：\n\n- 以中文、英文字母开头。\n- 可以包含中文、英文字符、数字、”_”、” -”。\n- 不能以`http://`、`https:// `开头。\n- 长度为2-256个字符。",
        "en": "The description of the account. The description must meet the following requirements:\n\n*   The description must start with a letter.\n*   The description can contain letters, digits, underscores (\\_), and hyphens (-).\n*   The description cannot start with `http://` or `https://`.\n*   The description must be 2 to 256 characters in length."
      }
    },
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名。",
        "en": "The name of the database account."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb modify-account-description --db-cluster-id am-bp1xxxxxxxx47 --account-name testacc --account-description AccDesc --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb ModifyAccountDescription --region cn-hangzhou --AccountDescription AccDesc --DBClusterId 'am-bp1xxxxxxxx47' --AccountName testacc"
  }
}
