{
  "name": "ModifyDBClusterAccessWhiteList",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterIPArrayAttribute",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "白名单分组属性，默认为空。\n控制台不显示带有**hidden**属性的分组，带有该标签的分组通常是用于访问DTS、DRDS服务的。",
        "en": "The attribute of the IP address whitelist. By default, this parameter is empty. The IP address whitelists that have the **hidden** attribute are not displayed in the console. These IP address whitelists are used to access services such as Data Transmission Service (DTS) and PolarDB-X."
      }
    },
    {
      "name": "DBClusterIPArrayName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要修改的IP白名单分组名称，默认操作**Default**分组。\nIP白名单分组名称以小写字母开头、以数字或小写字母结尾，可包含小写字母、数字及’_’，长度为2~32。\n\n单个集群最多支持50个白名单分组。",
        "en": "The name of the IP address whitelist that you want to modify. Default value: **Default**. The name of an IP address whitelist must be 2 to 32 characters in length. The name can contain lowercase letters, digits, and underscores (\\_). The name must start with a lowercase letter and end with a lowercase letter or digit.\n\nEach cluster supports up to 50 IP address whitelists."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "ModifyMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "修改白名单的方式。取值：\n\n- Cover：覆盖原IP白名单。\n- Append：增加IP。\n- Delete：删除IP。\n\n默认值：Cover。",
        "en": "The method that you want to use to modify the IP address whitelist. Valid values:\n\n*   Cover: overwrites the IP address whitelist.\n*   Append: adds IP addresses to the IP address whitelist.\n*   Delete: removes IP addresses from the IP address whitelist.\n\nDefault value: Cover."
      }
    },
    {
      "name": "SecurityIps",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群的IP白名单，多个IP地址之间以英文逗号（,）隔开，不可重复，最多500个。支持如下两种格式：\n\n- IP地址形式，例如：10.23.12.24。\n- CIDR形式，例如：10.23.12.24/24（无类域间路由，24表示了地址中前缀的长度，范围为1~32）。\n\n> ModifyMode值为Delete时，此参数值可以是空，其他情况下必须有非空值。",
        "en": "The IP addresses that you want to use to modify the IP address whitelist of the cluster. Separate multiple IP addresses with commas (,). You can specify up to 500 distinct IP addresses. The following formats are supported:\n\n*   IP address. Example: 10.23.12.24.\n*   CIDR block. Example: 10.23.12.24/24. In this example, 24 indicates that the prefix of the CIDR block is 24 bits in length. You can replace 24 with a value that ranges from 1 to 32.\n\n>  This parameter must be specified unless ModifyMode is set to Delete."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb modify-db-cluster-access-white-list --db-cluster-id rm-uf6wjk5xxxxxxxxxx --security-ips 10.23.12.24 --db-cluster-ip-array-name test --db-cluster-ip-array-attribute hidden --modify-mode Cover --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb ModifyDBClusterAccessWhiteList --region cn-hangzhou --SecurityIps '10.23.12.24' --DBClusterIPArrayName test --DBClusterId 'rm-uf6wjk5xxxxxxxxxx' --DBClusterIPArrayAttribute hidden --ModifyMode Cover"
  }
}
