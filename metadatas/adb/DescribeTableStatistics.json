{
  "name": "DescribeTableStatistics",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。\n\n> 您可以调用[DescribeDBClusters](~~129857~~)接口查看集群ID。",
        "en": "The cluster ID.\n\n>  You can call the [DescribeDBClusters](~~129857~~) operation to query a list of cluster IDs."
      }
    },
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询关键字，支持根据表名模糊搜索。",
        "en": "The keyword that is used to query information by table name."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "根据指定字段进行排序，格式为JSON，是一个有序JSON数组，按输入数组的顺序进行复合排序，包含`Field`和`Type`两个字段，例如`[{ \"Field\":\"TableName\", \"Type\":\"Asc\" }]`。其中：\n* `Field`表示需要排序的字段名。支持排序的字段包括：TableName、ColdDataSize、DataSize、PrimaryKeyIndexSize、RowCount、IndexSize、SchemaName、PartitionCount。\n* `Type`表示排序类型，支持如下取值（取值均不区分大小写）：\n    * **Desc**：降序。\n    * **Asc**：升序。",
        "en": "The order in which to sort the queried information. Specify this parameter as an ordered JSON array that consists of the `Field` and `Type` fields. Example: `[{ \"Field\":\"TableName\", \"Type\":\"Asc\" }]`.\n\n*   `Field` specifies the field that is used to sort the queried information. The following fields are supported: `TableName`, ColdDataSize, DataSize, PrimaryKeyIndexSize, RowCount, IndexSize, SchemaName, and PartitionCount.\n\n*   `Type` specifies the sorting order. Valid values (case-insensitive):\n\n    *   **Desc**: descending order.\n    *   **Asc**: ascending order."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值为大于0的正整数。默认值为**1**。",
        "en": "The page number. Pages start from page 1. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值：\n- **30**（默认值）\n- **50**\n- **100**",
        "en": "The number of entries per page. Valid values:\n\n*   **30** (default)\n*   **50**\n*   **100**"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n> 您可以调用[DescribeRegions](~~143074~~)接口查看地域ID。",
        "en": "The region ID of the cluster.\n\n>  You can call the [DescribeRegions](~~143074~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SchemaName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库名称。"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb describe-table-statistics --db-cluster-id am-bp183qgrwywa60000 --page-size 30 --page-number 1 --order Field_:_TableName_,_Type_:_Asc --biz-region-id cn-hangzhou --keyword you_table_name --schema-name cust_pay_instruction --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb DescribeTableStatistics --region cn-hangzhou --Order 'Field_:_TableName_,_Type_:_Asc' --PageSize 30 --Keyword you_table_name --PageNumber 1 --DBClusterId 'am-bp183qgrwywa60000' --RegionId 'cn-hangzhou' --SchemaName cust_pay_instruction"
  }
}
