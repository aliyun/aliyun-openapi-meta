{
  "name": "UntagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑实例上的所有标签，默认值为false，取值：\n\n- **true**：解绑实例上的所有标签。\n- **false**：不解绑实例上的所有标签。\n\n> 如果同时传入TagKey和本参数，本参数不生效。",
        "en": "Specifies whether to remove all tags from clusters. Default value: false. Valid values:\n\n*   **true**\n*   **false**\n\n>  If you specify TagKey and this parameter, this parameter does not take effect."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n> 您可以调用[DescribeRegions](~~143074~~)接口查看AnalyticDB MySQL版支持的地域和可用区信息，包括地域ID。",
        "en": "The region ID.\n\n>  You can call the [DescribeRegions](~~143074~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "集群ID，N的取值范围：1~50。",
        "en": "The ID of cluster N. Valid values of N: 1 to 50."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值固定为**ALIYUN::ADB::CLUSTER**。",
        "en": "The resource type. Set the value to **ALIYUN::ADB::CLUSTER**."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键，N的取值范围：1~20。",
        "en": "The key of tag N. Valid values of N: 1 to 20."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb untag-resources --biz-region-id cn-hangzhou --resource-type ALIYUN::ADB::CLUSTER --all false --resource-id am-t4n6k19t9p9f3d0j0 --tag-key productvalue --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb UntagResources --region cn-hangzhou --All false --ResourceId.1 'am-t4n6k19t9p9f3d0j0' --TagKey.1 productvalue --ResourceType 'ALIYUN::ADB::CLUSTER' --RegionId 'cn-hangzhou'"
  }
}
