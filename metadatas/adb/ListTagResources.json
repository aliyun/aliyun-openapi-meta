{
  "name": "ListTagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来返回更多结果。第一次查询不需要提供这个参数，如果一次查询没有返回全部结果，则可在后续查询中传入前一次返回的Token继续查询。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. You do not need to specify this parameter for the first request. You must specify the token that is obtained from the previous query as the value of NextToken."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，您可通过接口[DescribeRegions](~~143074~~)查看可用的地域ID。",
        "en": "The region ID of the cluster. You can call the [DescribeRegions](~~143074~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "集群ID。支持同时传入多个集群ID，N的取值范围为：1~50。\n> ResourceId.N参数和Tag.N.Key参数至少传入一个。",
        "en": "The cluster ID. You can specify multiple cluster IDs. Valid values of N: 1 to 50.\n\n> You must specify at least one of the following parameters: ResourceId.N and Tag.N.Key."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。唯一取值：**cluster**。",
        "en": "The type of the resource. Set the value to **cluster**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ],
      "description": {
        "zh": "标签。",
        "en": "The tags that are added to clusters."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun adb list-tag-resources --biz-region-id cn-hangzhou --resource-type cluster --next-token 212db86sca4384811e0b5e8707ec21345 --resource-id am-bp1l20nxxxxxxxxxx --tag Value=testvalue1 Key=testkey1 --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb ListTagResources --region cn-hangzhou --ResourceId.1 'am-bp1l20nxxxxxxxxxx' --Tag.1.Key testkey1 --NextToken 212db86sca4384811e0b5e8707ec21345 --Tag.1.Value testvalue1 --ResourceType cluster --RegionId 'cn-hangzhou'"
  }
}
