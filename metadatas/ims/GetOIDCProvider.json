{
  "name": "GetOIDCProvider",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OIDCProviderName",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims get-oidc-provider --oidc-provider-name TestOIDCProvider --region cn-hangzhou",
    "legacyCli": "aliyun ims GetOIDCProvider --region cn-hangzhou --OIDCProviderName TestOIDCProvider"
  }
}
