{
  "name": "ListApplicationProvisionInfos",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SourceType",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims list-application-provision-infos --source-type external --region cn-hangzhou",
    "legacyCli": "aliyun ims ListApplicationProvisionInfos --region cn-hangzhou --SourceType external"
  }
}
