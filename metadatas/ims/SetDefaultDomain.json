{
  "name": "SetDefaultDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DefaultDomainName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ims set-default-domain --default-domain-name examplecompany.onaliyun.com --region cn-hangzhou",
    "legacyCli": "aliyun ims SetDefaultDomain --region cn-hangzhou --DefaultDomainName 'examplecompany.onaliyun.com'"
  }
}
