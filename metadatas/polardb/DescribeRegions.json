{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun polardb describe-regions --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribeRegions --region cn-hangzhou"
  }
}
