{
  "name": "UpdateRecursionRecord",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。"
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "MX记录的优先级。取值越小，优先级越高，取值范围：[1,99]。"
      }
    },
    {
      "name": "RecordId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析记录的ID。"
      }
    },
    {
      "name": "RequestSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析线路，默认为**default**。参见：\n\n<props=\"china\">[解析线路枚举](https://help.aliyun.com/document_detail/29807.html)</props>\n\n<props=\"intl\">[解析线路枚举](https://www.alibabacloud.com/help/zh/doc-detail/29807.htm)</props>"
      }
    },
    {
      "name": "Rr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主机记录。主机记录就是域名前缀，常见用法有www、@、*（泛解析）、mail（常用于邮箱）。\n\n例如要解析@.exmaple.com，主机记录要填写\"@”，而不是空。"
      }
    },
    {
      "name": "Ttl",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本地缓存时间。目前取值仅支持5、30、60、3600（1小时）、43200（12小时）、86400（24小时），单位秒（s），默认60。"
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析记录类型，目前支持如下： A:IPV4记录，支持将域名映射到IPv4地址使用 AAAA:IPV6记录，支持将域名映射到IPv6地址使用 CNAME:别名记录，支持将域名指向另外一个域名 MX:电邮交互记录，支持将域名指向邮件服务器地址 TXT:文本记录，是任意可读的文本DNS记录 SRV:服务器资源记录，用来标识某台服务器使用了某个服务，常见于微软系统的目录管理"
      }
    },
    {
      "name": "Value",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "记录值。根据不同解析记录类型，填写相应的记录值。"
      }
    },
    {
      "name": "Weight",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "权重值（1-100 内的整数，包括 1 和 100，默认为1），支持对每个地址设置不同的权重值，实现解析查询按照权重比例返回地址。"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns update-recursion-record --client-token 21079fa016944979537637959d09bc --record-id 90000000 --rr test --type A --request-source WebSDK --weight 2 --priority 5 --ttl 60 --value 1.1.XX.XX --region cn-hangzhou",
    "legacyCli": "aliyun alidns UpdateRecursionRecord --region cn-hangzhou --Rr test --Type A --Priority 5 --Value '1.1.XX.XX' --RecordId 90000000 --RequestSource WebSDK --ClientToken 21079fa016944979537637959d09bc --Ttl 60 --Weight 2"
  }
}
