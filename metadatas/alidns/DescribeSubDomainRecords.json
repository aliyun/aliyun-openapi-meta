{
  "name": "DescribeSubDomainRecords",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名名称。",
        "en": "The domain name."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English"
      }
    },
    {
      "name": "Line",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析线路。",
        "en": "The DNS resolution line."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，起始值为**1**，默认为**1**。",
        "en": "The page number. Pages start from page **1**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**。",
        "en": "The number of entries per page. Valid values: **1 to 100**. Default value: **20**."
      }
    },
    {
      "name": "SubDomain",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "`例如参数SubDomain为\"a.www.example.com\"为例：`\n\n`如果参数DomainName为空，则查询域名为example.com、主机名为”a.www“的解析记录。`\n`如果参数DomainName为\"www.example.com\"，则查询域名为\"www.example.com\"、主机名为\"a\"的解析记录。`\n`如果参数DomainName为\"a.www.example.com\"，则查询域名为\"a.www.example.com\"、主机名为\"@\"的解析记录。`",
        "en": "If you set SubDomain to `a.www.example.com` and leave\n\nDomainName empty, the system returns the DNS records that contain the hostname `a.www` for the domain name example.com. If you set SubDomain to a.www.example.com and set DomainName to www.example.com, the system returns the DNS records that contain the hostname `a` for the domain name www.example.com. If you set SubDomain to a.www.example.com and set DomainName to a.www.example.com, the system returns the DNS records that contain the hostname `@` for the domain name a.www.example.com."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析记录类型。若不传值，则返回子域名对应的全部解析记录类型。\n\n解析类型包括(不区分大小写)：**A、MX、CNAME、TXT、REDIRECT_URL、FORWORD_URL、NS、AAAA、SRV**。",
        "en": "The type of DNS records. If you do not specify this parameter, all types of DNS records for the subdomain name are returned.\n\nValid values: **A, MX, CNAME, TXT, REDIRECT_URL, FORWORD_URL, NS, AAAA, and SRV**."
      }
    },
    {
      "name": "UserClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户端IP。",
        "en": "The IP address of the client."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns describe-sub-domain-records --lang en --user-client-ip 192.0.2.0 --sub-domain a.www.example.com --page-number 1 --page-size 20 --type MX --line default --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeSubDomainRecords --region cn-hangzhou --SubDomain 'a.www.example.com' --Line default --Type MX --PageSize 20 --PageNumber 1 --DomainName 'example.com' --UserClientIp '192.0.2.0' --Lang en"
  }
}
