{
  "name": "DescribeDnsCacheDomains",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "关键字，按照”%Keyword%”模式搜索，不区分大小写",
        "en": "The keyword for searches in \"%KeyWord%\" mode. The value is not case-sensitive."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，取值：\n- **zh**：中文\n- **en**：英文\n默认为**zh**",
        "en": "The language."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，起始值为1，默认为1",
        "en": "The page number. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**",
        "en": "The number of entries per page. Maximum value: **100**. Default value: **20**."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns describe-dns-cache-domains --lang zh --keyword a --page-number 1 --page-size 20 --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeDnsCacheDomains --region cn-hangzhou --Keyword a --PageSize 20 --PageNumber 1 --Lang zh"
  }
}
