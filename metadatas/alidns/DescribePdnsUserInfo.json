{
  "name": "DescribePdnsUserInfo",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid value:\n\n*   **zh**: Chinese.\n*   **en**: English."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns describe-pdns-user-info --lang en --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribePdnsUserInfo --region cn-hangzhou --Lang en"
  }
}
