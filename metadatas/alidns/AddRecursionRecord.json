{
  "name": "AddRecursionRecord",
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。"
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "MX记录的优先级。取值越小，优先级越高，取值范围：[1,99]。"
      }
    },
    {
      "name": "RequestSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析线路，默认为**default**。参见：\n\n<props=\"china\">[解析线路枚举](https://help.aliyun.com/document_detail/29807.html)</props>\n\n<props=\"intl\">[解析线路枚举](https://www.alibabacloud.com/help/zh/doc-detail/29807.htm)</props>"
      }
    },
    {
      "name": "Rr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主机记录。主机记录就是域名前缀，常见用法有www、@、*（泛解析）、mail（常用于邮箱）。\n\n例如要解析@.exmaple.com，主机记录要填写\"@”，而不是空。"
      }
    },
    {
      "name": "Ttl",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本地缓存时间。目前取值仅支持5、30、60、3600（1小时）、43200（12小时）、86400（24小时），单位秒（s），默认60。"
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "解析记录类型，目前支持如下： A:IPV4记录，支持将域名映射到IPv4地址使用 AAAA:IPV6记录，支持将域名映射到IPv6地址使用 CNAME:别名记录，支持将域名指向另外一个域名 MX:电邮交互记录，支持将域名指向邮件服务器地址 TXT:文本记录，是任意可读的文本DNS记录 SRV:服务器资源记录，用来标识某台服务器使用了某个服务，常见于微软系统的目录管理 NS:名称服务器记录，支持将子域名委托给其他DNS服务商解析 CAA:CAA资源记录，可以限定域名颁发证书和CA（证书颁发机构）之间的联系 URL:将域名指向另外一个已经存在的站点 SVCB:服务绑定记录，主要用于服务发现，以DNS记录的方式披露服务支持的协议和服务参数信息 HTTPS:针对HTTPS服务的特定记录类型，使用HTTPS记录，可约定安全的HTTPS建连协议，以及最优的服务站点地址"
      }
    },
    {
      "name": "UserClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户端IP。"
      }
    },
    {
      "name": "Value",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "记录值。根据不同解析记录类型，填写相应的记录值。"
      }
    },
    {
      "name": "Weight",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "权重值（1-100 内的整数，包括 1 和 100，默认为1），支持对每个地址设置不同的权重值，实现解析查询按照权重比例返回地址。"
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Zone ID。",
        "en": "Zone ID。"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns add-recursion-record --user-client-ip 192.168.0.1 --zone-id 173671468000011 --rr www --type A --request-source default --weight 2 --priority 1 --ttl 60 --value 1.1.1.1 --client-token 6447728c8578e66aacf062d2df4446dc --region cn-hangzhou",
    "legacyCli": "aliyun alidns AddRecursionRecord --region cn-hangzhou --Rr www --Type A --ZoneId 173671468000011 --Priority 1 --UserClientIp '192.168.0.1' --Value '1.1.1.1' --RequestSource default --ClientToken 6447728c8578e66aacf062d2df4446dc --Ttl 60 --Weight 2"
  }
}
