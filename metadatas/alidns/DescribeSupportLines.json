{
  "name": "DescribeSupportLines",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名名称。"
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English"
      }
    },
    {
      "name": "UserClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户端IP。"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alidns describe-support-lines --lang en --user-client-ip 1.1.0.0 --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeSupportLines --region cn-hangzhou --DomainName 'example.com' --UserClientIp '1.1.0.0' --Lang en"
  }
}
