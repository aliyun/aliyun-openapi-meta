{
  "name": "CheckServiceRole",
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/ram/check-service-role",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs check-service-role --roles AliyunCSManagedAutoScalerRole --region cn-hangzhou",
    "legacyCli": "aliyun cs POST /ram/check-service-role --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"roles\":[\"AliyunCSManagedAutoScalerRole\"]}"
  }
}
