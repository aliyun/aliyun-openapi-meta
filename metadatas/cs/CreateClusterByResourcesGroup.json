{
  "name": "CreateClusterByResourcesGroup",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/resource_groups/[ResourceGroupId]/clusters",
  "parameters": [
    {
      "name": "ResourceGroupId",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}