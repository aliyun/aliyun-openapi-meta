{
  "name": "GrantPermissions",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/permissions/users/[uid]",
  "parameters": [
    {
      "name": "uid",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}