{
  "name": "RunTriggerHook",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/triggers/probe/[token]",
  "parameters": [
    {
      "name": "token",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ]
}