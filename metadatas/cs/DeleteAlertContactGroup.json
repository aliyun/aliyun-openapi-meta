{
  "name": "DeleteAlertContactGroup",
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/alert/contact_groups",
  "parameters": [
    {
      "name": "contact_group_ids",
      "position": "Query",
      "type": "Json",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs delete-alert-contact-group --contact-group-ids 12345 --region cn-hangzhou",
    "legacyCli": "aliyun cs DELETE /alert/contact_groups --region cn-hangzhou --contact_group_ids '[12345]' --header \"Content-Type=application/json\""
  }
}
