{
  "name": "DescribeUserQuota",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/quota",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun cs describe-user-quota --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /quota --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
