{
  "name": "CreateCluster",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/clusters",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun cs create-cluster --region cn-hangzhou",
    "legacyCli": "aliyun cs POST /clusters --region cn-hangzhou --header \"Content-Type=application/json\" --body \"cluster-demo\""
  }
}
