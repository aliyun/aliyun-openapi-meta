{
  "name": "DescribePolicyDetails",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/policies/[policy_name]",
  "parameters": [
    {
      "name": "policy_name",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}