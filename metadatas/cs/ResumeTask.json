{
  "name": "ResumeTask",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/tasks/[task_id]/resume",
  "parameters": [
    {
      "name": "task_id",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}