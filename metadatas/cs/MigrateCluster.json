{
  "name": "MigrateCluster",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/clusters/[cluster_id]/migrate",
  "parameters": [
    {
      "name": "cluster_id",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}