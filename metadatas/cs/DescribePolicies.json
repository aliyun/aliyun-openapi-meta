{
  "name": "DescribePolicies",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/policies",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun cs describe-policies --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /policies --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
