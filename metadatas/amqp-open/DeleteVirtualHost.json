{
  "name": "DeleteVirtualHost",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除的Vhost所属的云消息队列 RabbitMQ 版实例ID。",
        "en": "The ID of the ApsaraMQ for RabbitMQ instance to which the vhost you want to delete belongs."
      }
    },
    {
      "name": "VirtualHost",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除Vhost的名称。",
        "en": "The name of the vhost that you want to delete."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun amqp-open delete-virtual-host --instance-id amqp-cn-v0h1kb9nu000 --virtual-host test --region cn-hangzhou",
    "legacyCli": "aliyun amqp-open DeleteVirtualHost --region cn-hangzhou --InstanceId 'amqp-cn-v0h1kb9nu000' --VirtualHost test"
  }
}
