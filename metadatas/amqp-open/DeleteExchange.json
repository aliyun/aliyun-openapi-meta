{
  "name": "DeleteExchange",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExchangeName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除的Exchange的名称。",
        "en": "The name of the exchange that you want to delete."
      }
    },
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除Exchange所属的云消息队列 RabbitMQ 版实例ID。",
        "en": "The ID of the ApsaraMQ for RabbitMQ instance whose exchange you want to delete."
      }
    },
    {
      "name": "VirtualHost",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除Exchange所属的Vhost名称。",
        "en": "The vhost to which the exchange that you want to delete belongs."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun amqp-open delete-exchange --instance-id amqp-cn-v0h1kb9nu000 --virtual-host test --exchange-name DemoExchange --region cn-hangzhou",
    "legacyCli": "aliyun amqp-open DeleteExchange --region cn-hangzhou --InstanceId 'amqp-cn-v0h1kb9nu000' --VirtualHost test --ExchangeName DemoExchange"
  }
}
