{
  "name": "ListInstances",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "最大结果集。建议取值在1~100之间。",
        "en": "The maximum number of entries to return. Valid values: 1 to 100."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来标记当前查询结束的位置，以便下一次调用时作为参数传入，继续翻页。首次调用或返回最后一页时，取值为空字符串。",
        "en": "The token that marks the end position of the previous returned page. To obtain the next batch of data, call the operation again by using the value of NextToken returned by the previous request. If you call this operation for the first time or want to query all results, set NextToken to an empty string."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所属资源组ID。",
        "en": "The ID of the resource group to which the instances belong."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun amqp-open list-instances --next-token caeba0bbb2be03f84eb48b699f0a0000 --max-results 1 --resource-group-id rg-aekzu74zjgdu4mq --region cn-hangzhou",
    "legacyCli": "aliyun amqp-open ListInstances --region cn-hangzhou --NextToken caeba0bbb2be03f84eb48b699f0a0000 --ResourceGroupId 'rg-aekzu74zjgdu4mq' --MaxResults 1"
  }
}
