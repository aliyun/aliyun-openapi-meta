{
  "name": "UpdateFileUploadLimit",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun sas update-file-upload-limit --limit 100 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas UpdateFileUploadLimit --region cn-hangzhou --Limit 100"
  }
}
