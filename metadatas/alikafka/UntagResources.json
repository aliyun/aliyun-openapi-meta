{
  "name": "UntagResources",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解除资源绑定的全部标签。TagKey.N为空时，该参数有效。默认值为**false**。",
        "en": "Specifies whether to detach all tags from the resource. This parameter only takes effect when the TagKey.N parameter is not configured. Default value: **false**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源的地域ID。",
        "en": "The ID of the region in which the resource is deployed."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "需要解绑的资源ID。",
        "en": "The IDs of the resources from which you want to detach tags."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。枚举类型，目前支持的资源类型：\n- **INSTANCE**\n\n- **TOPIC**\n\n- **CONSUMERGROUP**\n\n> 不区分大小写。",
        "en": "The type of the resources. Valid values:\n\n*   **INSTANCE**\n*   **TOPIC**\n*   **CONSUMERGROUP**\n\n>  The value of this parameter is not case-sensitive."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键。",
        "en": "The key of the resource tag."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun alikafka untag-resources --biz-region-id cn-hangzhou --resource-type INSTANCE --all false --resource-id alikafka_post-cn-v0h1fgs20000 --tag-key FinanceDept --api-version 2019-09-16 --region cn-hangzhou",
    "legacyCli": "aliyun alikafka UntagResources --region cn-hangzhou --All false --ResourceId.1 'alikafka_post-cn-v0h1fgs20000' --TagKey.1 FinanceDept --ResourceType INSTANCE --RegionId 'cn-hangzhou'"
  }
}
