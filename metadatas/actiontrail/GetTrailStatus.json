{
  "name": "GetTrailStatus",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsOrganizationTrail",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询多账号跟踪状态，取值：  \n\n- true：查询多账号跟踪状态。\n\n- false（默认值）：查单账号跟踪状态。",
        "en": "Specifies whether to query the status of a multi-account trail. Valid values:\n\n*   true: Query the status of a multi-account trail.\n*   false: Query the status of a single-account trail. It is the default value."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "跟踪名称。  \n长度为6～36个字符，必须以小写英文字母开头，可包含小写英文字母、数字、短划线（-）和下划线（_）。  \n\n> 同一账号内跟踪名称不可重复。",
        "en": "The name of the trail.\n\nThe name must be 6 to 36 characters in length. The name must start with a lowercase letter and can contain lowercase letters, digits, hyphens (-), and underscores (\\_).\n\n> The name must be unique within your Alibaba Cloud account."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun actiontrail get-trail-status --name trail-test --is-organization-trail false --api-version 2020-07-06 --region cn-hangzhou",
    "legacyCli": "aliyun actiontrail GetTrailStatus --region cn-hangzhou --Name 'trail-test' --IsOrganizationTrail false"
  }
}
