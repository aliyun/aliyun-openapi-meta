{
  "name": "DescribeAdvancedQueryTemplate",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "模板列表的页码。起始值：1。默认值：1。",
        "en": "The page number. The value starts from 1.\n\nDefault value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "允许返回的最大结果数目。\n默认值：20。",
        "en": "The maximum number of results to return.\nDefault value: 20."
      }
    },
    {
      "name": "TemplateName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板名称。用户可以通过输入部分模板名称来检索符合条件的所有模板。如果输入的内容与多个模板名称的部分字符相匹配，则返回所有这些模板的列表。若不提供任何输入，则默认返回系统中所有可用的模板。\n输入: a\n输出: [a1, a2]\n输入: `` （无输入） 输出: [a1, a2, b1, c1]",
        "en": "The name of the template. This operation performs a case-insensitive, fuzzy match. If you do not specify a name, all templates are returned.\n\nFor example, if you specify `a`, templates named `a1` and `a2` are returned. If you leave this parameter empty, templates named `a1`, `a2`, `b1`, and `c1` are returned."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun actiontrail describe-advanced-query-template --template-name example-template --page-number 1 --page-size 20 --api-version 2020-07-06 --region cn-hangzhou",
    "legacyCli": "aliyun actiontrail DescribeAdvancedQueryTemplate --region cn-hangzhou --PageSize 20 --PageNumber 1 --TemplateName 'example-template'"
  }
}
