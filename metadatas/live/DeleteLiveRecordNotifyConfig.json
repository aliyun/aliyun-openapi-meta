{
  "name": "DeleteLiveRecordNotifyConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-record-notify-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveRecordNotifyConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}
