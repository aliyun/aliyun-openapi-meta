{
  "name": "DeleteLiveAppSnapshotConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun live delete-live-app-snapshot-config --domain-name example.com --app-name liveApp0000 --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveAppSnapshotConfig --region cn-hangzhou --DomainName 'example.com' --AppName liveApp0000"
  }
}
