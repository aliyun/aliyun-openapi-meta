{
  "name": "GetProject",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ProjectName",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "WithStatistics",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun imm get-project --project-name test-project --with-statistics true --api-version 2020-09-30 --region cn-hangzhou",
    "legacyCli": "aliyun imm GetProject --region cn-hangzhou --ProjectName 'test-project' --WithStatistics true"
  }
}
