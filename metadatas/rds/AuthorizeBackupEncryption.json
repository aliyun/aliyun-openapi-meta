{
  "name": "AuthorizeBackupEncryption",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds authorize-backup-encryption --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds AuthorizeBackupEncryption --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
