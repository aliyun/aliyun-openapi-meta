{
  "name": "DescribeRdsResourceSettings",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceNiche",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun rds describe-rds-resource-settings --resource-niche noticeBar --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeRdsResourceSettings --region cn-hangzhou --ResourceNiche noticeBar"
  }
}
