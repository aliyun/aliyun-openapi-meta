{
  "name": "EnableDiskEncryptionByDefault",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ecs enable-disk-encryption-by-default --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs EnableDiskEncryptionByDefault --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
