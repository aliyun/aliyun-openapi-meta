{
  "name": "DescribeClusters",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun ecs describe-clusters --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeClusters --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
