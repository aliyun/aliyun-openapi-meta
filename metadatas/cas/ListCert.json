{
  "name": "ListCert",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书的类型 。取值：\n\n- **CA**：表示CA证书。\n- **CERT**：表示签发的证书。",
        "en": "证书的类型 。取值：\n\n- **CA**：表示CA证书。\n- **CERT**：表示签发的证书。"
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页。当前页，默认1。",
        "en": "The number of the page to return. Default value: 1."
      }
    },
    {
      "name": "Identifiers",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KeyWord",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询关键字。支持通过名称、域名、SANS模糊查询。",
        "en": "The keyword for the query. You can enter a name, domain name, or Subject Alternative Name (SAN) extension. Fuzzy match is supported."
      }
    },
    {
      "name": "ShowSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页，每页展示数据大小，默认50条。",
        "en": "The number of entries to return on each page. Default value: 50."
      }
    },
    {
      "name": "SourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书来源，取值：\n\n- **upload**：上传证书\n- **aliyun**：阿里云证书",
        "en": "The source of the certificate. Valid values:\n\n*   **upload**: uploaded certificate\n*   **aliyun**: Alibaba Cloud certificate"
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书状态，取值：\n\n- **ISSUE**： 已签发\n- **REVOKE**： 吊销",
        "en": "The status of the certificate. Valid values:\n\n*   **ISSUE**: issued\n*   **REVOKE**: revoked"
      }
    },
    {
      "name": "WarehouseId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "仓库ID。可通过[ListCertWarehouse](~~453246~~)接口查询。",
        "en": "The ID of the certificate repository. You can call the ListCertWarehouse API operation to query the IDs of certificate repositories."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cas list-cert --key-word test_name --warehouse-id 12 --status ISSUE --source-type aliyun --cert-type CERT --current-page 1 --show-size 50 --identifiers aaa,bbb --api-version 2020-04-07 --region cn-hangzhou",
    "legacyCli": "aliyun cas ListCert --region cn-hangzhou --Status ISSUE --CurrentPage 1 --KeyWord test_name --SourceType aliyun --CertType CERT --ShowSize 50 --WarehouseId 12 --Identifiers 'aaa,bbb'"
  }
}
