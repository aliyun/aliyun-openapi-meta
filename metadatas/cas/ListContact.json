{
  "name": "ListContact",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页，当前页，默认1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键词。例如姓名、邮箱和电话中的关键字。",
        "en": "The keyword used in the query. For example, you can specify a keyword in names, email addresses, and mobile phone numbers."
      }
    },
    {
      "name": "ShowSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，设置每页显示联系人的个数。",
        "en": "The number of contacts per page."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cas list-contact --keyword 186 --show-size 20 --current-page 1 --api-version 2020-04-07 --region cn-hangzhou",
    "legacyCli": "aliyun cas ListContact --region cn-hangzhou --Keyword 186 --CurrentPage 1 --ShowSize 20"
  }
}
