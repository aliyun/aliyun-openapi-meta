{
  "name": "QueryRegionConfig",
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v5/region_config",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun edas query-region-config --region cn-hangzhou",
    "legacyCli": "aliyun edas GET /pop/v5/region_config --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
