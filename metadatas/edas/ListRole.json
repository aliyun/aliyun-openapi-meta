{
  "name": "ListRole",
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v5/account/role_list",
  "parameters": [],
  "example": {
    "unifiedCli": "aliyun edas list-role --region cn-hangzhou",
    "legacyCli": "aliyun edas POST /pop/v5/account/role_list --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}
