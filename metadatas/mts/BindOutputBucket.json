{
  "name": "BindOutputBucket",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bucket",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun mts bind-output-bucket --bucket example-bucket-0000 --api-version 2014-06-18 --region cn-hangzhou",
    "legacyCli": "aliyun mts BindOutputBucket --region cn-hangzhou --Bucket 'example-bucket-0000'"
  }
}
