{
  "name": "ListSecrets",
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/secrets",
  "parameters": [
    {
      "name": "gatewayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网关类型过滤，目前支持**AI**、**API**两种网关类型。",
        "en": "Gateway type for filtering secrets of specific gateway type"
      }
    },
    {
      "name": "nameLike",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "密钥名称，模糊搜索。",
        "en": "Secret name for fuzzy matching, supports filtering secrets by name"
      }
    },
    {
      "name": "pageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页页码。",
        "en": "Page number"
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，有效范围[1, 100]，不填默认10。",
        "en": "Number of items per page"
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun apig list-secrets --name-like my_secret --page-number 1 --page-size 10 --gateway-type AI --region cn-hangzhou",
    "legacyCli": "aliyun apig GET /v1/secrets --region cn-hangzhou --nameLike my_secret --pageNumber 1 --gatewayType AI --pageSize 10 --header \"Content-Type=application/json\""
  }
}
