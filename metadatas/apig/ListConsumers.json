{
  "name": "ListConsumers",
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/consumers",
  "parameters": [
    {
      "name": "gatewayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网关类型过滤，目前支持**AI**、**API**两种网关类型。",
        "en": "The instance type. Valid values: **AI** and **API**."
      }
    },
    {
      "name": "nameLike",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按接口名称模糊搜索接口。",
        "en": "The name used to perform a fuzzy search for operations."
      }
    },
    {
      "name": "pageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页页码，从1开始，不填默认1。",
        "en": "The page number to return. Pages start from 1. Default value: 1."
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小，默认10",
        "en": "The number of entries per page. Default value: 10."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun apig list-consumers --name-like UI-test --page-number 1 --page-size 10 --gateway-type AI --region cn-hangzhou",
    "legacyCli": "aliyun apig GET /v1/consumers --region cn-hangzhou --nameLike 'UI-test' --pageNumber 1 --gatewayType AI --pageSize 10 --header \"Content-Type=application/json\""
  }
}
