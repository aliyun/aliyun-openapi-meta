{
  "name": "DisassociateTransitRouterMulticastDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that it is unique among all requests. The client token can contain only ASCII characters."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会解除交换机和组播域的关联关系。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后直接解除交换机和组播域的关联关系。",
        "en": "Specifies whether only to precheck the API request. Valid values:\n\n- **true**: prechecks the request but does not disassociate the vSwitch from the multicast domain. The system checks the required parameters, the request format, and the service limits. If the request fails the check, an error message is returned. If the request passes the check, the DryRunOperation error code is returned.\n- **false** (default): sends the request. The vSwitch is disassociated from the multicast domain after the request passes the precheck."
      }
    },
    {
      "name": "TransitRouterAttachmentId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPC连接ID。\n\n本参数为交换机所属的专有网络VPC（Virtual Private Cloud）被连接至转发路由器实例后生成的VPC连接的ID。",
        "en": "The ID of the virtual private cloud (VPC) connection.\n\nA connection ID is generated after a VPC to which the vSwitch belongs is connected to a transit router."
      }
    },
    {
      "name": "TransitRouterMulticastDomainId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "组播域ID。",
        "en": "The ID of the multicast domain."
      }
    },
    {
      "name": "VSwitchIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "交换机ID列表。",
        "en": "The IDs of vSwitches."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cbn disassociate-transit-router-multicast-domain --client-token 123e4567-e89b-12d3-a456-426655440000 --transit-router-multicast-domain-id tr-mcast-domain-91wpg6wbhchjeq0000 --transit-router-attachment-id tr-attach-g3kz2k3u76amsk0000 --dry-run false --vswitchids vsw-p0w9s2ig1jnwgrbzl0000 --region cn-hangzhou",
    "legacyCli": "aliyun cbn DisassociateTransitRouterMulticastDomain --region cn-hangzhou --TransitRouterAttachmentId 'tr-attach-g3kz2k3u76amsk0000' --VSwitchIds.1 'vsw-p0w9s2ig1jnwgrbzl0000' --TransitRouterMulticastDomainId 'tr-mcast-domain-91wpg6wbhchjeq0000' --DryRun false --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}
