{
  "name": "DeleteTransitRouterMulticastDomain",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that the value is unique among different requests. The client token can contain only ASCII characters."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会删除组播域。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后直接删除组播域。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n*   **true**: performs a dry run. The system checks the required parameters, request syntax, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and sends the request."
      }
    },
    {
      "name": "TransitRouterMulticastDomainId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "组播域ID。",
        "en": "The ID of the multicast domain."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cbn delete-transit-router-multicast-domain --client-token 123e4567-e89b-12d3-a456-42660000 --transit-router-multicast-domain-id tr-mcast-domain-40cwj0rgzgdtam0000 --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun cbn DeleteTransitRouterMulticastDomain --region cn-hangzhou --TransitRouterMulticastDomainId 'tr-mcast-domain-40cwj0rgzgdtam0000' --DryRun false --ClientToken '123e4567-e89b-12d3-a456-42660000'"
  }
}
