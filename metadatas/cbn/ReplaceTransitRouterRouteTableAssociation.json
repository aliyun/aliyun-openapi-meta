{
  "name": "ReplaceTransitRouterRouteTableAssociation",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that it is unique among all requests. The client token can contain only ASCII characters.\n\n>  If you do not set this parameter, **ClientToken** is set to the value of **RequestId**. The value of **RequestId** for each API request may be different."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会更换网络实例连接关联的转发路由器路由表。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后直接更换网络实例连接关联的转发路由器路由表。",
        "en": "Specifies whether only to precheck the API request. Valid values:\n\n*   **true**: prechecks the request but does not associate the network instance connection with another route table. The system checks whether the required parameters are set, whether the formats of the values are valid, and the service limits. If the request fails to pass the precheck, the corresponding error message is returned. If the request passes the precheck, the `DryRunOperation` error code is returned.\n*   **false**: sends the request. After the request passes the authentication, the network instance connection is associated with the specified route table. This is the default value."
      }
    },
    {
      "name": "TransitRouterAttachmentId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例连接ID。",
        "en": "The ID of the network instance connection."
      }
    },
    {
      "name": "TransitRouterRouteTableId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例连接待关联的转发路由器路由表ID。",
        "en": "The ID of the route table with which you want to associate the network instance connection."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cbn replace-transit-router-route-table-association --client-token 123e4567-e89b-12d3-a456-4266550000 --dry-run false --transit-router-attachment-id tr-attach-071g5j5tefg4x60000 --transit-router-route-table-id vtb-bp1cprmc6xmzjd66i0000 --region cn-hangzhou",
    "legacyCli": "aliyun cbn ReplaceTransitRouterRouteTableAssociation --region cn-hangzhou --TransitRouterAttachmentId 'tr-attach-071g5j5tefg4x60000' --TransitRouterRouteTableId 'vtb-bp1cprmc6xmzjd66i0000' --DryRun false --ClientToken '123e4567-e89b-12d3-a456-4266550000'"
  }
}
