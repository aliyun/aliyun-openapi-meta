{
  "name": "ResolveAndRouteServiceInCen",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessRegionIds",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "访问云服务的地域ID列表。",
        "en": "The IDs of the regions where the cloud service is accessed."
      }
    },
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云企业网实例ID。",
        "en": "The ID of the CEN instance."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not set this parameter, **ClientToken** is set to the value of **RequestId**. The value of **RequestId** for each API request may be different."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云服务的描述信息。\n\n描述可以为空或长度为1～256个字符，不能以http://或https://开头。",
        "en": "The description of the cloud service.\n\nThis parameter is optional. If you enter a description, it must be 1 to 256 characters in length and cannot start with http:// or https://."
      }
    },
    {
      "name": "Host",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云服务IP地址或地址段。\n\n> 通常云服务会使用多个地址或地址段，建议您重复调用本接口，添加该云服务所有地址或地址段。",
        "en": "The IP addresses or CIDR blocks of the cloud service.\n\n> In most cases, multiple IP addresses or CIDR blocks are assigned to a cloud service. We recommend that you call this operation multiple times to add all IP addresses and CIDR blocks of the cloud service."
      }
    },
    {
      "name": "HostRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云服务所在的地域ID。",
        "en": "The ID of the region in which the cloud service is deployed."
      }
    },
    {
      "name": "HostVpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云服务关联的VPC实例ID。",
        "en": "The ID of the VPC that is associated with the cloud service."
      }
    }
  ],
  "example": {
    "unifiedCli": "aliyun cbn resolve-and-route-service-in-cen --client-token 02fb3da40000 --cen-id cen-ckwa2hhmuislse0000 --host 100.118.28.0/24 --host-region-id cn-hangzhou --host-vpc-id vpc-o6woh5s494zueq40v0000 --description descname --access-region-ids cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun cbn ResolveAndRouteServiceInCen --region cn-hangzhou --HostRegionId 'cn-hangzhou' --Description descname --CenId 'cen-ckwa2hhmuislse0000' --AccessRegionIds.1 'cn-hangzhou' --Host '100.118.28.0/24' --ClientToken 02fb3da40000 --HostVpcId 'vpc-o6woh5s494zueq40v0000'"
  }
}
