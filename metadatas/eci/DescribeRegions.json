{
  "name": "DescribeRegions",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "example": {
    "unifiedCli": "aliyun eci describe-regions --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun eci DescribeRegions --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
