{
  "name": "GetPhysicalConnectionServiceStatus",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ],
  "example": {
    "unifiedCli": "aliyun vpc get-physical-connection-service-status --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun vpc GetPhysicalConnectionServiceStatus --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}
